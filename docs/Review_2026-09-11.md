# Review 2026-09-11 — 近 24h 提交审查（R19.22 生存伙伴批 t1030–t1034 五连 + review0910 #1 清偿 + 野狼中立收口）

- 审查对象：`a7b2bf1..026681c`（2026-09-10 11:20 → 09-11 08:45，共 19 笔：6 笔 src fix（含 1 笔纯注释登记）、6 笔测试腿（全部只动 `tools/redstone_matrix_test.cpp`，+275/+73/+358/+350/+225/+315）、7 笔 docs/plan/state）。矩阵 505→519（t1030 +2、t1036 净 0 改写迁移、t1031 +2、t1032 +4、t1033 +3、t1034 +3）。与上轮（Review_2026-09-10，止于 `4c83422`）边界核对：`4c83422..a7b2bf1` 间仅 a7b2bf1 本身（docs review0910 采纳笔），无缝衔接无遗漏。
- 审查方式：6 笔 fix 逐笔 diff 精读（d8e5756 / d4be270 / 9bc2c73 / 482b9fa / 12f4558 / 181ad6d），关键路径在 HEAD 交叉核对——bedSpawn 双信号全消费面（C++ 4 emit 点 + QML handler 改名 + 矩阵双信号钉）、aiWolf 分发链（tick MobWolf 唯一入口 + 全部 `chasing = true` 写点归属 + 实体持久化有无）、t1032 空转门沿完备性（ItemEntityManager notifyChanged 全发射点 + hasLiveMember 与 getInstanceBuffer 谓词同参比对）、附魔台书节点世界变换换算（S·T 均匀缩放平移逐位推导）**及两条渲染路径的动画相位来源比对（本轮新发现来源，见 #1）**、t1033 判等谓词与 finishMiningAt 床分支逐字对照 + destroySphere 变体单点覆盖确认、t1034 三分支门与同函数机关件八分支模式比对；矩阵各 final 日志 awk 实读（t1036=507、t1031=509、t1032=513、t1033=516、t1034=**519/0** 全绿，t1034_neg 恰红 3 腿 / t1032 两轮各恰红 1 腿 / 病灶签名 refused=2 与 stairs=1 在案）；app 证据链核对（build_app_t1033.log 05:58、voxelsandbox.exe mtime 09-11 08:42 > 全部源改动、smoke EXIT=124 + tail20 三份留存零 QML 错误）。
- 总评：**无高 / 中严重度新缺陷**，1 低（新）+ 1 低（carry-over 待办）+ 2 Info。五个功能 fix 主链（骨粉口径登记、NOTIFY 信号职责分离、野狼中立 + 驯服缝、形状族 instancing 批 2 + 空转门、爆炸毁床锚失效、门/床/活板门潜行旁路）逐项核对成立，review0910 #1（NOTIFY 参数语义）与 #3（空转恒走钟）正式清偿、review0909 #2 存量缺门清偿。唯一新发现是 t1032 把附魔台小书上移为 entRoot 直属后，**桶路径下书与台体的动画相位分属两套独立时钟**——静态变换换算逐位等价的主张成立，但动态相位面未被「观感一致」注释覆盖；dev-plan 已把「94 桶内/桶外书观感一致」列为待实机确认项，本轮源码级推导预测该项会红（修复建议见 #1）。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | 低 | **t1032 附魔台（94）桶路径下书-台动画相位脱同步**：dropBookNode 上移为 entRoot 直属（instancing 无法承载 per-instance 子树，方向正确），但其 bob/rotY 由 entRoot 的 QML 动画驱动——`NumberAnimation on rotY { duration:3000; running: entRoot.visible }` + `SequentialAnimation on bobY`（相位起点 = **delegate 创建时刻**）；而桶内台体由 feeder 解析式驱动——`ph = m_clock.elapsed()/1000 + slot×0.37`（相位起点 = **feeder 构造的绝对钟 + slot 错峰**）。两套相位独立且批 1 已登记口径变化（创建时刻 → slot×0.37s）。旧代码书是 shape Model 子级 → 台书恒同链同相；**桶路径下台走 feeder 相位、书走 entRoot 相位**：转速相同（360°/3s）→ 恒定旋转偏移（任意值 0–360°，书 V 口朝向与台体错开）；bob 同波形（0↔0.15、2s）异相 → 书相对台体垂直振荡幅度最大 0.15 格（书静态位置仅台顶 +0.138，反相时书心可低于台顶 → 嵌入台体观感）。delegate 路径（桶满溢出/桶释放）两链同相无此问题。触发条件：≥1 个 94 掉落物存活且 94 在形状桶 8 槽内 | Main.qml:6304-6308（dropBookNode position 读 `entRoot.bobY + 0.46*0.3`、随 entRoot eulerRotation）；:6653-6657 附近（entRoot `NumberAnimation on rotY`/`SequentialAnimation on bobY`，`running: entRoot.visible` 对桶内实体恒 true）；blockdropinstancing.cpp getInstanceBuffer（`ph = t + slot*0.37`、rotY = fmod(ph/3,1)×360、bob = 0.075×(1−cos(π·fmod(ph,2)))）；blockdropinstancing.h 头注释「相位口径变化（创建时刻 → slot×0.37s）已登记」；dev-plan t1032 待实机确认项「94 桶内/桶外书观感一致」 | **推荐：94 从形状桶指派中排除**——reassignShapeBuckets 计数循环 skip id 94（hasShapeBucket 对 94 恒 false → 台+书恒走 delegate 同链同相，旧观感逐位恢复；instancing 无法承载子树的正确推论就是 94 整体不桶化）。改动面：Main.qml reassignShapeBuckets 1 处 + P-t1032a「25 id 逐 id 入桶」腿同步改（94 改断言不桶化）+ dev-plan 登记；94 系低频掉落，合批损失可忽略。备选（不推荐）：书节点改解析式按 feeder 同公式驱动（需把相位公式复制进 QML，两处公式漂移风险）；或接受脱同步 + lessons 登记（观感影响面小，但「观感一致」注释须改写） |
| 2 | 低（carry-over 待办） | **review0910 #2 实机定向冒烟仍未执行，且批 2 后盲区翻倍**：reassignDropBuckets / hasBucket / reassignShapeBuckets / hasShapeBucket 两池均为纯 QML JS 编排（信号 handler 改表类），headless 探针不可达；本项目仍无 qt_add_qml_module（QML 语法错误构建期不报，仅 qmldir/qmlcachegen 弱覆盖）。t1032 收口已如实 scoped（headless 可达面做腿 + 源码钉 + 待实机项登记，处理诚实），但冒烟本身未跑。**溢出常态性加重**：3D 族 25 id > 8 桶——火把+活板门+多级台阶+多材质栅栏/门/蘑菇的正常游玩即可 >8 种活跃并存，溢出走 delegate 保底（不丢渲不双渲，正确性无虞）但合批收益损失面比批 1（16 色羊毛）更宽 | Main.qml:6770-6790（reassignShapeBuckets / hasShapeBucket 纯 JS）；dev-plan t1032「QML 编排面盲区如实 scoped 不虚报」+「待实机确认（观感类）：≥10 种 3D 族 id 定向冒烟（每掉落恰渲一次 + 94 书在位）、形状桶旋转/浮动动画一致性、94 桶内/桶外书观感一致、F3 draw call 前后对比」 | 随 R19.22 批收口（t1035 后）一次定向 in-world 冒烟覆盖两池：丢/破 ≥10 种 3D 族 id + ≥10 种整立方 id，核对每掉落恰渲一次、无 z-fight、桶释放/重指派无闪烁，顺带执行 #1 的 94 书核对（若 #1 已修则确认书台同步恢复）；「QML 编排面探针不可达」 lessons 盲区簿登记（review0910 #2 原项，批 2 后适用面 ×2） |
| 3 | Info | **`bedSpawnAnnounce(bool restored)` 参数恒 false**：唯一发射点 trySleepAt 字面量 `emit bedSpawnAnnounce(false)`；QML handler `if (player.bedSpawnValid && !restored)` 的 `!restored` 恒真（bedSpawnValid 在 emit 前已置真，双条件亦恒真）。参数是为「来源沿」语义预留，无任何真值消费面——与刚清偿的 review0910 #1（参数语义与类型系统约定冲突）同型但无冲突面（bedSpawnAnnounce 非 Q_PROPERTY NOTIFY，无属性新值约定约束）。无害 | playercontroller.cpp:3234（唯一 emit，字面量 false）；Main.qml:3147-3148（handler 双恒真条件） | 维持现状可（future-proof 预留）；若追求最小面可无参化 `bedSpawnAnnounce()` 同 bedSpawnLost 款，改 2 处 + 1 钉。非必改，登记即可 |
| 4 | Info（pre-existing，非本窗引入） | **爆炸毁半床 → 另一半残留浮空**：destroySphereSilent 逐格破坏，无床配对清除（玩家挖掘链 finishMiningAt 有 t428 双格联动，爆炸链没有）；小半径爆炸只毁一半时剩半张床浮空。MC 口径为另半掉落。t1033 只加了锚失效链（锚判定按单半语义正确），未触碰残留面 | world.cpp:2736-2826（destroySphereSilent 无 isBed 配对清除）；对照 playercontroller.cpp:1726-1730（挖掘链双格联动先例） | 登记后续批次评估（爆炸床配对清除 / 半床掉落语义）；与 t1033 锚链正交，不影响本窗收口 |

## 已核实安全面（审查结论，非发现）

- **d4be270（t1036，review0910 #1 清偿）**：`bedSpawnValidChanged` 无参化（纯属性 NOTIFY，Qt 6.11 约定回归）+ 播报源独立 `bedSpawnAnnounce`。全消费面迁移核对：4 个 emit 点（setBedSpawn / clearBedSpawn / sleepAdvanceToDawn 守卫沿 / trySleepAt 恒发沿）全部无参化且 announce 只在 trySleepAt 发（其余三沿静默语义逐沿注释+矩阵双信号钉在案）；QML handler 改名 onBedSpawnAnnounce（Main.qml:3147），其余 2 处消费为命令式现读不受影响；矩阵探针 dual-signal 记录 + pinSet 迁移（d0cd4b3），旧签名零残留（全库 grep 证）。**播报恰一次语义**：trySleepAt 恒发 announce + sleepAdvanceToDawn 幂等守卫沿不发 → 同一入睡会话不双播；读档回填/置假沿静默——review0909 #5 契约随迁保真。净矩阵 507（改写迁移不增腿）。
- **9bc2c73（t1031 野狼中立 + 驯服缝）**：(a) 测试缝 `m_tameRollOverride`（缺省 -1、`roll % 1000 / 1000.0`、同 m_wanderFrozen 先例）生产路径零调用、不持久化、负 roll 被 >= 0 守卫拦（无负模病）；(b) 未驯服分支收口为纯 aiWander——**残留面干净**：tick 分发 MobWolf 唯一入口 aiWolf（entitymanager.cpp:7787-7790），全库 `chasing = true` 写点仅 3 处（aiHostile/aiArcher/aiGolem 族内，狼不可达）；实体无序列化（无 QDataStream/saveEntities）→ 无跨会话 chasing 残留；tame 成功沿清 chasing/chaseTimer/attackCooldown + bump revision + 爱心（t831 链）；kWolfDetectRange 常量连注释整体移除，零死引用（grep 证）；QML onMobAttackedPlayer 无 MobWolf 特判消费（退役后无死代码）；MC 1.0 野狼中进口径正确，「被激怒反击面登记不做」为明示简化（代价：打野狼零风险——口径自洽登记）。(c) 中立化与驯兽语义冲突消除（旧敌对面先咬后驯的自相矛盾）。
- **482b9fa（t1032 批 2 + 空转门，本窗最大面 +242/−43）**：(a) **空转门（review0910 #3 清偿）沿完备**：entitiesChanged 是活体集变更完备沿（spawn/merge/removeAt/setCountAt/clearAll/tick 内 despawn/焚毁全经 notifyChanged 单点 emit——itementitymanager.cpp:90/139/172/214/246/289/395/407/674/692 十点核对；tick 物理 dirty 尾发同样覆盖）；hasLiveMember 与 getInstanceBuffer 收纳谓词同源同参（familyId 粒度双模式）；setManager 换源先 disconnect 防双投递；m_clock 恒走 → 重启钟后相位连续无跳变；P-t1032c 状态机腿（构造/接线/指派无活体三停 + 首活体沿启动 + 泵 50ms 活跃保持 + 拾走停）+ 阴性轮②摘门恰红实证。批 1 池同批收口（16 feeder 全量，~500 次/秒空唤醒归零）。(b) **桶池编排与批 1 逐字同构**：确定性 tiebreak（活体数降序 + id 升序）、整体数组替换保 notify、Component.onCompleted 兜底、kept 不让位（review0910 #4 维持 + minCount=2 钉）；桶 Model 单 ItemShapeGeometry 单材质全实例共享、Model 本体 transform 恒 identity；材质五参（NoLighting/Mask/0.5/0.99/voxelAtlas/天光乘子）与 delegate ItemShapeGeometry 分支逐字同参（Main.qml:6282-6289 比对）。(c) **书节点静态换算逐位等价**：旧 T(0,bobY,0)·S(0.3)·T(0,0.46,0) ≡ 新 T(0,bobY+0.138,0)·S(0.3)（均匀缩放下 S·T = 缩放平移向量，推导成立）；页 scale/position 原样不预缩放（review27 #9 口径）；两路径（桶内/桶外）书均渲染不丢。残余风险即发现 #1（动态相位）。(d) 25 id 跨族互斥（楼梯 16 排除、torch 不进整立方桶双模式钉）、几何共享等价断言（同 id 两实例 vertexData 全等）。
- **12f4558（t1033 爆炸毁床锚失效）**：判等谓词与 finishMiningAt 床分支（playercontroller.cpp:1726-1742）**逐字同构**（y 同层 ∧ 本格∨配对半 == 锚格）；state 毁前 capture（t134 门：id 变更重置 state=0——destroySphereSilent 在 setBlock(Air) 前登记，bedPartnerOffset 单一权威）；destroySphereSilent 单点覆盖两爆炸入口（TNT detonateTntSphere / 苦力怕 detonateStalker——world.h 全库无第二 sphere 破坏函数）；信号在世界写收口后发（消费者见终态零重入）；双半同毁恰一次播报（首发清锚翻假、次发 !valid 早退）；玩家挖掘链 + 爆炸链同汇 clearBedSpawn 不双播；岩浆/水冲/换代三自然路径不毁床的豁免论证核对成立（flammable/isAttachableBlock 表不含床、seedChanged 既有链）。setWorld UniqueConnection 同 seedChanged 模式。
- **181ad6d（t1034，review0909 #2 存量清偿）**：门/床/活板门三分支 `!sneakPlace` 前置与同函数机关件八分支（工作台/熔炉/箱子/附魔台/铁砧/发射器/投掷器/音符盒）模式逐字对齐；**床门加在 placeBlock 分支头而非 trySleepAt 内**（潜行放置整链优先、trySleepAt 直调面语义不变——选型正确）；空手潜行 → 下方 m_selectedBlock==Air 守卫拦（t1028 口径）；铁门 fall-through 放置不受影响（t722）；t523 注释旧口径「门/活版门/床不绕过」同步改写（旁路口径统一：机关/浆果丛/末地门/传送门仍不绕过——注释与代码一致性核对成立）；MC 潜行右键旁路口径正确。阴性轮病灶签名（refused=2 = 潜行点击进睡链）实证敏感。
- **d8e5756（t1030 纯注释）**：成熟骨粉「无效应不消耗」口径登记（MC Java 对齐：骨丸只在有效时应消耗），早退语义本就正确，零行为改动。
- **测试腿 6 笔 + docs 7 笔**：测试全部只动 tools/redstone_matrix_test.cpp 无 src 触碰；docs 只动 docs/（dev-plan ✅✅ 回标 + agent-state 同步——新纪律执行到位，7/7）。**矩阵证据链**：505→507（t1030）→507（t1036 改写迁移）→509（t1031）→513（t1032）→516（t1033）→**519（t1034）**，各 final 日志 awk 实读全绿（total FAIL: 0）；阴性轮 t1032 两轮各恰红 1 腿（①形状过滤摘 → 楼梯混入 diag=1；②空转门摘）、t1034 三摘恰红 3 腿、t1033/t1031/t1036 neg 在案——摘即红敏感面实证。**app 证据链**：build_app_t1033.log（05:58）+ voxelsandbox.exe mtime 09-11 08:42 > 全部源改动（t1034 无独立 build log 但 binary mtime 核对通过）+ smoke EXIT=124 存活 12s ×3 + tail20 留存（零 QML/C++ 错误）；smoke_*.log 0 字节系 stdout 实走 voxelsandbox.log 的既登记现象，tail20 纪律（review0910 #2 首例）已连续三任务执行。

## 工作区状态注记（非本窗提交，供续接棒）

- `tools/redstone_matrix_test.cpp` +114 行未提交：**t1035 豹猫驯服分化探针骨架**（P-t1035a-e 五腿设计 + 阴性轮敏感标注已具；盘点结论按生产现状钉——t878 跟随/t963 项圈已先行演化掉 dev-plan 过时设想，行为零改动；include loottable.h）。agent-state Recovery Point 未及更新（next_task: t1035 已登记）。续接棒：先编译再续写，探针齐后按惯例单 commit。
- `.codex/` 外部工具产物：豁免口径不变（不删不提交）。

## 清偿状态

| 项 | 处置 |
|---|------|
| review0910 #1（NOTIFY 参数语义） | **已清偿**（t1036 无参化 + 播报源分离，全消费面迁移 + 双信号钉，本轮复核零残留） |
| review0910 #2（桶溢出运行期验证盲区 + 冒烟证据链） | **部分清偿**（headless 可达腿 + 源码钉 + tail20 纪律已立；实机定向冒烟待办 → 本文件 #2，批 2 后盲区 ×2） |
| review0910 #3（空转恒走钟） | **已清偿**（t1032 空转门两池同款 + probeTickerActive + 摘门恰红阴性轮；t858 XpOrb 单 feeder 维持登记） |
| review0910 #4（桶不让位） | **维持登记**（kept 钉 minCount=2 在位，实测劣化再评估） |
| review0909 #2 存量（门/床/活板门缺门） | **已清偿**（t1034 三分支 + 两向探针 + 阴性轮病灶签名） |
| 本文件 #1（94 书-台相位） | **待定夺**（修法：94 排除出形状桶，面小；或接受 + 注释改写——建议随 t1035 后批收口顺带，勿混入 t1035 单 commit） |
| 本文件 #3 / #4 | 登记（dead 参数无冲突面；爆炸半床残留 pre-existing） |
