# Code Review — 2026-08-24（近 24 小时提交）

- **审查范围**：`1727847..1bf9168`（2026-08-23 21:50 → 2026-08-24 08:19，共 24 个提交；12 个纯 docs 提交只改 `docs/dev-plan.md` 与 Review 标记，已跳过，实际审查 **12 个代码提交**）。本窗口提交几乎全部是 Review_2026-08-23 各条（#1-#36）的修复批次 + t813 版本戳 / t814 红石探针 / t822 铁砧探针 / t852+t853 死亡链四个新功能。24 小时窗口更早的部分（08-23 08:33–21:50）已由 `Review_2026-08-23.md` 覆盖，不重复。
- **审查基线**：HEAD = `1bf9168`，工作区干净。行号均以 HEAD 为准。
- **方法**：7 路并行子系统审查（逐提交读全量 diff + HEAD 交叉核对 + 矩阵探针断言强度评估）+ 主审对 7 条关键中危逐条亲核（world.cpp 火门联动 / clearBlockSilent 时序 / rpm revision 顺序 / Main.qml onDied / ResourceBrowser -1 路由 / 探针正则 / entitySource 直返——**全部属实**）。
- **结论速览**：**高 0 / 中 10 / 低 25（含 4 条备注）**。昨日 Review #1-#36 的修复批次本体全部判定**有效非死码**（消费端切换完整、防线数学成立、探针真实驱动生产路径）；本次发现集中在修复自身的边角缺口：火抑制层同循环内一个漏改的兄弟写点（#1）、两处静默清格路径的口径破绽（#2/#3）、贴图缓存 revision 的时序窗口（#4/#5）、诊断信号污染（#6）、死亡链 try/finally 的残余开口（#7/#8）、探针自身的误 PASS 面（#9/#10 及低危若干）。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高 | 0 | — |
| 中 | 10 | #1 火吞门配对半扇绕过防火带；#2 红石火把幽灵网格；#4 mobhead 首次切包陈旧；#7 onDeath 首行在 try 外 |
| 低 | 25 | 见第三节 |

---

## 一、中危（建议尽快修复）

### #1 `7b83fbd` (review-g) 门配对半扇强制点燃绕过湿燃料防火带 —— 不变式被同循环内漏改的兄弟写点打破

- **提交**：7b83fbd fix(review-g): fire spread suppression layer（Review #5 修复）。
- **问题**：湿燃料防火带判定只覆盖本窗掷中的主目标 `(nx,ny,nz)`（`world.cpp:1529`）；其下 15 行的门配对联动写 `setBlock(nx, py, nz, Fire)`（`world.cpp:1541-1545`）**无任何 `fireWaterNeighborAt` 检查**。门两半各占一格，水通常只邻其中一半（护城河沿的门、船坞门）：火从干半扇侧逼近掷中 → 被水保护的湿半扇被连带焚毁（`setBlock(Fire)` 走放置语义、无掉落、**不可逆**）。新生火格虽会因邻水 ~1.25s 内被 40%/窗抑制熄灭，但方块本体已永久损失——恰是防火带承诺要阻止的损害。
- **证据**：`src/World/world.cpp:1529`（主目标湿判）vs `:1541-1545`（配对写无判）；`BlockRegistry::flammable` 含木门（`blockregistry.cpp:945`）。主审亲核。
- **修复**：配对写前同样过 `fireWaterNeighborAt(nx, py, nz)`；更贴合 #16「门作为整体燃烧」语义的做法是把湿判定提前为「主目标**或其配对半扇**含水 → 整扇不燃」。

✅ 已修（d97f465）：湿判提前为整扇口径（主目标或配对半扇含水 → `igniteFlammableAt` 整扇 return false，三入口统一拦）+ (d) 烧毁收尾兜底对湿对偶跳过（点燃后才泼水的边角同护）。矩阵探针 r24#1（600 蔓延窗整扇存活 + 烧尽兜底对照组）。

### #2 `42d0136` (review-c) clearBlockSilent 路径：recheck 火把分支静默清格发生在 worldChanged/clearAllDirty **之后** → 红石火把幽灵网格残留

- **问题**：`recheckAttachmentsAfterClear` 的火把分支刻意不 emit，注释承诺「两 caller 末尾的批量收口 emit 覆盖」——对 dropGravityColumn 成立（emit 在循环之后），对 clearBlockSilent **不成立**：`emit worldChanged()`（`world.cpp:672`）与 `clearAllDirty()`（`:673`）在 `recheckAttachmentsAfterClear`（`:679`）**之前**。若上方无沙柱，`:680-682` 的三个钩子均不再 emit。红石火把（129）是 chunk mesh 几何（`partialblockgeometry.cpp:766`），其格虽被直写标脏（`:2384-2392`）但已错过本次重建信号 → **视觉幽灵火把残留**（同时掉落物已生成 = 一把火把两处可见），直到下一次任意编辑才被顺带重建。普通火把（13）是 QML delegate、由 `blockBroken` 信号移除，不受影响。
- **触发面**：TNT 点火三路径（`playercontroller.cpp:2951/4866/5118`）与水下链式引燃（`entitymanager.cpp:4224`），火把/红石火把可附着在 TNT（ShapeFull）上。
- **证据**：`src/World/world.cpp:672-682`、`:2384-2392`。主审亲核。修复前旧三件套（压力板/铁轨/雪层）各自自 emit（`:2218-2219`/`:2528-2531`/`:2304-2305`），无此残留——是本次「收口到 recheck」新引入的口径破绽。
- **修复**：recheck 火把分支在「实际掉落 ≥1 个火把」时自 emit `worldChanged` + `clearAllDirty`（与 check\* 兄弟同口径）；或把 clearBlockSilent 的 recheck 上移到 `:672` 之前（dropGravityColumn 侧 emit 本就在循环后，两 caller 都需复核）。

✅ 已修（d97f465）：取推荐修法一——火把分支实际掉落 ≥1 时自 emit（无掉落零 emit）；两 caller 复核过：dropGravityColumn 至多叠加 1 次幂等 emit（destroySphereSilent 逐格调 check* 先例），clearBlockSilent 主 emit 在前、自 emit 在后恰补重建信号。矩阵探针 r24#2（掉落信号之后必须观测到 worldChanged 的时序断言）。

### #3 `42d0136` (review-c) 「口径合一」漏改两条兄弟路径：destroySphereSilent 与 tickLavaFlow 焚毁 —— #15 轨悬浮与 #27 熄门经岩浆/爆炸路径存活

- **问题**：提交注释宣称「未来新增附着物只扩 recheck 一处」（`world.cpp:676-677`），但两条同类静默清格路径未收口：
  - `destroySphereSilent`（爆炸）逐破坏格仍只补 rail/snow/gravity 三钩子（`world.cpp:2074-2079`）——爆炸掀掉仙人掌/枯灌木/花/压力板/甘蔗的支撑后仍悬空（火把族由 EntityManager 单独兜底）；
  - `tickLavaFlow` 焚毁路径逐格只补 cactus/deadbush/sugarcane/snowlayer/gravity（`world.cpp:1427-1445`），**不含 checkRailOnEdit / 压力板 / 花，也不含 2faefb1 #27 新加的 `breakNetherPortalsAround`**——岩浆焚毁铁轨支撑后轨照样悬浮（#15 病征的另一入口）、岩浆烧掉门面邻格可燃木不熄门（与火吞噬同块木语义分叉）。
- **非新回归**（两路径修前即如此），但与「单点扩展」承诺相悖，且 #15/#27 的失败模式在这些路径存活。两条路径的批量收口 emit 均在循环之后，追加调用不会重蹈 #2。
- **修复**：两处逐格循环改为（或追加）`recheckAttachmentsAfterClear(d.x, d.y, d.z, oldId)` + burnTargets 循环内补 `breakNetherPortalsAround`。

✅ 已修（d97f465）：两循环原三/四钩子替换为 recheck 单一收口（严格超集：+仙人掌/枯灌木/花/压力板/甘蔗/轨/活板门门/6 邻火把族）；岩浆焚毁循环补 `breakNetherPortalsAround`。重力复检仍显式补调（recheck 刻意不含，防柱内重入；drop\*Column 不调 recheck → 无递归无双掉）。矩阵探针 r24#3（爆炸掀支撑 → 球外压力板+轨掉落；岩浆焚毁木板支撑 → 轨掉落）。

### #4 `c9dd880` (review-e) mobhead 头像族：apply() 先重建后 ++revision → 冷启动后**首次**切包仍同名覆盖 `_r0.png`，Review #6 陈旧 bug 在第一个切包窗口残留

- **问题**：mobhead 图标是**构建期**生成的（`ensureBuiltLocked` 内传 `s.revision`，`resourcepackmanager.cpp:2021`），而 `apply()` 顺序是 `ensureBuiltLocked()`（用**旧** revision 生成，`:2154`）→ `++s.revision`（`:2155`）。启动后的首次构建走懒查询路径（revision=0、**不自增**）：用户第一次切包时重建仍写 `_r0.png` 同名原地覆盖 → `mobHeadIconSource` 缓存命中直返同一 URL（`:3662-3664`，无 `?r=` 查询串）→ QML Image 按 URL 缓存不重载，图鉴继续显示旧包头像。第二次切包起（写 `_r1.png`）才自愈。woolface 族不受影响（纯懒生成、用后增值，`:3344`）。
- **证据**：`src/Core/resourcepackmanager.cpp:2153-2155`、`:2021`、`:3549-3550`、`:3662-3664`。主审亲核（apply 顺序）。
- **修复**：`++s.revision` 移到 `ensureBuiltLocked()` 之前；矩阵探针补「模拟启动懒构建(rev0) → apply 切包 → 断言 URL 变化」时序用例（现探针只测两次直调生成器，没锁 apply 时序）。

### #5 `8886325` (review-a) #12 直返 cache-bust 只修了皮肤族，四族同构直返路径全部漏改

- **问题**：#12 的失效场景是「pack 原路径不变、原地换包内容 → `file:///` URL 不变 → Quick3D Texture 按 URL 复用旧像素直到重启」。`playerSkinSource` 的 `h==w/2` 直返路径加了 `?r=<revision>`（`resourcepackmanager.cpp:3234`），但同构直返路径还有四族全都没带 revision：`entitySource` probe 直返（`:3024-3034`）、`mobTextureSource`（`:3316-3323`）、`effectIconSource`（`:2946-2951`）、`paintingSource`（`:2970-2973`）。同场景（同路径改包内容 + 重 apply）下这些一样陈旧——修复不完整，且后续维护者会误以为「直返路径都带 cache-bust」是既定模式。
- **证据**：主审亲核 `:3024-3034`（probe lambda 直返 `file:///` + sub，无查询串）。
- **修复**：抽 `packFileUrl(path, revision)` 帮助函数统一补 `?r=`；或在 #12 注释明确「其余直返族按 miss-不陈旧论暂不处理」的决策依据。

### #6 `2faefb1` (review-f) #35 qWarning 兜底把图鉴合法的 -1 输入判成接线 bug → 假警报污染 #35 专门建立的诊断信号

- **问题**：#35 把 setMobType 的静默钳猪改成 `qWarning + 兜底 Pig`（`mobmodel.cpp:370-374`），但没审计 QML 侧合法传入 -1 的路径：ResourceBrowser 的 `selectedMobType` 在未选生物/生物蛋时是 **-1**（`ResourceBrowser.qml:190-191`），而承载 MobModel 的 Node 只用 `visible:` 门控（`:771`，QML 对象始终实例化、绑定恒求值），`mobType: root.selectedMobType`（`:778-779`）→ 每次打开图鉴选非生物条目、每次 mob↔非 mob 切换都刷一条「has no geometry branch (not in k wiring bug)」告警。同 delegate 的 `mobPreviewCentY/Scale` 都把 -1 当预期输入优雅返 0（`:186`），证明 -1 是合法状态——此告警是误报。Main.qml 侧无此问题（mobType 0 走 UnitCube、刷怪笼默认 4）。
- **证据**：主审亲核三处。
- **修复**：`mobType: Math.max(1, root.selectedMobType)`，或 `Loader { active: root.selectedIsMob }` 包裹，或 setMobType 对 -1 静默兜底（区分「无选择」与「哨兵/越界」）。

### #7 `1bf9168` (t852) onDied 首行 `progress.onDeath()` 仍在 try/finally 保护伞外 —— 与已修根因同构的残余开口

- **问题**：本次修复的结构主张是「辅助段任何异常不得吞掉死亡契约」，但 `progress.onDeath()`（`Main.qml:9552`）位于 `try {`（`:9564`）**之前**。若未来该行抛 TypeError（PlayerProgress API 改名 / 属性化误加括号——正是 t312 教训的同款笔误形态），onDied 在进入 try 前中断，`dropAllItems`/`release`/`m_dead` 置位全部不执行，t852 四症状整套复发，try/finally 完全不设防。
- **证据**：`src/ui/Main.qml:9551-9564`；`playerprogress.h:106` 当前为 `Q_INVOKABLE void onDeath()`（今日安全，防护目标是未来异常）。主审亲核。
- **修复**：`progress.onDeath()` 移入 try 块首行（统计属辅助非契约）。

### #8 `1bf9168` (t853) 死亡态 ESC 的 C++ eventFilter 逃生口与 QML 注释不变量相悖 —— 按注释收紧会造成真死锁

- **问题**：keyInput 死亡闸门（`Main.qml:10037-10040`）只在事件到达 QML 时吞 ESC；而 `PlayerController::eventFilter`（`playercontroller.cpp:577`）在 `m_captured` 时先吃 ESC 直接 `release(); return true`，该路径**无 m_dead 判断**。现状这恰好是纵深场景（死亡但 captured 残留）的自救口，行为正确；但 `Main.qml:10033-10036` 注释宣称「死亡态按 ESC 至多无效」与实际机制不符——不变量实际由 finally 里的 release 而非闸门保证。若未来有人依据注释把 eventFilter 的 ESC 分支也按 m_dead 拒绝，纵深场景将变成**真死锁**（视角冻结 + 指针不可见 + ESC 无效 + 按钮不可点）。
- **修复**：双向钉死语义——eventFilter ESC 分支与 Main.qml 注释明确「死亡态 ESC 允许 release（指针逃生口）」，或在注释里显式记录 captured 残留场景靠此路径自救。

### #9 `5c1b0fe` (t814) 红石探针 (e) 冷却断言无时间语义 → 发射器冷却时长回归不可见（误 PASS 面）

- **问题**（两层）：
  1. 冷却递减只发生在 `scanDispenserTraps`（`playercontroller.cpp:4998-5002`），由 `m_timer` 驱动的 tick() 推进；探针 C++ 直造 PlayerController **不启 16ms 定时器**（探针注释自认），全程不触 tick → `m_dispenserCooldowns` 永不递减、永不过期。「sub-2s-cooldown re-power」断言（`redstone_matrix_test.cpp:6428`）实际验证的是「`contains` 即拦截」（`playercontroller.cpp:5071`），**与 2 秒无关**——`kDispenserCooldown` 被回归性改成 0 探针仍 PASS。
  2. `arrows3 == arrowsAfterB` 在 World 侧根本没发 `powerDispenserTriggered` 时也恒真（无信号发射计数），无法区分「冷却拦截」与「信号未发出」。
- **修复**：复置前显式推进冷却递减路径（临时驱动 tick 或等价递减），补「冷却过期后真上升沿会再发射」正向断言；给 `powerDispenserTriggered` 加计数器先断言信号确发出。

### #10 `fc8ec84` (t822) 探针两处基建缺陷：固定临时文件名并发互踩 + 手工序列镜像漂移

- **问题**：
  1. **t822b 固定临时文件名**：`%TEMP%/voxel_t822_probe.sqlite`（`redstone_matrix_test.cpp:6689`），前后各一次 `QFile::remove`。两个矩阵实例并发时 Windows 对被占用文件的 remove 静默失败 → 两进程共库互相 `INSERT OR REPLACE` 覆盖，或环境性假 FAIL，甚至因两边数据形状相同而巧合双 PASS 掩盖竞态本身。
  2. **t822a 手工序列镜像**：A1-A3 逐行重放 `AnvilUI.slotLeft`/`takeProduct` 对真 Hotbar VM 的调用序（`:6560` 起），但**跳过 `InventoryOps.resolveClick/readSlot/writeSlot` 的 JS 分派**（真实代码里 `r.heldEnch/r.heldDur` 等恰由 resolveClick 计算——正是用户症状最可能的根因位），且镜像与 QML 源的同步纯靠人工注释（引用行号随 QML 演进漂移）。AnvilUI 未来改调用序（例如漏 `heldDurability`——正是这类丢失 bug 的典型形态）→ 探针按旧序列继续 PASS，掩盖真回归。
- **修复**：文件名拼 `QCoreApplication::applicationPid()` 或改 `QTemporaryDir`；A 段注释加「与 AnvilUI.qml 同步义务」显式警示，中期用 qml.exe 挂真 Hotbar C++ 对象替换 t792 的桩消掉拼接缝。

---

## 二、低危（按提交分组）

### 7b83fbd（火抑制层）

- **火格自身邻水检测是内联副本而非谓词**：pass (a) 的自身检测（`world.cpp:1490-1497`）与 `fireWaterNeighborAt`（`:1578-1588`）双实现今日语义等价，但提交信息/world.h:334「一判定两用」宣称不成立——t843 火语义重做时只改谓词会漏掉自熄侧。建议二选一：兜底调用谓词，或注释改「语义镜像、非单一实现」。
- **抑制态未联动 pass (c) 上窜**：`suppressed` 只影响 (a)(b)，(c) 上窜仍全率 3% 掷（`world.cpp:1549-1556`）。影响小（新火格下窗即被抑制），但与层语义不对称；建议同步减半或注释声明豁免。

### 42d0136（world tick/block）

- **二级级联柱静默清格不递归复检**：`dropSnowLayerColumn`/`dropSugarcaneColumn`/`dropCactusColumn`（`world.cpp:2282-2297`/`:2227-2245`/`:2092-2110`）纯静默直写。雪层 `isCollidable` 特例 true → 立式火把可放雪层上：沙柱坍落 → 雪柱整列清空 → **柱顶火把悬空**（还发光）。建议 drop\*Column 每清一格补 recheck（火把不支撑任何方块，无递归风险）。
- **#19 `isCollidable ∨ isFullCube` 覆盖面超出提交列举**：睡莲（ShapeNone 但 isCollidable 特例，`blockregistry.cpp:1373`）、压力板、活版门都算「实体面」——栅栏向睡莲伸横档、雪傀儡踩睡莲水面铺雪、画可钉门面。`partialblockgeometry.cpp:230` 新注释「ShapeNone 仍不连」与睡莲行为矛盾。建议注释如实写「isCollidable 判定（含睡莲特例）」或谓词显式排除 LilyPad。
- **#16 门配对只验 `isDoor(partner)`** 不核对 bit3 互补/同材质（`world.cpp:1541-1544`）：损坏档/第三方写入的异常 state 下可能烧掉无关门半扇。可加互补校验收口。
- **P24 未覆盖**柱中段格的侧附着、二级柱上附着、ladder/lever/button 族。

### c9dd880（sheep/dye/recipe）

- **`docs/item-ids.md` 未同步 0x20E 退役**（`:263`、`:306` 仍称「杀羊掉落」），按文档排查掉落链会误判。
- **`QFile::remove` 返回值全忽略**（`resourcepackmanager.cpp:3560/3566/3630/3637`）：Windows 占用竞态下旧 `_r*.png` 遗留一轮靠下次 apply 自愈（正确性无影响）；建议 remove 失败 `qCWarning` 留痕。矩阵探针 (c) 写**真实 AppLocalData** 并清掉当前真身——与运行中游戏并发靠 exists-miss 重生成自愈成立，但建议 `QStandardPaths::setTestModeEnabled` 隔离。

### 8886325（skin/render）

- **#11 清理只挂 64×64 裁切路径**（`resourcepackmanager.cpp:3254-3266` 在 `cropped.save()` 之后）：从 64×64 包切到 64×32 包走直返（`:3229-3234`）不落盘也不清理 → 旧 `_rN.png` 与 legacy 文件永久残留。建议清理挪到 `ensureBuiltLocked()` reset 段（`:1708` 附近，持锁必经）。
- **三处镜像文档未同步**：`playerskinbox.h:46-49`（slim 属性文档仍教 3×12×3/u52..56 错误数值）、`resourcepackmanager.h:204-207`（仍写旧探测区 u[52,54) 死区）、`Main.qml:2072-2073`（「臂/腿盒区仅 3px 宽」——腿已改回 4px）。正是本提交 lessons-learned 自述的「跨文件数值镜像单边改」事故模式在文档层复发。
- **probeSlimSkinLayout 无高度下界守卫**（`resourcepackmanager.cpp:3164-3167`）：64×16 图循环零次执行 → 空真判 slim（与「保守方向」注释相反）。当前消费端有 guard 无实害；建议补 `height < 32*sc → classic` + 探针 64×16 用例。
- **#13 UV 翻转对燃烬者「棒条」盒的 pack 关路径也生效**（`mobmodel.cpp:135`/`:889-896`）：提交只验证了「头面对称无差异」，棒条侧面若程序贴图有左右不对称渐变会镜像。跑一次目检并如实记录验证范围即可。

### 5c4b02d（static_assert）

- **只钉下界 63**（`recipe.cpp:1200`），未钉上界 77 与 15 步连续性：63..77 段紧邻床段，未来插入新方块/重排色序 → QML 掉错色且矩阵全绿。建议补 `LastWoolVariant == 77` 与 `FirstWoolVariant + 14 == LastWoolVariant`。

### f9abcc6（minecart，无高中危）

- **参数换位陷阱**：`scanRailColumn(world, cx, topY, cz)` vs `scanRailColumnRiding(world, cx, cz, topY, ...)`（`minecartmanager.cpp:217`/`:240`）——cz 与 topY 互换编译期不可见。当前 5 调用点全对；建议统一参数顺序。
- **`kCartCenterSnapPerTick` 实为 per call**（`:479` 每次调用重置，渲染帧率驱动）：144Hz 下 35ms 收敛、20fps 卡顿下 0.5s——注释「~5 tick (83ms)」口径不符，防跳效果随帧率漂移。建议预算乘 `dt/名义tick` 或注释登记。
- **「同一张面」宣称过强**：双线性 vs mesher 三角剖分在非共面拐角（≥3 侧邻轨抬高）中心差 ≤0.25 格——与旧版误差同量级非回归，注释补一句即可。
- **宽容支路隐式扩窗到 topY-3**（`:243` 循环窗 topY-2，`:247-249` 查 y-1）：当前几何不可达（邻列轨层差 ∈[-1,+1]），死码级注释失配；建议补 `y-1 >= topY-2` 约束。
- **P21(b) pitch 断言对失联闪 0 不敏感**：46° 容差与症状（45→0→45 单跳 45°）同量级，靠 (a) yOk 兜底才不漏。建议改断「坡中段 |p|>40 持续性」。

### 2faefb1（review-f）

- **#28 记账写入在 damageEntity 之后**（`entitymanager.cpp:5319-5320`）：当前安全（damageEntity 只 emit 不增删槽），但未来 QML handler 同步 spawn 会 `push_back` realloc → `Entity &m` 悬垂 UB。一行换序（先记账后扣血）零成本消除整类风险。
- **#35 查表无 static_assert 钉长度**（`mobmodel.cpp:342-362` 硬编码 19 项靠注释对位）：未来枚举中部插值 → 整体错位静默（t782 根因复刻，只是从钳猪变误告警+钳猪）。建议 `static_assert(sizeof(kValidMobModelType)/sizeof(bool) == EntityManager::MobAnvil + 1)`。
- **#31 敌对预算耗尽仍全量扫描**（`:1115` per-cage continue）：33³ ≈ 3.6 万次 blockAt/6s，量级可忽略且是被动笼继续刷的必要代价。仅记录。

### 18eeec2（spawn/progress）

- **spawnInitialMobs 钳制域在 width/depth<9 时反向**（`Main.qml:823-824`：`Math.max(4, Math.min(width-5, …))` 下界超上界恒得 4 → 越出栅格）：160×160 生产世界不触发，但 `World` 默认构造/矩阵测试可达小尺寸。另 Y 取 `heightAt+1` 不查水——**水面存档点**时三只 mob 生成在湖床被淹，「进世界必见三物种」恰在远离出生列的存档点落空。建议对称钳制 + 生成列遇水沿环扫挪陆列。
- **adoptSpawnColumn 注释漏计第四写点**（`playercontroller.cpp:141-143` 声称仅三写，实际 `:189-196` onWorldSeedChanged 是第四写——写的正是 pristine 常量，判据不受影响）：注释是判据正确性的论证载体，补一句。

### 46e1a55（版本戳）

- **t813 探针对 `nogit` 判 FAIL 与 CMake 回退语义自相矛盾**：`WriteBuildStamp.cmake:29-44` 无 git 时诚实回退 `"nogit"` 且「构建不因此失败」，但探针硬套 `^[0-9a-f]{7,10}$`（`redstone_matrix_test.cpp:6311-6313`）→ 无 .git 环境（源码导出包/CI 缓存构建）矩阵恒红。探针注释自称有意「诚实暴露优于静默糊弄」——两层各有道理但互斥，建议至少区分「环境缺 git」与「格式回归」（nogit 打 SKIP 或单独计数）。
- **`execute_process(git rev-parse)` 无 TIMEOUT**（`WriteBuildStamp.cmake:31-40`，configure 与构建 target 两处）：git 因仓库锁/杀软挂起时 configure/build 无限卡死且 `ERROR_QUIET` 吞掉诊断。建议 `TIMEOUT 5` 超时按 nogit 回退。
- **（备注）跨分钟构建两个 exe 都重链接**：设计取舍已在提交声明（stamp 变 → 重编 buildinfo.cpp → voxelsandbox 与 redstone_matrix_test 重链），增量构建延迟可感知，无正确性问题。

### 1bf9168（死亡链）

- **try 段中段异常跳过其后全部辅助清理**（`Main.qml:9577` 起）：`craftingTablePanel.returnCraftToHotbar()` 若抛，其后 survival/inventory 归还、`settingsOpen/progressOpen/statsOpen` 关闭、`returnHeldToHotbar()` 全跳——丢一串而非注释宣称的「至多一条」。物品面安全（held 有 dropAllItems 兜底），但面板标志残留时「立即重生」路径不清理 → 重生后设置/统计面板叠显。建议三行归还各自独立 try，或把纯 bool 面板标志清理移入 finally 首部。
- **t650 竞态在「背包满余数留槽」边界未封死**（`CraftingTableUI.qml:244`）：onDied 直调归还后槽可能非空；面板 visible 绑定延迟重求值触发的第二次归还若发生在 dropAllItems 清空背包之后，余数归还进已清空的背包 → 重生凭空多材料。触发需「死亡时合成格有物 + 背包满到 addToAny 溢出」，极端；但 Main.qml 注释宣称竞态已杀过强。建议死亡路径归还改「无条件清槽 + 余数直接 spawnItem」。
- **t852 探针断言缺口**（`redstone_matrix_test.cpp:1435-1500`）：掉落 y 坐标未断言（lambda 第二参匿名丢弃）；头盔附魔只断首元未断 `.size()==4`（剑有、不对称）；**QML 路由修复本身（裸 id/try-finally/ESC 闸门）零自动化覆盖**——下一个再写 `window.<panelId>` 的人不会被任何探针拦截。建议补两条断言 + dev-plan lessons 立「信号处理器首行必须在 try 外零调用」编码规约。

### 5c1b0fe / fc8ec84（探针）

- **(c) 场景绝对计数依赖未言明的前序清场前提**（`redstone_matrix_test.cpp:6452` 用 `items.count()==1` 绝对值、`:6479` 却用相对基线还专门注释了为什么）：(a) 的失撑补口可能生成掉落物而 (a) 只 `ents.clearAll()` 不清 items——拉杆附着语义一变即环境性假 FAIL。建议统一相对基线。
- **QML 转发层未动态执行**：探针用 C++ connect 镜像 Main.qml:9779/9783 的转发 handler（当下镜像等价成立），但 t814 头号怀疑点「QML 转发在真实 UI 状态下的活性」无动态覆盖。本探针的「洗冤」应严格理解为**仅 C++ 消费端**——建议 PASS 文案/注释显式标注。
- **rig 加固的 qFatal**：耗尽编号 off-by-one（`slotIdx` 已 ++）；且只防坐标越界这一条静默拒绝路径——`World::setBlock` 同 id 无变化早退（`world.cpp:453`）仍会静默吞 rig 放置。建议关键放置后回读 blockAt 校验落位。
- **QCoreApplication → QGuiApplication**（`:85`）：headless Linux 无 offscreen 平台时直接 fatal，211 个基线探针被动继承该要求。文件头注释登记即可。
- **源覆盖不全**：用户报三源（粉/块/火把）探针只驱动拉杆+红石块；消费端无差别可接受，注释说明取舍。
- **t822b vmEq 漏比 count 字段**（`:6751`）：「JSON→VM 灌入丢 count」回归在 VM 级漏检（JSON 级 slotEq 可兜存档层）。一行可补。
- **（备注）"saves/ untouched" 措辞**：dbPath 的 mkpath 写权限探测会确保 `<工程根>/saves` 目录存在（无数据写入），严格说只是「无数据污染」。

---

## 三、修复质量与专项核验（通过项）

- **8886325 (review-a)**：slim 探测区 u[54,56) 与两表条带右缘精确互推（classic 56 / slim 54，v 区两布局同 20..32），4 条 static_assert 与表值逐项一致，HD 整数倍缩放无取整损失（非整数倍截断偏保守 classic）；#10 预览头钳与 Main.qml headNode 同序同值；#13 翻转表 armorlayerbox/playerskinbox 24 角点逐字相同、`writeMobUV` 是 mobmodel 唯一 box-UV 插值点；#14 宽度门 Core/QML 两态同用 `>64` 无 off-by-one；legacy 文件名排除精确比较无误配。**High#1 修复有效**。
- **5c4b02d**：断言与当时 QML 字面量逐字一致；QML 写死处全量排查（sheepWoolDropId 单点收口、woolPalette 无方块 id）；c9dd880 正确重钉为 27/63/0x20E 三连，契约链在 HEAD 未断。
- **f9abcc6 (review-b)**：**无高中危**。5 消费端切换完整（严格版仅存探测器占用 1 处，全仓 grep 核实）；隔板假支撑防线数学恒拒（差值 ∈[0.9375,1.9375] > 0.5，含坡顶最坏情形）；双线性角点与 mesher armLift 逐角点同源、直臂线性分支未动；pitch 钳对称不丢方向；渐进收敛必达（拐角到心落位自动清旧轴偏移、死端不经预算）；探测器帧差分清位无永久带电。
- **42d0136 (review-c)**：recheck 主逻辑与 setBlock 破坏路径口径逐位一致（oldId/守卫/火把偏移解码同源）；无双掉（主入口不调 recheck、`!isRail(oldId)` 防直破双掉）；无递归（刻意不含 gravity 钩子，静默直写不回调）；性能每清格 O(7 早退 + 6 邻) 相对 recomputeLightAround 可忽略。#15 守卫覆盖冰融/焚毁/异形替换；#16 先快照后 setBlock 规避 t134 陷阱、铁门天然不触发；#17 钳界数学核实无共面 z-fight；#18 两处字面量与注册表一致；#19 复合谓词 ⊇ 旧谓词无收紧回归。
- **c9dd880 (review-e)**：**0x20E 退役彻底**（全工程仅剩常量/名字/图标/pack 映射/断言/反向探针，无掉落源与配方消费；旧档物品可显示不可合成=声明的兼容策略）；Glass dropId 全消费端（挖掘/爆炸/级联/作物）统一走表无硬编码残留，自掉系有意偏离 MC 且有生存链闭环论证；bed_red 无多重集冲突；剪毛死亡快照先于延迟 emit、shearSheep 有 dead 守卫；腿罩枢/摆角/幅度/尺寸与本体逐值对位、剪毛随父 Loader 卸载；清理 pattern 数字边界不误删（1 与 11 不互配）。
- **2faefb1 (review-f)**：**#27 七入口全覆盖**（setBlock×2/FromEntity/clearBlockSilent/setWaterSilent（批量早退**之前**）/setBlockSilent/destroySphereSilent/dropGravityColumn）、无递归（`m_inRemoveNetherPortal` 守卫）、点火不自杀（填 Air 内腔不触发）、同 id 早退谓词安全；#28 spawnSerial 全局单调、槽复用清零、同帧多目标各结算一次；#29 窄盒只换 XZ 铁砧侧半宽、mob 用自身碰撞箱无系统性漏压、Y 扫掠防穿透保留；#30 wantWater 上界守卫正确；#31 三重闸门齐留；#35 表-枚举-rebuild 三方逐值一致；#36 量化 6°/步与 48° 精确整除、两消费端对齐。
- **7b83fbd (review-g)**：sky 光独立通道（火把 blockLight 不误判露天）、Fire 自身 lightOpacity=0 不遮自格、叶层 14<15 不误抑制；本引擎有燃料的火本就永不自熄（无「烧完」下界可削弱），40%/窗方向正确且比 MC 水邻即灭更慢；`kFireSpreadPermille` 全仓唯一消费点、FNV-1a 掷点 %1000 模偏差可忽略、三盐互异；湿燃料每窗现读 blockAt 无状态残留（水抽走即解除）；P21a 统计带与常量同步。
- **18eeec2 (review-d)**：resnap 时序链正确（beginLoad→finishLoad→applyPlayerState→adoptSpawnColumn，loadSavedState 不写 m_spawnPos 已核）；seedChanged 无加载中误发（尺寸不入存档、生产无 setSize 调用、消费端幂等复位）；farmer 回放旧档缺字段默认 0 不触发、回放幂等补齐祖先、guard 防成环；三件套 clearAll 先于 spawn 无双重生成。
- **46e1a55 (t813)**：三层生成链正确（configure bootstrap / 恒跑 target / 内容变才写保 mtime，不触发全量重编）；BuildInfo 单例 CONSTANT 注册链同 FrameProfiler 先例；QML 三处消费可达（MainMenu 自 import 合法、F3 在 10Hz Timer 路径无绑定活性问题）。
- **1bf9168 (t852/t853)**：**根因诊断成立**（window.\<id\> 恒 undefined → TypeError 静默掐断，t603/t312 病理）；m_dead 解锁路径完备无永久锁死（立即重生/回主菜单/再进世界三路都有 unlock，暂停菜单死亡态不可达双封）；掉落无重复（归还先清槽幂等 + dropAllItems 先 emit 后清空 + held 兜底）；全文件已无 `window.<面板id>` 残留（grep 零命中）；探针 C++ 本体四段断言强（附魔/实例名/耐久逐参 + 幂等）。
- **5c1b0fe / fc8ec84**：**真源直编非复制品**（playercontroller/dispenserstore/worldstore/hotbar 全是生产本体）；(a)-(d) 断言有实质判据（格心 1e-3、库存扣减、实体计数，无恒真/注释掉/catch-all 吞错）；SQLite 往返真落盘（close→open 重开）；nextSlot 加固数学正确；playercontroller.h 仅 Q_INVOKABLE 访问段移动，**零行为风险**。

---

## 四、跟进建议（优先级排序）

1. **#1 / #2 / #7**（三处一行级修复，风险低收益高）：门配对湿判、recheck 时序 emit、onDeath 首行入 try。
2. **#4 / #5**（贴图缓存一族收口）：revision 顺序调整 + `packFileUrl()` 统一 cache-bust，顺手关掉 #6 的告警噪音与低危清理残留。
3. **#3**：destroySphereSilent/tickLavaFlow 补 recheck + 熄门（注意 emit 在循环后，别重蹈 #2）。
4. **#8**：死亡 ESC 语义双向钉死（纯注释 + 可选断言）。
5. **#9 / #10 及探针低危**：冷却时间维度断言、PID 文件名、(c) 相对基列、vmEq 补 count——探针是本工程的回归网，误 PASS 面值得专项一批修掉。
