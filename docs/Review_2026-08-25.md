# Code Review — 2026-08-25（近 24 小时提交）

- **审查范围**：`020d5f9..7bbf27d`（2026-08-24 09:30 → 2026-08-25 09:16，共 33 个提交；其中 12 个纯 docs 提交只改 `docs/dev-plan.md` 与 Review 标记，已跳过，实际审查 **21 个代码提交**）。更早的 08-23/24 窗口已由 `Review_2026-08-24.md` 覆盖，不重复。
- **审查基线**：HEAD = `7bbf27d`，行号均以 HEAD 为准。工作区另有 26 个未提交的 `textures/icon_*.png` 修改，不属于本报告对象。
- **方法**：6 路并行子系统审查（逐提交读全量 diff + HEAD 交叉核对 + 探针断言强度评估）+ 主审亲核（#1-#13 共 13 条已逐行读 HEAD 源码确认；#14-#17 为子代理核、主审未逐行复核）。另按上轮惯例核查了 `6f49f77` 的「所有四个 setBlock 钩子入口」声明——**属实**（checkFlowerMushroomOnEdit 调用点恰 4 处：4 参 setBlock / 5 参 setBlock / setBlockSilent / recheckAttachmentsAfterClear，唯二不调的 setWaterSilent 与 setSnowLayerMerge 均不可能造成植物失撑）。
- **结论速览**：**高 1 / 中 7 / 低 9**。本窗口主体是 t843 火焰语义重做、t811 生物乘载具、t828-833 生物组、t836 钓鱼翻修等大型 feat + Review_2026-08-24 十条修复批次的落地。问题集中在五个模式：①燃烧侧表的世界生命周期缺口（#1 高）；②侧表摘除与呈现层信号失同步（#2）；③新代码绕过本仓库自己用血泪换来的 emit 节流纪律（#3）；④QML 机械替换的块作用域事故（#6，252 探针全绿掩盖）；⑤cache-bust「已收口」叙事与实际覆盖面的差距（#7）。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高 | 1 | #1 `m_burningCells` 不随 generate() 清空，换世界后旧燃烧态点燃/烧毁新世界方块 |
| 中 | 7 | #2 浇熄摘表无信号→面火 overlay 永久残留；#3 骑乘每帧双发 entitiesChanged 绕过 20Hz 节流；#6 三处 tooltip `let e` ReferenceError；#7 物品图标/atlas 族仍裸 `file:///` |
| 低 | 9 | 见第四节 |

---

## 一、高危（建议立即修复）

### #1 `a504ec9` (t843) `m_burningCells` 没进 generate() 的清空清单——regenerate/换尺寸/换 seed 后旧世界燃烧态污染新世界

- **提交**：a504ec9 feat(t843) 燃烧侧表引入。
- **问题**：t843 把 `m_burningCells.clear()` 加进了 `beginLoad`（`world.cpp:76`，读档路径）和 `rebuildFireCells`（:377），但 `generate()` 自己的内联清空清单（`world.cpp:4434-4440`：decayingLeaves/growth/water/lava/ice/**fireCells**/powerDirty）**唯独漏了新表**。而 `regenerate`（`world.cpp:420-426`，QML「新世界」入口）与 `setSeed/setWidth/setDepth/setHeight`（:436-439）都直调 generate、不经 beginLoad。
- **后果**：换世界后旧坐标燃烧项残留。新世界在同坐标生成可燃块（原木/树叶/木板遍地，命中率高）时：
  1. `isBurningAt` 假阳性 → 玩家/mob 踩上无辜木块被点燃；
  2. tickFire (d) 继续倒计时，≤5s 把新世界无辜方块 `setBlock(Fire/Air)` 烧毁并掷同态蔓延（烧毁走放置语义替换、**无掉落不可逆**）；
  3. 这些格上打火石被 t841 守卫拦成静默 no-op。
  触发条件：regenerate 前世界里有燃块（放火后按「新世界」逃离是典型场景）。矩阵探针从不带燃烧态调 regenerate，探针盲区。
- **证据**：`src/World/world.cpp:4430-4442`（generate 清单缺项）vs `:76`（beginLoad 有）；`world.h:1397` 注释自称「generate/beginLoad 清空」与实现不符。主审亲核（generate/regenerate/setter 四处全文）。
- **修复**：`generate()` 清单 `m_fireCells.clear()`（:4439）旁补一行 `m_burningCells.clear();`。
- ✅ 已修（346ee12）：按建议一行修——generate() 清单 m_fireCells.clear() 旁补 `m_burningCells.clear()`；world.h 侧表注释同步登记 generate 为清空入口（regenerate/setSeed/setWidth/setDepth/setHeight 全经它）。

## 二、中危（建议尽快修复）

### #2 `a504ec9` (t843) 浇熄摘侧表零信号——QML 面火 overlay 残留成「假燃块」（同 id 早退同根因）

- **问题**：浇熄设计为「火灭块存」：`tickFire` 抑制掷中后 `m_burningCells.remove(it.key()); continue;`（`world.cpp:1601-1607`）——栅格不变故无 blockBroken，侧表直摘故无 worldChanged/blockIgnited。而 QML burningHost 的摘除只有两条路：`onBlockBroken → removeBurningVis`（`Main.qml:10110`）与 `onWorldChanged → cleanupVis`（`Main.qml:10308-10310`）。浇熄路径**两条都不触发** → 面火 overlay delegate 永久贴在已不燃烧的方块上（假火视觉 + delegate 泄漏），直到世界里任意其他 setBlock 凑巧发 worldChanged。打火石直燃场景（只有燃烧格、零火格）下泄漏可无限期存在。`Main.qml:10308` 注释自称「浇熄摘表由 cleanupVis 兜底」，但没有任何东西在那个窗口调用 cleanupVis——注释承诺落空。`world.cpp:456-459` 同 id 早退清表（t843 特意放在早退前）同样不发信号，属同根因的第二实例。
- **证据**：主审亲核（tickFire 1581-1649 全段 + Main.qml burningHost 摘除链两处）。
- **修复**：douse 分支摘表时置脏标记，tickFire 循环收尾处批量补发一次 `emit worldChanged()`（同窗多次摘表只发一次，避免信号风暴）；或新增 `blockDoused(x,y,z)` 信号精确驱动 `removeBurningVis`。同 id 早退路径一并审视。

### #3 `0175e6d` (t811) tickVehicleRiding 无节流直发 entitiesChanged 且每帧双调——重新引入 t500 已修的「每帧全 delegate 绑定激活」卡顿

- **问题**：`EntityManager::tick` 专门把 dirty 收口到 `m_pendingEmit`、每 kEmitEveryN 帧（~20Hz）才 `++m_revision + emit`（`entitymanager.cpp:6815-6820`，注释明言每帧 emit 曾是 mob 卡顿主因、实测 22ms/帧）。而 t811 新增的 `tickVehicleRiding` 末尾 `if (dirty) { ++m_revision; emit entitiesChanged(); }`（`entitymanager.cpp:4821`）**直发**，且被每帧调用两次（`playercontroller.cpp:816` mob 桶内常开——暂停/菜单期 BoatManager::tick 仍在推船；`:887` step 后补钉）。乘客跟随移动中的矿车/漂移的船时每帧都 dirty → 最坏每帧 2 次全量 revision 递增 + emit，激活全体实体 delegate 的 revision 绑定 + 行走 MobModel 全几何重建。触发条件：任意一只 mob 骑上移动中的车/船。矩阵探针直驱 EntityManager 不经 QML，探针看不到。附带：登乘扫描每帧 2 次 × 每非骑乘 mob × 全量 carts+boats 也无粗筛/节流，量级尚小但同属热路径新遍历。
- **证据**：主审亲核（4821 直发 vs 6815-6820 节流；816/887 双调；816 处确认常开在暂停 return 之前）。
- **修复**：tickVehicleRiding 不直接 emit，改为置 `m_pendingEmit = true` 复用 tick 的 kEmitEveryN 收口（或自带同款 20Hz 节流）；登乘扫描可按 mob 错峰复用 m_tickPhase 或先做 AABB 粗筛。

### #4 `5f9a40c` (t828) 鱿鱼持续浮力无天花板碰撞——可穿冰层/封顶水池被顶到方块上方

- **问题**：垂直积分段只为下落设计：扫描列自中心格向下（`entitymanager.cpp:6754-6768`），落定分支 `mobNewY <= restY || e.vy < 0.0f`（:6776）。浮力使 vy 持续为正（:6742-6743），鱿鱼中心升入头顶固体格（冰面、水洞顶）那一帧，mobSupportTopY 命中该格、restY=顶面+halfH 高于中心当前位置 → `mobNewY <= restY` 成立直接 `setY(restY)` 把鱿鱼整段抬到方块顶上——穿透/snap 上顶。原版 aiSquid 喷水只是 1.5-3s 短脉冲、净位移向下，穿顶偶发；本提交的持续浮力把冰湖、封顶水池场景变成必现。
- **证据**：主审亲核（6733-6786 全段，确认上浮分支无任何向上阻挡检查）。
- **修复**：vy>0 时对头顶格（脚位 + 2·halfH 所在格）做碰撞检查：上方格有碰撞盒则钳 vy=0 贴顶悬停，与落定分支对称复用 collisionAABBsAt 口径。

### #5 `f28ff44` (t835) 末影珍珠「任意接触」判据未豁免无碰撞植物族——草丛/花/作物提前触发传送，截断「更远投掷」

- **问题**：命中豁免表只有 5 个 id（`entitymanager.cpp:5539-5541`）。TallGrass/花/蘑菇/树苗/枯灌木/小麦均为 ShapeNone 无碰撞方块，MC 1.0 投掷物 raytrace 穿过它们，本工程自己的箭也按空碰撞盒穿过。结果：草地群系/农田上平抛珍珠弧线下降段进入任一植物格即结算传送——平抛 ~24 格的目标在实际长草地形上常在数格内被草截断，反噬本提交自己的核心目标。分支头注释把「撞铁轨/薄板」列为意图命中，植物族不在其列，属判据疏漏而非声明取舍。
- **证据**：主审亲核（5530-5544 全段；TallGrass ShapeNone 见 blockregistry.cpp 注册行）。
- **修复**：豁免表补入 ShapeNone 植物族（可按 isGroundPlant + DeadBush 口径），或改「本格有碰撞盒才命中」的白名单判据，与箭保持一致。

### #6 `08ffc71` (t824-t827) 三处 tooltip 攻击行引用块作用域外的 `let e` → 运行期 ReferenceError，攻击行静默失效

- **问题**：`AnvilUI.qml:1662`、`EnchantingTableUI.qml:1263`、`Inventory.qml:1869` 的 `let e = null` 声明在 `if (!Number.isNaN(idx)) { ... }` 嵌套块内，而消费行 `root.hotbar.displayAttackDamage(root.hoveredItemId, e || [])`（AnvilUI:1677 / EnchantingTableUI:1277 / Inventory:1882）在**块外**。`let` 块级作用域，三份文件均无外层 `e` 声明/QML id → 求值即抛 `ReferenceError: e is not defined`，`hoveredAttackText` 恒空 + 每次 hover 刷控制台错误。触发条件：铁砧 UI、附魔台 UI、创造背包（含底部 hotbar 行 / main 行）hover 任意剑/斧（`itemAttackDamage > 1` 过门后必走到该行）。同批改造的另外 5 处（ChestUI/CraftingTableUI/DispenserUI/FurnaceUI/SurvivalInventory）因 `e` 声明在函数级而不受影响——恰好带 `e || []` 兜底的三处出事。矩阵探针只锁 C++ 权威值，不覆盖 QML 悬浮路径，252 全绿掩盖。
- **证据**：主审亲核（三份文件现行为 08ffc71 原样，含 AnvilUI 函数头 1648-1656 确认无外层声明）。
- **修复**：`let e = null` 提升到 `hoveredAttackText` 函数体顶部（与 ChestUI.qml 同型）；建议冒烟测试补「铁砧 hover 剑」步骤防回归。
- ✅ 已修（346ee12）：三文件 `let e = null` 提升至 hoveredAttackText 函数体顶部（AnvilUI:1652 / EnchantingTableUI:1253 / Inventory:1860，ChestUI 同型）。冒烟防线：本批冒烟 stderr 零 ReferenceError；建议后续把「铁砧 hover 剑看攻击行」固化进人工冒烟清单。

### #7 `d6051e6`/`20a0efa` (review24 #5) 「直返族 cache-bust 收口」漏了真正的高频族——物品图标/空护甲槽/方块图标回退/atlas/四条 strip 仍裸 `file:///`

- **问题**：#5 的失效场景（同路径原地换包内容 + apply() 重解析 → URL 不变 → QML Texture 按 URL 缓存旧像素）对以下各族完全成立，但均未走 `packFileUrl`：`itemIconSource` 主返回（`resourcepackmanager.cpp:2422`，热栏/图鉴最常用图标入口，消费端 ToolIcon.qml 的 Image 未设 `cache: false`）、`emptyArmorSlotSource`（:2448）、`blockItemIconSource` 回退族（:2496-2516）、`atlasSource`（:2234，atlasFile 是**固定名** `voxelsandbox_rp_atlas.png`，apply 原地覆写 URL 永不变）、四条 strip（:2247-2281）。d6051e6 加了五族源序钉，给后续维护者「直返族已闭环」的假 assurance。
- **证据**：主审亲核（2415-2448 段；leather 分支 2419 因文件名嵌 `_r<rev>` 不受影响，2422 裸直返属实）。
- **修复**：上述各族改走 `packFileUrl(path, s.revision)`；atlas/strip 若刻意依赖 active 切换刷新，至少注释登记决策依据并修订「已收口」叙事。

### #8 `20a0efa` (review24 #9) 探针「冷却时长两方向钉死」宣称不成立——kDispenserCooldown 回归改 0（或任意小值）矩阵仍全 PASS

- **问题**：`fireDispenserAt` 的冷却门是纯 `contains` 检查不看值（`playercontroller.cpp:5217` `if (m_dispenserCooldowns.contains(key)) return false;`，:5251 写入 2.0f）。探针 (e)①② 段从不调 `scanDispenserTraps`（等价真实 0ms 流逝）→ 即使常量改成 0.0f，contains 仍拦下复置沿，断言照过；③ 的一笔 2.5f 递减同样把它过期并再发射。实际钉死的只有上界（>2.5s FAIL）与「机制整体删除」（② FAIL）两个方向，时长下界（2.0→0.5/0）不可见。提交信息「改 0 则②触发发射 FAIL」与 contains 语义不符。时间语义等价性本身（一笔 2.5f ≈ 156×16ms）成立，问题在断言强度宣称过强。
- **证据**：游戏侧主审亲核（5213-5252）；探针段（redstone_matrix_test.cpp:7284-7305）子代理核。
- **修复**：② 段前先 `pc.scanDispenserTraps(1e-3f)` 走一步递减再复置（0 值冷却即被 erase → 0 回归会发射 → FAIL）；或 `fireDispenserAt` 改 `contains && value > 0` 才拦（同时修正真实语义）。
- ✅ 已修（346ee12）：取建议 ②（值门）——门改 `contains && value > 0`（零/负冷却无合法语义，tick 递减循环本就 erase 非正值；纯 contains 下 0 表项永驻恒拦是假语义），探针 (e)② 复置前补 `scanDispenserTraps(1e-3f)` 真实递减步双保险。阴性验证一轮：常量临时改 0 → 矩阵 250 PASS / **2 FAIL**（t814 e + t856，复置沿发射、库存多扣——下界现形），还原 2.0f → 252 PASS / 0 FAIL。

## 三、低危

### #9 `a504ec9` 雨浇熄的门对偶半扇仍被烧尽收尾连带清 Air——守卫只护「水湿」不护「雨浇」

- 露天门在雨中点燃后某半扇大概率被雨浇掷中浇熄（「雨浇保块」语义），对偶随后烧尽时收尾守卫只判 `!fireWaterNeighborAt(x, py, z)`（`world.cpp:1628`），不判 `fireRainExposedAt` → 已被雨救下的半扇仍被 `setBlock(Air)` 连带清除且误发 blockBroken。水泼能保住、雨浇保不住，抑制源行为不对称。修复：守卫补 `&& !fireRainExposedAt(x, py, z)`（与点燃入口同口径）。主审亲核（1585-1639 段）。

### #10 `57e30d5` dropUnsupportedDoorsAround 活板门「下方依附面」off-by-one（被时序掩蔽的潜伏缺陷）

- 活板门在 `uy = y+1`，其下方依附面应是 `(x, y, z)`（刚被挖的格）；代码查 `attachOk(x, y-1, z)`（`playercontroller.cpp:1552`）——挖掉格的再下一格。对照 World 侧同一谓词（world.cpp checkTrapdoorDoorSupportOnEdit 查 `by-1` 相对活板门自身，正确）。当前无可见错误：finishMiningAt 开头的 `setBlock(Air)` 已先触发 World 钩子清掉失撑活板门，本函数随后看到 Air 早退。但与「放置与掉落口径零漂移」的注释承诺相悖——一旦钩子顺序调整或新增直调路径，下方有实体的活板门会被误判「仍被支撑」而悬空。修复：改 `attachOk(x, y, z)`（blockAt 越界安全返 Air，`y-1>=0` 守卫可去）。主审亲核。

### #11 `15a1000` 发射器排出口被实体方块堵住时，TNT 生成在墙格内并于 ~5s 后就地引爆

- `dispenseFromDispenser` 的 TntBlock 分支直接在朝向邻格 `spawnPrimedTnt`（`playercontroller.cpp:5379-5381`），不查目标格占用；primed TNT 水平积分刻意不查碰撞 → 贴墙安装的发射器激活后 TNT 在墙格内爆炸，炸穿墙并波及发射器自身。MC 语义是弹出到可达空位/堵口时不弹。修复：spawn 前查目标邻格 `collisionAABBsAt(...).empty()`，非空沿 dir 再探一格或退化为普通掉落。主审亲核。

### #12 `0a1acb5` 浮标 Ground 态永不复查支撑——挖掉所贴方块后悬空滞留至 180s 寿命兜底

- Water 态每 tick 复查 `blockAt != Water` 失浮转 Flying（entitymanager.cpp:5616-5626），Ground 态 `continue` 无任何复查（:5647-5649）。玩家挖掉浮标贴靠的方块（或被 TNT 清掉）后浮标永久悬空。修复：Ground 分支节流复查贴靠格，支撑消失转 Flying 零速下落，与 Water 排水路径对称。主审亲核。

### #13 `0a1acb5` 飞行段钩 mob 判定先于方块碰撞且无视线门——可隔墙钩住生物并拉拽穿墙

- 钩 mob 扫描（entitymanager.cpp:5655-5665 起）只用 `next` 点位对 mob AABB 外扩重叠测试，既不先判 `next` 是否已进入实体方块（碰撞测试在其后），也无视线遮挡 → 浮标贴 1 格厚墙飞过、墙后有 mob 即隔墙钩住，Hooked 态钉位 + 收竿拉拽可把 mob 拉过墙。修复：钩 mob 前先判 next 格实体方块命中即走 Ground 不钩，或补 DDA 视线检查。主审亲核（结构段）。

### #14 `0a1acb5` 浮标寿命/等待计时复用 dt 累计，未沿箭矢已修的墙钟先例，卡顿下计时漂移

- 180s 寿命、5-30s 等待、0.5s 咬钩窗口全部走 `dt` 累计（entitymanager.cpp:5594 起同源），而 dt 在 tickImpl 钳到 50ms——真实帧耗时 >50ms 的每次卡顿都使计时变慢（方向只慢不快，咬钩窗口变慢反而有利玩家），故低危。同仓库 Arrow 曾因 dt 累计漂移显式改墙钟（:4868-4876 注释）。修复：寿命兜底改墙钟（同箭）；等待/窗口计时可维持 dt（确定性探针依赖）并注明取舍。子代理核。

### #15 `8054984` achQuickDock 拖到窗口底边后 hover 展开，底部溢出视口

- dock 首次拖动断开 `y` 绑定后可贴底（drag.maximumY = parent.height - 36）；hover 展开使 height 涨到 ~90+ 而 `clampIntoView` 只监听 parent 尺寸变化不监听自身 height（Main.qml:12984-13076）→ 「最近解锁」名单下沿出屏，收起自愈。纯视觉、会话态。修复：dock 自身 `onHeightChanged` 补钳位。子代理核。

### #16 `d97f465` 爆炸失撑火把的掉落反馈通道从 t738 刻意设计的「静默 explosionDroppedItem」换成 recheck 的 blockBroken 通道

- recheck 火把分支 `emit blockBroken` + `blockDroppedAsItem`（world.cpp:2709-2710）→ 爆炸每震落一个火把发一组破块粒子+音+worldEditRev++，恰是 t738 头注释明确要避免的 spam（entitymanager.cpp:4081-4086）。功能正确（双掉被「先清者留 Air、后扫者判跳」挡住）、有界，纯表现层。修复：接受现状则把 t738 注释如实改掉；或给 recheck 加系统事件旁路。子代理核。

### #17 `57e30d5` 仙人掌碰撞收窄的注释宣称了并不存在的伤害口径（误导性文档，行为本身正确）

- `blockregistry.cpp:1668-1670` 注释两处与实现不符：①kCactusInset 实际 0.1 而非 1/16；②接触伤害**并不**走收窄后的 collisionAABBs——玩家侧刻意满格 AABB+容差皮、mob 侧格邻接判定，「须真站进柱内才受伤」是假的。风险：此区域有多次伤害判据回归史，后人按此注释「对齐」会重新引入「侧撞仙人掌不扣血」类已修回归。修复：更正注释指向 playercontroller 侧权威注释。子代理核。

---

## 四、审查过但未成案的重点区域（备案）

- **t836 钓鱼生命周期**：死亡/读档/重生/暂停/切世界全链收口核过（onDied finally → cancelFishing、loadSavedState/respawn/tickImpl 暂停、clearAll 后惰性自动收竿即 6f49f77 (g) 声明行为）；槽复用双查 kind==Bobber、emit 悬垂（e 写在 emit 前）、耐久/掉落表边界、0x25B 无 id 冲突——均干净。
- **6f49f77 其余修改**：铁砧快照（emit 窗口后全读面用快照、唯一写回分支重取引用）、死亡链（onDeath 独立 try、三 close*/三 returnCraftToHotbar 各自 try、finally 幂等复位 11 个面板标志）、皮革 `_r<revision>` 世代（apply 先 bump 后重建、五前缀清扫与落盘名逐一匹配、QFile::exists 复验）——逐项核对无新 bug。
- **08ffc71 C++ 侧**：附魔池过滤后无空池/零除路径（权重 `std::max(1, w)`、空池早退）；伤害/击退单源化干净。
- **7437d5a**：画放置四象限 face/锚格逐向核对正确；画失撑五写入入口接线齐全；垃圾桶单格语义全仓无漏改路径（Shift 搬运/双击合并无 destroy 目标组）；t847 草丛放置/失撑单边不对称**已被 6f49f77 的 isGroundPlant 收口修复**（HEAD 两面共用同一谓词）。
- **888fa22 传送门**：21/22 边界、共享柱双门、角省略逐一核对无误。**0a936b1 铁轨转辙器**：bit6/7 记忆位防振荡、降沿不回弹、重算守恒与存档兼容齐备。**d76c4a0 火焰外围批**：守卫插入位置正确、退役字段清理彻底。**1c582d5 视觉批**：UV 半纹素内缩只落 box-UV 分支、床 z-fight 反向法线修法几何正确。**e6b2681 盔甲/皮肤**：盒与模型几何对齐、spectator 门控完备、旧固定色引用清零（其 FP 手臂皮肤 URL 新鲜度缺口已被 d6051e6 补上）。
- **d97f465 三条修复本体**：#1 整扇湿判三入口统一、#2 火把分支自 emit 时序、#3 recheck 收口严格超集且无递归/双掉——全部真实闭合（残留见 #16 表现层通道变化）。

## 五、总评（按提交）

| 提交 | 评价 |
|---|---|
| a504ec9 (t843) | 核心状态机扎实，但侧表生命周期漏两条（#1 高、#2 中）——「表存在」与「世界/呈现层同步」的边缘恰是探针盲区 |
| 0175e6d (t811) | 双向链对账设计稳健，但违背本仓库 emit 节流纪律（#3） |
| 5f9a40c (t828-833) | 六子任务收口质量高，唯鱿鱼浮力漏向上碰撞（#4） |
| f28ff44 (t835) | 液体/虚空判定自洽，「任意接触」一刀切误伤植物族（#5） |
| 08ffc71 (t824-827) | C++ 单源化干净；九处 QML 机械替换三处只看变量名没看块作用域（#6） |
| d6051e6/20a0efa (review24 批) | #4/#6/#9/#10 目标达成；#5/#9 收口承诺口径大于实际覆盖（#7/#8） |
| 0a1acb5 (t836) | 分层与边界防御全面超平均，仅余三条 MC 语义近似缺口（#12-#14） |
| 6f49f77 (终审修复) | 纯防御性批，四入口声明属实，未发现新引入 bug |
| 57e30d5 / 15a1000 / 8054984 / 888fa22 / 0a936b1 / d76c4a0 / 1c582d5 / e6b2681 / 65636e4 / 7437d5a / d97f465 / 19e5577 | 未发现或仅低危（#9-#11、#15-#17 及各备案），总体质量良好 |

**修复优先级建议**：#1（一行修，换世界即触发）→ #6（三文件各一行，用户可见功能失效）→ #2/#3（信号纪律，各一小段）→ 其余按严重度排。
