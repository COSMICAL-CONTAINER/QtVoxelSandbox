# Review 2026-09-12 — 近 24h 提交审查（R19.23 instancing 批 3/批 4 收官 t1039/t1041 + t1037 睡中毁床唤醒 + t1038 书台同相定夺修 + t1042 被动惊逃/野狼反击）

- 审查对象：`026681c..a833004`（2026-09-11 12:14 → 09-12 06:32，共 23 笔：5 笔 src fix（t1037 / t1038 / t1039 / t1042 / t1041，批 3 +403、批 4 +1008 为本窗最大面）、6 笔测试腿（全部只动 `tools/redstone_matrix_test.cpp`）、12 笔 docs/plan/state（含 parity-ledger 立账与两份 Review/audit 文件）。矩阵 521→522（t1038）→522（t1040 rig 稳健化不改腿数）→526（t1039）→530（t1042）→**534（t1041）**。与上轮（Review_2026-09-11，止于 `026681c`）边界核对：4459d33 父提交即 026681c，`026681c..a833004` 恰 23 笔，无缝衔接无遗漏。
- 审查方式：5 笔 fix 逐笔 diff 精读，关键路径在 HEAD 交叉核对——①t1039/t1041 六条 QML 排除链的**绑定反应性**逐链追踪（entId 属性依赖 / buckets var 整体替换 notify / t1039 hasShellAt 纯函数调用三类形态，本轮新发现来源，见 #1）；②三新谓词与 QML 十分支条件逐字同源（isCrossBlock→isCrossBillboard 薄委托、isMaterial 单边 >=、type8 钓鱼竿无分支维持，均已核）；③aiWolf 门序（坐门最顶先于幼崽惊逃分支）与 setPanicFlee/setWolfProvoked 类型门、aiWander 的 wanderSpeed 重掷复位面、headPitchAt 包络冻结面；④t1037 clearBedSpawn 招标次序（cancelSleep 先于清锚）与三调用方互斥性；⑤t1038 双面排除（QML skip + C++ hasLiveMember/getInstanceBuffer 镜像）同参性；矩阵各 final 日志 tail 实读（t1039/t1041/t1042 均 `=== total FAIL: 0 ===`）；app 证据链（build/voxelsandbox.exe mtime > 全部源改动 + build_t1041_app.log）。
- 总评：**1 中 + 2 低 + 1 低（定夺）+ 2 Info**。五个功能 fix 主链（睡中毁床即醒、94 书台同相、光晕壳全族合批、被动惊逃 + 野狼受击反击、工具/billboard 三族合批收官）逐项核对成立；昨日 Review #1（94 书-台相位）由 t1038 正式清偿（本轮复核落地面与昨日建议修法逐字一致）。**唯一中severity发现是 t1039 壳排除链的 QML 反应性缺口**：`visible: !glowShellInstHost.hasShellAt(index)` 无任何 QProperty 依赖（index 是 delegate 常量、C++ Q_INVOKABLE 内部读取不入 QML 依赖追踪）→ 绑定创建时求值一次后永不重算；而壳池成员资格取决于**槽序排名**（其他槽的活死翻转），恰是批 1/2/4 排除链（entId 自身 id + buckets 池表双属性依赖）覆盖不到的依赖维度——>128 活体的溢出态（设计声明真实可达且 P-t1039c 专测 130 活体）下每次跨界翻转产生永续的双壳或丢壳，违反本单「同 id 壳恰在一侧渲染」的明示主张。修法一行（触碰 revision，t256 既有模式）。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | **中** | **t1039 壳排除链非反应绑定 → 溢出态双壳/丢壳且永续**：`entShell.visible: !glowShellInstHost.hasShellAt(index)` 的绑定表达式只引用 `index`（Repeater 附着属性，delegate 常量——count 单调不降永不重排）与两层纯函数调用（host JS 函数 → C++ Q_INVOKABLE）；C++ 方法内部的 `aliveAt()/count()` 读取不进入 QML 依赖追踪 → **绑定在 delegate 创建时求值一次后永不重算**。壳池收纳谓词 = 「槽序前 128 个活体」，某槽的成员资格随**其他槽**的活死翻转而变（spawn 复用早槽 → 后槽排名跌出 128 → 实例表当帧摘除该壳；早槽拾取 → 128 名槽升入池 → 实例表当帧补入），而绑定不感知 → 升入池的槽 delegate 壳仍 visible=true（**双壳**：实例壳 + delegate 壳并存，两钟相位差的半透壳叠加闪烁）；跌出池的槽 delegate 壳仍 visible=false（**丢壳**：该掉落物无光晕）。且 delegate 永不重建（t256 slot-reuse 设计 count 单调）→ 槽拾取复用也不刷新 → **陈旧一旦发生对该槽永续**。触发条件：>128 活体掉落（刷怪farm / 掉落物囤积，manager 上限 200，P-t1039c 专测 130 活体 = 该态设计内真实可达）。常态（活体个位到几十）hasShellAt 恒 true 无影响。批 1/2/4 排除链无此问题（已逐链核对：`hasBucket(entRoot.entId)` 经 entId 属性〔revision 依赖〕+ buckets var〔整体替换 notify〕双依赖，覆盖「自身 id 变」∪「池表变」恰完备——它们不依赖排名）；t1039 的谓词依赖排名，是多出来的那个维度 | Main.qml:6617（`visible: !glowShellInstHost.hasShellAt(index)`，无属性触碰）；:6894（host 薄委托纯函数）；glowshellinstancing.cpp hasShellAt/getInstanceBuffer（前 128 活体槽互为镜像——C++ 侧逐位一致，缺陷纯在 QML 绑定层）；Main.qml:6194-6207（Repeater model=count 单调 + entRoot.visible/entId 显式触碰 revision 的 t256 模式先例）；dev-plan t1039「>128 掉落物溢出壳保底」列为待实机确认项（盲区登记相邻但未指认此缺陷） | **推荐：绑定触碰 revision（t256 同款一行）**——`visible: { const _r = itemEntities.revision; return _r >= 0 ? !glowShellInstHost.hasShellAt(index) : true }`。排名翻转必伴随 spawn/release → entitiesChanged → revision++ → 恰完备覆盖（tick 物理尾 bump 只是多余重算，代价 200 槽 × O(slot) aliveAt 每沿一次，可忽略）。备选：C++ 侧给 GlowShellInstancing 加 `Q_PROPERTY(int shellRevision ...)` 于 onManagerEntitiesChanged 自增，绑定改读该属性（语义更窄、少算，但多一个属性面）。修后 P-t1039d 源码钉的该行文本需同步（pinSet 改 1 针） |
| 2 | 低 | **t1041 stringPass feeder 空转门不感知弓语义 → 非弓工具桶各挂一个废 16ms 钟**：QML 结构上 8 个工具桶每桶都实例化两个 Model（本体 + 弦），弦 Model `visible: ... toolType(bucket)===7` 仅弓桶显——但两个 feeder 的 `hasLiveMember()` 均不区分 stringPass：非弓桶的弦 feeder 只要桶内本族活体 >0 就走钟（16ms markDirty），其表永无渲染消费（Model invisible 不进渲染列表）。最坏 7 个废钟 × 60Hz ≈ **420 次空唤醒/秒** + 每次 markDirty 空转——恰是 review0910 #3 空转纪律要归零的那类周期唤醒（批 3 壳池为此专门收口，本处漏网）。无视觉缺陷（表不可见） | tooldropinstancing.cpp:105-115（hasLiveMember 无 stringPass/弓判定，仅 isTool3DDrop）；Main.qml toolDropInstHost 弦 Model（visible 弓桶门 + `ToolDropInstancing { stringPass: true; familyId: buckets[index] }` 每桶实例化）；setStringPass 注释自称「stringPass 表只服务弓桶」但时钟面无此门；P-t1041c 钉「弦表只服务弓桶」钉的是收纳 familyId 绑定语义，非时钟面 | `hasLiveMember()` 追加一行弓门：`if (m_stringPass) { const auto *td = ToolRegistry::tool(m_familyId); if (!td || td->type != BlockRegistry::Bow) return false; }`（该 cpp 已 include toolregistry.h，零新依赖）——非弓桶弦 feeder 恒停钟恒空表，语义与注释/登记口径对齐；P-t1041c 的谓词感知腿可加一针（shears 桶弦表 ticker 恒停） |
| 3 | 低（定夺项，与已钉口径冲突） | **t1042 驯服幼狼被打惊逃离主人（MC 原版偏差）**：setPanicFlee 的 wolfBaby 门 = `mobType==MobWolf && e.baby`，不含 wolfTamed；而生产面狼幼崽**恒驯服**（t480 父代继承，dev-plan/头注释自认）→ 该门的实际生效面 = 打自家小狗 → 幼犬背离主人疾走 8s（跟随语义中断、随后自行恢复）。MC 原版（1.0 及现代 Java）狼实体无 PanicGoal——被打只受击闪红，不逃（幼崽同）。按用户 e131797 定夺口径「一切以 MC 原版为准，偏差即 bug」这是偏差；但 **P-t1042c 探针把「幼崽只惊逃」钉为 MC caliber**（"wolf babies only flee (MC caliber)"）——口径与原版事实相悖，修复需探针 + 头注释 + 登记（39afab6/f5e75eb）三面同步。坐姿幼犬无此问题（坐留守门在 aiWolf 最顶 3104 行，先于 3182 幼崽惊逃分支——已核对） | entitymanager.cpp setPanicFlee（wolfBaby 门无 wolfTamed 条件）；:3103-3108（坐门最顶）→ :3179-3183（幼崽惊逃分支其后）；头注释「生产面狼幼崽恒驯服（t480 父代继承）」；matrix_t1042_final.log P-t1042c（"a bred wolf puppy struck by the player only arms the 8s panic state ... (MC caliber)"） | **定夺后二选一**：①wolfBaby 门收紧为 `e.baby && !e.wolfTamed`（野幼崽惊逃保留——MC 口径野狼被打成体反击、幼崽行为原版本无 panic，此支也可整体摘除；摘除则 P-t1042c 幼崽腿改断言零 panic）；②维持现状 + 登记「驯服幼崽受击惊逃为工程自定口径」（须改写 P-t1042c 的 "MC caliber" 措辞 + 头注释）。倾向 ①：一行门 + 探针改腿，与用户 parity verdict 一致 |
| 4 | 低（视觉，自愈） | **t1042 惊逃打断羊吃草 → 低头姿态冻结狂奔**：eating 门改 `eatTimer>0 && panicTimer<=0` 后，惊逃期 eatTimer 冻结保留（登记口径「自然续吃」）——但 `headPitchAt` 的低头包络只看 eatTimer（`eatTimer>0 → kEatHeadPitch·sin(πp)`，p 冻结在中途）→ 羊以**低头吃草姿态**背离玩家疾走最长 kEatDuration（惊逃 8s > 吃草周期则跨全周期），惊逃结束续吃时包络走完自愈。纯视觉、有界、自愈；登记面只提了「打断吃草」未提头部姿态副作用 | entitymanager.cpp:7801-7804（eating 门 panic 豁免）；:1699-1706（headPitchAt 仅 eatTimer 门，无 panic 门）；t1042 提交注「panic gates ... interrupt sheep grazing」未覆盖 headPitch 面 | 一行修：headPitchAt 返 0 条件追加 `&& e.panicTimer <= 0.0f`（惊逃期头回正，续吃时重新入包络——包络从当前 p 继续，视觉上「抬头跑、回身续吃」）；或不修仅登记。改则 P-t1042 相关腿如有 headPitch 断言需核对 |
| 5 | Info | **t1039 getInstanceBuffer 每实例每帧 enchantsAt 调用**：≤128 实例 × 60fps ≈ 7680 次 QVariantList 堆分配/秒（ QVariantList 按值返回 ×4 pack 读）。与批 1/2 全表重算同款既有口径，非缺陷；若后续 F3 实机帧剖发现 item 面热点，首选把附魔判定改 4×int 读口（Q_INVOKABLE 逐 pack 或一次性 int 数组） | glowshellinstancing.cpp getInstanceBuffer（`const QVariantList ench = m_manager->enchantsAt(slot)` 循环内）；对照 QML entHasEnch 每帧同源读（既有） | 登记；非必改。若修，探针 P-t1039a 颜色腿数据源不变（同判定语义） |

## 已核实安全面（审查结论，非发现）

- **1e7eca3（t1037 睡中毁床即醒）**：统一收口点 clearBedSpawn 的**招标次序正确**——cancelSleep 先于清锚（wake 瞬移 leaveBedTeleport 需读 m_bedAnchor 定位「前床」周扫可站位格，次序颠倒则扫原点）；m_sleeping 翻假后 updateSleep 早退 → sleepAdvanceToDawn 不可达（绝不跳晨，病灶「Settled 满重写重生点回已毁床」根断）；三调用方互斥性论证成立（换代必经 release 先清睡态 = 防御性、挖床与睡眠互斥〔睡眠中输入域冻结〕= 防御性、爆炸槽 = 病灶本体）；非睡眠态 cancelSleep 自守 no-op，t1024 幂等面（已 pristine 且 invalid 静默零 emit）不变。
- **a1bc01c（t1038，昨日 Review #1 定夺修）**：落地与昨日建议修法逐字一致——QML reassignShapeBuckets 计数循环 skip 94（hasShapeBucket(94) 自然恒 false，无第二特判）+ C++ 收纳侧双面同参排除（hasLiveMember 早退 / getInstanceBuffer continue，review0910 #3 谓词同参纪律保持，生产不可达 = 第二道防线定位诚实）；94 台+书恒走 delegate 单链同钟同相，批 2 前观感逐位恢复；批 1 池/其余 24 id 桶化零触碰。
- **9dfd7d0（t1039 光晕壳批 3）**：C++ 侧无缺陷——收纳（前 128 活体槽）与 hasShellAt（活体 ∧ 先行活体数<128）互为镜像逐位一致（缺陷纯在 QML 绑定层，见 #1）；空转门完备（构造不启钟 + entitiesChanged 沿 refreshTicker + setManager 换源先 disconnect + skyLight/minLight 只 markDirty）；hasTransparency 使表 alpha 生效为 Qt 官方面；色彩线性落表口径（sRGBToLinear 多项式镜像）探针侧一致；呼吸解析式 0.28+0.17·½(1−cos(πs)) 与 t696 双腿 800ms InOutSine 等价推导成立；壳-体相位脱锁三项评估（独立 halo 非 t1038 嵌入件）登记接受，口径自洽。
- **7c9878d（t1041 批 4 收官，本窗最大面 +1008）**：①三谓词与 QML 十分支条件**逐字同源**——isTool3DDrop ↔ isTool∧toolType∈{1,2,3,4,5,7} 六分支、isIconBillboardDrop ↔ (type∈{6,9})∨isMaterial（`Hotbar::isMaterial` 单边 `>= MaterialIdBase` 同判已核，hotbar.cpp:386-389）、isBlockIconBillboardDrop ↔ (isPartialBlock∨isCrossBlock∨isBed)∧!isItem3DFamily（`Hotbar::isCrossBlock` 薄委托 `BlockRegistry::isCrossBillboard` 已核，hotbar.cpp:422-426；头注释里 isCrossBlock 字样仅为 QML 侧旧名引用，非行为差异）；type8 钓鱼竿无分支维持既有不渲现状（登记，非本单范围）——所有 id 在六池（整立方/3D 形状/工具 3D/图标/异形图标/壳）+ delegate 保底间路由完备无孤儿无重叠。②排除链反应性完备（entId 属性 + buckets var 整体替换双依赖，见 #1 反证）。③billboard rotation=(camPitch,camYaw,0) 与旧 delegate 本地 (x, y−rotY, 0) 挂 entRoot(rotY) 的世界合成 Ry(camYaw)·Rx(camPitch) 等价（roll=0 时 fromEulerAngles=Ry·Rx，与材料段旧注释同款推导；新旧同读 cam.eulerRotation 同源）；billboard 不自转（旧链显式抵消 rotY 的语义保持）；camPitch/camYaw 沿 setter markDirty 边完备。④三桶池编排与批 1 逐字同构（确定性 tiebreak 活体数降序+id 升序、kept 不让位 review0910 #4 维持、整体数组替换 notify、Component.onCompleted 兜底）；图标池 wrapper Item 双图标显隐切（ToolIcon/MaterialIcon 互斥）与谓词两支对齐；异形桶材质同参（alphaCutoff0.5+opacity0.99+generateMipmaps:false+无 flipV 覆写）。⑤空转门两新 feeder t1032 同款（构造不启钟/谓词感知/活跃沿 start+markDirty）——唯一漏网是弦 feeder 的弓语义（#2）。
- **f5e75eb（t1042 被动惊逃 + 野狼反击）**：①类型门收口干净——setPanicFlee 仅被动五型+狼幼崽、setWolfProvoked 仅未驯服成体狼，disjoint 无交叉；attackMob 三写点序（Golem→Panic→Provoked）对任一目标至多触发一支。②aiWolf 坐门最顶（坐姿狼含幼崽不动摇）；!playerTargetable 清反击态（创造/观察者切换无残留追击，t290/aiIronGolem 先例）；chasing 只能由受击沿置位、侦测带内仅续期（kDetectRange 带内刷 kChaseMemory、带外衰减归零弃追）——旧 t480「见人就咬」不复活；咬击经 mobAttackedPlayer + m_playerHitCooldown（t321 全局节流与敌对共用）。③惊逃优先级置顶完备：通用链 panic 分支短路求偶/引诱/幼随/选向/公共 aiWander（防同帧双移），eating 门 panic 豁免，aiOcelot 未驯服分支 / aiWolf 幼崽分支各自置顶；t1029 冻结缝语义保持（惊逃同样冻结不位移）。④panicTimer 放 struct 末尾（t256 元教训）+ spawnMobCore 整体 move 入槽 → 槽复用自动清零，无跨实体残留；实体无持久化无跨会话面。⑤wanderSpeed 惊逃后残留 ≤0.4s（aiWander 重掷复位，entitymanager.cpp:2941-2949，可忽略）。⑥t1031 驯服狼零反击豁免维持。
- **测试腿 6 笔 + docs 12 笔**：测试全部只动 tools/redstone_matrix_test.cpp 无 src 触碰（+320/+114〔t1040 改写〕/+446/+378）；docs 只动 docs/。**矩阵证据链**：521→522（t1038）→522（t1040 rig 稳健化）→526（t1039）→530（t1042）→**534（t1041）**，t1039/t1041/t1042 final 日志 tail 实读均 `=== total FAIL: 0 ===`；阴性轮在案（t1039 两轮恰红 1/2 腿、t1041 两轮恰红 1/3 腿、t1042 恰红行为腿）；rig 教训（t256 槽复用 LIFO 拾取断言）如实登记。**app 证据链**：build/voxelsandbox.exe mtime > 全部源改动 + build_t1041_app.log EXIT=0 + offscreen 冒烟 EXIT=124 + tail20 留存（dev-plan 登记，WRN 均既登记项）。

## 工作区状态注记（非本窗提交，供续接棒）

- `src/Core/blockregistry.h` + `src/World/world.cpp` + `tools/redstone_matrix_test.cpp` 未提交改动，且根目录已有 build_t1043_{test,neg1,neg2,pos}.log / matrix_t1043_{pos,neg1}.log = **t1043（parity bug 波首单）在途**（e131797 队列：t1043-t1046）。本 Review 不含该未提交面。
- `.codex/` 外部工具产物：豁免口径不变（不删不提交）。

## 清偿状态

| 项 | 处置 |
|---|------|
| Review_2026-09-11 #1（94 书-台相位脱同步） | **已清偿**（t1038 定夺修 = 昨日推荐选项「94 整体排除出形状桶」，本轮复核 QML/C++ 双面 + 钉面齐整） |
| Review_2026-09-11 #2（桶池 QML 编排盲区 + 实机定向冒烟） | **待办且盲区扩大**：批 3/4 后纯 JS 编排面 ×5（reassignDrop/Shape/Tool/Item/BlockIcon）——review0910 #2 同型登记延续；实机冒烟范围应扩至「≥10 种 3D 族 + 工具多 tier + 剪刀/打火石/材料/异形图标各若干 + 壳显/呼吸/夜间变暗 + >128 溢出壳保底（顺带验本文件 #1）+ 弓弦观感」 |
| Review_2026-09-11 #3（bedSpawnAnnounce 恒 false 参数） | 维持登记（future-proof 预留，无冲突面） |
| Review_2026-09-11 #4（爆炸毁半床另半残留） | 维持登记（parity 波 t1043-t1046 候选池可评估） |
| 本文件 #1（t1039 壳排除链反应性） | **待修**（一行 revision 触碰 + P-t1039d 钉面 1 针同步；建议随 parity 波前的下一个单顺带，勿混入 t1043 在途 commit） |
| 本文件 #2（stringPass 废钟） | 待修（hasLiveMember 一行弓门 + P-t1041c 可加一针；低优先） |
| 本文件 #3（驯服幼狼惊逃 vs MC 原版） | **待用户定夺**（与 P-t1042c 已钉 "MC caliber" 措辞冲突；定夺后修门/摘支 + 探针 + 登记三面同步） |
| 本文件 #4/#5 | 登记可（headPitch 冻结一行修可选；enchantsAt 分配非必改） |
