# Review 2026-09-09 — 近 24h 提交审查（review0905 收尾两修 · t1024 床语义 · t1025 繁殖回标 · t1026 麦种窄增 · t1028 音符盒全链）

- 审查对象：`cc238ee..b91cb87`（2026-09-08 11:00 → 09-09 07:35，共 19 笔：6 笔 src fix、5 笔测试腿（全部只动 `tools/redstone_matrix_test.cpp`）、8 笔 docs/plan/governance），矩阵 479→502。与上轮（Review_2026-09-08，止于 `cc238ee`）边界无缝衔接，无重叠。
- 审查方式：6 笔 fix 逐笔 diff 精读（80593c1 / bbed08a / bac476b / 11a1a2f / 7ad0e9e / 816cb9a），关键路径在 HEAD=b91cb87 交叉核对（file:line 均为 HEAD 当前行号）；`World::Weather` 枚举值、`world_meta` 写入模式（INSERT OR REPLACE）、`MobType` 枚举尾、`baby=true` 全部赋值点、`beginMining` 前置守卫（`canBreak()` 观察者早退）、`placeBlock` useBlock 链的 `sneakPlace` 门分布、长大还原与产崽减半的互逆性等逐项到源码验证；测试腿只核 file 面（均单文件探针，矩阵 502/0 为提交自述未独立复跑，同既往口径）。
- 总评：**无高 / 中严重度新缺陷**，5 低 / 5 Info。六个 fix 的主链（神殿保底 R 门、火把边界收口、床锚存档 round-trip、繁殖 MC 回标 + 幼体物理盒、麦种 1-3、音符盒 25 档全链）均逐项核对成立，存档兼容（枚举尾追加 + INSERT OR REPLACE + 读侧逐键缺省）处理干净。低级发现集中在三类：**口径面**（雷暴拒睡与 MC 相悖待考据、潜行右键旁路缺门）、**手抄常量**（kMobTypeCount=21 / kNotePitchCount=25 两处与枚举尾手工同步）、**UX 噪音**（每次进世界弹「重生点已设置」）。均不阻塞。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | 低 | **t1024 雷暴拒睡与 MC 口径方向相悖（待 wiki 考据）**：MC 中雷暴恰是合法入睡窗口（雷暴天白天也可睡，睡醒顺带清雷暴——「雷暴天睡觉」是床的经典用法）；本提交按 dev-plan「白天/雷暴不可睡」实现为拒绝，且雷暴门排在夜晚门**之后**（夜间雷暴 →「雷暴中无法入睡」）。若项目口径以「机制等价 MC 1.0」为校准，此门方向反了；若确为 dev-plan 刻意取舍，则属登记项而非 bug | playercontroller.cpp:3146-3149（kWeatherThunder=3 门，置于夜晚门后）；world.h:1081（Thunder=3 编码已核对一致） | 走 review0905 式 wiki 考据（MC 1.0 / Beta 1.6+ 雷暴入睡规则）定案：MC 允许 → 改「雷暴视为可睡（甚至白天可睡，醒来清雷暴）」或至少登记分歧理由；MC 1.0 确不允许 → dev-plan 补登记「刻意偏离」一行 |
| 2 | 低 | **t1028 音符盒右键调音缺 `!sneakPlace` 门（潜行持方块无法对方块面放置）**：同函数内工作台/熔炉/箱子/附魔台/铁砧/发射器/投掷器 useBlock 分支全部有 `!sneakPlace` 旁路（潜行+右键 = 跳过使用、走放置），音符盒调音分支无此门 → 潜行持方块右键音符盒仍会调音，放置语义被吞。MC 口径潜行右键 useBlock 旁路。注意门/活板门/床分支同样缺此门（pre-existing，t1028 沿袭旧模式，非本窗引入） | playercontroller.cpp:3579（调音分支，无条件门）vs :3473-3496（enchantingTable/Furnace/Chest/Anvil/Dispenser/Dropper 各分支均 `!sneakPlace` 前置）；sneakPlace 定义 :3471 | 音符盒分支补 `!sneakPlace &&`（与同函数新机关件模式对齐）；门/床/活板门的缺门为存量，登记到 lessons 或单独任务，不在本修内顺手改（防行为面扩大） |
| 3 | 低 | **两处与枚举尾/常量手工同步的手抄数，追加新值时静默失效**：(a) t1025 `kMobTypeCount = 21`（MobCaveSpider=20 枚举尾 +1 手抄）——`m_foodLure[21]` 容量与 `setFoodLure` 越界静默 no-op 都依赖它，而本工程 mob 枚举的扩展契约恰是「新类型枚举尾追加」（entity.h:247 同款注释），未来加 MobX=21 忘改 → 新 mob 食物引诱静默失效（无崩溃无断言无矩阵腿）；(b) t1028 `kNotePitchCount = 25`（与 BlockRegistry::NoteBlockPitchCount 同口径手抄，Audio 层不依赖 Core 头的分层选择）——同款风险面较小（音高档不太会变） | entitymanager.h:1712（kMobTypeCount）；entitymanager.cpp:7815（m_foodLure[e.mobType] 消费）；audiomanager.cpp:194（kNotePitchCount） | (a) 改 `static constexpr int kMobTypeCount = MobCaveSpider + 1;`（enum 值自动跟随，零成本消除手抄）；(b) 若坚持不 include Core 头，加 `static_assert(Data::kNotePitchCount == 25)` 旁注或注释登记同步点即可 |
| 4 | 低 | **t1024 床锚失效链只覆盖玩家挖掘路径**：爆炸 / 岩浆 / 水冲等自然破坏销毁锚床不触发 `clearBedSpawn` → 锚悬空（重生回已消失床的坐标，`snapSpawnToGround` 贴地兜底，不崩不卡，但「床被 TNT 炸了重生点还在」与 MC 观感不符）。代码注释已自登记「spec 口径 = 玩家挖床」 | playercontroller.cpp:1653-1661（finishMiningAt 床分支，注释明示「自然破坏不在此链」） | 短期登记即可（无软锁）；后续若做爆炸-锚联动，可在 EntityManager 爆炸清块路径回调 player 清锚（同 seedChanged 收口模式），注意分层方向（Entities 不能反向依赖 Game——经 QML 编排或信号转发） |
| 5 | 低 | **t1024 每次进有床档的世界必弹「重生点已设置」**：`onBedSpawnValidChanged` 置真沿 = 成功入睡 + 读档回填（`enterWorld` → `setBedSpawn`）两路都播报。睡前播报合理；**读档回填也播** → 每次进世界一条系统消息，UX 噪音（对比：锚失效换代路径刻意不发播报，注释「切世界无需用户面提示」——回填同理） | Main.qml:3147-3148（onBedSpawnValidChanged 播报）；Main.qml:864-871（enterWorld 回填路径）；playercontroller.cpp:215-225（setBedSpawn 发 bedSpawnValidChanged） | 把播报从信号处理器挪到入睡成功点（trySleepAt 设锚处直接 appendChatMessage 或独立信号），或 onBedSpawnValidChanged 加来源参数区分「入睡设置 / 读档恢复」，读档路静默 |
| 6 | Info | **t1025 狼 / 豹猫无食物引诱**：`breedFoodMatches` 只覆盖牛/羊/猪/鸡四物种；狼（骨头）/豹猫（生鱼）可繁殖、可喂食进求偶，但手持其食物不会被吸引跟随（MC 中狼被骨头吸引）。注释已登记「狼/豹猫肉食/鱼食映射不经本表」= 登记过的简化 | playercontroller.cpp:59-68（breedFoodMatches 四物种）；entitymanager.cpp:7814-7815（引诱门控消费） | 已登记，无需动作；后续做狼引诱时注意驯服态门（野狼跟骨吸引 vs 已驯跟随主人的优先级） |
| 7 | Info | **t1025 食物引诱未查观察者模式**：引诱分支只查门控表 + 距离，未查 `playerSpectator`（aiWolf/aiOcelot 跟随分支有此门：观察者不跟随）→ 观察者持小麦仍会吸引动物围观。无功能危害（观察者不可交互、不可被攻击目标锁定），一致性缺口 | entitymanager.cpp:7814-7825（引诱段无 spectator 门）；对比 :7773-7775（aiWolf playerSpectator 门先例） | 登记；若顺手补，同 aiWolf 一行门（`&& !playerSpectator`）即可，注意该变量在本作用域已可用 |
| 8 | Info | **t1028 kick（石下方）音色 ×1.0 与 piano 完全同声**：三族速率倍移 bass ×0.5 / kick ×1.0 / snare ×2.0——kick 与兜底钢琴同为原速，「石=低鼓」的注册口径实际听感零差。属登记简化的一部分（同一钢琴采样倍移），但注释只说「非独立采样」未点明 kick 当前无听感差 | audiomanager.cpp:849-857（playNote family→rate 映射，case 2 rate=1.0 与 default 同） | 登记即可（audiomanager.h playNote 注释补一句「kick ×1.0 当前与 piano 同声，独立采样留后续」）；或临时给 kick 一个轻微倍移（×0.92）制造可闻区分——取决于后续是否马上做独立采样 |
| 9 | Info | **t1028 右键调音触发整 chunk 重网格化（纯浪费，量级可忽略）**：音高只存 state 且**不参与几何/贴图选择**（各面同贴图、无 per-state 渲染差异），调音写 state 走 5 参数 setBlock 的重网格化路径 = 每次调音一次无谓 remesh + worldChanged。右键调音是低频交互，实测无感；与门/活板门同路径（门开合确有几何差，音符盒没有） | playercontroller.cpp:3581（setBlock 5 参数版 + 注释「走重网格化路径，同门/活板门口径」） | 登记即可；若未来出现高频调音场景（如音符盒阵列批量调），再评估「state-only 写不触发 remesh」的旁路口 |
| 10 | Info | **t1028 音名八度口径 C4 起与 MC 实际音域不同（已登记）**：本工程 n=0→C4（A4=9=440Hz，dev-plan 公式），MC note block 实际两八度 F#3–F#5。dev-plan 自定公式、dev-plan 内登记，探针同源断言。另 Main.qml:3237 注释引 dev-plan 文案例「音高：C#3」与实际产出 C4 起不符（注释笔误级） | blockregistry.cpp:2824-2830（noteBlockNoteName，4 + p/12 → C4..C6）；Main.qml:3237（文案例注释） | 无动作（口径已登记）；顺手可把 Main.qml 注释里的 C#3 改 C#4 消歧义 |

## 已核实安全面（审查结论，非发现）

- **神殿保底口径升级（80593c1，review0905 #3）**：触发门 `nearestCenterSq > R²`（R=56，world.h 单一权威）逻辑对称——sites 空 ⟺ nearestCenterSq 保持 1e18 > R² → 保底必触发（与旧 placed==0 语义同位）；补座选列「距中心最近合格列」不变，R 内有合格列 ⇒ 补座天然落 R 内的推导成立；中心距离并列取扫描序（x 外 z 内）先到者胜，确定性保持；小世界退化（R 超半边 / R 内无合格列）已在 world.h 常量注释登记契约。`desertTempleSiteOk/jungleTempleSiteOk` 上提 public 只读口，探针同源零复刻。
- **矿井壁挂火把收口 + F3 分母（bbed08a，review0905 #4/#6）**：贴面格/支撑壁水平四坐标显式界内 continue，越界整对跳过（不落火把不回退地板）——与 putStruct「越界静默跳过」语义对齐；`wy >= m_height` 上界查保留，豁口回退分支 `curY >= 0` 下界守卫原样（越界 blockAt 返 Air 落不进墙插分支，行为收敛正确）；`cap()` 暴露 kCap 单一权威，Main.qml 读口迁移零字面量残留。
- **t1024 床锚全链（bac476b）**：`kWeatherThunder=3` 与 world.h:1081 `enum class Weather { Clear=0, Rain=1, Snow=2, Thunder=3 }` 逐值核对一致；存档四键走 `INSERT OR REPLACE INTO world_meta`（worldstore.cpp:458）幂等无重复键，`{valid:false}` 只写 bed_valid=0、旧坐标键残留被 valid=0 门住；`loadBedSpawn` 防御半写（valid=1 但坐标缺 → 按无床）、旧档缺键 → hasBed=false 回世界出生点（t388 语义）；`setBedSpawn` 反解（floor(x-0.5 余)、floor(y)-1）与写入约定（格心 +0.5、by+1）精确互逆，'g'9 短往返无损；挖锚床判定覆盖 head/foot 两半（`x==anchor ∨ x+pd==anchor`，床同层 y 判等成立）；`clearBedSpawn` 幂等（已 pristine 且 invalid → 零 emit）；换代（onWorldSeedChanged）先清锚再复位 spawnPos，与 Main.qml enterWorld「seedChanged → loadBedSpawn 回填」时序自洽；雷暴/怪物拒睡在夜晚门后依次排列、互不吞文案。
- **t1025 繁殖回标 + 幼体物理盒（11a1a2f）**：`applyMobCollisionBox` 显式传 mobType（「勿改读 e.mobType」的教训已在注释钉住）；产崽减半（halfW/halfH ×0.5、maxHealth/2、health=上限、pos.y 下沉 ΔhalfH）与长大还原（×2、health×2 clamp、pos.y 抬升 ΔhalfH）**精确互逆**；`baby=true` 全仓仅两处赋值（tickBreeding 产崽 + 孵蛋路径，均同步缩盒挂 timer）——不存在「baby=true 但无计时器首帧误长大 ×2 血」的悬空路径；MobBabyShambler（t952）是独立 mobType=19 非 baby 链，不受影响；引诱/跟随段 yaw `atan2(-dx,-dz)` 与寻偶段（:7802）符号约定一致；引诱表生命周期闭环（setHotbar/setEntityManager 注入即刷、UniqueConnection 防叠连、无 hotbar → 持物 0 → 全物种清门控）；优先级链（寻偶 > 引诱 > 幼崽跟）else-if 互斥无穿透；测试缝 setBreedTimings 只影响此后新设计时器，不重写已挂值。
- **t1026 麦种 1-3（7ad0e9e）**：`bounded(1,4)` 上界开区间 = {1,2,3} 语义正确；玩家破块 / TallGrass 掉种 / 失撑掉落三路 spawnItem 收敛 dropCropDrops 单一权威，成熟判定快照时序（t134）不变；水蚀/踩踏简化口径登记与 wash 清单实况（耕地非附着块不在列）一致。
- **t1028 音符盒全链（816cb9a）**：NoteBlock=143 枚举尾追加 + Count 143→144，`kDefs[Count]` 编译期长度守卫强制表同步；AtlasTileCount 185 与 tools/build_atlas.py TILES 尾项（default_note_block=184）同步；25 个 wav qrc 注册数与 CMakeLists 清单数一致（grep 计 25）；升沿检测 bit5 记忆位语义正确（稳定通电不复响、断电清位、存档 round-trip 后重载不双响——重载时 powered 且 bit5=1 → 无沿）；红石接收族双注册（isPowerFamilyBlock + recomputePowerLocal receivers 集）与 IronDoor/Rail 先例同款；攻击发声分支位于 mob/船/矿车分流后、模式分流前，`canBreak()` 观察者早退在函数头（:1440 起 dead → canBreak 序列）→ 观察者不响不破；调音保留 bit5（通电中调音不即时响，待下次升沿）符合 MC 手感；配方多重集 {Planks:8, Redstone:1} 与箱子/熔炉/TNT 无冲突；「IronBars 保留 GroupStone」的 transient slip 已核实当前在位（blockregistry.cpp materialGroup case 列）；IronBars 本身是 t998 旧改动，t1028 未触碰其行为。
- **测试腿（ea3eb1a / 8182e11 / 9bcaff0 / c3db460 / b91cb87）**：五笔均只改 `tools/redstone_matrix_test.cpp`（+311/+330/+310/+319/+302 行，累计矩阵 479→502），无 src 触碰、无钉腿外溢；P-t1028「12 种红石源全覆盖」与 isPowerFamilyBlock 源清单的对应关系未逐腿展开核（测试侧自述），沿用「矩阵绿为提交自述」口径。
