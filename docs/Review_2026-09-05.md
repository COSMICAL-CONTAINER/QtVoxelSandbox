# Review 2026-09-05 — 近 24h 提交审查（t1003/t1004 神庙重建 · t1010 群系保底 · t1011 矿井补光 · t1007 遥测）

- 审查对象：`3d239bc..e5745c7`（2026-09-04 10:10 → 09-05 08:07，共 21 笔：5 笔 src 代码、6 笔探针（其中 863c804 含运行时遥测）、10 笔 docs/plan）。另盘点未提交工作区（t1012 进行中）。
- 审查方式：逐笔 diff 精读 + 关键路径代码交叉核对（`ChunkManager::blockAt/setBlock` 越界语义、火把附着常量、worldgen 阶段顺序、探针断言口径），未运行矩阵（HEAD 矩阵 431 PASS / 0 FAIL 为提交自述）。
- 总评：**无阻塞级新缺陷**。本窗口五笔 src 提交里四笔是 worldgen 纯函数改动（新世界才生效，旧存档零影响），越界/基岩/群系守卫齐全；863c804 遥测为纯增量。发现 1 项**窗口外引入、但 HEAD 在带病运行**的高优先级回归（矿井巷道段 B 不推进，t1001 引入、t1012 修复在工作区未提交），以及若干中低风险项与口径核对项，见下表。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | **高**（窗口外引入，HEAD 现存） | **矿井巷道段 B 整腿不推进**：t1001（57270c0，09-04 02:43，恰在本窗口前 ~7h）重写 pieceCorridor 时把 t565 推进条件改成 `leg == 0 && step > 0` 并在拐角预推进一格 → 段 B 只在拐角动一步、`lenB` 步重复刻同一截面（L 折线第二腿缺失；斜坡段变同柱逐层坑）。**t1011 的跨中壁挂火把（4ef957a）与 P-t1011 密度窗都是在该退化几何上验证通过的** —— 密度/火把类探针测不出「巷道没往前走」，属探针盲区实锤 | HEAD world.cpp:7350（`leg == 0 && step > 0`）；工作区未提交 diff 已恢复 `leg == 1 \|\| step > 0` 并移除拐角预推进（t1012 ③） | ① 尽快落 t1012 修复提交；② 给矩阵补一条「巷道推进几何」断言（拐角后第二腿截面坐标互异 ≥N 格 / 净样巷道总长下限），堵住密度类探针的盲区 |
| 2 | 中 | **t1004 红石锁初始 state 硬编码稳态字节**：`kMergeStates = {0x1F,0x3E,0x3E,0x3F,0x3E,0x6E}` + 拐角 `0x2F`（conn<<4\|power）与红石电力语义（火把 15、粉逐格衰减、t869 形状连接）强耦合。后续任何人改红石常量/传播规则，worldgen 直写的首帧 state 即与真值不一致；「约 5 tick 收敛」依赖脏集回插 + 火把自回插行为不变 | world.cpp:7858（kMergeStates 定义处）；P-t1004 有 state 钉腿（部分保护） | 在 dev-plan 登记耦合告示「改红石语义必须同步 kMergeStates 并重跑 P-t1004」；可选根治：worldgen 放完电路后对电路包围盒触发一次与运行期同源的 dust 重算，消灭手写稳态 |
| 3 | 中 | **t1010 保底只兜 `placed == 0`**：主路径在世界边角落 1 座时保底不触发，玩家出生区仍可能 0 神殿 —— 用户原始诉求「所在地即 Desert 却从未见神殿」只解决了「全世界 0 座」一半。属登记过的设计口径（「有群系必见神殿」按世界级 ≥1 解释），非实现 bug | world.cpp:7773（`if (placed == 0 && kDesertBiomeGuarantee)`，丛林 8074 同构） | 如需「中心可达」口径：保底触发条件改「距世界中心 R 格内 0 座」或补座时选「距最近已有神殿最远」的合格列；不改则维持登记 |
| 4 | 低 | **t1011 壁挂火把用裸 `m_chunks.blockAt/setBlock`**，无显式界内判断（同函数其余写入均走 putStruct 守卫口）。已核实不会崩：`blockAt` 越界返 Air、`setBlock` 越界拒绝（chunkmanager.cpp:56/135）——但极端越界巷道（margin 16 失守段）会走「壁空→地板回退」分支多落地板火把或静默漏放，与「墙插主形态」口径有偏差 | world.cpp:7398-7416（step%4==2 壁挂块） | 卫生收口：两处改走 putStruct 同口，或给 px/pz/wx/wz 加显式界内 continue，让越界语义与函数内其余写入一致 |
| 5 | 低 | **t1003 正面安卡 v4 横杠被门楣覆写**：D 步在四面 v=4 \|u\|≤2 写 WoolOrange 后，E 步门楣在 +Z 面同 5 格写 CutSandstone → 正面羊毛 16 格 vs 其余三面 21 格，「每面 21 格四折对称」口径在门面破缺（探针按实现断言故绿；与 wiki「门面无完整安卡」是否一致未核） | world.cpp placeDesertTemple E 步（lintel 循环） | 核对 wiki 门楣/安卡共存口径；若要求四面等量，把门楣行上移一层或行表 v4 改 \|u\|≥3 避开门洞 |
| 6 | 低 | **t1007 F3 mobs 分母硬编码 `"/64"`**（Main.qml 字符串内联），EntityManager::kCap 将来变更（如按设备分级）时 F3 读数即错。其余遥测面（primedCount O(64) 扫描 @10Hz、slotHighWater/liveHighWater 无 NOTIFY 普通 JS 读、mobHost.children 视觉树扫）均为纯增量、零绑定成本，已核对无行为风险 | Main.qml entities 行（863c804） | 暴露 `Q_INVOKABLE int cap() const` 替换字面量 64，一处定义 |
| 7 | Info | **同 seed 世界输出变化**（预期内）：t1003/t1004 神庙完全重建、t1010 补保底神殿、t1011 矿井加火把 → 老 seed 在新版本生成不同世界；已生成存档不受影响（worldgen 仅新世界跑） | — | 无需动作；发行说明可提一句 |
| 8 | Info | **峡谷在神殿之后 carve**（world.cpp generate 顺序 5126→5129）：神殿可被峡谷切开、密室/TNT 直接裸露。t1003/t1004 footprint 从 11×11 涨到 21×21/15×15 后被切概率上升；探针按 ravine-damage 弃样登记，实机尚无观察项 | world.cpp:5126-5130 阶段注释 | 加入实机目视清单：「神殿被峡谷切开的观感与密室裸露 TNT 是否可接受」 |
| 9 | Info | 6e43533 侧箱朝向勘误（state 1→0 = +X）只影响新生成神殿；旧存档箱 state 保持 1（朝 -X），无兼容问题。已核对 chestFrontFace 编码 0=+X 与两箱注释一致 | world.cpp placeJungleTemple I 步 | 无需动作 |
| 10 | Info | t1005/t1006 探针墙钟数字仅落盘 diag、不作断言（「机器相关量不作断言」已写死在探针注释）→ 本窗口未新增 flaky 阈值面；矩阵既有 flaky 仍只有 t882 钓鱼 RNG（登记在案） | 863c804/0c830e3 探针段 | 无需动作 |

## 已核实安全面（审查结论，非发现）

- **越界崩溃面为零**：`ChunkManager::blockAt` 越界返 0(Air)、`setBlock` 越界拒绝写（chunkmanager.cpp:54-60/133-140）；t1003/t1004 全几何走 putSolid/put/carveAir 守卫口，t1010 包作用域重构未动几何字节（t1003/t1004 逐方块断言 + 源码钉护航）。
- **t1011 火把附着编码逐支核对一致**：走廊壁挂 `(w*(-dz), w*dx)` 墙向 → TorchOnNX(1)/PX(2)/NZ(3)/PZ(4) 与 blockregistry.h:1984-1988「支撑=方向邻」语义全部对号；两处地板回退 state 分别传 `0` 与 `TorchFloor`，而 `TorchFloor == 0` —— 等价。壁挂步在支撑组/蛛网/轨之前只查空气格，与后置放置件无覆盖冲突。
- **t1003 层账自洽**：floorY=S-12、密室内空 y∈[S-11,S-8]、暗渠 k=10 端头 (4, S-9..S-8, 0) 恰好穿 dx=4 墙列开口接入内空顶层；塔顶越界 / 密室贴基岩 / 海域 / 非 Desert 四守卫齐全（后由 t1010 siteOk 收口）。
- **t1004 几何自洽**：三层内空/楼板与双跑楼梯末阶豁免格 (2,-5)/(4,-4) 吻合；铁门 x=3 dz=0 覆写龛墙两格、上格 0x08 半门位；陷阱发射器嵌隔断/外墙 + 板贴邻（scanDispenserTraps 4 邻口径）；西门槛 dx=-7/-8 在 margin=8 内安全。
- **t1010 主路径逐字保留**：siteOk 五守卫与旧内联同序同语义；保底为确定性纯函数（并列距中心取扫描序先到）；全图合格列扫描先过 isDesert 短门（列级 memo），贵守卫只跑沙漠列，一次性成本与一次高度 pass 同阶（160² rig × 32 seed 已实测）。
- **t1007 遥测纯增量**：primedCount/slotHighWater/liveHighWater 无持久化、clearAll 不重置为文档化意图（跨重载判读面）；Main.qml children 扫描用 `typeof dn.mon !== "undefined"` 防子节点误计。

## 遗留待办（跨批跟踪）

1. t1012 修复提交落地后，按发现 #1 补「巷道推进几何」探针腿（本表最高优先级）。
2. 发现 #2 红石耦合告示登记进 dev-plan（改红石语义 → 同步 kMergeStates + 重跑 P-t1004）。
3. 发现 #5/#8 的 wiki 口径核对与实机目视项并入下一个实机清单批次。

## 清偿状态（2026-09-07，#1/#2/#3/#4/#5/#6 全清）

矩阵 **480 PASS / 0 FAIL**（479 基线 + P-t1010b 加严腿；offscreen；`fix/test/docs(review0905)` 四笔）。逐项：

- **#1（高）巷道段 B 不推进** → ✅ t1012 ③ 闭环 + P-t1012 巷道推进几何探针腿（前批已清，本表存档）。
- **#2（中）t1004 红石锁手写稳态耦合** → ✅ 告示登记：dev-plan t1004 回标加「改红石语义必须同步 kMergeStates 并重跑 P-t1004」+ world.cpp 定义处 ⚠ 注释行；可选根治（包围盒运行期同源 dust 重算）登记后续候选，本波不做。
- **#3（中）t1010 保底只兜全世界 0 座** → ✅ 触发门改「主路径最近座距世界中心 > kTempleSpawnGuaranteeRadius(56)」且群系在场（两神殿同构；补座选列不变；确定性纯函数；siteOk 公开只读口供探针同源直读）。探针 P-t1010b 加严腿：**合格列**在 R 内 ⇒ R 内神殿证据 ≥1（块级核对；三轮实测迭代修正探针侧假红：群系列⇒合格列前提、全高扫替代 y 带、「恰 4 箱完整簇」⇒独占块证据在场〔箱/切制砂岩/门/拉杆/发射器，均 worldgen 独占〕，行为面零改并登记）+ R 内簇数 ⊆ 日志总数（不重复）。**阴性轮**：触发门还原 placed==0 → 恰红收敛 P-t1010 源码钉随动 + P-t1010b（9 条 VIOLATION：角落神殿世界出生圈 0 证据）；restore → 480/0。
- **#4（低）t1011 壁挂火把裸 blockAt/setBlock** → ✅ 显式界内 continue（越界整对跳过 = 不落火把不回退地板）+ 两处写入收口 putStruct 同口；正常域零行为差（P-t1011 数字逐位不变），免阴性轮。
- **#5（低）正面安卡 v4 被门楣覆写** → ✅ 考据收口（结论=现状即忠实，几何不改）：MC **完整安卡形**（橙陶瓦、蓝陶瓦心）只在地面层中央（Layer 0）与密室（Layer -4）各一次，外立面仅前侧双角塔面条带装饰、门面门楣上即切制砂岩（minecraft.wiki Desert_Pyramid/Structure 蓝图 Layer 3 `AAA`；Materials Orange Terracotta=66 / Blue Terracotta=2），本尊全埋无地面门洞，**门面本就无完整安卡**（raw + 蓝图归档 docs/review0905_wiki_desert_pyramid/）。落地=P-t1003 注释/PASS 文案改「三面完整安卡 + 门面截断 = MC 忠实」+ world.cpp D 步注释 + dev-plan t1003 考据段（含出处）。
- **#6（低）F3 mobs 分母硬编码 /64** → ✅ EntityManager::cap() Q_INVOKABLE（kCap 单一权威）+ Main.qml 改读 cap()；P-t1007 (f) 源码钉随行迁移。
- **#7-#10（Info）**：#7 同 seed 世界输出变化已随 t1003/t1004/t1010/t1011 发行说明口径登记；#8 峡谷切塔实机目视项、#10 flaky 观察维持遗留清单；#9 无需动作。
