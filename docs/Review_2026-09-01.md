# Code Review — 2026-09-01（近 24 小时提交）

- **审查范围**：`afd6396..35d5a72`（2026-08-31 09:38 → 09-01 07:30，21 个提交，其中 11 个纯 docs(plan)/docs(review)/docs(test) 提交已跳过）。Review_2026-08-31.md 覆盖到 `afd6396`（其文末注明批 C「在途未提交」不在其范围），本文从其断点起审，深审全部 **10 个代码提交**：
  - `c29dadb` review0830 批 C（生物十修一登记 #5/#6/#13–#19/#26）
  - `dc98f27` review0830 批 D（附魔/探针加固五修 #24/#25/#20/#22/#27）
  - `1a89929` t970 收杆摔落豁免 + mob 通用坠落链
  - `4ef7413` t971 鱼粒子预告窗收窄
  - `6377e87` t972 进世界空白区修复（visible 解链 + 渐进同步 + finishLoad 流体脏修复）
  - `457acf7` t973 生物占格放置门
  - `4964ffc` t974 保存退出写完成门 + 关窗存档路径
  - `c426c67` t975 创造调色板拿取语义再翻案
  - `0b89af1` t976 背包耐久条显隐决策上收
  - `dc5e55e` t977 切槽物品名浮显
- **审查基线**：HEAD = `35d5a72`，工作区 clean。
- **方法**：10 个提交的全部 src hunk 逐行读 diff + HEAD 现行源码交叉核对。关键面亲核：t974 `onClosing` 与 `saveAndExitToWorldList` 前置序逐行对比（`gatherPlayerState` 快照域核实）；t970 坠落基准保鲜/落地结算/槽复用清除三面推演；t973 AABB 口径与 `mobAabbHitsSolid` 逐式对拍（pos 为中心、`±halfW/halfH`、严格相交）；t972 `kickWorldMeshSync` 分组 step 与 `cutoutSegmentRestored`（默认 false = 5 段/chunk）对齐核实、`buildMesh` 空 chunk 守卫核实；t975 `setHeldBlock` 元数据清空面核实（换拿无附魔泄漏）；t976 全树 `DurabilityBar {` 用点清点（恰 5 处全上收 + SurvivalInventory 内联 armorDurBar 同单元无 AOT 面）；批 C 各门位（#5 跟随门在求偶后/跟随前、#6 两处 dayShadeAi 同式、#13 枚举门、#14 跳探外门）与消费点对拍；批 D #20 防抖饥饿面（1s 自愈轮询兜底）推演。**未重跑构建/矩阵**（397/0 为提交自述），本文为源码级复核；`tools/redstone_matrix_test.cpp` 本窗新增约 2000 行探针仅抽查、未逐行深审（其两处已知低危见附录）。

## 结论速览

**高 0 / 中 1 / 低 6（另登记 2 条）**。十个代码提交中 t970/t971/t973/t975/t976 及批 C/D 的全部修复面源码级核实正确（含各自登记的取舍面）。**t974 的关窗存档新路径漏掉整个「瞬态物品归还」前置序**是本窗唯一的实质性发现——关窗（X / Alt+F4）时机不对会静默丢失具体物品，与 t650/t690c 在按钮退出路径上早已堵死的丢物类同根，且该提交自述的「onClosing 同链」只同了存档链、没同归还链。其余为注释失实、口径劈叉与性能登记级。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高 | 0 | — |
| 中 | 1 | #30 `4964ffc`（t974）`onClosing` 关窗存档路径不做任何瞬态物品归还——光标手持 / 附魔台·铁砧·发射器面板槽 / 三处合成格物品不进 `gatherPlayerState` 快照，关窗即静默丢失 |
| 低 | 6 | #31 骑乘钉位段重复行（批 C #19 编辑残留）；#32 护甲耐久显示口径劈叉（t977 浮显 vs HUD/背包耐久条）；#33 meshSyncTimer 暂停期照跑且注释失实（t972）；#34 t972 全幅绘制 + 队列双重建的性能登记；#35 `lastExitSaveOk` 死属性（t974）；#36 关窗存档失败即时重试无退避（t974） |

---

## 一、中危（建议尽快修复）

### #30 `4964ffc`（t974）`onClosing` 关窗存档路径跳过整个瞬态物品归还序——背包/面板开着关窗 = 静默丢具体物品 ✅ 已修（afdcd1c）

- **问题**：t974 新增的关窗兜底（Main.qml `onClosing`，现 :925-932）只做了 `runExitSave()`（两次重试 + warn）+ `closeWorld()`，而按钮路径 `saveAndExitToWorldList`（:941-957）在存档前有一整套**同步归还前置序**：
  ```qml
  if (enchantingTableOpen) closeEnchantingTable()   // t650：附魔台 A 槽物品回背包
  if (anvilOpen) closeAnvil()                       // t650：铁砧 A/B 槽物品回背包
  if (dispenserOpen) closeDispenser()               // t650：发射器面板物品回背包
  craftingTablePanel.returnCraftToHotbar()          // t690(c)：三处合成格材料回背包
  survivalPanel.returnCraftToHotbar()
  inventoryPanel.returnCraftToHotbar()
  returnHeldToHotbar()                              // t56：光标手持栈回背包
  ```
  `gatherPlayerState`（:879-902）只快照 hotbar/main/armor 三数组——**光标手持栈（heldBlock/heldCount/heldEnchants/heldCustomName）与三个面板的 QML 本地槽、三处合成格都不在快照里**（t650/t690c 注释自述「面板数组是 QML 本地、永不进存档」）。正常关包路径 `closeInventory()`（:1306-1311）会 `returnHeldToHotbar()`，但 `onClosing` 一个都不调。
- **触发场景**（存档本身成功、退出也正常，唯独这些物品消失）：
  - 背包开着、光标捏着一组方块/一件工具时直接 Alt+F4 / 点标题栏 X → 该组物品丢失；
  - 附魔台开着、钻石镐放在输入槽 A 时关窗 → 钻石镐丢失（铁砧 A/B、发射器面板同理）；
  - 工作台 3×3 / 生存背包 2×2 合成格摆着材料时关窗 → 材料丢失。
- **为何是回归面**：t974 之前关窗 = 上次存档点之后**全部**进度丢失（大坏）；t974 之后关窗 = 只丢瞬态持有者上的**具体物品**（小坏但更隐蔽——存档时间戳是新的，玩家不会怀疑存档，只会发现物品没了）。这与 t650/t690c 在按钮路径上修的是同一类丢物洞，新路径漏接。
- **修复建议**（把前置序抽成共用函数，两路径同源，杜绝第三条路径再漏）：
  ```qml
  // t974+ 退出前瞬态物品归还未一实现（按钮退出 / 关窗兜底共用；新增退出路径必须先调本函数再存档）
  function returnTransientItemsBeforeSave() {
      if (enchantingTableOpen) closeEnchantingTable()
      if (anvilOpen) closeAnvil()
      if (dispenserOpen) closeDispenser()
      craftingTablePanel.returnCraftToHotbar()
      survivalPanel.returnCraftToHotbar()
      inventoryPanel.returnCraftToHotbar()
      if (inventoryOpen) closeInventory()   // 内含 returnHeldToHotbar + grab/焦点（关窗场景无副作用）
      returnHeldToHotbar()                  // 背包外持物（箱子/熔炉面板开着时的光标栈）兜底
  }
  ```
  `saveAndExitToWorldList` 的 :947-957 段与 `onClosing` 的 `runExitSave()` 之前各调一次。`closeInventory` 内的 `player.grab()`/`forceActiveFocus` 在关窗场景无害。顺带在探针 P-t974 加一条行为腿：开着铁砧放物品 → 走关窗链 → 重进世界物品在背包。
- **登记**：关窗路径同样跳过了 `finishExitToWorldList` 的实体清理 / `audio.stop*`——进程即退，无观测面，不算缺陷。

---

## 二、低危（择期修复 / 登记）

### #31 `c29dadb`（批 C #19）骑乘钉位段出现逐字重复行——编辑残留 ✅ 已修（afdcd1c）

- **问题**：entitymanager.cpp **:5640-5641** 连续两行完全相同：
  ```cpp
  if (!e.resting) e.resting = true;          // 钉位态视作「支撑」（无物理语义，仅状态一致）
  if (!e.resting) e.resting = true;          // 钉位态视作「支撑」（无物理语义，仅状态一致）
  ```
  diff 显示 #19 在既有行**之前**插入了一行同文（jumpG 清理之后顺带重排时手误），现存文件里两句叠着。功能幂等无影响，但属编辑残留；且自家终审报告引证「#19 jumpG 清理在钉位段（5637-5639）」也未发现。
- **修复建议**：删除其一（保留 :5640，注释挂 jumpG 清理同段之后即可）。顺带可让矩阵源码钉把「钉位段 `e.resting = true` 恰一处」钉住，防同类手误再进。

### #32 `dc5e55e`（t977）护甲耐久显示口径劈叉——浮显有双段判定，HUD/背包耐久条全没有 ✅ 已修（afdcd1c）

- **问题**：t977 新权威 `Hotbar::slotDetailText`（hotbar.cpp :1021-1036）对耐久行做**双段判定**（`toolMaxDurability<=0 && ArmorRegistry::isArmor → ArmorRegistry::maxDurability`），而所有耐久条消费面仍是单段：
  - HUD hotbar 条（Main.qml :13430）：`maxDur = toolMaxDurability(...)`，无护甲兜底；
  - 背包主栏 / hotbar 行条（Inventory.qml :1544/:1737、SurvivalInventory.qml :915/:1093，t976 刚上收 visible 的那五处中的四处）：`mDur = toolMaxDurability(...)` 同样无兜底。
  护甲件（工具段无行）放 hotbar 槽且已受损（穿上挨打再收回背包即得）：**切槽浮显会写出「耐久: 87/240」，而 HUD 槽底、背包两处一条都不显**——同一数据新旧两面矛盾。t977 注释称「t349『有无耐久』判法推广」，但 :13430 的 t349 实现并没有护甲段，推广只发生在本提交的 C++ 侧。
- **修复建议**：在 Hotbar 加一个双段判定 Q_INVOKABLE 单一权威（如 `maxDurabilityFor(int itemId)`，与 slotDetailText 同式），上述 5 处条位改调它（装备槽本就走 `armorMaxDurability`，不受影响）。一并把 t977 提交信息里「t349 判法推广」的表述与实现对齐——要么推广到条，要么浮显也收窄回单段（前者更符合「hotbar 可持护甲」的事实）。

### #33 `6377e87`（t972）meshSyncTimer 暂停期照跑——注释宣称的「暂停 idle（t889 硬档停口径）」不成立 ✅ 已修（afdcd1c，取案 (b)：注释改「UI/呈现件豁免 worldRunning」显式登记 + 源码钉）

- **问题**：Main.qml meshSyncTimer（:624-644 附近）`running: window.appState === "playing"`，而 ESC 暂停叠层**不改 appState**（全文件 appState 写点仅 :651/:802/:1022/:1055/:13950/:13963，均非暂停），即暂停期排空泵照跑（每 16ms 一段 ~2-4ms 重建）。注释「仅游玩态跑（菜单/暂停 idle——硬档停语义同 worldRunning 口径，t889）」与实现不符。
- **影响**：方向上是良性的（暂停期继续渐进填满远处地形），但弱机暂停菜单可能出现周期性小卡顿，且注释契约失实会误导后续按 t889 口径做清点的人。
- **修复建议**：二选一并写清——(a) 真停：`running: window.appState === "playing" && window.worldRunning`；(b) 保留现状（渐进加载继续），把注释改成 t977 hold Timer 同款「UI/呈现件豁免 worldRunning」的显式登记。推荐 (b)（暂停期填地形对观感有利），若选 (a) 需同步确认暂停恢复后队列不清空（现状 `running` 翻转不清 `_meshSyncQueue`，天然续排，两案都安全）。

### #34 `6377e87`（t972）全幅绘制与队列双重建——性能登记（弱机 / Android 面） ✅ 已登记（afdcd1c，①绘制面 Android 验证轮登记 + 源码钉）；✅ 已修（afdcd1c，②排空端统一三条件复查跳过重复重建 + meshRebuilt 计数腿）

- **登记 1（绘制面）**：六段模板 visible 解链 chunkInRange 后，有限世界边到边全绘（提交自述 segs visible 101→202，翻倍）。t470 在开发机实测绘制剔除零 FPS 收益成立，但那是单点数据（桌面 + 主线程 CPU 瓶颈）；Android 目标面（GPU 更弱、t972 时点 Android 输入链本就是首要阻塞项）全幅 draw call 翻倍有回退风险。建议在 Android 真机验证轮里加一项「进世界后帧率 / draw 数」对照，必要时给设置页留「绘制半径」开关（语义已随本提交改成「重建半径」，UI 文案已同步，改动面小）。
- **登记 2（重建面，微优化）**：稳态欠账扫描把段推入 `_meshSyncQueue` 后，排空端 `refreshMesh()` 无条件重建——若期间该段已被 `setChunkInRange(false→true)` catch-up 重建过（玩家走近），队内残留条目会造成一次纯浪费的重复重建。正确性无损。可在排空时对「稳态来源」条目复查 `deferredRebuildPending() || lightStale()` 跳过（进世界全量队列有意无条件重建，需区分来源或统一复查 `lightStale || deferred || meshWasCleared`）。

### #35 `4964ffc`（t974）`lastExitSaveOk` 是死属性——生产零消费 ✅ 已修（afdcd1c，取首案消费：世界列表「上次退出未保存」角标 + onClosing 路径补写 + 探针钉）

- **问题**：Main.qml :49 声明、:972 赋值（onClosing 路径还不写它），全工程唯一读者是矩阵探针的源码钉（redstone_matrix_test.cpp :29126）。承诺的「false = 写盘失败已重试仍败」没有任何用户可见出口。
- **修复建议**：要么消费（如世界列表页对 `lastExitSaveOk===false` 的最近世界挂「上次退出未保存」角标，正好把 t974 的 toast 补成可回溯面），要么连属性带源码钉一起删，别留只有测试在读的生产态。

### #36 `4964ffc`（t974）关窗/退出存档失败的「立即重试」无退避——锁窗场景大概率两连败 ✅ 已登记（afdcd1c，两路径注释登记取舍 + ≤300ms 退避上限方向 + 源码钉）

- **问题**：`if (!exitSaveOk) exitSaveOk = runExitSave()`（:966-967 与 onClosing 内同型）：外部进程持 `.sqlite` 锁（杀软/索引器/同步盘，提交自述的成因）时锁窗通常是百毫秒到秒级，0ms 间隔的同步重试几乎必然再败，重试只对「瞬态已释放」的窄窗有效。
- **修复建议**：登记即可（toast 已兜住用户面）；若要提质，可在大世界量级下先 `saveProgress`/`savePlayerData`（小事务）探锁、失败再 `QThread::msleep(100-200)` 后整链重试一次——注意别把关窗阻塞成秒级「未响应」，退避上限建议 ≤300ms。

---

## 三、核实无问题的重点面（源码级证据）

- **t970 坠落链四要素 + 三边角**：贴地保鲜（resting 分支 aiTick 级重置——resting 期 Y 恒定，节流无害）、滞空逐帧 max、落地沿「先取值后清」+ 基准复位 + 水豁免走 `mobFeetInWater` 单一权威、`damageEntity` 前完成全部容器引用写（悬垂窗口纪律）；拉拽豁免一次性（落水同样消费，不跨坠落存活）；槽复用 `spawnMobCore` 整体 move 清零；骑乘骑手 resting 由钉位段强制真 → fallPeakY 随钉位脚位保鲜，解除骑乘后基线正确（与批 C #19 组合无伪摔伤面）。坠落起算不跨事件、水中上浮后落底被水豁免覆盖（鱿鱼浮沉不伪伤）。
- **t971 谓词纯读 + 镜像值变才发**：`bobberApproachAt` 对 tick 本体零写；四镜像并入同一 `fishingChanged`（`|| approach != m_bobberApproach`），每等待周期恰两翻（开沿/咬沿）；收竿/断线/换槽/失效四清零点齐全；短等待（唤潮 III）「落定即 true」无裸窗；QML 仅加 running 项，380ms 节流未动。
- **t972 三件套结构**：`kickWorldMeshSync` 分组 step 与 `cutoutSegmentRestored`（默认 false=5 段）同源对齐，窗口判定取组首 chunkCX/CZ 与段布局一致；`clearMesh` 文档序完整（clear→空 vertex/index→stride→bounds→原语→属性→update）+ meshRebuilt 通知；`buildMesh` 对空 chunk/world 有守卫（:548 `if (c && m_world)`），refreshMesh 打到无数据段只产空 mesh 无害；欠账双标记（deferredRebuild/lightStale）在 buildMesh/clearMesh 双清；finishLoad 清 `fluidOnlyDirty` 与 generate 路径对齐，载入首建不再被 t188 吞。
- **t973 占格门口径**：AABB 与 `mobAabbHitsSolid` 同式（pos 中心 + `±halfW/±halfH`，非脚位），严格相交（贴面不拦）；排除面（dead/非 Mob kind/矿车船域外）与 MC 语义对齐并在头注释登记；三合并位（同格半砖/邻格互补半砖/雪层堆叠）+ 门/床双写格全覆盖，序位在 t198/t146 之后、族预检之前；拒绝 = 无挥无耗 no-op 同预检口径。
- **t975 paletteTake**：三分支结构与 t896 前左键逐字对应；换拿路径 `returnHeldToVoidRequested → heldBlock=新 id` 经 `setHeldBlock`（hotbar.cpp :1811-1826）自动清附魔/改名/重置耐久——**无元数据泄漏**；右键续拿 cap 走 `maxStackSize` 单一权威、满组静默 no-op 已登记；右键 TapHandler 与 root 右拖 DragHandler 共存依赖「palette 组不在 localDragGroups」——已核实（Inventory.qml :464）；两调用点恰 2、无限源字面量恰 2（探针钉）。
- **t976 五处上收完备**：全树 `DurabilityBar {` 组件用点恰 5（Inventory×3 + SurvivalInventory×2）全部带 revision 触碰的 delegate 级 visible；SurvivalInventory 装备槽条是同单元内联 armorDurBar（:731），无跨单元 AOT 面；t931 满耐久隐条语义逐字保留；组件内 visible 留作语义兜底 + 头注释契约。
- **批 C 十项**：#5 门位（求偶后/跟随前）与 aiWolf 同字面量；#6 两处 dayShadeAi `&& e.armorHelmet == 0` 与燃烧调用点同式；#13 枚举门补 MobBabyShambler + 长期方向登记；#14 `distXZ > kAttackRange` 外门（跟随/求偶路径 kFollowMinDist 2.5 > 1.6 不受波及，注释自证）；#15/#16 缺省单源 + 骑乘三向排除；#26 `isUndeadFamily` 与 `undeadBurnsInDaylight` 语义分立双侧注释（戴盔亡灵不烧但仍吃对族加成）。
- **批 D 五项**：#24 consteval 守卫 + 定长探针表 static_assert（22 号附魔漏 homeCategory = 编译红）；#25 `familyAttackBonusFor` 单支权威（2.5f 全工程唯一字面量），attackMob 按族取支——交叉恒 0 族门不外泄，heldEnch 数组与旧 `selectedItemEnchantLevel` 同源等价；#20 真 trailing-edge 防抖——持续写源会顺延饿死重扫，但 1s 低频自愈轮询兜底（双通道设计天然免疫饥饿）；#22 两面板 itemTaken 守卫后发射、消费端与 Inventory 同一 onItemTaken；#27 钮宽 25 后 8 钮行 228 ≤ 234 由构造成立。
- **t977 组件本体**：AOT 契约成立（Connections 处理器内 Q_INVOKABLE 直读、显隐决策只读本单元属性，结构性规避 t976 类跨单元链）；`lastShownSlot` 守卫拦同槽内容变更信号；active=false 只重同步基线（applyPlayerState → appState=playing 时序下进世界不闪名）；空槽即隐、快切 restart 防抖；挂载锚 vitalsBar.top 为兄弟几何锚（Creative 下 vitalsBar 隐但几何在，浮显位不漂）；与 t508 下船提示同位重叠已在提交内登记。#30 之外的浮显面干净。

---

## 附录：已由窗口内自查登记、本文不重复展开的条目

- **终审报告自登记低危 2 条**（docs/test-reports/review-r1917-final.md L1/L2）：t935 漏斗收口注释计数漂移（36 处 vs 注释 35/34）；NetherPortal/EndPortal 词根标识符为窗口外基线（R19.12 改名批同族遗留）。
- **各提交内自登记取舍**（核实与实现一致）：t977 浮显与 t508 提示同位不互斥；t975 右键不接 t318 toggle（防拿/还振荡）；t973 矿车/船不占格、桶/种植分支不走门；t972 渲染半径语义收窄（设置页文案已同步改「重建半径」）；批 C #17 游荡态不避光（注释登记，非回归）。
- **探针侧未深审**：`tools/redstone_matrix_test.cpp` 本窗 +~2000 行（P-t970..P-t977、批 C/D 探针腿）仅抽查结构与关键断言（#24 定长表、t974 源序钉、t975 计数腿），未逐行复核腿逻辑；矩阵 397/0 为提交自述未复跑。
