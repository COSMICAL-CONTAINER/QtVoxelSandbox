# Review 2026-09-10 — 近 24h 提交审查（t1028 编译收口 · t1029 wander 冻结缝 · review0909 五连清偿 · t1027 掉落 instancing 首批）

- 审查对象：`b91cb87..4c83422`（2026-09-09 09:41 → 09-09 23:54，共 18 笔：5 笔 src fix（含 1 笔纯注释订正）、4 笔测试腿（全部只动 `tools/redstone_matrix_test.cpp`）、9 笔 docs/plan/governance/state），矩阵 502→505（t1029 +1、t1027 +2；review0909 腿净 +0 系改写）。与上轮（Review_2026-09-09，止于 `b91cb87`）边界无缝衔接，无重叠无遗漏。
- 审查方式：5 笔 fix 逐笔 diff 精读（d27d980 / 86cc7fc / ccef2b1 / 020ed36 / 1bf02e9），关键路径在 HEAD=4c83422 交叉核对——bedSpawnValid 全消费面（C++ 4 个 emit 点 + QML 3 处读取）、睡眠相位链（Lying→Settled→Waking / 早醒按钮沿）、`playerSpectator` 作用域（tick 形参）、t1027 feeder 与旧 delegate 的坐标 / 缩放 / 旋转 / 材质 / 动画公式逐参比对、isItem3DFamily 25 id 逐 id 对照、kMobTypeCount 枚举自动跟随落地；Qt 6.11 官方文档（properties.html）核对 NOTIFY 信号参数约定；矩阵日志 pos/neg/restore/final 链 PASS/FAIL 计数 awk 实读（两份 final 均 PASS=505 FAIL=0）；app 构建与运行日志时间线核对；测试腿核 file 面 + 关键断言抽查。
- 总评：**无高 / 中严重度新缺陷**，2 低 / 2 Info。四个 src fix 主链（app 目标编译收口、wander 冻结测试缝、review0909 五项清偿、方块整立方掉落族 instancing 首批）逐项核对成立，矩阵终态 505/0 与日志实证一致。低级发现两处均为**潜在面**而非现行 bug：#1 NOTIFY 信号参数语义冲突（当前 QML 消费面全为命令式现读、不产生错值，但对未来任何声明式绑定是陷阱）；#2 t1027 桶编排溢出路径（>8 种活跃 id）无运行期验证（QML 面 headless 探针不可达的盲区，逻辑读侧核对成立但零执行证据）。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | 低 | **`bedSpawnValidChanged(bool restored)` 的信号参数与属性新值语义相悖（NOTIFY 约定冲突，当前不咬人）**：Qt 6.11 properties.html 明文「NOTIFY signals ... must take zero or one parameter, which must be of the same type as the property. **The parameter will take the new value of the property**」（MEMBER 属性为硬性要求，READ 属性为同向生态约定——工具链 / 后续维护者 / QML 引擎值携带路径都按此约定消费）。本信号带 bool 参且类型与 `bedSpawnValid` 属性重合，但**参数含义是「来源沿」不是「新值」**：入睡设锚沿（:3212）与 sleepAdvanceToDawn 守卫沿（:3134）emit 参数 false 而属性同拍变 **true**——按约定语义读即错值。当前不咬人的原因：QML 全部消费点均为命令式现读（runExitSave 函数读 :1031 / respawn handler 现读 :1327 / Connections handler 现读 :3146），无任何声明式绑定挂在 `player.bedSpawnValid` 上 | playercontroller.cpp:3212、:3134（参数 false ∧ 属性→true 的两沿）；:247 / :259（setBedSpawn true 沿 / clearBedSpawn false 沿——参数恰与属性新值巧合一致，掩盖了冲突）；playercontroller.h:292（Q_PROPERTY NOTIFY 挂本信号）+ :711（信号声明）；Main.qml:1031 / :1327 / :3145-3146（三处命令式消费，均现读不受影响）；tools/redstone_matrix_test.cpp:42587（探针 lambda 直连记录 (valid, restored) 对——测试侧同样自记录自消费，不依赖引擎值携带路径） | 信号改回**无参** `bedSpawnValidChanged()`（属性 NOTIFY 职责单一）+ 播报源另立信号（如 `bedSpawnAnnounce(bool restored)`，只在入睡设锚沿 emit），或参数改 int 枚举避开与 bool 类型重合；同步改 QML handler（Main.qml:3145）与矩阵钉（hdr-bedSpawnValid-signal-src、P-t1024a (e) 腿的 restored 断言）。改动面约 6 处（信号声明 + 4 emit + QML handler + 1 钉 1 腿），低风险；若维持现状则至少在 playercontroller.h 信号注释里点明「参数非属性新值、勿在声明式绑定上依赖」并登记 |
| 2 | 低 | **t1027 桶池溢出路径（>8 种活跃整立方 id）零运行期验证 + QML 编排面探针盲区**：桶池 8 个（Main.qml:6667），而整立方 id 面 = 16 色羊毛 + 泥 / 石 / 圆石 / 木板 / 砂 / 砾石…——正常游玩（清一座彩色建筑、多矿种挖掘）即可超 8 种活跃并存，溢出不是边角场景。溢出路径逻辑读侧核对成立（确定性 tiebreak、hasBucket 排除与 feeder 谓词同源、delegate 保底不丢渲），但：P-t1027a 只覆盖 C++ feeder 直调（spawn / 拾取 / 移除 / 清空 / 合并），P-t1027b 只钉谓词与源码；`reassignDropBuckets` / `hasBucket` 是纯 QML JS，headless 矩阵探针不可达，且本项目未配 qt_add_qml_module / qmlcachegen（QML 语法错误构建期也不报）。app 侧证据：build_reviewfix_app.log（09-09 23:12 构建成功，含 t1027 C++ 侧）+ logs/voxelsandbox.log（同刻运行、含世界切换遥测）成立，但 **smoke_reviewfix.log 为 0 字节**（stdout 实际走了 voxelsandbox.log，验收证据链弱化），且 >8 id 并存场景几乎肯定未被该次冒烟触达 | Main.qml:6667（kBucketCount=8）、:6680（reassignDropBuckets）、:6672（hasBucket）、:6251（delegate 排除链 hasBucket 尾门）；itementitymanager.cpp:38-45（isPlainCubeDrop：方块段 (0,Count) 内非 partial/cross/bed/3D 族——羊毛全 16 色在列）；CMakeLists.txt 无 qt_add_qml_module（QML 不经构建期编译）；日志时间线：build_reviewfix_app.log 23:12（75KB）/ smoke_reviewfix.log 23:12（0 字节）/ logs/voxelsandbox.log 23:12 | 补一次定向 in-world 冒烟：创造模式丢 / 破 ≥10 种整立方 id（羊毛多色 + 石 / 泥），核对每个掉落**恰渲一次**（无 z-fight 双渲、无丢渲、旋转 / 浮动动画在位），再拾取至 <8 种验证桶释放回 delegate 无缝切换；「QML 编排面（信号 handler 改表类逻辑）headless 探针不可达」登记 lessons 盲区簿——t1028 已登记 app-target 编译盲区（d27d980），QML 侧同型盲区本批未登记 |
| 3 | Info | **8 桶 feeder 各挂 16ms Precise QTimer 常走（空场景 ~500 次唤醒 / 秒）**：构造即 `m_ticker.start()`，无条件；空桶（familyId=0）getInstanceBuffer 早退空表，但 tick 仍每 16ms markDirty → 渲染 sync 每帧重取 8 张空表。t858 先例是单 feeder 同款，本批放大 ×8；主菜单 / 无掉落场景照走。量级可忽略（每次空早退 + 空 QByteArray），属模式成本登记 | blockdropinstancing.cpp:33-38（构造 start 无门）、:60-63（tick 无条件 markDirty）、:67-68（familyId<=0 早退空表） | 登记；后续批次若扩桶池（族治理 ②-⑥ 分步），加「familyId>0 且桶内活体>0 才走钟、空转暂停」门——活跃切换沿由 setFamilyId / entitiesChanged 侧补 markDirty 兜底，不引入丢帧 |
| 4 | Info | **桶指派「在用桶保持」不让位**：kept 判定只看 `counts[id] > 0`，不与候选活体数比较——1 活体老 id 可长期钉住桶，40 活体新 id 溢出走 delegate 路径（单 draw 合批收益损失，无正确性问题）。防几何重建抖动的刻意取舍（注释明示），极端长尾场景（常驻小族 + 后到热族）收益劣化 | Main.qml:6693-6697（kept 只查活体>0）、:6700-6702（freeSlots 只填空位，无让位逻辑） | 登记；若实测出现热族长期走 delegate，可加带滞后的按活体数让位（候选数 > 桶内 2× 才换 + 保持确定性 tiebreak），仍零抖动 |

## 已核实安全面（审查结论，非发现）

- **t1027 方块整立方掉落族 instancing（020ed36，本窗最大面 +350/−9）**：主链逐项核对成立——(a) **谓词单一权威**：`isPlainCubeDrop` / `isItem3DFamily` 收编 C++（itementitymanager.cpp:18-45），QML `isItem3DFamily` 退化薄委托，25-id 表与 QML 旧字面量表逐 id 对照一致（13/20/136/15/87/58/109/44/24/94/43/25/17/60/88/19/135/89/115/48/102/129/112/113/114——计数 25，头注释「24 id」笔误已由 1bf02e9 订正）；delegate 排除侧与 feeder 收纳侧同源谓词，无双渲 / 丢渲逻辑缝；(b) **渲染口径逐参对齐**：坐标同源（feeder `posAt(slot)` = delegate 根节点 `itemEntities.posAt(index)`，Main.qml:6204）、缩放 0.3 均匀、旋转 euler (0, rotY, 0)、材质 PrincipledMaterial{NoLighting + voxelAtlas + terrainLight(skyLight) + alphaCutoff 0} 与旧 plain-cube 分支逐字同参（Main.qml:6250-6260）；(c) **动画解析式逐字推导核对**：rotY = fmod(ph/3,1)×360 ≡ 旧 `NumberAnimation on rotY {to:360; duration:3000}`（Main.qml:6643）；bob = 0.075×(1−cos(π·s)) ≡ 旧 0↔0.15 双腿 1s InOutSine（Main.qml:6645-6649，InOutSine out-and-back 三角波推导成立）；相位口径变化（创建时刻 → slot×0.37s）已在头注释登记；(d) `static_assert(Count<=0x100)` 编译期钉死「工具 / 材料段经 Count 上界排除」前提（itementitymanager.h 尾，当前 Count=144 成立）；(e) **数据链零扰动**：拾取 / despawn / 合并 / 物理路径无改动，feeder 纯只读，P-t1027a 行为探针覆盖空表 / 分桶计数 / 拾取 / 移除 / 清空 / 就近合并六链（137d870）；(f) 桶指派确定性（活体数降序、id 升序 tiebreak、整体数组替换赋值保 notify、Component.onCompleted 兜底首算）；(g) moc 完整类型要求（Q_PROPERTY(ItemEntityManager*) 直接 include 全量头）规避了 xporbinstancing.h 靠 include 顺序存活的同型坑，教训已写入头注释。
- **ccef2b1（review0909 五连清偿）**：(1) 雷暴入睡新口径链完整——夜 ∨ 雷暴二选一入门（trySleepAt :3157-3158）、Settled 早醒按钮沿（wakeUpFromBed）**不碰天气不跳晨**、坐满计时才 sleepAdvanceToDawn 跳晨 + 清雷暴回 Clear（:3125-3127），与 MC「sleeping resets the weather cycle / 只在完成沿重置」语义一致；kWeatherClear=0 / kWeatherThunder=3 与 world.h Weather 枚举编码核对一致；无 worldClock 降级 = 拒睡（沿用旧降级方向，注释登记）。(5) 播报源契约：入睡设锚沿**恒发**（restored=false，:3212——修掉旧 `if(!m_bedSpawnValid)` 守卫吞沿的断线）、读档回填沿 restored=true 静默、置假沿恒静默，P-t1024a (e) 腿记录 (valid, restored) 对全沿断言；残余风险见本文件 #1。(2) 音符盒 `!sneakPlace` 门与同函数工作台 / 熔炉 / 箱子 / 附魔台 / 铁砧 / 发射器 / 投掷器七分支模式对齐，潜行空手右键由下方 m_selectedBlock==Air 守卫拦（无双动作）；P-t1028b 随新口径改写（sneak 旁路断言折入）。(3) `kMobTypeCount = MobCaveSpider + 1` 枚举尾自动跟随落地（entitymanager.h:1721 附近，手抄 21 消除）；kNotePitchCount 手抄保留 + 双侧矩阵钉（aud-note-pitchcount / hdr-note-pitchcount）的登记处理与 Review_2026-09-09 #3 清偿方案一致。(7) 观察者引诱门 `!playerSpectator`——playerSpectator 是 EntityManager::tick 形参（entitymanager.cpp:5949 签名），引诱段（:7839）在 tick 作用域内直接可用，与 aiWolf/aiOcelot 跟随门（t947① 透传通道）同款。
- **86cc7fc（t1029 wander 冻结测试缝）**：早退位在时间片推进 / RNG 选向 / 速度写入**之前**（entitymanager.cpp:2877-2880），冻结期 wanderTimer 保持、QRandomGenerator 全局流不消费、yaw/wanderSpeed/位置逐字节保持、仅 moveSpeed 归零；重力 / 水推 / 击退 / 拉拽物理在 aiWander 之外不受影响（「冻结 ≠ 悬停」，P-t1029 行为腿钉空中生成仍落地）；缺省 false = 生产路径零改动，同 m_chickenJockeyChance / setBreedTimings 缝先例；不 bump revision / 不持久化（纯 AI 输入门控）取舍自洽。t882/t927/t960 三族 flake 同源（甩钩窗内猪 wander RNG 漂移）经显式冻结确定性化，P-t882/P-t927/P-t960 接线随腿落地（82e72f8）。
- **d27d980（t1028 app 目标编译收口）**：裸名 `Clip` → `auto&`（audiomanager.cpp:857），根因 = Clip 是 Data 嵌套类型、矩阵测试目标不含 audiomanager.cpp 编译单元——盲区已登记且 build_t1028_lesion.log 验证链同批补齐，属流程自愈而非代码缺陷。
- **测试腿 4 笔（82e72f8 / a1da7bd / 137d870 / 6abfb5c）**：全部只动 tools/redstone_matrix_test.cpp（+109/+134/+170/+40 行），无 src 触碰、无钉腿外溢；review0909 腿含阴性轮敏感标注（摘门即红，matrix_reviewfix_neg.log 实证恰红 → restore 后恒绿）；P-t1027b bare-contains 钉迁移 pinSet（6abfb5c）为钉法升级不扩面。**矩阵证据链**：matrix_reviewfix_pos/neg/final + matrix_run_t1027_neg/restore/final + matrix_t1029_neg×3/seam×3 全链在库根目录，两份 final awk 实读 **PASS=505 / FAIL=0**，与 dev-plan 自述一致（matrix 链条归属勘误 19e280d 已把 503→505 订正为 t1027，本轮复核认同）。
- **docs 9 笔**：治理流闭环（批次收口 2a12fc8、audit #2/#3 GREEN、agent-state 恢复点 4c83422）与代码面无交叉污染；4c83422 的 t1030 恢复点准确登记了 WIP 现场（见下）。

## 工作区状态注记（非本窗提交，供续接棒）

- `tools/redstone_matrix_test.cpp` 工作区 +266 行未提交（t1030 探针骨架：P-t1030a/b 配方腿 / 催熟推进腿 / 阴性轮骨架），**引用未实现 src API（Bonemeal 物品、WheatCropStageMax 等）、未编译未验证**——agent-state.md Recovery Point 已如实登记（API 限额 1302 中断现场），与本轮审查无涉；续接棒按恢复点反推目标 API 后先编译再续写，勿直接提交。
- `.codex/` 外部工具产物：豁免口径不变（不删不提交）。

## 清偿状态

| # | 处置 |
|---|------|
| 1 | **待定夺**（当前无错值面，不阻塞；建议下批顺手收口或头注释 + 登记降级） |
| 2 | **待办**（定向冒烟 + lessons 盲区登记，随 t1027 后续批次 / R19.22 收口） |
| 3 | **登记**（模式成本，扩桶池时再加空转门） |
| 4 | **登记**（防抖动刻意取舍，实测劣化再评估让位） |
