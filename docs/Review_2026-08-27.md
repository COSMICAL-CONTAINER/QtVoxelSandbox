# Code Review — 2026-08-27（近 24 小时提交）

- **审查范围**：`ae47f31..43cb355`（2026-08-26 09:04 → 2026-08-27 09:04，共 48 个提交；其中 24 个纯 docs 提交只改 dev-plan / Review 标记，已跳过，实际审查 **24 个代码提交**，含 2 个仅探针/测试基建（`f55f1d3` t897 探针加固、`d238a8e` t832 窗口放宽，零产品改动快查通过）。上一窗口的收尾修复提交（review26 #1-#11 的 d2d4c22 / 5c79ee7 / a93f982 / 131f263）落在本窗口内，一并复审修复本身是否引入新问题。
- **审查基线**：HEAD = `43cb355`。**工作区不干净**：审查期间有未提交 WIP（review26 #15/#16/#24 的跟进：发射器冷却键加 Y、TNT 堵口降级、注释登记，涉及 blockregistry / world / playercontroller / minecartmanager / 测试文件，且审查过程中仍在被并发编辑）——本文所有行号均已用 `git grep 43cb355 -- <file>` 在 **HEAD blob** 上钉准，与工作区行号可能有 ±几行偏差。
- **方法**：7 路并行子系统审查（火焰伤害/侧点燃/羊吃草；静水位 7/8 + 流向动画；岩浆点燃扩展/烈焰弹/高草收紧/碰撞真顶修复；睡床传送/垃圾桶/创造复制；活板门贴图/3D 物品模型/成就 UI/画背面/耕地图标；乘员攻击重路由+序列守卫/F3/潜行者缩放；review26 #5-#11 修复批 + 玻璃透明度），每路逐提交读全量 diff + HEAD 交叉核对。**主审逐行亲核**：#1（Q_PROPERTY 区 + 绑定点 + 全仓引用清点）、#2（锚声明/drag 守卫/全仓无清锚代码，子代理另用两套 Qt 的 qml.exe 实测复现锚覆盖 x/y 写入）、#3（站顶分支全文 + 仙人掌先例 7438 行对照）、#4（双分支 visible + isItem3DFamily/isPartialBlock 两谓词亲核）、#5（nightwalkerDodge 函数体 + 雪球/蛋两调用点 HEAD 原文）、#6（smashLilyPads 扫层 + waterSurfaceY 新口径亲核）、#7（烧尽终局 setBlock + 4 参钩子清单逐行）、#8（Main.qml 相机绑定全文）；其余为中危亲抽 + 子代理核。
- **结论速览**：**高 3 / 中 10 / 低 12**。本窗口主体是 t888-t903 十六轮功能/视觉提交 + review26 四批修复。问题集中在五个模式：① **QML ↔ C++ 契约缺位**（sleepLying 无 Q_PROPERTY → 躺姿功能整体死亡 #1，探针 C++ 直调掩盖；treeMinimap 锚未清 → 拖拽功能整体死亡 #2，注释自知"不摘除则写 x/y 无效"但没有摘除代码）；② **新判据漏抄先例的边界半边**（火焰站顶分支漏仙人掌的 Y 界定 #3、mob 侧漏站顶分支 #10）；③ **权威迁移的隐式消费方外溢**（水面降 7/8 漏了船撞睡莲扫描层 #6；点燃改道烧尽终局丢了附着复检 #7）；④ **修复声称的覆盖面与实况不符**（雪球/蛋闪避链漏改 #5、walkPhase 骑乘/死亡不生效 #12、粒子系统族漏出 #13）；⑤ 相机/缩放基准错位（Waking 期镜头沉床 #8、掉落物书被二次缩放 #9）。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高 | 3 | #1 t898 躺姿 Q_PROPERTY 缺失（功能 100% 失效）；#2 t887 小地图拖拽被锚吞（功能 100% 失效）；#3 t890 跳过火堆被误点燃 + 满 8s 燃烧 |
| 中 | 10 | #4 附魔台掉落物双渲染 z-fight；#5 雪球/蛋免费净化 exploit 未封；#6 船撞睡莲失效；#7 烧尽后火把/活板门悬空；#8 醒来镜头穿床 |
| 低 | 12 | 见第四节 |

---

## 一、高危（建议立即修复）

### #1 `144fe17` (t898) `sleepLying` 缺 Q_PROPERTY 声明——QML 拿到 undefined，F5 第三人称躺床姿态 100% 不生效

- **问题**：t898 新增的躺姿门 `sleepLying()` 只是普通 public 成员函数，既无 `Q_PROPERTY` 也非 `Q_INVOKABLE`，不在 metaobject 属性表里。Main.qml 用**属性语法** `player.sleepLying` 消费它——QML 属性查不到 → undefined（falsy）→ 三元恒取 0.0。t898 的核心交付之一「第三人称躺床仰卧」完全不出现：F5 看到的是玩家**站着**贴在床顶。
- **证据**（主审亲核）：`src/Game/playercontroller.h:251-254` 睡眠 Q_PROPERTY 区只有 sleeping/sleepFade/sleepLie/sleepSettled 四件，无 sleepLying；`playercontroller.h:388` `bool sleepLying() const { ... }` 普通函数；`src/ui/Main.qml:4641` `eulerRotation: Qt.vector3d(player.sleepLying ? 90.0 : 0.0, player.yaw, 0)` → `undefined ? 90 : 0` 恒 0。配套的两处值翻转补发 `emit sleepingChanged()`（playercontroller.cpp:2726/2876，专为驱动该绑定设计）全部落空——绑定根本没注册到任何存在的属性。
- **探针为何没抓住**：P-t898 全部 C++ 直调 `pc.sleepLying()`，不走 QML metaobject。教训同 review26 #4 的 TapHandler 修饰键（C++ 直调测不到 QML 契约面）——Q_PROPERTY 三件套（声明/READ/NOTIFY）应进源码钉。
- **修复**：h 属性区补 `Q_PROPERTY(bool sleepLying READ sleepLying NOTIFY sleepingChanged)`（NOTIFY 复用 sleepingChanged 与既有补发点吻合）；探针补一条源码钉（Q_PROPERTY 存在性），或改经 QMetaObject::property("sleepLying") 读。
- **需目视**：睡觉中切 F5——模型 +90° 平躺头朝床头。
- ✅ 已修（fd0b3f4）：睡眠 Q_PROPERTY 区补 `Q_PROPERTY(bool sleepLying READ sleepLying NOTIFY sleepingChanged)`；P-t898 探针改经 QMetaObject::indexOfProperty/property()（QML 同一解析路径）在躺/醒两态断言属性表项存在且读回一致（阴性轮：去掉声明 → t898 FAIL 实测复现）。目视项同上（睡觉中切 F5 看平躺）。

### #2 `8d10666` (t887) treeMinimap 声明了 `anchors.top/right` 却用 `drag.target` 拖——锚定项的 x/y 写入被锚布局同步覆盖，拖拽 100% 无效

- **问题**：t887 的唯一功能目标（用户「成就小地图可拖动」）不生效。`treeMinimap` 顶部锚 + 右锚 + margins 在身，MouseArea `drag.target: treeMinimap` 是命令式写 x/y——Qt Quick 锚定项的 x/y 会被锚布局在下次 polish 同步覆盖回去，拖 10px 环带 item 纹丝不动。**代码注释自己写明了这个机制**（Main.qml:12013-12014「anchors.top/right/margins 是另一套约束——不摘除则写 x/y 无效」），但全仓**没有任何清锚代码**；`onXChanged` 的守卫 `if (!anchors.top && !anchors.right)`（12022 行）恒 false，`clampIntoOverlay()` 是死代码。
- **证据**（主审亲核 + 实测）：`src/ui/Main.qml:12000-12002`（锚声明）、`:12064`（drag.target）、`:12022`（守卫恒 false）；全仓 `git grep "anchors.top = undefined"` 零命中。子代理用仓库配套的两套 Qt（6.11.1 mingw / 6.9.1 msvc 的 qml.exe）在临时文件复现：对 top+right 锚定 item 写 `x = 100`，同一条 JS 语句内读回仍为锚定值 632。Qt 官方文档（anchors Restrictions）：锚布局与绝对定位不可混用，结果 undefined，可用置 `undefined` 清锚。
- **触发条件/影响**：每次拖小地图环带无任何移动（中央跳转区仍工作，无回归）；用户诉求（t840 误解 → t887 二次修正）第三次落空。
- **修复**：`minimapMoveArea.onPressed` 显式清锚再拖：`treeMinimap.anchors.top = undefined; treeMinimap.anchors.right = undefined`（清锚后 12022 守卫与 clamp 自然活）；或改默认 x/y 绑定（`x: parent.width - width - 8`），首次拖写自然断绑定。顺带修：drag 边界按 progressOverlay 而父链 `treeViewport` 有 `clip: true`（11700 行）——拖出视口部分被裁，注释「不在任何 clip 祖先内」与父链不符（treeMinimap 是 treeViewport 直接子项）。
- **需目视**：成就面板拖小地图环带跟随、窗口缩放钳回。
- ✅ 已修（fd0b3f4）：onPressed 清两锚（两套 qml.exe 实测清锚后写 x/y 生效）+ 新增 userMoved 旗——旧守卫形态修不活（实测清锚后 `!!anchors.top` 仍 true，AnchorLine 恒真值对象），旗接管钳位门；drag 边界/钳位全部改按父 treeViewport 口径（clip 取舍：**约束在视口内**，不 reparent——默认锚定位跨父映射非可绑定依赖，注释钉死取舍）；视口缩放经 Connections 钳回（旧版只 onXChanged 钳）。P-t887b 源码钉：清锚语句/userMoved 守卫/视口口径正向钉 + 旧死守卫与 progressOverlay 边界负向钉（阴性轮：前置 blob 上正向钉全 0、负向钉全命中 → 必 FAIL）。目视项同上（拖环带跟随、窗口缩放钳回）。

### #3 `f149041` (t890) 玩家「站顶分支」漏抄仙人掌先例的 Y 界定——跳跃越过/下落掠过火源顶 1 格内即被误点燃并烧满 8s

- **问题**：新增的玩家侧站顶分支只查 `fireCellAt(cx, footY - 1, cz)`，**没有任何 Y 接触校验**。`footY = floor(m_pos.y())`，只要脚底处于 `[燃块顶, 燃块顶+1.0)` 区间（腾空最高 1 格内）且 XZ 足印盖到该列就判接触。跳跃跨过立地火/燃烧木板（kJump=8.4 顶点约 1.25 格，必经过该窗口）、或从高处下落掠过燃块上方 <1 格，都会**不碰火却被点燃**，且接触期间每帧刷新 fireTimer = 8s 余燃 ≈ 10.6 HP。MC 里腾空越过火格（AABB 不交火格）不点燃。提交信息自称"t716 cactus same shape"，但仙人掌先例的站顶分支**有** Y 界定——不是同构。
- **证据**（主审亲核，行号 HEAD）：`src/Game/playercontroller.cpp:7268`（站顶分支无条件 `if (fireCellAt(cx, footY - 1, cz)) touchingLava = true;`）；对照仙人掌先例 `playercontroller.cpp:7438`（`&& pMinY <= cMaxY + kTouchSkin) touch = true;`——Y 被钉在支撑面 ±0.002 内，注释明言「该分支只查脚下支撑格一层，无误伤面」）。`kJump = 8.4f` 见 playercontroller.h:1582。
- **触发条件/影响**：野外跳过营火/燃烧残骸、下落擦过燃烧树冠 → 平白 8s 燃烧 ~10.6 HP（生存 20HP 过半）。另叠加 #11（玩家无水/雨灭火路径）后无任何提前止损手段。
- **修复**：站顶分支补仙人掌同款 Y 界定：`&& pMinY <= float(footY) + kTouchSkin`（脚底贴住支撑面才算；腾空时 pMinY > footY+skin 不豁免）。一行即修，与 #10 的 mob 侧补分支可同批做。补探针：跳跃越火堆顶（顶点 <燃块顶+1）不点燃 / 落在火堆正顶点燃。
- ✅ 已修（fd0b3f4）：站顶分支整 predicate 迁移仙人掌先例（`pMinY <= float(footY) + kTouchSkin`）；探针另抓出 **t843 中心列快速路径 `footY-1` 行同洞**（review 原文未列全——快速路径无 Y 校验，单独修站顶分支时 hoverClean 仍 FAIL），同款 Y 界定一并补上（任一路放开即复活）。P-t890 新增 (f)：悬停燃板顶 +0.5 格（足印盖列）首 tick 不燃、落回板顶复燃（阴性轮：去站顶 Y 界定 → t890 FAIL 实测复现）。目视项：跳跃越过燃块顶不被点燃、落在燃烧方块顶仍点燃。

---

## 二、中危（建议尽快修复）

### #4 `4831402` (t880) 附魔台（94）在掉落物与资源浏览器**双渲染**——BlockCube 与 ItemShapeGeometry 同时可见，侧面共面 z-fight

- **问题**：`isItem3DFamily` 家族里除附魔台外所有成员都被既有谓词挡在 BlockCube 分支外（活板门/台阶/雪层/楼梯是 `isPartialBlock`，草丛是 `isCrossBlock`，火把有 `entId !== 13` 显式排除），但 **EnchantingTable(94) 不在 `BlockRegistry::isPartialBlock` 谓词内**（mesher 是靠 chunkgeometry 显式 case 并入的）——BlockCube 分支的 visible 对 94 仍为 true，与新增的 ItemShapeGeometry 分支叠加。观感：附魔台掉落物同时画满格 1×1×1 立方（缩 0.3）与 0.75 高矮台，四面侧壁共面 z-fighting 闪烁，读感是「满格方块」而非矮台，**比改动前（单一 BlockCube）更差**。
- **证据**（主审亲核）：`src/ui/Main.qml:5701`（BlockCube 分支 visible 链无 isItem3DFamily 排除）、`:5721`（`visible: isItem3DFamily(entRoot.entId)`，94 在家族内，见 `:2050-2051`）；`src/Core/blockregistry.cpp:967-980` isPartialBlock 枚举无 EnchantingTable（段区间 15..20 亦不含 94）。浏览器侧同病：ResourceBrowser `selectedIsCube` 对 94 为 true，BlockCube 与新 Node 同时可见。
- **修复**：两处 BlockCube 分支 visible 追加 `&& !isItem3DFamily(entRoot.entId)` / `&& !root.selectedIsItem3D`（最小改动）；不建议把 94 并入 isPartialBlock（影响放置/失撑/碰撞链，回归面大）。补探针：源码钉两分支互斥。
- ✅ 已修（49b5759）：两处 BlockCube 分支 visible 追加 `!isItem3DFamily(entRoot.entId)` / `!root.selectedIsItem3D`（94 不并入 isPartialBlock）；探针 review27-4 源码钉两分支互斥（阴性轮去子句 → FAIL）。需目视：附魔台（94）掉落物只剩矮台 + 悬浮书、无满格立方闪烁；资源浏览器选中 94 同。

### #5 `a93f982`（review26 #8 修复）雪球/鸡蛋两条投射物闪避链漏改——「免费远程净化夜行者」exploit 原封保留

- **问题**：#8 修复只给箭链与浮标两处闪避传了 `clearAggro=false`，但夜行者弹射物免疫共**四条**路径：雪球和鸡蛋命中走 `nightwalkerDodge()`，其内部调 `teleportEntity` 用默认 `clearAggro=true`，照旧清空 `enraged/rageTimer/windupTimer`。雪球 0 伤害、4 雪块合 1 可无限复购——review26 #8 定性过的「免费可无限重复的远程净化 + 打断攻击前摇」经这两条路径完全开放，且与箭/浮标的新口径（MC 1.0 投射物闪避不解仇恨）自相矛盾。
- **证据**（主审亲核 HEAD 原文）：`src/Entities/entitymanager.cpp:3997-4009`（nightwalkerDodge 内 `teleportEntity(i, e, world, kNightwalkerTeleportMin, kNightwalkerTeleportMax)` 未传 clearAggro）；`:3955`（`if (clearAggro) { e.enraged = false; ... }`）；雪球 `:5177`、鸡蛋 `:5264` 两处 `nightwalkerDodge(mi, world);`（注释自述 t727 弹射物免疫）；头注释 entitymanager.h:174 自认「箭/雪球/蛋命中的目标是 Nightwalker → projectile dodge」。
- **修复**：5177/5264 调用链传 `clearAggro=false`（给 `nightwalkerDodge` 加形参最干净——近战 30% 闪避路径保持 true）。补源码钉：两处调用显式 false。
- ✅ 已修（49b5759）：nightwalkerDodge 加 clearAggro 形参（默认 true 保近战 30% 闪避「打断激怒」原语义），雪球/蛋两调用点显式 false；探针 review27-5 源码钉两调用 false + 闪避入口全枚举（`m.mobType == MobNightwalker` == 4：箭/雪球/蛋/浮标）+ 近战链默认 + review26-8 计数口径同步扩到四链（阴性轮 → FAIL）。**第五条闪避入口排查结论：无**——火球命中 mob 走真伤害直击（无闪避分支、无位移）；末影珍珠/末影眼只判方块接触不判 mob 命中。

### #6 `9c6e1cb` (t892) 船静水降位后 smashLilyPads 扫描层错位——高速撞碎睡莲契约失效，船穿叶而过

- **问题**：睡莲只存在于「顶水格 + 1」层；t892 把船浮水稳态 Y 从 `W+1.0` 降到 `W+0.875`（waterSurfaceFrac 源 7/8，kBoatDraft=0），而撞碎扫描仍只向下扫 `cy`、`cy-1` 两层——`cy = floor(W+0.875) = W`，永远错过叶所在的 `W+1` 层。旧口径 `floor(W+1.0) = W+1` 恰命中。t630/t711「快=碎、慢=挡」的快半边被打破（慢速阻挡正常：boatFootprintBlocked 查 cy/cy+1 两层）。
- **证据**（主审亲核）：`src/Entities/boatmanager.cpp:325`（`for (int y = cy; y >= cy - 1; --y)`）；`:376-377`（`return float(topWaterY) + BlockRegistry::waterSurfaceFrac(...) - kBoatDraft;`）；睡莲放置位置（顶水格+1）见 world.cpp:5294（worldgen）与 playercontroller.cpp:4314-4318（玩家放置）；高速豁免阈值 kBoatLilySmashSpeed=3.0 < 水档满速 8（boatmanager.h:397、boatmanager.cpp:729/747）。
- **触发条件/影响**：静水睡莲面驾船 ≥3 格/s 穿过 → 叶不碎也不掉落，直接穿叶（视觉+资源双丢）。
- **修复**：smashLilyPads 扫层改 `cy`、`cy+1`（或三层 cy-1..cy+1）；顺带更新 :314-315 已失真的「浮水船中心 Y = 水面顶」注释。补探针：W 层顶浮船撞 W+1 层睡莲 → smashed。
- ✅ 已修（49b5759）：smashLilyPads 扫层改 **cy-1..cy+1 三层**（+1 盖稳态浮水叶层、cy 盖刚放置/高位过渡、-1 盖出水低位过渡；越界层 blockAt 返 Air 天然 no-op）；失真注释同步重写（.cpp/.h 两处）。探针 review27-6 行为级双半边：满速 W 层骑乘船撞碎 W+1 层叶 + lilyPadSmashed 信号；低速 2.0 b/s 叶完好且船被挡在叶列前（防过度修复破「慢=挡」契约；阴性轮 → FAIL）。需目视：静水睡莲面开快船 → 叶碎掉落、慢船被叶挡停。

### #7 `00ed81d` (t891) 岩浆点燃改道「烧尽终局」后丢失附着复检——贴墙火把/活板门/门悬空残留不掉落

- **问题**：岩浆掷中可燃木块从「legacy 焚毁路径」改道为「点燃 → 计时烧尽」。legacy 路径批量段逐格调 `recheckAttachmentsAfterClear`（含活板门/门失撑钩子 + 6 邻火把扫）；烧尽终局走的是 **4 参数 `setBlock`**，其钩子清单没有 `checkTrapdoorsDoorSupportOnEdit`，也没有 6 邻火把扫。烧尽后：贴在被烧木块侧面的火把、墙侧活板门、以木块为支撑的门**悬空残留**。改造前同场景会立即焚毁并经复检掉落这些附着物（该缺口对 t843 火蔓延路径是既有问题，但**岩浆道**从带复检的路径迁到无复检路径是本提交引入的回归）。
- **证据**（主审亲核）：改道点 `src/World/world.cpp:1484-1488`；烧尽终局 `world.cpp:1675` `setBlock(x, y, z, flare ? Fire : Air);`（4 参版）；4 参钩子清单 `world.cpp:487-502`（cactus/deadbush/flower/plate/sugarcane/snow/gravity/rail/endportal/fire/netherportal/painting——**无 trapdoor/door 钩子、无火把邻扫**；5 参版在 :636 才有）；legacy 复检 `world.cpp:1517`（`recheckAttachmentsAfterClear(b.x, b.y, b.z, oldId)`，内部 :2755-2782 调 trapdoor/door 钩子 + 扫 6 邻火把）。
- **触发条件/影响**：火把/活板门/门所依附的木块邻岩浆且干 → 5s 烧尽置 Fire/Air → 附着物漂浮。提交信息只核对了铁轨（checkRailOnEdit 确在 4 参清单内，轨会掉），未核对火把/活板门/门。
- **修复**：`tickFire(d)` 烧毁分支的 `setBlock(...)` 后补调 `recheckAttachmentsAfterClear(x, y, z, id)`；或给 4 参 setBlock 补挂 `checkTrapdoorsDoorSupportOnEdit`（与 5 参对齐，注意热路径成本评估）。补探针：燃烧木块侧面挂火把 → 烧尽后火把成掉落物。
- ✅ 已修（49b5759）：取改动面小的方案——`tickFire(d)` 烧毁分支 setBlock 后补调 `recheckAttachmentsAfterClear(x, y, z, id)`（**门格除外**：烧尽门的配对半扇走上方的湿/雨守卫专用分支，通用复检无湿守卫、抢跑会误清 review24#1/review25#9 特意保住的湿/雨半扇）；t843 火蔓延共用本终局 = 既有缺口顺带收口（dev-plan t843 行已备注）。探针 review27-7 行为级：岩浆点燃的木板 → 顶立火把 / 顶立铁门（两扇）/ 侧贴铁活板门随烧尽全部成掉落物（铁制附着载体非 flammable，防燃烧板的同态蔓延掷骰把门/板拐进自己的燃烧链污染断言；阴性轮 → FAIL）。需目视：贴墙火把的木板被岩浆点燃烧尽 → 火把当场掉落不悬空。

### #8 `144fe17` (t898) Waking 渐显期相机偏移与出床瞬移基准错位——镜头沉到床面/邻墙内部再升起

- **问题**：第一人称相机偏移（沿 -look ×1.4、Y−1.35）按「躺位在床顶（Y=by+1）」标定；但进 Waking 时 `leaveBedTeleport` 已把 m_pos 瞬移到床边**地面**格（Y=by）。sleepLie 从 1 渐降期间：眼位 = `by + 1.62 − 1.35·lie`，水平向床头移 `1.4·lie` 格——lie>0.44 时眼位低于床顶（嵌床模型投影区/床对面格），床贴墙摆时终点嵌墙。fade<0.5（画面半透可辨）时 lie 仍>0.5，观感是「镜头从床边站位沉进床里再升起」。t496「镜头落到底」教训在新基准下变体复发。蹲姿入睡（kCrouchEye=1.35）时 lie=1 眼位贴地。
- **证据**（主审亲核）：`src/ui/Main.qml:2874-2882`（`if (player.sleepLie > 0.0)` 对三模式统一加偏移）；`src/Game/playercontroller.cpp:2904`（leaveBedTeleport 写 `m_pos = ... float(m_sleepBy) ...` = 床层）；`:2826-2827`（Waking 内 fade 与 lie 同步 `1.0f - t` 递降）。
- **修复**：相机偏移门与 sleepLying 同条件（`sleeping && 非 Waking`）或 Waking 入口直接把 m_sleepLie 清 0（渐显期站位眼位）；保躺渐变只在入睡方向（Lying 阶段 0→1）使用。
- ✅ 已修（8ffd351）：取「Waking 入口清 0」单点方案——leaveBedTeleport（出床瞬移唯一入口，覆盖 sleepAdvanceToDawn / wakeUpFromBed / cancelSleep 三路）顺带清 m_sleepLie+发 sleepLieChanged，updateSleep Waking 分支 lie 恒 0（fade 照常 1→0）；躺渐变只在 Lying 0→1。探针 review27-8 行为级（挂窗 captured 载体驱动 updateSleep）：Lying ramp 保留 / Waking 入口 lie==0 且 fade==1 / Waking 中段 lie 恒 0 / 中断醒 lie==0（阴性轮：还原两处 → FAIL 实测复现）。

### #9 `4831402` (t880) 掉落物附魔台的「悬浮书」被父级 0.3 缩放二次压缩——埋进台体内部完全不可见

- **问题**：`dropBookNode` 位于 scale 0.3 的 Model 内，其 `position y=0.14` 被父级 scale 再乘：实际场景高度 0.3×0.14=0.042，而台顶在 0.3×0.375=0.1125——书整体（页心 0.0525）在台体 [-0.1125,+0.1125] 体积内部，被不透明台体完全遮挡。浏览器正确版书在 y=0.46（> 台顶 0.375），掉落物版疑似把手足误做了预缩放（0.46×0.3≈0.14）。页尺寸同样只有浏览器版一半（0.19 vs 0.38）。
- **证据**：`src/ui/Main.qml:5723`（父 scale 0.3）、`:5740`（position y=0.14）、`:5745-5747`（页 position ±0.088、scale 0.19/0.03/0.23）；对照浏览器正确版 `src/ui/ResourceBrowser.qml:834`（y=0.46）、`:840-842`（页 scale 0.38）。子代理用 Qt Quick3D scenePosition 实测：书场景 y=0.042 < 台顶 0.1125。
- **修复**：dropBookNode/页改用与浏览器相同的局部坐标（y≈0.46、页 ±0.176、scale 0.38），父级 0.3 统一缩小，无需手动预缩放。需目视：附魔台掉落物台上有书。
- ✅ 已修（8ffd351）：dropBookNode/页改浏览器同款局部坐标（书心 y=0.46、页 ±0.176/0.045、页 scale 0.38×0.03×0.46），父级 0.3 统一缩小；探针 review27-9 源码钉切片含新坐标 + 负向钉旧值（0.14/0.088/0.19 系列）不残留 + 浏览器 etBookNode 同 y 互钉（阴性轮还原 y=0.14 → FAIL）。需目视：附魔台（94）掉落物台上有悬浮书、不埋台体。

### #10 `f149041` (t890) mob 侧没有站顶分支，而注释谎称 t843 中心列判定「已冗余」——部分悬站不点燃 + 未来清理即全回归

- **问题**：提交信息称"Mob side shares the identical scan"，实际 mob 侧只有 Y 严格主扫描（yy 从 footY 起，恒不覆盖 footY-1 支撑格），**无站顶分支**。mob 站燃烧方块顶的唯一覆盖是保留下来的 t843 中心列行，但其上方新注释却写「t888 后为**中心列快速路径**冗余（AABB 扫描已覆盖其语义）」——错误：Y 严格主扫描永远不覆盖支撑格。后果：① 行为不对称（玩家足印发覆盖即燃，mob 只有中心列压在燃块列才燃，部分悬站不燃）；② 后续维护者按注释清理「冗余」→ mob 站燃块顶全体失效（真实回归陷阱，review26 #1 同款「注释承诺与代码不符」模式）。
- **证据**：`src/Entities/entitymanager.cpp:6291-6293`（误导注释 + 中心列判定）、`:6266`（主循环 `for (int yy = footY; ...)`）、玩家侧对应分支 playercontroller.cpp:7268。
- **修复**：与 #3 同批：mob 侧补同款**含 Y 界定**的站顶分支；或至少把注释改为「mob 侧站顶唯一覆盖，勿删」。
- ✅ 已修（8ffd351）：mob 侧补玩家侧同构站顶分支（XZ 足印覆盖列 + Y 界定 `mMinY≤footY+kTouchSkin`，kTouchSkin 提升到火段共享），t843 中心列快速路径**同款 Y 界定**（不补则跳越经中心列复活——玩家侧 review27 #3 同教训），误导注释重写（主扫描 Y 严格恒不覆盖支撑层 = 站顶分支非冗余）。探针 review27-10 行为级：燃板正上 1×1 石井内猪悬停窗（footY-1=燃板、脚底未贴面）不燃 + 落定板顶复燃（阴性轮去分支+Y 界定 → FAIL）。与玩家侧口径统一（部分悬站点燃 / 跳越不点燃对称）。

### #11 `9b2d914` (t888) 随机熄灭退役后玩家侧没有任何雨灭/水灭路径——着火必烧满 8s，误导注释掩盖缺口

- **问题**：t888 归零随机熄灭的依据是「雨灭走独立路径」，玩家侧注释也写「雨灭走 mob/世界侧独立路径」——但 playercontroller 全文不存在 `isPrecipitatingAt` 或任何灭火调用：雨灭只在 mob 侧（entitymanager.cpp:6306-6314），世界侧只管方块火。玩家也无入水灭火。旧版 0.15/脉冲随机熄灭客观上提供平均 ~3-4s 提前止损；t888 拿掉且未补 → 雨中、跳进水里的着火玩家照烧满 8s 吃满 ~10.6HP（叠加 #3 后更是雪上加霜）。MC 1.0 语义（实体入水/雨立即熄灭）玩家侧不成立。
- **证据**：`src/Game/playercontroller.cpp:7293-7307`（火段只有燃尽归零与恒 false 熄灭分支，无 feetInWater/isPrecipitatingAt）；mob 侧雨灭对照 entitymanager.cpp:6306-6314。
- **修复**：玩家火段补 `if (feetInWater() || eyeInWater()) { m_fireTimer=0; m_fireDmgTimer=0; }`，雨灭复用 mob 侧判据；或改注释明确「玩家无雨灭」为已知缺口。
- ✅ 已修（8ffd351）：玩家火段补水灭（feetInWater/eyeInWater，置于点燃刷新后 / 推进前）+ 雨灭走**新增 World::rainExtinguishesAt**（见天 skyLightAt≥15 ∧ 列降水——抽到 World 单一权威，mob 侧同批改调它，不复制判据）。探针 review27-11 行为级（Plains 列 rig）：撤火干燥对照仍燃（防假阳性）→ 脚位格换水即灭；双 pc 露天/檐下强降雨 → 露天熄、檐下仍燃（见天半边隔离）。误导注释同步改写（阴性轮去水/雨灭 → FAIL）。

### #12 `3f293ec` (t897) walkPhase 归零对骑乘/死亡态不生效——与注释、提交信息三方矛盾

- **问题**：t897② 的注释（entitymanager.cpp:6686「静止（idle/吃草/撞墙/**骑乘**）→ walkPhase 归零」、entitymanager.h:45「死亡 → 归零」）和提交信息（"idle/graze/wall/riding"）都声称覆盖骑乘与死亡，但骑乘态 mob 在 6169-6174 行 `continue`、死亡态在 6159 行 `continue`，都**早于** 6710-6713 行的归零块。行走中被放上矿车/船或被击杀的 mob 腿永远冻结在半步相位——恰是 t897② 自称修掉的观感 bug 在骑乘/死亡态仍然存在。
- **证据**：`src/Entities/entitymanager.cpp:6159`（dead continue）、`:6169-6174`（骑乘 continue）、`:6710-6713`（归零块）；矛盾注释 `:6686`、`entitymanager.h:45`。
- **修复**：登乘写双向链处（清 moveSpeed 同位置）与死亡翻转处顺带 `walkPhase=0; stepAccum=0`；或修正注释删「骑乘/死亡」字样。
- ✅ 已修（8ffd351，真修）：矿车/船登乘写链（清 moveSpeed 同位置）与死亡翻转三处顺带 `walkPhase=stepAccum=0`；矛盾注释三方改写（h:45 / tick 骑乘守卫 / t897② 块头——骑乘/死亡早退不达归零块、各自在翻转处归零）。探针 review27-12 行为级+源码钉：行走中（walkPhase≠0 瞬间）damageEntity 致死→即刻归零 / 行走中贴身 spawnCart+tickVehicleRiding 登乘→归零（rideCartAt≥0）/ 三写点源码钉（阴性轮还原三处 → FAIL）。

### #13 `131f263`（review26 #11 修复）「全仓纯视觉 Timer 清点」漏网同族世界锚定粒子系统与动画——暂停语义修了一半

- **问题**：#11 把清点范围限定为 Timer，但同症状（硬暂停世界锚定视觉照跑）的 **ParticleSystem3D 族与世界锚定动画族**整体漏网：TorchSmoke / AmbientParticles / WeatherParticles 三个 `ParticleSystem3D { running: true }`（ESC 硬档火把烟/火星落叶/雨雪照发照飞，与被修的「白字持续飞」完全同族同口径）；且同组件内自相矛盾——附魔书 pageFlipTimer 被 gate 了，但**同一个组件**的书 bob 浮沉（Main.qml:9143）与静息翻页 flutter（:9237）照跑；刷怪笼 mini-mob 自旋+浮沉（:9802/9812）与刚被 gate 的 t878 爱心同族未 gate。暂停叠层遮住世界故无观感损害，实际影响是暂停/菜单期粒子与动画 CPU/GPU 开销未清零 + 冻结语义不一致（豁免清单不含它们，爱心动画纳入而刷怪笼未纳入，标准不自洽）。
- **证据**：`src/ui/TorchSmoke.qml:27`、`src/ui/AmbientParticles.qml:46`、`src/ui/WeatherParticles.qml:35`（running: true 无 worldRunning 门）；`src/ui/Main.qml:9143/9237/9802/9812`。
- **修复**：三处 ParticleSystem3D 的 running 经 Loader 注入 `window.worldRunning`（同 BlockParticles 模式）；书 bob/flutter 与刷怪笼动画补 `&& window.worldRunning`；或把「ParticleSystem3D/Animation 族不在清点范围」登记为知情取舍并同步豁免判据措辞。
- ✅ 已修（8ffd351，真 gate）：三粒子组件加 `worldRunning` 属性 + `running: root.worldRunning`，Main.qml 三 Loader.onLoaded 以 Qt.binding 注入 window.worldRunning（BlockParticles 模式）；书 bob/faceTimer（#13 顺带清点的同组件漏网 Timer）/静息 flutter/刷怪笼自旋+浮沉全部 gate；flutter 与大摆互斥改**声明式**（running: worldRunning && !pageFlipAnim.running）——旧 onStarted stop()/onFinished restart() 命令式调用会夺 running 绑定（gate 失效），大摆起摆仍显式清零 flutter。探针 review27-13 源码钉（三组件门+无 running:true 残留 / 三注入 / 三无限循环动画+flutter+faceTimer；阴性轮还原 TorchSmoke+book y → FAIL）。需目视：ESC 硬档火把烟/雨雪/书浮沉/刷怪笼全冻结，恢复续跑。

---

## 三、低危（择机修复）

### #14 `00ed81d` (t891) 烈焰弹三个边缘：朝脚下近距发射落火自燃 / 实体达上限弹被吞仍消耗 / 落火不试下界门框
- 朝下近距发射：直击豁免（shooter==-1）有效，但撞非可燃块时立地火放在来向格 = 玩家自身格（entitymanager.cpp:5426-5432 用未更新的 e.pos）→ 自己点燃（MC 勉强 parity，低危登记）。修复：来向格与玩家 AABB 相交时偏移命中面外邻格。
- spawnFireball 达 kCap 返回 -1 未检查，playercontroller.cpp:3427-3429 无条件 takeStack 消耗（同雪球既有先例，但烈焰弹是生存合成资源）。修复：`slot >= 0` 才消耗。
- 火球落火未调 `tryIgniteNetherPortal`（打火石路径 playercontroller.cpp:3972 有），与「打火石同源口径」宣称不符——若有意裁剪请注释登记。
- ✅ 已修（6d645e8 / 探针 9dd1045）：① 玩家侧火球来向格与玩家 AABB 相交 → 落火偏移到首个「Air 且不交 AABB」邻格（火球水平来向一格优先、四向兜底、无合格格不落火；**仅 shooter==-1 玩家侧**——燃烬者落火贴玩家脚边是敌意投射物合法后果保持）；② `fireballSlot < 0` 早退（不消耗 / 不挥手 / 不置冷却，雪球既有口径不动已注释钉）；③ 落火前补 `tryIgniteNetherPortal`（成门不落火，与打火石真同源）。探针 review27-14 行为级双半边（自身格 Air + 邻格 Fire / kCap 满载 count 不减 swing 0）+ P-t891 直下断言同步改钉新落位；阴性轮：全还原与仅撤②两轮均复现 FAIL。

### #15 `9c6e1cb` (t892) 水面降位三个小外溢：睡莲视觉悬浮 3/16 / 两层冰墙骑船 Y 振荡 / eyeInWater 无迟滞
- 睡莲 quad 烘在格底+1/16，按满格水面校准；静水降 7/8 后叶面悬空约 0.19 格（partialblockgeometry.cpp:550-559）。修复：叶高改读 waterSurfaceFrac。
- 岸边冰堆 2 格高时冰 snap-up 逐帧 0.325 格振荡（boatmanager.cpp:276-278 两层扫描取 max + :790-795 高差>0.5 不 snap 回钉 surfY 的组合）。修复：snap 前查目标层上方无冰。
- eyeInWater 液面比较无 epsilon/去抖（playercontroller.cpp:5981-5983），眼位贴 7/8 平面时蓝雾/耗氧逐帧翻转（游泳物理无浮力平衡点，实测风险低）。
- ✅ 已修（6d645e8 / 探针 9dd1045）：① PartialNeighborCtx 增 belowId/belowState（仅 LilyPad 填，chunkgeometry 产 / partialblockgeometry 消费），叶 quad 世界高 = 下方水格 waterSurfaceFrac + 1/16（源 7/8 → 局部 y=−1/16，跨 cell 边界无碍；下方非水 fallback 旧值 1/16 贴冰面）；② 新 boatFootprintLayerIsIce 埋位守卫——snap 目标层（floor(iceRestY)）有冰 = 舱位被埋是墙不是面，拒 snap 稳钉水面（单层冰面 snap 不受影响，对照半边钉死）；③ eyeInWater 加 kSurfaceDebounce=1/32 单侧去抖（贴面粘滞向「水下」侧，蓝雾不再逐帧翻转）。探针 review27-15：①三方源码钉+旧行负向钉、②行为级双半边（两层墙 maxY≤水面+0.125 且终态 84.875 / 单层照常 snap 到 85.2 稳定）；阴性轮全还原 → FAIL。

### #16 `292b3a5` (t893) 两个已声明取舍：45° 对角流量化偏差 / 每流水格 mesh 重建 +8 次邻查
- 梯度量化到主轴（chunkgeometry.cpp:783-786），45° 对角流/汇合格动画与实体实际被推方向最多偏 45°（物品漂移/玩家推力用连续向量）——commit 已声明取舍，可不改。
- 每流水格每次 buildMesh 新增 4 blockAt + 4 stateAt（跨 chunk 走路由），长水梯重建开销线性增——相对既有邻居查小幅，如需可缓存 4 邻 state。
- 📌 登记不改：两条均属 commit 已声明的知情取舍（45° 量化是流向四向动画的表意精度上限；+8 邻查与既有邻居面剔除路径同量级、重建窗短暂）。4 邻 state 缓存留待实测 perf 数据支持再做（无实测热点不预优化）。

### #17 `9b2d914` (t888) 两个小项：恒 false 熄灭分支死代码 / 低帧率火伤脉冲失真
- `generateDouble() < 0.0` 恒 false，两侧各留一个永假分支并每脉冲消耗一次全局 RNG（entitymanager.cpp:6323、playercontroller.cpp:7300）；常量一旦被改回非零，t888 全套节奏标定静默失效。修复：`if constexpr` 包裹或删分支留锚点注释。
- mob 火伤单次 `if` 结算叠 4 帧错峰节流：帧率 < ~5.3fps（aiDt ≥ 0.75s）时实际火伤速率低于名义值。玩家侧同构。低优先级。
- ✅①已修 / ②登记（6d645e8）：① 玩家 / mob 两侧掷骰与熄灭分支整体 `if constexpr (kFireExtinguishChance > 0.0f)` 编译期剔除（不再耗全局 RNG、无死分支），锚点注释钉死「常量回改非零前须重标 t888 节奏（kFireDuration 8s 必烧满 / kFireDamageInterval 1s 按无随机熄灭校准）+ Peaceful 难度复活本口」；② 登记不修——<5.3fps 属极端边缘帧率，火伤节流按 dt 累积（t500 语义：平均速率与每帧一致）在正常帧率下成立，为极端帧率改结算结构得不偿失。

### #18 `3f293ec` (t897) 两个小项：腾空羊可开吃 / sheepEatGrass 注释 t903 后失真
- 吃草检测 `groundY = floor(pos.y - halfH) - 1` 垂直窗口放宽约 1 格：小跳/下落中的羊可进入吃草周期并在 0.5s 后空中消耗 Grass→Dirt（entitymanager.cpp:4589-4595、6423-6426）。修复：开吃门控加 resting/onGround 或距格顶 <0.1。
- 函数注释宣称 consume 走 setWaterSilent「免粒子/音/掉落」——t903 给 setWaterSilent 挂钩后，羊吃的 Grass 上方有 TallGrass 时会爆粒子+掉种子（t903 有意设计，t897 注释失真）。更新注释指向 t903 钩子。
- ✅ 已修（6d645e8 / 探针 9dd1045）：① resting 门收在 sheepEatGrass 入口（**检测与消耗同门**——既挡腾空开吃、也挡「落地开吃后离地仍消耗」半态），落地照常吃由 P-t897 草栏探针（grassDirt≥1）行为看守；② 头注释改写为「t903 挂钩后 Grass 上方有 TallGrass 会正常爆粒子+掉种子（有意设计），完全免粒子/掉落仅对无附着裸草方块成立」。探针 review27-18 源码钉（入口与首行坐标读之间含 resting 门）；阴性轮全还原 → FAIL。

### #19 `4831402` (t880) 三个小项：楼梯浏览器覆盖不全 / 每 delegate 独立几何 / 火把 UV 注释不符
- 浏览器 selectedIsItem3D 只含木楼梯 16，CobbleStairs(59)/StoneBrickStairs(110) 仍 2D 大图标（ResourceBrowser.qml:257-260）——掉落物侧三者一致 billboard 无回归，仅浏览器预览形态分裂；若 spec 只取木楼梯请在谓词注释写明。
- 每个掉落物 delegate 独立实例化 ItemShapeGeometry（Main.qml:5722），slot 换 entId 时 CPU 重建 + GPU 上传；密集掉落场景多 N 份几何（旧 BillboardQuad 全场景共享单实例）。
- 火把头注称顶/底小窗取焰心区，实现六面统一 2/16 柱带（itemshapegeometry.h:19-21 vs .cpp:155-156）。
- ✅①③已修 / ②登记（bd20783）：① 59/110 补进 selectedIsItem3D（两侧家族表差集 = 楼梯三族 {16,59,110}，注释互指同步）；③ 头注改「六面统一中央列带 u[7/16,9/16]，侧/顶/底同窗」与实现一致；② 登记——每 delegate 独立几何与 t858 掉落物 instancing 批一并解（先 instancing 后谈共享，避免两改对冲）。

### #20 `144fe17` (t898) 三个小项：出床位不排液体 / 骑乘态右键床无门控（不确定）/ 睡态相机维护缺口（不确定）
- leaveBedTeleport 可站格判定只查非碰撞+支撑，不含液体（playercontroller.cpp:2901-2903）——岩浆湖旁贴床睡觉醒来直接站进岩浆。修复：加 `blockAt != Lava/Water`。
- 床 useBlock 分支无 riding 门（对照 respawn 显式 dismount）——骑乘态右键床的睡态/钉位互斥未验证，需实测（矿车轨道旁放床）。
- 睡态 updateCameraDistance 停摆 + trySleepAt 直写 yaw/pitch 未发 lookChanged（目前被 positionChanged 驱动掩盖，防御性登记）。
- ✅①已修 / ②③登记（①=6d645e8）：① leaveBedTeleport 候选格加 `blockAt == Lava/Water → continue`（液体非 collidable 故原三查拦不住；全邻皆液体仍走床顶 fallback，MC respawn 排液体同语义）；② 登记待实测——骑乘态右键床行为未验证前不加门（盲加 dismount 可能引入新互斥面），留待矿车旁放床实测后处置；③ 防御性登记——当前被 positionChanged 驱动掩盖无用户可见症状，动相机维护链无回归面支撑。

### #21 `31ae7b0` (t900) 「清空整个背包」未覆盖盔甲槽与 2×2 合成格；全清无确认是产品风险
- 清空循环只碰 hotbar 9 + main 27 + 光标（Inventory.qml:743-748）；盔甲（armorSetStack 独立存储）与合成格原料残留、输出槽仍显示产物——「整个背包」语义不完整，用户可能误以为已全清。
- shift+左键一键全清无确认无 undo，且与全界面 shift+左键=搬运的肌肉记忆冲突（垃圾桶槽紧邻 hotbar 行，搬物点偏一格即不可逆全清）。语义是用户 8-25 定稿（注释已钉），但建议至少加二次确认或合成格一并清。
- ✅①已修 / ②登记产品建议（bd20783 / 探针 9dd1045）：① shift 清空段补盔甲四槽 armorSetStack(a,0,0) + 2×2 合成格平行数组全清 + craftRev++（输出槽随派生绑定重算为空）；探针 review27-21 源码钉 QML 段（盔甲循环 + craftSlots/Counts 清 + craftRev）+ P-t900 行为腿扩真护甲填充/清空（armorSetStack 拒非护甲——Glass 填充会静默 no-op 成假绿）、源码钉窗口改 tier② 行动态收尾；② 不加确认（语义用户 8-25 定稿），注释补「邻位误触风险」提示登记——确认弹窗留待用户实测后再议。

### #22 `1b9fbea` (t896) 中键克隆在光标已有持物时静默销毁旧持物（既有语义，暴露面扩大）
- `if (heldBlock !== 0) returnHeldToVoidRequested()`（Inventory.qml:494/929/934）：旧光标整组回虚空无提示。t896 把整组获取移到中键后中键频率上升，误销毁概率随之增大（创造可找回，低危）。
- 📌 登记不改：创造模式可随时找回（损失为零），且「旧持物回背包而非虚空」会改变中键克隆的替换语义面（MC 拾取在持物时同样是整组替换，回背包反而不对标）；生存模式中键克隆本就受限。若实测误触频繁再议（可选项：回背包 + 背包满才虚空）。

### #23 `a93f982`（review26 #6）burnout 表项在火把被拆/重放后残留——同格新火把继承计数/冷却
- m_torchBurnout 唯一摘表路径是到期（world.cpp:3389-3410）；挖火把（notePowerWrite）不触碰该表。(a) 计数窗内拆火把同格重放 → 新火把继承 flips，可能不到 8 次就熔断；(b) 冷却锁定期内拆后重放 → 锁定门 `continue`（:3890）跳过新火把评估，基座已供电也错误亮到冷却到期（最长 8s）。修复：notePowerWrite 中 oldId==RedstoneTorch 时 erase 该格键（一行）。MC 重放即重置熔断。
- ✅ 已修（6d645e8 / 探针 9dd1045）：notePowerWrite 的火把分支补 `oldId==RedstoneTorch → m_torchBurnout.erase(packGrowthCell(x,y,z))`（失撑自拆走同口同摘 = 语义一致；熔断翻转自身是静默直写不经本口不受影响）。探针 review27-23 行为级双半边（拉杆 NOT 门 rig）：窗内 4 翻后拆放 → 后续 4 翻全正常（旧版累计第 8 翻会锁定灭）；锁定冷却中拆放 + 基座供电 → 新火把立即评估为灭（旧版错误亮到到期）；阴性轮全还原 → FAIL。

### #24 其余低危杂项（注释漂移 / 口径 nit / 既有语义登记）
- `5bab33c` (t879)：两处注释错误——resourcepackmanager.cpp:3039「铁活板门 103」应为 136（103 是 Rail）；chunkgeometry.cpp:578 cutout 注释未提木活板门。功能无误。
- `fa61cb3` (t899)：blockregistry.h:383-385 注释仍写 opacity≈0.45，实际已 0.30（Main.qml:4287）——第三处引用点漂移，build_glass.py/Main.qml 双钉已更新。
- `9c6e1cb` (t892)：entitymanager.cpp:5664 液面注释仍写「源=1.0」，同函数 :5854 已改 waterSurfaceFrac（源 7/8）。
- `c5357fd` (t895)：精确 45° 对角朝向基数取整与 MC 不同（严格 `>` 恒取 Z 轴 vs MC 四舍五入进位，仅浮点罕见形态差 1 档）；bl/ol 采样脚下格而非眼位（注释自洽，非 MC 1.0 原生行）。
- `5c79ee7`：重路由打车打的是「射线最近车」不一定是乘员所骑的那辆（t866 以来既有，MC「打视线最先碰到的载具」可辩护，登记）；冷却中瞄乘员/生物吞点击 vs 瞄空载具可挖方块，口径不一致（既有）。
- `290b115` (t894)：ResourceBrowser.qml:175 潜行者居中注释跨度是缩放前数据（偏差 <0.05 格视觉不可辨）。
- `131f263` #10：船+车同帧都载客移动时 ridersChanged 每帧发 2 次（声明「每帧恰一次」不精确，仅多一次绑定重算，无需改码）。
- ✅ 注释五处已修（bd20783）/ 其余登记不改：已修——resourcepackmanager「铁活板门 103」→136；chunkgeometry cutout 段行内注释补木活板门；blockregistry.h 玻璃 opacity 两处 0.45→0.30；entitymanager 浮标注释「源=1.0」→「源 7/8（waterSurfaceFrac 单一权威）」；ResourceBrowser 潜行者居中注释改 0.85 缩放后跨度 [-0.77,0.69]（0.05 值沿用，偏移差 <0.05 格视觉不可辨）。登记不改——F3 朝向取整（浮点罕见形态差 1 档）、bl/ol 脚位采样（注释自洽）、打车射线「射线最近车」（MC 可辩护）、ridersChanged 每帧 2 次（仅多一次绑定重算）。

---

## 四、核实为「未发现引入性 bug」的提交/要点

- **`a7747d7` (t903 高草收紧)**：放置堵死面完整（plantGroundBlock 仅 3 消费方，worldgen 自带守卫）；羊吃草组合正确（Grass→Dirt 经 setWaterSilent 钩子清上方高草+掉种子，正是设计目标）；误触发面为零（唯一写 Grass 格的调用方就是羊吃草）；性能与既有钩子同量级；花/蘑菇口径分裂已注释钉死。
- **`d2d4c22`（review26 #1-#3）**：脚位格豁免边界逐一验算（站整格时 y0 是脚上空气行不参与豁免；薄支撑 0.5/0.9375/0.1875 二进制精确值等号成立）；「走进矮墙仍被挡」语义保持；isJumpObstacle 7 调用点全传 feetY 且为文件局部函数无遗漏；物品 hBlocked 与 resting 复探同源、摩擦指数衰减会停（无斜坡无永久滑）。
- **`5c79ee7`（review26 #4+#9）**：值门/冷却/m_hitDist 三方交叉核验无缺陷（attackMob 自带冷却门无双发；m_hitDist 恒有效且 findCartHit 自带近界）；28 处守卫清点数字与残留全部核实（9 处保留判断准确、list4 归一语义无回归、7 文件 import 齐）。
- **`a93f982` #5（红石粉臂轴）**：四形真值表逐一对比，唯一翻转面恰为「端点垂直侧」（预期修复面）；dot 早退两版同在（提交信息准确）；爬墙连接位干扰已在 world.cpp:3562-3569 登记（review26 #15 知情近似）。
- **`a93f982` #6（火把熔断）主体**：独立计时（脏集早退之前）、单位折算（600t=60s 窗、80t=8s）、清表路径（beginLoad/generate 全覆盖、无维度切换路径）、表空 O(1)、锁定段状态一致——除 #23 的拆/重放残留外无误。
- **`a93f982` #7（获物列扫）**：两段循环各有 128 硬上限无死循环无越界；静水新值与旧值精确相等（视觉不变级属实）；冰盖/实心覆盖弹到上方空气格正确。
- **`131f263` #10 主体 / #11 主体**：静止载客帧零发射（pin 精确比较）、QML 触碰面仅一处 position 绑定、kCap=64 有界；Loader 注入无空转窗口（默认 false）、Timer 冻结续跑语义正确、翻书帧停位正确、bobber/f3Refresh 等 10 项豁免判据核实成立。
- **`5bab33c` (t879)**：AtlasTileCount 181 与 atlas 实测尺寸一致、tile 180 孔位实测吻合、cutout 扩族对既有成员零影响、mesher 两侧同步、icon5→6→7 缓存链无残留（仅 2 处注释错误见 #24）。
- **`b8ed1c1` (t901 画背面)**：双 quad 几何/分层/光照逐项验证（法线相反各背面剔除至多一个面向相机；z 序 1/64 间隙无共面；NoLighting 不受法线影响；玻璃排序正确）。
- **`058ea8f` (t902 耕地图标)**：显式 case 分支顺序正确不被截胡；def 契约与探针一致；icon7 换代覆盖完整；pack-off 不受影响。
- **`c5357fd` (t895)** / **`290b115` (t894)** / **`fa61cb3` (t899)** / **`1b9fbea` (t896 主体)**：未发现实现级引入 bug（核对项见各子代理报告；F3 的 Column/光照查询/坐标行/朝向表、Stalker 缩放链与爆炸侧隔离、玻璃 opacity 单点、克隆数量权威与元数据链均核实）。

---

## 五、 Lessons（供 lessons-learned 参考）

1. **QML ↔ C++ 契约面必须进探针**：#1 的 C++ 直调探针测不到 Q_PROPERTY 缺失（同 review26 #4 TapHandler 修饰键教训的 Q_PROPERTY 版）——凡 QML 消费的新属性，源码钉应包含 Q_PROPERTY 存在性。
2. **「同构先例」迁移要整段对照**：#3/#10 漏抄仙人掌先例的 Y 界定半边——迁移判据族时先列出先例的每个条件再逐条勾稽，注释声称 same shape 的地方恰是最容易漏的地方。
3. **锚与 drag 不可混用是 Qt Quick 硬约束**：#2 的注释作者知道机制但没有实现摘除——QML 位置接管类改动需要一条「首次交互断绑定」的实测冒烟（拖动 1px 断言位置变化）。
4. **权威迁移要扫「隐式液面/几何消费方」**：#6/#15 的睡莲两处都是按满格水面校准的隐式消费方——改全局常量时 grep 的不只是新权威函数名，还有按旧值标定的魔法偏移（1.0、1/16、+0.35 类）。
5. **修复提交的「覆盖面声明」要全链 grep 验证**：#5（雪球/蛋）、#13（粒子族）都是修复声称「全仓清点/四条路径」但清单不完整——声称封闭 exploit 的修复应把同族入口全部枚举进探针。
