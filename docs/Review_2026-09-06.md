# Review 2026-09-06 — 近 24h 提交审查（t1012 矿井走廊/洞穴蜘蛛/水冲毁 · t1013 箱子矿车 · t1014 矿脉 · t1015 载具攻击 · t1016 时钟持久化 · t1017 仙人掌 · t1018 铁轨松绑 · t1019 V 谷能量）

- 审查对象：`dc9de62..6c704f2`（2026-09-05 11:15 → 09-06 06:30，共 20 笔：9 笔 src fix、5 笔探针、6 笔 docs/plan，矩阵 439→450）。另盘点未提交工作区（t1008 进行中）。
- 审查方式：三路并行深审（worldgen 组 / 实体组 / 方块物理组）逐笔 diff 精读 + 关键路径交叉核对，**全部高/中级发现由主审在 HEAD 上逐行复核代码证据**（file:line 均为 HEAD=6c704f2 当前行号）；未运行矩阵（HEAD 矩阵 450 PASS / 0 FAIL 为提交自述）。
- 总评：**3 项高严重度新缺陷**（2 项在 t1013 箱车：清空重开无限刷物品、标记格双容器别名致车凭空消失；1 项在 t1012 满网走廊：蛛网把刷怪笼所有合法刷怪位堵死 = 笼子功能死亡），另有 5 中 / 8 低 / 2 Info。本批探针内质量依旧扎实（阴性轮、源码钉、探针侧假红归因都做了），但系统性盲区清晰可见：**跨任务组合面**（t1012 满网 × 刷怪谓词、t1012 蛛笼 × t1013 箱车登乘）、**时序型状态机**（开箱→搬空→关→再开）、**键所有权**（标记格上放方块箱）完全无探针覆盖——单腿断言式探针天然测不到这三类。同 seed 世界输出变化（矿井走廊/蛛网/矿脉重写）为预期内，已生成存档不受 worldgen 改动影响。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | **高** | **t1012 满网蛛网走廊把刷怪笼的全部合法刷怪位堵死——洞穴蜘蛛笼永远不刷怪**。满网 fill 把廊体（含笼位 ±1 恒 3 层夹网巢）全部空气格变 Cobweb，而刷怪找位谓词严格要求 `here==Air && above==Air && isCollidable(下)`；笼的 8 水平邻 × 2 高度候选逐格核对全灭（沿廊邻=网、跨宽/对角=网或未 carve 岩壁、cy+1 层=网），entitymanager 全文件无 Cobweb 豁免。洞穴蜘蛛生存模式唯一自然来源失效——这是 dc9de62（满网）与 ab3d047（笼转正洞穴蜘蛛）两特性的组合效应，revert 单提交不显形 | world.cpp:7420-7438（nestH 夹网巢 + 满网 fill）；entitymanager.cpp:1545-1552（找位谓词） | 找位谓词对 Cobweb 放行（`here==Air \|\| here==Cobweb`，MC 矿井蛛网笼本就在网里刷）；补一条「满网走廊几何 × 找位谓词」组合探针腿 |
| 2 | **高** | **t1013 箱子矿车「清空后再开」无限重 roll——无限复制矿井战利品**。gate 把「cart 键 + 27 槽全空」显式当作未开箱放行重 roll，而 `setSlot` 只写槽不摘 `m_cartKeys` → 玩家把首开 6 件全拿走后条目回到与「从未开过」不可区分的状态，`Main.qml:1480` 每次开箱都调 `populateMineshaftLoot` → 再 roll 同一批 6 件（坐标确定性 seed）→ 无限复制铁矿/钻石/附魔书。pre-t1013 内存 gate 是「条目存在即拒」，session 内不会重 roll；跨存档重 roll 是既有口径，**session 内重 roll 是本提交引入的回归** | cheststore.cpp:320-327（gate）、357-358（roll+键回插）、108-124（setSlot 不摘键）；Main.qml:1477-1480 | 区分「从未 roll」与「roll 后被清空」：`registerCart` 时另记 `m_cartUnrolled` 待 roll 集合，roll 成功后移出；gate 改为「在待 roll 集合 && 条目空」才放行。补「开→搬空→关→再开」时序探针腿 |
| 3 | **高** | **t1013 在标记格放普通箱子 = 与箱子矿车共享同一份 27 槽（库存别名 + 回生键连锁被抹 → 车凭空消失）**。转正后标记格是 Air（车吸附到邻轨），玩家在这个轨旁空格放 Chest 方块 → 方块箱开箱（坐标寻址）与箱车右键（键寻址）打开同一份条目，两边互见互取；破坏这个箱子 → `clearChest` 把矿车回生键一并抹掉 → 活着的箱车再被存入物品时 `setSlot` 自动重建条目但**不补回 cart 键** → 存档无 `"cart":true` → 下次进世界 path (b) 不回生，箱车消失，物品留在空气格幽灵条目里 | cheststore.cpp:167-170（isCartCell 纯按键查不看方块）、128-136（clearChest 摘键）；playercontroller.cpp:3282-3285（方块箱）/4523-4531（箱车）同键发射 chestOpened；Main.qml:1444-1457 分流、11299-11315 破箱清键 | 矿车键与方块箱分立寻址：`openChest` 的 isCartCell 分支加前提「键格当前为 Air（非 Chest 方块）」；方块箱写入前若该键是 cart 键先摘键并走箱车销毁链（掉内容）。补「标记格放箱→开→破」组合探针腿 |
| 4 | 中 | **t1013 生物可登乘箱子矿车**（Pass C 登乘扫描未排除 chest 车）——玩家侧 `tryMount` 明确拒绝箱车（「箱子矿车不可骑」），生物侧漏同款守卫；t1012 蛛网走廊的洞穴蜘蛛距轨旁箱车 0.8 格内即触发，蜘蛛钉坐进箱体模型视觉穿插 | entitymanager.cpp:5624-5631（扫描无 chestAt 排除）；minecartmanager.cpp:231-234（tryMount 拒箱车） | Pass C 矿车扫描循环加一行 `if (m_cartMgr->chestAt(i)) continue;`（读口已存在） |
| 5 | 中 | **t1018 坡臂恒真分支不验证邻居端点拓扑——可对上层既有定向线（c!=0）产生持久单向幽灵连接臂**。`railProbeEndpointAligned` 对 up/down 层有轨恒真（RailProbe 结构性缺失上下层 state），当上层邻是一条垂直于连接方向的既有 EW/NS 线时并非端点相对却放行；新轨持有指向它的连接位、对方 `c!=0` 走 `axisCon` 存在性路径不回连 → 单向幽灵臂：mesher 画成翘头坡、矿车 pickTrackStep 单向驶入不可返，直到对方被拆才自愈 | blockregistry.cpp:2633（坡臂恒真首行）、2578-2586（规则③松弛）；blockregistry.h:2152-2160（RailProbe 无上下层 state）；world.cpp:3094-3095（bit5 镜像清向） | RailProbe 增补 up/down 层 state（或仅 2 bit 轴信息），坡臂分支改查「邻居连接位/轴包含连接方向」；至少对上下层 c!=0 且无本方向连接位的邻居拒绝。探针补「上层垂直定向线」腿（现有腿全为同层邻居，坡臂分支零行为覆盖） |
| 6 | 中 | **t1019 能量判据在下行半幅早评，漏算谷内剩余下行收益——存在约 19.8 v² 宽的错杀带**。判据在车 d>0 的每个 tick 采样当前 v²，捕获是接管性的（跳过坡物理、v 单调减）→ 车永无「滑到谷心能量变大再翻身放行」的机会；而谷物理积分（t939 梯度覆盖按 slope=±1）每 0.5 格水平下行回收 19.8 v²。hOpp=1 时错杀带 = 入格缘 v²∈[~21.8, 41.6)：这些车按谷底账（+19.8 后 ≥ 阈值 41.6）本该通过却被捕获停谷心。壁顶带小助跑坡的重力滑入车（W 形起伏轨最典型）落入带内。t1019「能过则过」的核心卖点未完全兑现 | minecartmanager.cpp:601-604（判据采样）、587-589（只在 d≥0 捕）、459/467（谷面 rise=2\|axis-0.5\|）、1188-1194+1206（梯度覆盖 slope=±1 积分） | 判据改 `v2 + 2·kCartSlopeGravity·d ≥ pass2`（d=剩余下行水平距离，即折算到谷底账）；改后探针腿 (a)（21+19.8=40.8<41.6 仍该捕）结论不变保持绿。补带内速度档（v²≈30）探针腿——现有两腿恰好骑在带两侧 |
| 7 | 中 | **t1019 金轨谷格豁免对滑行中的车无断电刹车——t981 振荡症状在断电金轨 V 谷复发**。豁免 `blockAt==GoldenRail` 无条件 return false（不看通电态），而注释声称「断电金轨刹车（t939③）/ 通电 boost 语义优先」——t939③ 刹车实际只在 `\|speed\|<1e-3` 静置分支内；滑行空车/载人 coasting 在断电金轨谷格交还纯保守谷物理（下坡加速上坡减速零耗散）+ 两壁 kick 再供能 = 「两壁间多次往复」复发 | minecartmanager.cpp:561（无条件豁免）、1110-1117（t939③ 只在静置分支）、1206/1802-1805（两调用点） | 豁免收窄为「通电金轨」（`GoldenRailStateOnFlag` 置位才 return false），或先给滑行路径补断电金轨减速语义再豁免；同步修正 .h 注释。探针补金轨谷腿（现有腿全用普通 Rail） |
| 8 | 中 | **t1017 失撑复检谓词未同步排除仙人掌——同族「放置拒、残留不掉」口径劈叉**。放置预检三处收紧了，但邻块被破坏触发的 6 邻复检（梯子/机关）仍用 `isFullCube(支撑)==true` 保留（Cactus 是 ShapeFull→isFullCube 真）；同场景火把复检走 `torchSupportBlock` 会掉。旧存档挂在仙人掌上的梯子/拉杆/按钮在仙人掌存活期间永不掉落 | playercontroller.cpp:1774（梯复检）、1842（机关复检）；blockregistry.cpp:208（Cactus=ShapeFull）；对照 4952/4966（预检已拒） | 两处复检改与预检同口径（`isFullCube(x) && x != Cactus`），或抽 `mechLadderSupportBlock` 单一权威供预检/复检共享；补「邻块编辑后掉落」行为腿（P-t1017 只测放置拒绝） |
| 9 | 低 | **t1017 同族贴地/贴面件漏网**：活板门（贴仙人掌侧/顶）、红石粉、门、铁轨、雪层仍可依仙人掌放置——`trapdoorSupportBlock`/`isTopFlushSupport` 等谓词未排除，与 MC 1.0（仙人掌 15/16 缩体非可附着面）不符；commit 声称「附着族支撑黑名单一致」名不副实 | blockregistry.cpp:1044-1048、1328-1331；消费端 playercontroller.cpp:4935/4989-5003/5095-5098/4805 | 若追求口径统一：在支撑语义包装层排除 Cactus（不动 isFullCube 本体，渲染/剔除依赖它）；至少登记已知缺口 |
| 10 | 低 | **t1012 isAttachableBlock 水冲毁族漏 Ladder（及 Rail）**：MC 1.0 流水同样冲毁梯子；本工程放置侧附着族明确含梯子（t1017 措辞/失撑钩子俱在），水毁族只列 {Torch, RedstoneTorch, Cobweb} 且无「梯子刻意排除」登记——「单一权威」注释误导后续维护 | blockregistry.h:2233-2236 | 把 `Ladder`（评估后或加 `Rail`）加入水毁族（dropId(Ladder)=自身，掉落链免费成立）；或在注释登记刻意排除的理由 |
| 11 | 低 | **t1014 海列 cell 整格跳过 ≠ 旧「逐列跳过」**：新代码按 cell 中心列高跳过整个 16×16 cell，海洋中心 cell 内的陆地列从此完全无矿（只剩邻 cell blob 游走越界蹭入的少量补偿）；注释声称「同旧整列口径」名不符实。海岸带地下出现整 16×16 零矿 patch | world.cpp:6242-6245（cell 级跳过）对照 6229-6231（tryOre 逐列海检保留） | cell 跳过条件改「四角+中心全为海列」或直接去掉 cell 级跳过（veinN 计算便宜，纯海 cell 会在 tryOre 逐列被拒）；经济探针量全图总量平均掉了这一空间分布回归——可补「海洋 cell 内陆地列非零矿」分布腿 |
| 12 | 低 | **t1013 回生落车无可通行性守卫 + 开箱射线无方块遮挡**：path (b) 回生时键格被玩家填实 → 车嵌方块成幽灵实体（仍可射线开箱）；(a0) 开箱射线用 kReach 全程且 `findCartHit` 不做体素遮挡 → 可**隔墙右键开箱车取物品**（对比攻击分支用 m_hitDist 防隔墙） | minecartmanager.cpp:53-86（spawnCartImpl 无落格守卫）；playercontroller.cpp:4523-4531（(a0) 无遮挡）对照 1327/1339（攻击有） | spawnCartImpl 落格 isCollidable 时向上找空格或拒绝留键；(a0) 命中距离与 m_hitDist 取 min（与攻击分支同口径） |
| 13 | 低 | **t1015 攻击者眼位落入载具盒内时点击乘员身体恒命中载具**：`rayHitDistAt` 起点在盒内返 0，`0 ≤ 任何 mobDist` 恒真 → 载具恒胜。矿车爬坡段车厢盒抬到与玩家眼高重叠、贴轨站立瞬间（或船冲到身上）「点身体打身体」承诺失效 | minecartmanager.cpp:2042 / boatmanager.cpp:895（盒内返 0）；playercontroller.cpp:1328/1340（<= 比较） | `rayHitDistAt` 对 tmin<0（起点在盒内）返 -1 视为未中，或 caller 对 `cartDist==0 && mobDist>0` 特判 |
| 14 | 低 | **t1016 每次进世界 setWeatherState 覆盖 resetWeather 的「首场晴偏短」窗口**：enterWorld 在 resetWeather 之后无条件 `setWeatherState(wt.weather)`，缺键=Clear 分支把计时重抽为常规窗 45/120s（初始窗 20/45s 失效）→ 所有世界首场天气平均推迟约一倍，「进世界即见天气」登记口径被静默回退，kInitialClear* 常量实际不再生效 | world.cpp:88/5071（resetWeather）、4897-4898（Clear 分支重抽常规窗）；Main.qml:838（无条件恢复） | setWeatherState 对 Clear 增加初始窗口参数，或 enterWorld 仅在存档确实带 weather 键时才调 |
| 15 | 低 | **t1016 restoreTime→applyTime 用 float 折算 elapsedMs**：day≳2²² 后 phase 小数被 float ULP 吞（day≈4M 步进 0.5 天），applyTime 虽直写对 m_phase/m_dayCount，但 100ms 后 onTick 从 elapsedMs 重派生 → 恢复相位立即跳回整刻。正常游玩不可达；clock_day 读入无上限校验，改档/脏档可触发且无告警 | worldclock.cpp:88（float 折算）+180-197（onTick 重派生） | `m_elapsedMs = day*qint64(periodMs) + qint64(double(phase)*periodMs)`（double 折算或纯整数拆分） |
| 16 | 低 | **t1019 对壁扫描要求「逐格 +1」**：+2 跳升/L 形斜向爬升壁使 hOpp 停在低值 → 阈值被低估，能量边界车被放行去爬爬不出的壁，停驻反溜回落后被捕获（一次折返非极限环，可自愈，病态轨道几何下才触发） | minecartmanager.cpp:595-600（`railY+k` 无轨即 break） | 可接受则登记；要收口把 break 改「该列三高探针无轨才 break」 |
| 17 | Info | **t1018 bit5 单臂镜像的 else-if 链命中拐角形态**（con=Px\|Pz 含 X 臂即镜像 EW），与注释「拐角不算轴、保持偏好位」矛盾。bit5 对拐角目前是死位（railRiseAt/mesher/pickTrackStep 均先判 isCorner），无现行行为差异；存档往返无损 | world.cpp:3086-3095 | 两个 else-if 前加拐角短路，或更正注释——防未来「拐角读 bit5」消费者踩坑 |
| 18 | Info | **t1014 blob 竖向步触矿带边缘零位移卡步**：重挑 d 仍可能是竖向 → 同格重试到步数耗尽，脉提前夭折（非死循环、无越界，经济实测已含此效应，仅脉尺寸分布有偏） | world.cpp:6323-6331 | 重挑时限制水平 4 向 |
| 19 | Info | **t1013 提交自述已登记的套件弱点**：注释掩码可骗过源码钉（comment-masked pin，t934 同款陷阱在本批复现）——登记为未来加固批次事项 | 601b0ac 提交信息（阴性轮记录） | 已登记，无需动作；加固批次时优先处理 |
| 20 | Info | **同 seed 世界输出变化**（预期内）：矿井走廊/蛛网室/矿脉生成重写、矿井箱转为箱车 → 老 seed 新版本生成不同世界。旧存档进入时现存矿井标记箱会照常转成箱车（特性语义，非追溯缺陷）；已生成存档的地形不受 worldgen 重写影响（仅新世界跑） | — | 无需动作；发行说明可提一句 |

## 已核实安全面（审查结论，非发现）

- **越界/崩溃面为零**：wash/tickWaterFlow 全边界（含 y-1<0 拒绝）、tryOre 五重守卫（界内/基岩/海列/顶带/非 Stone）、placeMineshaft carveCell/putStruct 边界+Bedrock 守卫；ChunkManager 越界语义（blockAt 返 Air / setBlock 拒写）承接全部松散调用点。
- **水冲毁链无死循环/无非法掉落**：washed 与 obsidian/srcReg/evap 格集合不相交、washKeys 去重、dropId 三员（13/129/0x219）恒非 0、同 tick 补水成立、下 tick 不再 wash；isAttachableBlock 每 tick 常数次调用，t488 活动盒限流下成本可忽略。
- **枚举/存档兼容**：MobCaveSpider=20 尾追加、解码白名单同步、legacy 0x0E 仍解 MobSpider；spawner 0x28 round-trip（6-bit mask 内无截断）；实体不序列化；MobModel 表长/静态断言/探针三级互钉。t1016 三键与 chunks 同事务失败统一 rollback；phase 9 位有效数字 float 短往返、day qint64 往返、weather 值域静默拒；旧档缺键=Clear 重起与 pre-t1016 语义一致，且顺带关闭了 WorldClock 跨世界 phase 泄漏。
- **t1015 射线数学**：rayHitDistAt 与 find*Hit 同 ext 常量/同平行退化分支/同盒内返 0；lookDirection 归一 → mobDist 与 cartDist 同为归一 t 参数可比；船/矿车两套实现逐行对称；tie-break 同距判载具为登记设计；destroyCartTail 清骑手链无摔人异常；QML 无依赖旧复合碰撞箱的遗留路径。
- **t1018 松弛闸门**：c!=0 恒不松、fresh 级联不变、平行侧邻仍拒（t983 口径未回退）；双向闭合对计算顺序不敏感；worldgen placeMineshaft 传 curState=0 不触发松弛（矿井轨道零改动）；bit5 单臂镜像的 mesher/railRiseAt 两侧语义一致。
- **t1019 能量账自洽性（判据常量层面）**：stepCartAlongRail 水平推进 + 45° 坡积分 Δv²=±2g·h 与判据同量纲同常量；margin 0.05 的防棘轮语义成立（壁顶 kick +1.0 v² < margin 折算 1.98，不再放行）；扫描上限 16 在速度上界（v²/(2g)≈4.1 格）下不可达；方向判定无零向歧义；载人有输入不进捕获。
- **t1013 其余链路**：clearMineshaftChest 走 clearBlockSilent 不误发 blockBroken；虚空卷没保留键；毁车只发 chestCartBroken 不双掉；allChests/loadAll cart 标记往返；clearAll/loadAll 同步清键防跨世界串扰；packKey 对世界内坐标单射；交互优先级（方块箱→箱车→骑乘，sneak 绕过）正确；进世界钩子顺序（loadAll→t1016 恢复→carts.clearAll→t1013 转换→spawnInitialMobs）正确。
- **t1014 确定性**：hashVoxel 纯 unsigned 运算无平台差异源；矿种 salt 单射无碰撞；印章序=稀有度先到先得与 stamp 顺序自洽。

## 遗留待办（跨批跟踪）

1. **三个高优先修复尽快立任务**：#1 蛛网笼找位放行 Cobweb（洞穴蜘蛛功能死亡）；#2 箱车 gate 区分「从未 roll/搬空」（无限刷物品）；#3 标记格双容器分立寻址（车凭空消失 + 库存别名）。#2/#3 同在 ChestStore，建议一个任务收口并补两条组合/时序探针腿。
2. 中级四项（#4 蜘蛛坐箱车一行修、#5 幽灵臂 RailProbe 扩 state、#6 能量判据折算到谷底账、#7 金轨豁免收窄通电态、#8 失撑复检同口径）排后续批次；#4 是一行改动可顺手随 #1 落。
3. 探针盲区登记进 dev-plan：**组合面**（worldgen 几何 × 行为谓词）、**时序型**（清空→重开）、**键所有权**（同格双容器）三类本批全灭——单腿断言式探针的结构性弱点，建议下个加固批次与 comment-masked pin（#19）一并处理。
4. 工作区盘点：t1008 进行中未提交——`resourcepackmanager.cpp` 补 0x25D 小蹒跚者蛋 pack 直连映射行（① 图标管线统一）、`redstone_matrix_test.cpp` P-t1008 双修探针 + `tools/diag_t1008.cpp` 诊断工具；src 侧 t1008②（小鸡骑士越障跳）行为改动尚未见落地（探针先行阶段）。另 `.codex/` 未跟踪目录为外部工具产物，建议 .gitignore 或清理。

## 清偿状态（2026-09-07）

- ~~#1 蛛网堵死刷怪笼~~（高）→ ✅ review0907 第三波 C-P1-2（0968545；tickSpawners 陆生谓词 Cobweb 格豁免，探针 P-t1012⑤）。
- ~~#2 箱车清空重开无限重 roll~~（高）→ ✅ review0907 波 A A-P1-1（298b55b；持久 looted 标记面）。
- ~~#3 标记格双容器别名~~（高）→ ✅ review0907 第三波 C-P1-3（0968545；placeBlock 通用预检层拒绝键格放 Chest，探针 P-t1013(h)）。
- ~~#4 生物登乘箱车~~（中）→ ✅ review0907 第三波 C-P1-5（0968545；登乘扫描 `chestAt(i) continue`，探针 P-t1013(h) 拒载腿）。
- ~~#5 坡臂恒真幽灵臂~~（中）→ ✅ 2026-09-07 D-P1-1（RailProbe 增补 upState/downState + 坡臂分支读对应层 state；探针 P-t1018(e) 上层垂直定向线腿 + fresh stub 阳性对照）。
- ~~#6 能量判据下行半幅早评~~（中）→ ✅ 2026-09-07 D-P1-2（判据折算谷底账 v²+2g·d；探针 P-t1019(d) 带内中速档自搜索腿；(a)(b)(b2) 既有腿结论保持）。
- ~~#7 金轨谷格豁免不看通电态~~（中）→ ✅ 2026-09-07 D-P1-3（豁免收窄 GoldenRailStateOnFlag 置位；探针 P-t1019(e) 断电捕获无往复 + (f) 通电对照；.h/实现注释口径同步）。
- ~~#8 失撑复检未排除仙人掌~~（中）→ ✅ 2026-09-07 D-P1-4（mechLadderSupportBlock 单一权威四处共享——预检两处+复检两处；探针 P-t1017(e) 邻块编辑后掉落行为腿 + 石墙梯对照）。
- ~~#16 对壁扫描逐格 +1 即 break~~（低）→ ✅ 2026-09-07 D-P1-5（break 放宽为三高探针无轨 + hOpp=k+δ 层差记账；随 #6 同提交，扫描口径钉同步更新）。
- ~~#9 同族贴地/贴面件漏网~~（低）→ ✅ 2026-09-08 D-P2-1（BlockRegistry::solidSupportBlock 统一权威 = isFullCube && !Cactus 包装层，铁轨/雪层/活板门/门/粉预检与失撑复检全族一处收口，isFullCube 本体不动零回归；探针 P-t1017(f) 贴面/站顶全家族扫 + 石头对照 + P-t945(h) 钉同步）。
- ~~#10 水冲毁族漏 Ladder（及 Rail）~~（低）→ ✅ 2026-09-08 D-P2-2（isAttachableBlock 并入 Ladder，dropId=自身掉落链免费；Rail 刻意排除以注释登记——worldgen 矿井轨网不可再生资产；探针 P-t1012c 谓词 + 行为双腿，轨格完好对照）。
- ~~#11 海列 cell 整格跳过 ≠ 旧逐列跳过~~（低）→ ✅ 2026-09-08 D-P2-3（cell 级一票否决移除，海列拒绝兜底回归 tryOre 逐列；探针 P-t1014(h) 专测海 seed 150461/174218 陆地列矿量 ≥200（实测 747/782）+ 海 cell 结构钉——diag_t1014h 实证 t1014 固定 seed 窗零海 cell，旧候选腿恒 vacuous）。
- ~~#12 回生落车无守卫 + 开箱射线无遮挡~~（低）→ ✅ 2026-09-08 D-P2-4（spawnCartImpl 落格实心上扫可落格、顶满拒生成留键重试 + placeBlock 箱车分支 chestDist≤m_hitDist；探针 P-t1013b(a)(b)）。
- ~~#13 眼位落入载具盒恒命中载具~~（低）→ ✅ 2026-09-08 D-P2-5（BoatManager/MinecartManager::rayHitDistAt 盒内起点返 -1 未中；find*Hit 登乘寻的不改，口径分叉登记；探针 P-t1015(f)）。
- ~~#14 进世界 setWeatherState 覆盖首场晴短窗~~（低）→ ✅ 2026-09-08 D-P2-6（loadWorldTime 增 hasWeather 键，Main.qml 仅真带键才恢复；探针 P-t1016(a)(b) + t1016b(a) + QML 守卫钉）。
- ~~#15 applyTime float 折算大 day 吞相位~~（低）→ ✅ 2026-09-08 D-P2-7（整数拆分 day·periodI + double 折算 phase 段；探针 P-t1016b(b) 大 day round-trip 保真）。
- ~~#18 blob 竖向重挑零位移卡步~~（Info）→ ✅ 2026-09-08 D-P2-3 同批（重挑限水平 4 向 kH4；t1014(f) 源码钉 + (c)/(d) 脉形窗按实测校准：红石脉数 1390→1300、金 conn 95→85%）。
- #17 / #19 / #20（Info）→ 登记口径维持原审查建议（#17 bit5 拐角死位无现行行为面；#19 comment-masked pin 归加固批次；#20 同 seed 世界输出变化为预期内）。

> D2 波全矩阵 **479 PASS / 0 FAIL**（473 基线 + 6 新 PASS 行；offscreen）；阴性合并轮 matrix_d2_lesion.log = 474/5 恰红（#10/#12b/#13/#14 四病灶 → t1012c / t1013b / t1015(f) / t1016 / t1016b 五腿），restore 字节级核验后复跑 479/0。详见 dev-plan「review0906 低七项清偿（D2 波）」小节。
