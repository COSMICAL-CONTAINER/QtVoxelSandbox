# Review 2026-09-07 — 近 24h 提交审查（t1008 骑士越障/蛋图标 · t1020 成就树 17→31 · t1021 结构氛围音 · t1022 键位重映射 · t1023 动画门控+AO · review0907 修复波 A 五项）

- 审查对象：`6c704f2..4da6a79`（2026-09-06 09:48 → 09-07 09:21，共 23 笔：9 笔 src fix、6 笔探针、8 笔 docs/plan，矩阵 450→464）。
- 审查方式：9 笔 fix 逐笔 diff 精读（t1020 world.cpp 554 行 / t1021 audio 643 行 / t1022 keybind 576 行为重点）+ 关键路径在 HEAD=4da6a79 交叉核对（file:line 均为 HEAD 当前行号）；canonicalKey / Cobweb / chestKeyAt 等消费面全仓 grep 定界。未运行矩阵（HEAD 矩阵 464 PASS / 0 FAIL 为提交自述，同 09-06 审查口径）。
- 总评：**无高严重度新缺陷**，1 中 / 2 低 / 4 Info。review0907 修复波 A 五项（looted 标记 / 煤帽哨兵 / bit5 拐角守卫 / 结构边沿无条件化 / 键位别名丢弃+黑名单）逐项核对全部自洽，其中箱车 looted 标记的「生成格固定寻址」模型与回生语义两面分离论证成立。本批唯一的面上风险在 **33cdf6c 把「绑定目标黑名单」与「显示层合法键判定」共用同一个 `isValidKeyValue`**，导致昨天刚落地的键位设置页两行键名显示空白（中级 #1）。t1020 的选址上收 + 区域重推导是本批结构性最大的改动，常量逐值核对零漂移，仅矿井包络半径的推导注释有算术问题（低级 #3）。另：**09-06 review 的三个高危还有两个（#1 蛛网堵死刷怪笼、#3 标记格双容器）在本轮 grep 复核下仍未修复**，wave A 只闭合了 #2——跨批跟踪见文末。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | **中** | **33cdf6c 黑名单回归：键位设置页 sneak / modeCycle 两行键名显示空白**。`keyDisplayName` 用 `isValidKeyValue` 做门（`!isValidKeyValue → 返回空串`），而该函数本轮被扩成「绑定目标黑名单」——sneak 默认键 Shift、modeCycle 默认键 G 恰都在黑名单内 → 这两行的当前键名渲染成空串（既非键名也非「未设置」）；冲突提示里被占用键为 Shift/G 时同样显示「冲突：「」…」空键名；且新拒收码 `ApplyForbiddenKey` 落进 QML 录制器通用 else 臂显示「无法识别的按键」（键其实可识别，只是固定不可绑——.h 注释已登记兜底，但文案误导） | keybindmanager.cpp:49-76（isValidKeyValue 含黑名单）、251-256（keyDisplayName 同一判定）；Main.qml:12536-12541（行显示 `keyDisplayName(keyFor(id))`）、12582-12588（冲突文案 + else 兜底） | 判定拆分：`keyDisplayName` 只拒「非键值」（`<0x20 \|\| Key_unknown`），黑名单收进仅 `applyBinding`/`load` 使用的 `isBindableKey`；QML 录制器对 `ApplyForbiddenKey` 单出「该键为固定功能键，不可绑定」。补一条设置页行显示探针腿（本批 P-t1022 三面板只测返回码与 load 回退，不测 QML 显示层） |
| 2 | 低 | **sneak / modeCycle 一旦改绑就无法单独绑回默认键**。Shift / G 作为绑定目标恒被黑名单拒收，「同值幂等放行」只在仍绑定时成立（改绑走开即失效）；唯一恢复路径是「恢复默认键位」全量回滚 13 键。属设计口径的单向门，但与「重绑自由」预期有落差 | keybindmanager.cpp:225-226（黑名单判定在幂等之后）、30（sneak canonical=Shift）、36（modeCycle=G）、234-249（resetDefaults 全量） | 黑名单对本动作**自身 canonical 默认键**放行（`key != def.canonical` 例外一行），单键可回绑回默认；或登记「固定键不可回绑、仅全量恢复」口径 |
| 3 | 低 | **t1020 矿井区域包络 `kEnvHalf=26` 推导与 L 形巷道几何不符**：两段巷道是垂直关系（L 形），轴向最远 ≈ 厅半幅 5 + 单腿 10 = 15，+1 墙环 16 已足；注释按「5+10+10」把两腿当共线相加。结果「废矿来客」的触发 bbox（53×53×12）比结构实际足迹面积大近一倍——玩家在包络内的实心岩里自行挖洞也会触发进入成就与环境音（「不逐 piece 精确」已登记，但半径本身推导错了） | world.cpp:390（kEnvHalf=26 + 「5 + 10 + 10 + 1」推导注释）、381-430（mineshaftSites 足迹落表）、e179e44 后该 region 兼作氛围音区 | kEnvHalf 收到 16（或按 placeMineshaft piece 实际极值重新推导）；至少修正注释算术勘误。已生成存档不受影响（区域表纯重推导、无序列化） |
| 4 | Info | **「移动金库」描述与埋点口径劈叉**：描述「打开装货的矿车**取走物品**」，实际 `openChest` 的 isCartCell 分支开箱即解锁——空车 / 未取物也弹 toast | playerprogress.cpp:112（描述文案）、337（onChestCartOpened）；Main.qml:1525（开箱分支直调） | 口径可接受（MC 开箱型进度同款）；要收紧把埋点移到「首件取出」（setSlot 移出时） |
| 5 | Info | **绑定到「被其它动作占用的黑名单键」（如仍属 modeCycle 的 G）时 ApplyConflict 先于黑名单返回**——报「冲突」而非「固定键」，叠加 #1 后提示键名空白；两个拒收码语义二义 | keybindmanager.cpp:218-226（conflict 判定在 forbidden 之前） | 黑名单判定提到 conflict 之前（顺 #1 一起收口）；单独看可接受现状 |
| 6 | Info | **pre-t1013 掏空的矿井标记箱在转换后有一次跨档重 roll**：老档里已被玩家掏空但标记块仍在的矿井箱 → t1013 转换 registerCart 登记空条目且无 looted 标记 → 转换后首开按同 seed 重 roll 一次（t1013 之后的新链路已被 298b55b 闭合；这是旧档迁移缝隙，与「跨档重 roll 既有口径」同族，一次性、仅旧档） | cheststore.cpp:337-340（gate 只认 looted 标记）、372（roll 发生才置位）；转换链 registerCart 登记空条目无历史信息 | 可接受（登记）；要闭合需在转换路径识别「曾有条目但已空」的老标记箱预置 looted——历史不可辨，成本高于收益 |
| 7 | Info | **神殿保底全图扫描（160² biome 门 + siteOk）在 generate 期跑两遍**（place* 消费一次 + rebuildStructureRegions 再推导一次）、每次进档一遍——纯生成/加载期一次性成本，量级可忽略 | world.cpp:482（hasDesert 全图扫起）、325（rebuild 再调 sites()） | 登记即可；要省可在 generate 内把 sites() 结果传一次给 rebuild 复用 |

## 已核实安全面（审查结论，非发现）

- **review0907 A-P1-1（298b55b 箱车 looted 标记）**：三口同步（clearChest / clearAll / loadAll）与 m_cartKeys 同纪律；allChests `"looted":true` 仅随条目落盘（矿车键豁免空条目跳过 → 取空后条目仍落盘携标记）；**箱车内容按生成格固定寻址**（playercontroller.cpp:4611-4615 `chestKeyAt` 携生成格坐标，车沿轨驶离后 UI / 存取仍对同一键）→ 车移动不重键、looted 标记无需迁移，寻址模型自洽；gate 幂等序（非空即拒 → 空矿车键放行 → looted 拒）实现「回生 / loot 两面分离」；老档无键 = false 向后兼容。
- **review0907 A-P1-2 / A-P3-1（c4c99b4）**：bit5 拐角守卫加交叉轴零位后，拐角（X+Z 各一位）正确落穿两个 else-if 保持偏好位；贯穿分支（Px\|Nx / Pz\|Nz 全位）在链首不受影响，三臂混合形态仍走贯穿分支。煤 yMax=0 哨兵仅 kind==1 分支消费；64 高 rig hc-5 ≤ 58 < 60 等价性成立；逐块 tryOre 的 y ≤ h-3 石门兜底仍在，hc-5 < yMin 时 span = max(1, ·) 无越界无死循环。
- **review0907 A-P2-1（e179e44 结构边沿无条件化）**：loop 移出 m_insideStronghold 的 else 臂后四结构 region 检查 + structureEntered 沿 + m_insideStructure 更新每 tick 无条件执行，要塞环境音改 loop 后纯值覆盖（最高优先级保留、不再吞沿）；m_insideStronghold 在 t1000 段（本块上方）先更新后消费，tick 内顺序正确。
- **review0907 A-P2-2 4a（canonicalKey 丢弃）**：全仓 grep 证实 setKey 是唯一消费点；「按住旧键跨改绑 → 释放被丢弃 → m_keys 残留卡键」路径被 release() 全清 m_keys 防住（设置页必经暂停 → 指针释放 → release()，playercontroller.cpp:620-628）——该风险不成立。
- **t1023（67ed886）**：AO 角点探针轴向/符号推导逐面核对（±X→Y/Z、±Y→X/Z、±Z→X/Y；F.c 角点 0/1 → ±1 朝角偏移，经典 MC AO）；双侧同遮钳 3 曲线单点定义（voxellight.h:50-58）防薄墙三重压黑；AO 乘在光场钳制后（允许低过 kVcMin 地板，登记语义）；默认关 = 乘 1.0 顶点色逐字节旁路；t472 远端静默更新 + 回区 catch-up 与 setGreedyMeshing 同款；四处 `running: visible` 动画门控槽复用 from 重启 = 新实体语义（t561 教训引用正确）。
- **t1021（9046b01）**：滴水 / 虫鸣调度器单发重臂（stop 后 timeout 早退不再重臂）无堆叠无节拍器感；两循环音 setVolume 即时同步、析构 uninit 全量、退出世界双路径（exitWorld / returnToMenu）显式全停四音；finishWorldLoad 静默清 zone → 存档停结构内重进首 tick 值变重发恢复；engine / clip 降级路径全部静默早退；要塞低鸣 else 臂吞沿缺陷（本提交引入）已被次日 e179e44 修复并补 region-overlap 探针腿。
- **t1020（939b04a）**：选址段上收逐值核对零漂移（seed off 12037/15047/19487/22617、网格 24/36/48/40、概率 10/40/45/50%、yHi 公式、bit28/29 内空尺寸位、丛林屋顶守卫 `+kJungleTempleRoofY+1` ≡ 原 `kRoofLift=12`）；sites() 输入（heightAt / seaColumnHeight / biomeAt）全为 seed 纯函数 memo → generate 末与 finishLoad 末重推导同表成立；beginLoad 清表 → 错误中断路径空表恒 false 安全；成就定义父先于子（DFS 序）、探索/生活挂独立根避免父前置吞沿（首轮红腿教训吸收）；沿守卫 setWorld / finishWorldLoad / tick 三重置点与 m_insideStronghold 同纪律。
- **t1008（de473eb）**：蛋表补行无副作用（pack 缺图走 spawnEggTint 生成式回退，管线统一）；跳意图转移读序正确（先读 e.vy 再由既有行清零）、`mount.resting` 守卫防空中重点火（组合火箭不可能）、kKnockbackUp < kJumpSpeed 不误触、骑手 vy/滑流清零行未动（review0830 #19 stale-glide 防线保持）。
- **同 seed 世界输出变化（预期内）**：t1020 煤帽放开后 128 高世界高山列煤分布变化（c4c99b4，探针 t1014(g) 已钉）；区域表为纯重推导零序列化，旧存档进档即可判、无迁移面。

## 遗留待办（跨批跟踪 → 2026-09-07 第三波收口状态）

1. ~~**09-06 review 三高危仍开两个**~~ → **已处置（第三波 C 组）**：#1 满网蛛网走廊堵死洞穴蜘蛛刷怪笼 → tickSpawners 陆生谓词 Cobweb 格豁免（探针 P-t1012⑤ 满网笼长 tick 刷出洞蛛）；#3 标记格双容器别名 → placeBlock 通用预检层拒绝在登记 cart 键格放 Chest（探针 P-t1013(h) 放置拒 + 普通格对照）。09-06 遗留 #4（蜘蛛坐箱车）同波收口：登乘扫描 `chestAt(i) continue` 一行（探针 P-t1013(h) 拒载腿 + 普通车对照）。
2. 09-06 中优先 #5-#8（RailProbe 幽灵臂 / 能量判据谷底账 / 金轨豁免收窄通电态 / 失撑复检同口径）仍未动，排后续批次。
3. ~~**本批 #1 + #2 + #5 同根 isValidKeyValue 双职责**~~ → **已处置（第三波 C-P1-1）**：判定拆分 `isKeyLikeValue`（显示层）/ `isBindableKey`（绑定目标门，applyBinding/load 专用）+ own-canonical 默认键放行（#2 单向门）+ 黑名单先于 conflict（#5 二义）+ QML 录制器 ApplyForbiddenKey 专文案；探针 P-t1022 面板 D 补显示层腿（keyDisplayName 非空正确名 / 分流顺序 / 绑回默认）。
4. ~~**探针盲区登记：键位设置页 UI 显示层；t1020 矿井包络行为腿**~~ → **已处置（第三波探针）**：P-t1022 面板 D 收口键位显示层（行键名 / 拒收码语义 / QML 钉）；P-t1020(a'') 收口矿井包络行为腿（全图 Rail 格 ∈ region bbox + 半边=2×16 值钉——随低级 #3 的 kEnvHalf 26→16 重推导一并落地）。
5. 本 review Info #4（移动金库开箱即解锁）/ #6（pre-t1013 掏空旧标记箱跨档一次重 roll）/ #7（保底扫描 generate 期两遍）→ **登记不处置**（dev-plan review0907 C 组登记项，口径可接受 / 历史不可辨 / 量级可忽略）。
