# Code Review — 2026-08-26（近 24 小时提交）

- **审查范围**：`346ee12..ae47f31`（2026-08-25 12:11 → 2026-08-26 09:04，共 55 个提交；其中 27 个纯 docs 提交只改 `docs/dev-plan.md` 与 Review 标记，已跳过，实际审查 **28 个代码提交**）。更早窗口已由 `Review_2026-08-25.md` 覆盖（其 #1-#17 的修复提交 346ee12/d12eddc/a3ec458 落在本窗口内，一并复审）。
- **审查基线**：HEAD = `ae47f31`，行号均以 HEAD 为准，工作区 clean。
- **方法**：5 路并行子系统审查（矿车物理 / 钓鱼系统 / 红石与物品 / 生物支撑与动物模型 / 暂停语义与 UI，逐提交读全量 diff + HEAD 交叉核对）+ 主审亲核。**主审逐行亲核**：#1（完整逻辑链）、#2、#4、#5（规则函数全文）、#7（浮定公式数学）、#8、#9（ChestUI 实读）、#11、#18、#21、#23；其余为子代理核、主审未逐行复核。
- **结论速览**：**高 1 / 中 10 / 低 14**。本窗口主体是 t863-t866 矿车物理翻修、t881-t886 钓鱼反馈系列、t865/t867 支撑判定收口 `supportTopYAt`、t869 红石无稳态修复、t889 暂停语义统一。问题集中在五个模式：① **t865 把「碰撞真顶」引入支撑链，但水平碰撞 / 越障判据仍是 per-cell 整格口径**——脚位落进矮支撑格内部后旧谓词自锁（#1 高 + #2/#3/#19 同族，本窗口最大模式）；② 复用既有 helper 的隐藏副作用（teleportEntity 落定清仇恨 #8、Array.isArray 守卫残留 #9）；③ 矿车新 derailed 状态机的窗口与收尾遗漏（#4/#12/#13/#14）；④ 红石形状语义自一致性不足且无 burnout 兜底（#5/#6/#15/#17）；⑤ 钓鱼修复对流动水等形态覆盖不全（#7/#21）。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高 | 1 | #1 mob 走上矮碰撞支撑（下半砖/耕地/压力板等）后水平移动被锁死 |
| 中 | 10 | #2 掉落物贴薄支撑水平锁死；#4 瞄乘员上身攻击被吞；#5 红石拐角侧向供电自相矛盾；#7 流动水获物弧线被吞；#8 浮标零消耗安抚夜行者；#9 五面板攻击行恒缺 |
| 低 | 14 | 见第四节 |

---

## 一、高危（建议立即修复）

### #1 `b02be01` (t865) mob 走上矮碰撞支撑（下半砖/耕地/压力板/附魔台/合活板门等）后水平移动被自身所站格锁死——原地冻结

- **提交**：b02be01 feat(t865) 支撑/碰撞/越障谓词收口到碰撞语义。
- **问题**：t865 把 `mobSupportTopY` 收口到 `supportTopYAt` 后，mob 脚位第一次真正落到矮支撑的**真顶**（下半砖 +0.5、耕地 +0.9375、压力板 +1/16、附魔台 +0.75 等）——即脚位落进支撑格**内部**（feet < cell+1，t629 注释自己承认这种非整数脚位形态）。而水平碰撞 `mobAabbHitsSolid` 的 Y 扫描下界是 `y0 = floor(cy − halfH)` = floor(feet) = **支撑格自身**，该格 isCollidable=true → 任何水平试探恒命中 → 逐轴撤回 → mob 永久冻结。旧版 `mobSupportTopY` 对矮碰撞体一律返回 cell+1（feet 恒整数，AABB 不含支撑格），移动不受影响；t865 只为雪层 ≤0.5 保留了显式穿透豁免，其他矮碰撞体全部中招。`entitymanager.cpp:166` 注释自称「碰撞实体（半砖/门/活板门/压力板等）仍当整格墙挡——旧语义不变」，该承诺恰好被本提交自己的落定链破坏。
- **证据**（主审亲核）：`src/World/world.cpp:553-566`（supportTopYAt 真顶）；`src/Entities/entitymanager.cpp:137-171`（mobAabbHitsSolid 的 `y0=int(std::floor(miny))` 无脚下支撑豁免，158-160 行豁免仅覆盖 SnowLayer≤0.5）；`entitymanager.cpp:6785-6809`（resting 贴面链把 feet snap 到真顶，6806 行 `restY = supportTop + halfH`）。t865 探针只测了铁轨与草丛（两者 isCollidable=false，不在水平碰撞扫描内），探针盲区。
- **触发条件/影响**：mob 走上农田耕地、半砖建筑、压力板走廊即原地冻结（wander/chase 全部逐轴撤回）；跳跃仅在正前方有墙触发 isJumpObstacle 时才可能意外脱出。农田生物全员站桩是必现场景。
- **修复**：mobAabbHitsSolid 的 y 循环对「mob 正站在其顶」的格子豁免——`y == y0`（脚位格）且该格全部碰撞盒 `maxY ≤ miny − float(y) + 容差(如 1e-3)` 时 continue（把 t629 雪层豁免推广为通用「脚位格薄支撑」豁免）。该判据不破坏「走向矮墙仍被挡」语义（走向时 feet−y=0 < 盒高 → 不豁免）。补探针：mob 站下半砖/耕地上水平位移 >0。
- ✅ 已修（d2d4c22）：mobAabbHitsSolid 脚位格豁免（y==y0 且 supportTopYAt 真顶 ≤ 脚位+1e-3 → 视穿透，雪层豁免推广为通用）；配套 isJumpObstacle 增 feetY 参（7 调用点全传 e.pos.y()−e.halfH）——前方格真顶 ≤ 脚位 = 同高矮支撑非墙不跳（否则解冻后全程兔跳）。探针 review26-1（下半砖+耕地走廊贴真顶行走）钉死，禁豁免即红。

## 二、中危（建议尽快修复）

### #2 `83eadd1` (t867) 掉落物贴上薄支撑后水平滑动被锁死（#1 的物品侧同根因）

- **问题**：物品静息中心 = 真顶 + kRestOffset(0.3)。对顶面 <0.7 的薄支撑（压力板 1/16、睡莲、合活板门 0.1875、下半砖 0.5、床 0.31），中心落在支撑格内部 → 摩擦段的水平碰撞探测 `hcy = qFloor(e.pos.y())` = 支撑格自身 → `isCollidable(..., hcy, ...)` = true → 带初始弹出速度也滑不动，物品被「钉」在落点。旧版 restY 恒为 cell+1+0.3（hcy=上方空气格）可滑。冰面滑摩擦特性在这些支撑上同样失效。
- **证据**（主审亲核）：`src/Game/itementitymanager.cpp:534`（feetCell 落入板格自身的两格窗）与 `:590-596`（`hcy = qFloor(e.pos.y())` 单格水平碰撞探测）；598-599 行摩擦面查询已改两格窗同源，但碰撞探测没跟上——半改态。
- **修复**：水平碰撞探测与 resting 复探同源——目标格 `supportTopYAt ≤ e.pos.y() − kRestOffset + 容差` 时视作「正站其顶」不挡；或 hcy 改用脚底格 `qFloor(pos.y() − kRestOffset)` 并对支撑格做同判。
- ✅ 已修（d2d4c22）：水平探测改 hBlocked（目标格真顶 ≤ 当前底+0.05 = 正站其顶不挡，与 resting 复探同源）+ **下半**：resting 有支撑不再整段 continue 跳过 t468 水平积分（旧结构让着地后滑动只剩落定那一拍 = 「钉在落点」另一半根因；现按 t468 摩擦注释声明行为滑行——常规地面 ~0.5s 停、冰面数秒）。t886 获物探针改钉**首次触底点**距玩家 <2.2（终点位随滑行语义合法漂移，触底点才是抛物解锚）。探针 review26-2（带弹出速度滑行 + 满格墙对照）钉死，禁豁免即红。
### #3 `b02be01` (t865) isJumpObstacle 岩浆口径分裂：水沟跳过、岩浆沟直接走进去

- **问题**：`isJumpObstacle` 只对 Water 显式保留 ditch-jump（return true），岩浆（ShapeNone → isCollidable=false）落到 `isCollidable` 分支返回 false。旧版岩浆非 crop/fire 恒返回 true（mob 跳过岩浆格）。行为翻转且与水的保留口径自相矛盾；mob 侧点燃链存在，结果是被动生物/跟随玩家的狼猫径直走进岩浆殉死。
- **证据**（子代理核）：`src/Entities/entitymanager.cpp:33-39`（`if (bid == Water) return true; return world->isCollidable(...)`）；岩浆 ShapeNone 见 blockregistry 注册。
- **修复**：`bid == Water || bid == Lava` 同列 ditch-jump；若刻意改语义则在提交/注释里明示。
- ✅ 已修（d2d4c22）：isJumpObstacle 水岩同列 ditch-jump（两流体皆 ShapeNone，岩浆旧版恒跳行为恢复、与水口径自洽）。探针 review26-3（脚位层岩浆格触发越障跳 + 越过到达远端）钉死，去岩浆即红。
### #4 `aa6dc17` (t866) 瞄准乘员露出车斗的上身/头部时，攻击被完全吞掉（无效果但消耗冷却+挥手）

- **问题**：beginMining 的乘员攻击重路由**无视 hitCartFromRay 的返回值就无条件 `return`**。生物乘员 AABB 高出矿车命中盒一大截（mob 中心 = 车心 −0.3125 + halfH，Zombie 等半高 0.95 → 头顶高出车盒顶约 1.2 格），准星瞄乘员可见头部/上身时射线命中 mob 但不与车盒相交 → hitCartFromRay 返回 false → 既不打 mob（旧代码会 attackMob，属回归）、也打不到车（本提交的修复目标落空），而冷却已置位 + swingArm 已发 = 点击被静默吞掉。骑船分支（1183-1191 行）同构同病。
- **证据**（主审亲核）：`src/Game/playercontroller.cpp:1173-1192`（1180/1190 行无条件 `return`，hitCartFromRay 返回值丢弃）；车盒半宽常量见 `src/Entities/minecartmanager.cpp:147`；乘员钉位公式见 `src/Entities/entitymanager.cpp:4743`。
- **触发条件/影响**：攻击任何高个生物乘客时瞄上半身 → 连点全部无效，玩家感知「有时打得动有时打不动」。另：重路由用 kReach 全程射线而非 m_hitDist，极端角度可隔墙打车。
- **修复**：检查 hitCartFromRay 返回值，未命中时落回 `attackMob(mobIdx)`；射线长度改用 m_hitDist。
- ✅ 已修（5c79ee7）：重路由改「冷却门 && hitCartFromRay/hitBoatFromRay 返回值」值门调用——未命中车盒落回 `attackMob(mobIdx)`（冷却/挥手由 attackMob 自管，吞击几何恢复打乘员）；射线长度 kReach→m_hitDist（同三分支「实体比方块近才优先」口径，不可隔墙打车）；船分支同修。t866 探针 (a3) 源码钉改钉新契约（值门 `&& ...m_hitDist...` + 分支内 attackMob 落回）+ 新增 (a4) 行为几何钉（rig 乘员改 Shambler halfH 0.90，上身高度水平射线 findMobHit 命中 / findCartHit 恒 -1 / hitCartFromRay 返 false）。阴性验证：回退旧代码 → a3(src) 红（矩阵恰 1 FAIL）。矩阵 294 PASS / 0 FAIL。
### #5 `0cfb780` (t869) 拐角/单臂红石粉向全部垂直开放侧（最多 3 向）供电——与「贯穿直线垂直侧不供电」规则自相矛盾

- **问题**：`redstoneDustPowersNeighbor` 的判定等价于「目标向无连接位且非贯穿直线侧即供电」。单臂粉（只连 −Z 一根）向 +X、−X、+Z **三个**非连接方向全部返回 true；而贯穿直线中段的垂直侧明确不供电。同为「垂直于粉臂的侧面」，端点供电、直线不供电——一格之差行为翻转，用户无法预期（L 形线路拐角格的垂直侧贴 TNT/灯/发射器/铁门时意外通电）。头注释声称 "MC 1.0 parity"，该口径与 MC 两种语义（1.0 全向 / 现代指向式）均不符。
- **证据**（主审亲核）：`src/Core/blockregistry.cpp:1279-1294` 四个 return 分支；单臂时 straightZ/straightX 均为 false，三个垂直向全过 `!nx && !straightZ` 型判定。
- **修复**：统一为「臂轴延长端」语义——目标 ±X 供电 iff 粉在 X 轴有连接（px||nx），目标 ±Z 供电 iff（pz||nz），dot 仍 false。该规则下：直线侧向 false（与现状一致）、拐角开放端 true（与现状一致）、端点垂直侧 false（修掉矛盾）。补矩阵形状探针：拐角垂直侧 / 拐角延长端 / 端点垂直侧 / 端点延长端四形（现探针只覆盖端点朝基座、直线穿过、dot 三形）。
### #6 `0cfb780` (t869) 端点粉贴火把基座 = 永续 5Hz 振荡，无 burnout 兜底——t740「灯闪」原始投诉形态部分回归

- **问题**：t740 前「意外装饰环闪烁与有意时钟同样振荡」，t740 整体豁免把两者一起杀掉；t869 恢复了端点/拐角形态的回灌，只保住 dot 与贯穿直线两个反闪烁形状（P4/P14 仅钉 dot、t869(c) 仅钉直线）。火把立在方块上 + 基座旁任意 ≥2 格粉线（端点朝基座，铺装饰线/引线时极常见）→ 火把每 redstone tick 翻转（周期 2 tick = 5Hz）**永久**振荡。MC 有 torch burnout 熔断兜底，本实现明言未实现。伴随每 tick 的 recomputeLightAround 双调 + chunk mesh 10Hz 持续重建直到玩家干预。
- **证据**（子代理核）：`src/World/world.cpp:3800-3840`（attachPowered 形状判定与翻转写入）；振荡驱动链：火把熄灭→粉失电→火把重亮，无终止条件。
- **修复**：补 MC burnout 近似（同一火把短时间内翻转 N 次后锁定熄灭一段冷却）；「端点粉贴基座=可振荡」记入已知 tradeoff 文档而非只记 mesh 重建。
### #7 `0444b2f` (t886) 获物弹出点 +0.35 抬升在流动水（state≥2）表面未离开水格，弧线仍被浮水分支吞掉

- **问题**：`kFishCatchRiseOffset=0.35` 只在静水（state=0，液面=格顶）浮定时恰好把生成点送出水格（格顶−0.125+0.35=格顶+0.225）。浮定 Y 按水 state 折算液面（`surf=(8−min(s,7))/8`），流动水 state≥2 时 surf≤0.75 → 生成点 = by+surf+0.225 < by+1，**仍在水格内** → 掉落物中心格==Water → 浮水分支 vy 清零 + 恒速上浮，弧线整个消失，获物粘回浮标处。冰面下浮定同理嵌冰。t886 要修的「看不见获物飞来」症状在河流/溢流边缘复发。
- **证据**（主审亲核数学）：`src/Game/playercontroller.cpp:2580`（固定 +0.35，注释自述假设「抬升后中心格=空气」只在静水位成立）；液面折算见 `src/Entities/entitymanager.cpp:5797-5799`；浮水分支见 `src/Game/itementitymanager.cpp:454-483`。
- **修复**：生成点不按固定偏移，从浮标格向上扫到首个非 Water 格再 +0.225（与 ItemEntityManager 浮水分支自己的列扫同源）。
### #8 `7e707a9` (t883) 浮标闪避复用 teleportEntity：0 伤害、0 消耗即可远程安抚激怒夜行者并打断攻击前摇

- **问题**：浮标命中夜行者直调 `teleportEntity`，而该 helper 落定时无条件清 `enraged/rageTimer/windupTimer` 并重置瞬移冷却。浮标是 0 伤害、永不钩定、回收零耐久——与箭链先例（箭被消耗、失败兜底还造成伤害）语义不同，成了**免费可无限重复的远程「净化」**：被追击时对夜行者甩竿即清激怒+打断前摇，可循环。MC 1.0 末影人被投射物闪避后并不解除仇恨。
- **证据**（主审亲核）：`src/Entities/entitymanager.cpp:3923-3924`（helper 落定清仇恨三连）与 `:5753-5761`（dodge 直调）。
- **修复**：teleportEntity 增加「只移动不清仇恨」参数；或 dodge 前快照 enraged/rageTimer/windupTimer，成功后回写。
### #9 `e307e4c` (t874) sequence 守卫修复不完整：5 个面板悬停武器攻击行仍恒缺失、十余处附魔光晕恒不亮

- **问题**：t874 把根因定为「C++ Q_INVOKABLE 返回的 list 在 QML 是 sequence 对象，Array.isArray 恒 false」，但只把 AnvilUI 与 Main.qml HUD 两处守卫改为 truthy；其余消费点原样保留 `if (!Array.isArray(e)) return ""` 类守卫，仍直接消费 C++ 序列 → 恒早退。受影响（HEAD）：ChestUI.qml:764、FurnaceUI.qml:1117、CraftingTableUI.qml:953、DispenserUI.qml:818、SurvivalInventory.qml:1290（攻击行恒缺）；Inventory.qml:1902、EnchantingTableUI.qml:1278（锐锋括号恒缺）；附魔光晕/标记恒 false：Main.qml:5600、ChestUI.qml:310/460、FurnaceUI.qml:529/613/786、CraftingTableUI.qml:646、DispenserUI.qml:352/511、SurvivalInventory.qml:747/911/1088、Inventory.qml:1056/1464。t874 要消除的「附魔看似丢失」误判源在大多数界面依然存在（纯视觉，数据链已被 list4 归一保住）。
- **证据**（主审亲核 ChestUI 实例，其余子代理核）：`src/ui/ChestUI.qml:760-764`。
- **修复**：全仓统一替换为 truthy + length 下界遍历（同 t875 localCanPlace 写法）或统一经 `InventoryOps.list4()` 归一后再判；顺手补「面板 hover 附魔剑」冒烟步骤。
- ✅ 已修（5c79ee7）：全仓 grep 清点 28 处序列消费点（原清单 21 + 新发现 7：ChestUI/FurnaceUI/DispenserUI localWriteSlot setSlot 守卫、SurvivalInventory/CraftingTableUI/Inventory craft localWriteSlot、Inventory.copyStackToCursor——创造中键复制带附魔物品会被静默清白板），显示消费点（14 光晕 + 5 攻击行 + 2 锐锋括号）改真值守卫 `!!e`（t874/t875 口径），写半边 7 处改 `InventoryOps.list4()` 归一（AnvilUI 终局防御先例）；9 处纯 JS 数组合法用法保留（本地槽数组 / localDragGroups / list4 化 readSlot 输出）。源码钉阴性验证：改回一处 isArray → grep 复见序列消费点（红）。矩阵 294 PASS / 0 FAIL（无 QML 行为探针，QML hover 绑定需真指针 headless 不可驱）。**需用户目视**：开各面板 hover 附魔武器——攻击行出现、紫光晕亮、创造背包中键复制附魔物品不丢附魔。
### #10 `d12eddc`（review25 #3 副作用）mob 乘客 QML 刷新降至 20Hz，与矿车 60Hz 发射不同步——快速车载乘视觉锯齿

- **问题**：tickVehicleRiding 改置 m_pendingEmit 由 ~20Hz 收口（修复每帧双发卡顿，方向正确），但矿车自身位置仍**每帧**发 entitiesChanged（minecartmanager.cpp:701 位移即发）。C++ 侧乘客 pos 每帧钉车，QML 侧乘客 delegate 20Hz、车 delegate 60Hz → 快速矿车（~8 格/s）载 mob 时乘客相对车最大滞后约 3 帧 ≈ 0.4 格、每 50ms 跳变一次（旧代码乘客每帧直发=平滑）。玩家骑乘不受影响。
- **证据**（子代理核）：`src/Entities/entitymanager.cpp:4837`（pendingEmit）与 `:6940`（相位门 flush）；矿车每帧发见 `src/Entities/minecartmanager.cpp:701`。
- **修复**：乘客钉位时同步 bump 矿车侧 revision（一次 emit 覆盖车+乘客）；或乘客单开 ≤2 只的小名单每帧 emit。至少注释登记该呈现取舍。
### #11 `250ba94` (t889) 硬暂停未冻结纯 QML 视觉 Timer——附魔字形流动在 ESC 暂停时继续发射/飞行

- **问题**：硬档声称「一切停」，但 EnchantGlyphFlow 的 spawnTimer/tickTimer 的 running 只绑 `root.active && pairs.length > 0`，active 只派生自 appState==="playing"，与 window.worldRunning 无关。附魔台旁按 ESC——世界/实体/时钟全停，白色字形仍持续从书架飞向附魔台。与两档语义声明矛盾（MC Java ESC 暂停粒子冻结）；同构的其他 QML Timer 族可能同样漏网（t884 自己就记得 gate worldRunning，t873 漏了）。
- **证据**（主审亲核）：`src/ui/EnchantGlyphFlow.qml:141-145`。
- **修复**：spawnTimer.running 并入 `window.worldRunning`（tickTimer 可保留以清空在飞粒子）；建议按 worldRunning gate 全仓扫一遍纯视觉 Timer。

## 三、低危（择机修复）

### #12 `f0a9930` (t863④) pushEmptyCart 轨端推离路径未清 pitch——推离的车带着坡面俯仰平抛
- 证据：`src/Entities/minecartmanager.cpp:999-1004` 设 dir/speed/derailed/fallVy/yaw 无 pitch；deadEnd 路径（:586）清了。tickDerailedCart 只在轨面重挂时刷 pitch（:497），落地贴面（:504-508）永不刷。修复：推离分支补 `c.pitch = 0.0f`（与 deadEnd 对称）。
### #13 `f0a9930`+`aa6dc17` 骑船/睡觉分支跳过 tickPushedCarts → checkCartEnvironment 与 derailed 自由物理在这些窗口停摆
- 证据：`src/Game/playercontroller.cpp:6491-6492`（骑船 return 前仅补 updateDetectorRailOccupancy）、`:940-943`（sleeping return）。玩家骑船/睡觉期间：半空 derailed 车悬停冻结、触仙人掌/岩浆的车不销毁、失支撑不坠落；下车/醒来自愈。与提交信息「frame-level 全车种」叙述不符。修复：两个分支按 L5 先例补调 checkCartEnvironment，或文档明示窗口语义。
### #14 `f0a9930` derailed 矿车在车-车碰撞中是不可推动的「幽灵障碍」——出轨落地铁车可永久堵线
- 证据：`src/Entities/minecartmanager.cpp:841`（clampShift 无轨列恒 0）、`:815-819/888-889`（impulseDirOk 需合法轨连接）。出轨车滑停在在轨线路旁（贴地 y 与轨面差 0.0625，Y 层筛放行）→ 行进车每帧被顶回、derailed 车纹丝不动，动力轨也推不过去。t734 地面车存量行为，但 t863 大幅放大了这类车的数量与可达位置。修复：为 derailed 态加碰撞分支（去穿插+冲量，或行进车把它撞滑）。
### #15 `0cfb780` (t869) 爬墙连接位混入「贯穿直线」判定——坡上拐角被误判为直线
- 证据：`src/World/world.cpp:3499-3507`（t702 爬墙连接与同层连接共用 4 个水平连接位）+ `blockregistry.cpp:1287-1288`。粉沿台阶爬坡且一端贴基座/接收器时供电判定与平地不一致，坡上 NOT 门可能失效。布局罕见。修复：坡向连接另设位或回读邻格几何；至少注释钉死近似。
### #16 `30ffda1` (t868) 冷却键 (x<<32|z) 不含 Y——同柱垂直叠放两台发射器共享冷却，0.5s 内上台发射后台下合法沿被吞
- 证据：`src/Game/playercontroller.cpp:5407`（键编码）、`:5480` 同键。旧 2.0s 时代同样存在（非新引入），但 0.5s 语义重新承诺「逐沿发射」。修复：键加入 y（quint64 空间足够），或注释明确记录每柱唯一假设。
### #17 `0cfb780` (t869) 形状语义只收口火把基座，一般接收器（TNT/灯/发射器/铁门）仍全向 6 邻读粉——同一直线贴 TNT 引爆、贴火把基座静默
- 证据：`src/World/world.cpp:3293-3305`（isReceivingPower 全向判定）；blockregistry.h 注释已声明 v1 简化，属知情取舍。修复：后续任务把 redstoneDustPowersNeighbor 推广到接收器读取（注意 t740 回归面随之扩大）；当前保持文档一致即可。
### #18 `df62d8f` (t876) ResourceBrowser 羊头材质[1] 错绑 mobPrevTex——新程序羊头贴图在图鉴侧成死代码
- 证据（主审亲核）：`src/ui/ResourceBrowser.qml:881` `baseColorMap: root.selectedMobTexSource !== "" ? mobPrevTex : mobSheepHeadTex`——selectedMobTexSource 在 pack 关时也非空（=回退 qrc 贴图），三目恒走 mobPrevTex；同材质 alphaMode（:882）用的 selectedMobPackSrc 才是对的判据，注释（:876）声明「pack 关 → mobSheepHeadTex」落空。游戏内 Main.qml:7264 写法正确。修复：改 `selectedMobPackSrc !== "" ? mobPrevTex : mobSheepHeadTex`。
### #19 `b02be01`+`d9849e6` 宠物瞬移/刷怪支撑判定仍用 isSolid——瞬移落点可在花草/水面上（t878 阈值 24→12 放大暴露频率）
- 证据：`src/Entities/entitymanager.cpp:2548`（狼）、`:2638`（豹猫）、`:1106/1249`（刷怪）。落花草则下帧失支撑再坠落，落水则宠物瞬进水。t480 既有，但 t865 收口后属残留旧口径。修复：改 isCollidable 或 supportTopYAt≥0，与单权威对齐。
### #20 `83eadd1` (t867) resting 复检每物品每帧两格窗 supportTopYAt——异形支撑上大量堆积物品时每帧 vector 堆分配
- 证据：`src/Game/itementitymanager.cpp:534-540`（循环内 collisionAABBs 按值返回 std::vector）。满格地面走快路径不受影响。修复：resting 物品 N 帧节流复检，或 supportTopYAt 加免构建的轻量变体。
### #21 `0444b2f` (t886) 获物飞到玩家手上、经验球却留在最远 32 格外的钓点水格
- 证据（主审亲核）：`src/Game/playercontroller.cpp:2598-2600`（spawnOrb(qFloor(bp))，bp=浮标水格）；XpOrbManager 无重力纯磁吸（xporbmanager.cpp:63-105）。远距钓获时鱼到手、XP 留在钩点水面，须涉水去吃，离开则到寿命消散；MC 1.0 钓鱼 XP 直接入账无球。修复：球改玩家附近生成，或直接走 xpPickedUp 信号入账。
### #22 `250ba94` (t889) 软档 GUI 久开时浮标/掉落物墙钟照走——「GUI 开鱼线持久」只在硬档成立
- 证据：deferWallClocks 仅 worldRunning false→true 翻转时顺延（`src/Game/playercontroller.cpp:563-576`）；软档 EntityManager/ItemEntityManager 照 tick。开背包挂机 >180s 浮标到期自动收。是「软档世界照跑」的一致推论（Java 语义），但与 t885 叙事有用户预期落差。建议文档/提示明确，非代码缺陷。
### #23 `2219715` (t873) 自检 console.info 留在生产路径——每次方块编辑刷一行日志
- 证据（主审亲核）：`src/ui/EnchantGlyphFlow.qml:131`，rescanPairs 由 onEditRevChanged（:135）驱动，每次挖/放必触发。修复：加调试开关或降为一次性/低频采样。scale/100 修正本身正确。
### #24 `a3ec458`（review25 #11）发射器 TNT「再探一格」可隔 1 格墙生成
- 证据：dispenseFromDispenser TNT 分支目标邻格堵 → 直接在 x+2*tdx 格生成 primed TNT，未校验连通性；墙厚 1 格时 TNT 穿墙出现在墙后。修复：堵口一律降级掉落物，或校验两格连通。
### #25 `250ba94` (t889) 软档被 mob 攻击：伤害照扣但击退被吞
- 证据：`src/Game/playercontroller.cpp:1990` `if (m_dead || !m_captured) return;`——软档击退恒跳过而 takeDamage 无 captured 门（掉血继续，符合 Java「开背包照被打」）。MC Java GUI 开受击有击退。修复：该门只拦 m_dead（如需对齐）。

## 四、已核对未见新问题

- **c2ecf0f (t862)**：视觉内缩数值自洽（底板/车帮互不共面，骑乘脚底契约未动），kPackParts 采样窗口在贴图内，kCartRideH 注释同步常量未动。
- **2ae8ea2 (t864)**：纯测试（tools/redstone_matrix_test.cpp），无 src 改动。
- **a871650 (t881)**：断线路径含 bobberInWater 复位，六条 fishing 清除路径对新镜像完备；removeEntityAt 不在迭代内调用，时序安全。
- **e6da2c3 (t882)**：除零双守卫，角度语义正确，常量单一权威，耐久门不变。
- **0e5017c (t884)**：bobberSplashed 坐标在浮定赋值后发射；QML 偏移纯视觉不回写实体；Timer/动画 gate 对称（收竿/暂停即停、恢复即续）。
- **bbacca5 (t861) / 3d3735a (t868①)**：QML 表改动无双显残留，图标路由同 0x224 先例，契约钉覆盖单侧编辑。
- **a495a74 (t870)**：映射与 QML 红石 tab/破坏掉落同源，pickBlock 唯一消费点，其他方块 identity 不变。
- **e7b1610 (t871)**：kDispenserTntPopSpeed 仅 TNT dispense 分支消费，箭/雪球/掉落物各有独立常量，无连带。
- **fe85e24 (t877)**：纯 QML 符号回退+注释钉死，无逻辑风险。
- **d9849e6 (t878) 其余面**：骨头 no-op 后空手 sit/stand 路径仍可达（狼豹猫双分支齐备）；坐宠不传送的守卫在瞬移判定之前；mobTypeEggId static 化无静态初始化顺序问题；mob_heart.png 已进 CMake 资源链；爱心粒子门控链完整。
- **346ee12 / 2b8f258 / f163f7d (t875)**：generate() 补清正确；27 个 PNG 体量正常无超大文件；t875 门禁收敛无取出被拒/物品丢失路径。
- **d12eddc #2/#4/#5 / a3ec458 #9/#10/#12/#13**：blockDoused 两条路径恰一次不重复；鱿鱼天花板钳可自愈；珍珠豁免表精确、t835 基线不动；门烧毁/活板门 off-by-one/浮标快照复查口径一致。
- **e307e4c list4 本体**：readSlot 单一咽喉归一覆盖全部点击/drag/place 下游，list4 对 undefined/短列表/序列安全。
- **250ba94 其余面**：deferWallClocks 只动 alive 槽寿命字段；QElapsedTimer 构造 start 幂等；worldRunning 派生无可见错误翻转；死亡屏/respawn 链无竞争；keyInput 守卫只拦移动键不误伤聊天。

## 五、方法与盲区备注

- 矩阵 275 PASS / 0 FAIL 全绿与本报告并存不矛盾：#1（探针只测无碰撞形态）、#4（captured 门内路径头less 不可驱动）、#7（探针在静水位测）、#9（探针只锁 C++ 权威值不覆盖 QML hover）均在探针盲区。
- #12-#25 为子代理核或主审抽查（亲核项见标注），行号以 HEAD 为准；修复时以现源码复核为准。
- 建议下一批优先级：#1+#2+#3（同根因一次修掉，含豁免判据推广与探针）> #4/#9（用户可见面最广）> #5/#6/#7/#8。
