# Code Review — 2026-08-23（近 24 小时提交 · 两批合并版）

- **审查范围**：`0bf8ffc^..1727847`（2026-08-22 07:44 → 2026-08-23 21:50，共 94 个提交；35 个纯 docs 提交只改 `docs/dev-plan.md`，已跳过，实际审查 **65 个代码提交**，分两批）：
  - **第一批（早间窗口 `0bf8ffc..4410605`，41 个代码提交）**：附魔/铁砧、皮肤渲染、图鉴/成就、载具、爆炸/火等；
  - **第二批（晚间窗口 `4410605..1727847`，24 个代码提交，t777-t807 用户试玩修复批）**：配方审计 t802、染料 t788、骨粉 t791、附魔链 t795-t798、铁砧重力 t793/t794、刷怪笼 t785-t787、羊自然色 t789、生物模型 t777-t782、UI t783/t784/t790、传送门 t806、弹道渲染 t807。
- **审查基线**：HEAD = `1727847`，工作区干净。行号均以 HEAD 为准。
- **方法**：第一批 7 路并行子系统审查（逐提交读 diff + HEAD 交叉核对）+ 主审复核；第二批 3 路并行审查（铁砧/刷怪笼组、生物模型/羊色组）+ 主审自审其余 12 提交（配方/染料/骨粉、附魔链、UI/传送门/弹道）。皮肤与玻璃像素数据为 PIL 实测，QML 布局逐坐标推算，未经核实的猜测不上报。
- **结论速览**：高 1（未修）/ 中 7（仍开放，其中 1 项为第二批新增）+ 1 项第二批内已修复确认 / 低 28。唯一高危仍是**昨日 #8 slim 皮肤修复（`4fb6075`）整体无效**——第二批未触碰该文件，问题原样。

| 严重度 | 数量 | 最关键条目 |
|---|---|---|
| 高（未修） | 1 | #1 slim 皮肤修复无效（探测恒误判 + 盒区数值错） |
| 中（开放） | 7 | #2 scanRailColumn 断扫×坡道死车/俯仰清零；#3 坡臂拐角俯仰瞬跳；#4 沙柱/铁砧坍落依附物悬空；#5 火蔓延 6× 无雨/水抑制；#6 羊脸缓存换包陈旧；#7 染色链生存不可达（新增）；#8 玻璃/羊毛物品配方断供 |
| 中（已修） | 1 | #9 图鉴变体面板溢出 → t783 已修复（主审验证通过） |
| 低 | 28 | 见第五节（含 3 条备查/2 条口径备注） |

---

## 一、高危（建议立即修复，仍未修）

### #1 `4fb6075` slim 皮肤修复整体无效：探测区恒误判 classic + `kPiecesSlim` 盒区数值错误

- **提交**：`4fb6075` fix(review): med #8 slim-skin 3px arm/leg box UV（对昨日 Review #8 的修复）。晚间批次未触碰 `playerskinbox.cpp`/探测函数，问题原样存在。
- **问题**（两层，均经主审 PIL 实测坐实）：
  1. **布局假设错误**。修复假设 slim 布局是"臂/腿都 3 宽×12 高×**3 深**、条带 u40..52 / u0..12"。但 MC 1.8 标准 slim 布局是**臂 3 宽×12 高×4 深**（条带 2×(3+4)=14px，占 u40..**54**），**腿保持 4×12×4 不变**（占满 u0..16）。实测 demo 包 `alex.png` 臂条带不透明列 u40..53（u54..55 全透明）、腿条带 u0..15 全不透明；`steve.png` 臂 u40..55、腿 u0..15——与标准布局一致，与该提交注释"alex.png only paint u40..52 / u0..10"不符（当时的观察本身有误）。
  2. **探测区落在 slim 臂背面内 → 恒判 classic**。探测区 `u[52,54)×v[20,32)`：slim 臂背面占 u51..54，实测该区在 alex.png 上有 **24 个不透明像素**（steve 同为 24）→ 对真实 slim 皮肤永远返回 classic → 走 4px 采样 → 昨日 #8 的"臂后侧透明条纹"依旧，修复是死码。而 `u[54,56)×v[20,32)` 区分度完美：alex **0** / steve **24**。
  3. **盒区数值错误**：即使探测修好，`kPiecesSlim` 的 Arm `{40,16,3,12,3}` 深度 d=3 应为 **4**（六面全部错位 1~2px），Leg `{0,16,3,12,3}` 应保持 classic 的 `{0,16,4,12,4}`（真实 slim 腿条带 16px 满宽，按 3px 采样会大面积采错列）。
- **证据**：`src/Core/resourcepackmanager.cpp:3080`（探测区 x0=52）、`src/Renderer/playerskinbox.cpp:71-76`（kPiecesSlim 数值）、PIL 实测数据见上。
- **修复**：
  ```cpp
  // resourcepackmanager.cpp probeSlimSkinLayout：
  const int x0 = int(54 * sc), x1 = int(56 * sc);   // slim 臂条带 [40,54) → [54,56) 为布局外空白
  // playerskinbox.cpp kPiecesSlim：
  { 40.0f, 16.0f, 3.0f, 12.0f, 4.0f },  // 2 Arm slim：3宽×12高×4深（条带14px，与classic仅差宽1px）
  {  0.0f, 16.0f, 4.0f, 12.0f, 4.0f },  // 3 Leg：slim 腿不变，与 classic 同款
  ```
  建议用 demo 包两皮肤加断言（alex→slim、steve→classic）防再次按错误布局假设回归。
- **✅ 已修（commit 8886325）**：探测区改 u[54,56)（resourcepackmanager.cpp probeSlimSkinLayout，坐标 54/56 由 kPiecesSlim/kPiecesClassic 臂条带右缘公式派生并注释互指）；kPiecesSlim 改 Arm 3×12×4 / Leg 4×12×4（playerskinbox.cpp）；数值锁 static_assert×4 编译期拦截回归；矩阵探针 +1 PASS（合成 classic/slim 臂条带 base+HD2× 判定 + 退化小图守卫 + demo 包实测 alex→slim / steve→classic，基线 192→193 全 PASS）。

---

## 二、中危（仍开放）

### #2 `af9ec8e` 引入的 scanRailColumn 实心断扫 × 坡道车位居上 → 低顶坡道/紧凑螺旋上失联死车 + 俯仰清零 + 坡道 1 格隧道冻车（三症状同根因）

- **提交**：`af9ec8e`（断扫引入，修昨日 #4 隔板假支撑）；连带 `6f4eba3`（t769 俯仰采样）、`e83d255`（t768 车心抬高使阈值推到 rise>0.55）、`791eb61`（t775 确立 1 格隧道玩法后更易踩）。晚间批次未触碰矿车链，原样。
- **问题**：四处列扫描收口到 `scanRailColumn` 时加了"遇 isCollidable 即 break"。坡道段车 `pos.y = 轨Y + rise + 0.45`，`rise > 0.55` 时 `floor(pos.y) = 轨Y+1`，扫描从轨Y+1 起步——若该格实心（紧凑螺旋/多层轨道下层坡段、贴坡天花板），新代码先撞实心 break 返 -1，旧循环能继续向下找到轨。三症状：① `pinCartY`/`tickRiddenCart` 判离轨 → 坡上死车；② `updateCartPitch` 经 `railSurfaceYAt` 采样 → 1 格净空隧道里 45° 爬坡车头一帧跳回水平；③ 坡上探测轨占用失联不点亮。平轨不受影响。
- **证据**：`src/Entities/minecartmanager.cpp:215-223`（断扫）、`:317`（kCartRideH=0.45）、`:300/:236/:367` 与 `:786-792`（四消费端）。
- **修复**：按调用方分流——探测轨占用保留严格断扫（昨日 #4 本意）；`pinCartY`/`tickRiddenCart`/`railSurfaceYAt` 走宽容版：遇实心格时多看其下一格，若是轨则放行返回（"实心紧贴轨上沿"= 坡顶穿越/低顶净空，非隔板遮挡）。
- **✅ 已修（commit f9abcc6）**：新增 `scanRailColumnRiding`（实心正下一格是轨 + 骑乘高一致性 `|refY-(轨Y+rise+0.45)|≤0.5` 放行），骑乘族五消费端（`pinCartY`/`pickTrackStep`/`tickRiddenCart` 前置钉定/`railSurfaceYAt`/`clampShift`）全部切换；探测轨占用保留严格断扫（rise>0.55 低顶探测轨不亮为已接受边界，注释登记）。地面车隔板差 1.9375−rise ∈[0.9375,1.9375] 恒拒，af9ec8e 防线不动。矩阵探针 P21（低顶坡道不失联/达顶/pitch 连续）+ P21b（隔板回归守卫）。

### #3 `6f4eba3` (t769) 坡臂拐角 rise 为四边均值常数 → 拐角处俯仰/钉面不连续，~±56° 错误俯仰瞬跳（并暴露 t709 既有 0.75 格 Y 跳变）

- **问题**：`railRiseAt` 拐角格返回常数 `0.25×(eW+eE+eN+eS)`，相邻直臂格线性——格边界不连续。`updateCartPitch` 的 ±0.25 两点采样窗跨界时 `atan2(-0.75, 0.5) ≈ -56°`（真实坡度 ±45°），车头视觉猛甩；同帧 Y 钉面跳降 0.75 格（t709 遗留，t769 扩散到 pitch）。头注释"轨面分段线性且连续"在坡臂拐角不成立。
- **证据**：`src/Entities/minecartmanager.cpp:332-338`、`:340-356`、`:381-391`。
- **修复**：拐角格 rise 改双轴插值（与 mesher armLift 一致），或至少 pitch 钳 ±45° + 拐角用四角双线性替代四边均值。
- **✅ 已修（commit f9abcc6）**：拐角 rise 改 armLift 四角双线性（h00=eW+eN/h10=eE+eN/h11=eE+eS/h01=eW+eS，与 mesher 同一角点公式，渲染坡面/矿车 Y/俯仰采样同读一张面）+ `updateCartPitch` 钳 ±45°。格边界残差 0.75→0.5（t709 mesher 面固有台阶，复审已知接受项）。矩阵探针 P22（坡臂拐角每 tick |ΔY|≤0.55、|Δpitch|≤46、|pitch|≤45.5、达死端格心）。

### #4 `1d712a8` (t799) + `c1688fc` (t794) 重力坍落柱静默清格不级联失撑检查 → 柱顶/侧依附物悬空残留（铁砧加入后受面扩大）

- **问题**：`dropGravityColumn` 逐格 `m_chunks.setBlock(Air)` 直写（刻意不经 `World::setBlock` 防递归），但**不调用任何 check*OnEdit 族钩子、无邻格火把失撑扫描**。沙柱顶的火把/红石火把（照常发光/供电）、甘蔗/雪层/花蘑菇/压力板、柱顶铁轨，在支撑消失后全部悬空。t794 把铁砧三阶段并入 `isGravityBlock`（`blockregistry.cpp:1648`）后受面扩大——铁砧是 ShapeFull 实心、火把/梯子/铁轨均可依附，柱一坍附着物悬空。触发面广：挖柱底、爆炸、TNT 点火清格、焚毁、水漫支撑都走该路径。`a890bfa`（R1）统一了失撑**谓词**，但谓词要有人调用才生效——这条静默路径仍然全漏。
- **证据**：`src/World/world.cpp:2225-2247`（主体无钩子）；对照 `world.cpp:466-475`（setBlock 主入口钩子族）与 `playercontroller.cpp:1433-1454`（玩家路径 6 邻火把扫描）。
- **修复**：`dropGravityColumn` 循环里对每个被清格补邻格扫描——火把/红石火把 + 甘蔗/雪层/花/压力板/铁轨 失撑复检；建议抽 World 层"清格后邻域附着物复检"公共函数，与 clearBlockSilent 口径合一。**✅ 已修（commit 42d0136）**：新增 `World::recheckAttachmentsAfterClear(x,y,z,oldId)`——正上方族走既有 check*OnEdit + 6 邻火把/红石火把失撑扫描（含掉落物信号），`dropGravityColumn` 每清一格调用；`clearBlockSilent` 原三钩子散装段改为复用同一函数（口径合一）。刻意不含 `checkGravityBlockOnEdit`（坍落自身 while 循环即重力延续，逐格重入会指数递归），clearBlockSilent 侧单独保留。矩阵探针 P24（8 柱基座拆除→沙落 + 7 类附着物掉落/清除，含甘蔗 2 格级联、柱侧红石火把、雪层坍落实体、≥7 掉落信号、对照柱原样）。

### #5 `635fd02` (t804) 火蔓延提速 6× 且无雨/水抑制 → 多火源叠加下木建筑几十秒烧穿、雨天无法灭火

- **问题**：逐邻独立掷骰使单块可燃物点燃概率从 ~0.83%/窗升到 5%/窗，被 k 个火格包围时每窗 1−0.95^k（木板墙前沿 k=2~3 → 10~14%/窗 ≈ 3~6s/块），成片木屋/森林几十秒烧穿。(a) 寿命判定只查可燃邻、(b) 蔓延掷骰**全程无降雨/水邻条件**——雨天火不灭、水邻不阻止蔓延，玩家误点火后无反制手段，且烧毁是 `setBlock(Fire)` 无掉落替换、不可逆。抑制缺口是 t724 既有，本提交提速 6× 使后果从"烧得慢"质变为"救不回"。
- **证据**：`src/World/world.cpp:1476-1484`（逐邻掷骰无环境条件）、`:1449-1462`（寿命仅看可燃邻）；对照 mob 身上的火有雨灭（`entitymanager.cpp:5254-5265`）；`world.cpp:3415` 已有 `isPrecipitatingAt` 可复用。
- **修复**：寿命与蔓延加"露天降雨 → 熄灭/概率减半"；6 邻含 Water 的方向不蔓延且加速自熄；或逐邻 5% 降到 2~3% 补偿叠加效应。

### #6 `416dc1a` (t749) 羊合成脸/图鉴头像落盘文件名无 revision → 换包后贴图陈旧直到重启（t777/t789 均未修）

- **问题**：`generateSheepWoolFaceFile` 写死文件名 `voxelsandbox_rp_sheep_woolface.png`（HEAD 行号已移至 `resourcepackmanager.cpp:3562`）。换包后缓存清空重合成，但落盘路径不变 → URL 不变 → QML Texture 不重读 → 游戏内羊与图鉴显示旧包毛身/脸直到重启。t789 毛色 tint 与 t777 眼 overlay 显隐都架在这条链上，会跟着错。图鉴头像 `voxelsandbox_rp_mobhead_%1.png` 同样无 revision。**晚间批 t777/t789 动了羊贴图链但都没碰该文件名，仍未修**。
- **修复**：两族文件名追加 `_r%1`（`s.revision`），同皮肤/icon 族修法。
- **✅ 已修（commit c9dd880）**：两族文件名均嵌 `_r<revision>`（`generateSheepWoolFaceFile` → `voxelsandbox_rp_sheep_woolface_r%1.png`、`generateMobHeadIconFile` → `voxelsandbox_rp_mobhead_%1_r%2.png`；两函数签名加 `int revision`，全部调用点传 `s.revision`——apply() 每次 ++revision → 落盘 URL 逐版变化，QML Texture 重读新图）。落盘成功后逐版清理（Review #11 skin 族同款）：删 t749 期无后缀旧名 + 同族其余 `_r*.png`（刚写出的除外）。矩阵探针（review-e 段）：_r5/_r6 文件名嵌入断言 + 换版后旧版文件清除 + 无后缀旧名清除，202 PASS / 0 FAIL。

### #7 `5a8105b` (t788) 染色链在生存模式不可达：白羊毛方块无生存来源（新增，第二批发现）

- **提交**：`5a8105b` feat(t788): dye system（32 条染色配方全部锚定白羊毛**方块** Wool=27 与白床 BedWhite=78）
- **问题**：t788 的染料+白羊毛→彩色羊毛、染料+白床→彩色床共 32 条配方，原料是白羊毛**方块**（`recipe.cpp:286-290` bed_white 需 3 个 Wool 方块；染料配方同款）。但生存中白羊毛方块**无任何来源**：白羊（约 81.8% 的羊）剪毛/死亡掉的是材料段羊毛**物品** 0x20E（`Main.qml:1960-1961` `sheepWoolDropId`，t789 延续旧观感），而 0x20E **不可放置**（playercontroller 无 useBlock 分支，grep 全工程仅配方消费 `bed_red` 简化配方与名字表）、无物品→方块转换配方。白床配方（3 白羊毛+3 板）同样不可达。结果：染料本体可得（花掉落/烧仙人掌），但**染色主体功能在生存模式死链**——只有自然生成的有色羊（18.2%）掉有色羊毛方块可以合床。`recipe.cpp:283` 注释"white 用既有 Wool=27（剪/杀羊获得）"与实际掉落（0x20E 物品）不符，错误注释固化了断链。
- **证据**：`src/ui/Main.qml:1960-1961`（白→0x20E）；`src/Game/recipe.cpp:286-290`（bed_white 需 Wool 方块×3）；`hotbar.cpp:990`（0x20E 仅有名字，无放置路径）；`blockregistry.cpp:122`（Wool 方块 dropId 自掉方块，但无生存获取入口）。
- **修复**（任选其一，推荐 a）：
  - a) 白羊掉落改 Wool 方块 27（`sheepWoolDropId` 白→27），`bed_red` 简化配方原料同步改 Wool 方块（0x20E 物品退役或留作兼容）——与 MC"剪羊毛得羊毛方块"一致；
  - b) 给 0x20E 加右键放置（放成 Wool 方块 27，同树苗物品模式）；
  - c) 加合成配方 0x20E×1 ↔ Wool 方块 27 互转。
- **✅ 已修（commit c9dd880，方案 a，= dev-plan R19.13 t834 提前实现）**：`sheepWoolDropId` 白→Wool 方块 27（有色 63..77 不变；顺带 #33 的 [0,15] 钳）；`bed_red` 简化配方原料 0x20E→27。**0x20E 退役取舍**：常量 + Hotbar 名「羊毛」+ MaterialIcon 图标 + pack 映射保留（旧存档物品兼容——常量永不改号），但从一切掉落源/配方消费中移除，static_assert 改钉「保留 0x20E 仅兼容勿再接线」。注释（recipe.cpp:283 等）同步改真。#7 描述略 overstated：t565 已有 4 线→Wool 方块次要生存来源，但羊链主入口修复仍按 t834 规格落地。矩阵探针：QML 字面量公式 27/63+idx-1 ↔ BlockRegistry ↔ 16 染料配方产出逐位相等（白羊毛 27 入料全 16 条可合）+ 云杉板+Wool→bed_red + 0x20E 版 no-match。

### #8 `5f46f12` (t800) 玻璃/羊毛物品退出创造调色板 → 红石灯/床配方纯创造断供（t788/t802 均未修）

- **问题**：红石灯唯一配方原料是玻璃**物品** 0x204（`recipe.cpp:781`，HEAD 行号）、红床简化配方原料是羊毛物品 0x20E（`:278`）；两物品已不在创造调色板（`hotbar.cpp` 仅剩名字表 :977/:990）→ 纯创造流程合成红石灯需绕"熔炉烧沙"。t788 新配方已正确改用方块 id，但旧两条配方没跟上。
- **修复**：配方原料改方块 id（Glass=54 / Wool=27），或匹配层加 0x204↔54、0x20E↔27 等价映射（与 #7 修 a 联动一次收口最划算）。
- **✅ 已修（commit c9dd880，前案：只改配方侧，不加 matcher 等价映射）**：红石灯中心 0x204→Glass 方块 54（红床简化配方随 #7 改 27）。**关键补充决策**：只换原料会把断供从「纯创造」翻成「纯生存」（熔炉产物仍是 0x204、放置破坏后旧 dropId 也回 0x204 → 生存拿不到 Glass 方块）→ Glass 方块 dropId 改**自掉 54**（Wool/床族自掉先例）：生存链闭环 = 烧沙得 0x204（放置过境物品，t405 放置链不动）→放置→破坏回收 Glass 方块→入配方；创造调色板直接取方块，两模式同源。矩阵探针：红石粉十字+Glass 方块→红石灯 + 旧 0x204 中心版 no-match + Glass/Wool dropId==自身 + 烧沙仍产 0x204。

---

## 三、中危（本窗口内已修复，主审验证通过）

### #9 `db450cf` (t783) 修复早间 #7：图鉴变体面板溢出遮返回键 —— 验证通过

- **修复验证**：变体面板（toggle 对 + 16 毛色点 + 边界注）已整组迁入预览区作 `anchors.bottom` 定位悬浮层（`ResourceBrowser.qml:1185-1193`：z=10、半透明 Rectangle rgba 底板、不参与右列 Column 布局），右列总高回到 ≈360 ≤ 视口 366 恒不溢出，与 footer 返回按钮（y≈448+）无交集——早间 #7 的三个症状（溢出/点击遮蔽/误关浏览器）全部消除。面板为纯 Rectangle 无 MouseArea，不拦截预览区拖拽旋转（DragHandler 仍可达）。注释已立"新增预览侧控件一律走悬浮模式"规约。**关闭早间 #7。**

另：早间 #12（剪毛羊+pack 双层眼）由 `c02adf6`（t777）修复——游戏内毛茸态（`Main.qml:6877-6878`）、裸态（`:6954`）、图鉴（`ResourceBrowser.qml:847`，经 sheepPreviewPackFace 覆盖两态）三处眼 overlay 的 pack 门控齐全，验证通过，**关闭早间 #12**。

---

## 四、第二批提交的修复质量与专项核验

- **`fdbbb38` (t802) 配方审计**：两阶段匹配器（精确优先→变体规范化重试，仅 variant→base 单向）设计正确，混合板材输入、云杉专属配方不被截胡、9 个新配方多重集唯一性抽验无冲突；自匹配探针（147 条配方逐一 pointer-equal）是有效回归基建。无发现。
- **`89435a6` (t791) 骨粉**：`World::applyBonemeal` 单一入口收口完整，playercontroller 旧 +1 路径整段删除无残留；骰子时序（roll 先取值后 ++序号）自洽；树苗守卫（支撑/主干畅通/高度余量）边界安全。无发现。
- **`0657179` (t795)**：创造也要求青金石——青金石在创造调色板（`hotbar.cpp:583`）无死路；书架 tier 公式收编 EnchantRegistry 单一权威，创造旁路（t694）删除。无发现。
- **`6a0c3ba` (t796)**：piece 表 4→5 只新增镜像纸页（UV 值互换不动角点序，winding 保持）；书心抬升与 t797 终点 0.95 同步。无发现。
- **`1033646` (t797)**：保留 t765 的池架构与性能红线（spawnTimer 无对即停 `running: root.active && pairs.length>0`、tickTimer 清空即停、36 池硬上限、maxPerTick 封顶、距离门）——UI 未开零开销红线成立。无发现。
- **`188ec03` (t798)**：效率附魔收口 `miningTime` 第 3 参，`mul > 1.0f`（类型匹配且采掘达标）才加成 level²+1——镐挖土零加成、黑曜石 harvest gate 不受扰；旧 `effMul` 乘法路径删除干净。无发现。
- **`602db76` (t784)**：床 16 色 id 散布两段（0x20..0x27 + 0x4E..0x55）经 `Hotbar::isBed` 单一权威路由，非床 id 兜底 BedRed；盒布局下沉 `bedHalfBoxes` 逐字承值。无发现。
- **`0fd157b` (t790)**：透明全屏点击吸收层保留（modal 语义不变），暂停菜单在面板开时隐藏避免半遮。无发现（副作用"统计/成就面板不能叠开"提交已自述，可接受）。
- **`1727847` (t807)**：退役 Texture 无悬挂引用（grep 仅注释提及）；BillboardQuad 为既有 camera-facing 模式。无发现。
- **`1e8b577` (t806)**：门框检测泛化逻辑自洽（宽度封顶 4 + 矩形逐列复验 + 四角可选 = MC 语义）；熄灭钩子 `breakNetherPortalsAround` 下沉 World 层，但**仍只挂玩家挖掘路径**（`playercontroller.cpp:1269`），未进 setBlock 钩子族——见低危 #27。
- **铁砧/刷怪笼组**（`ccb82a1` t793 / `c1688fc` t794 / `dd2f651` t785 / `65433c3` t786 / `8d80d73` t787）：t793 瓦片 solidify 四瓦片全覆盖无白边；t794 铁砧三损坏阶段 id 独立（97/98/99）落体还原 metadata 天然保真、三叠柱无落地竞态、触发路径完整（除 #4 所述级联缺口）；t785 蛋 id 无冲突、野生态确认；t786 state bit1-5 布局旧档兼容（state=0→Shambler / 1→Silverfish）、哨兵 14..31 回退全覆盖；t787 retype 走 setBlock 主入口索引维护齐全、13 蛋↔笼互逆锁死、笼刷羊走 t789 自然色权重。
- **生物模型组**（`13ade6f` t789 / `c02adf6` t777 / `8c98ed8` t778 / `4d68510` t779 / `9b70536` t780 / `a7cf3bd` t781 / `9e3a543` t782）：t789 实体不入存档（无序列化兼容问题）、63..77 与注册表逐一对位、权重采样与繁殖继承正确、信号时序安全；t781/t782 的 g_texH 修复只动 16/17 分支、g_boxUvAlways 默认 false 零影响、碰撞/命中字段未动；t778 悬挂引用四处清理干净；t779 裁剪坐标按 base 比例缩放；t780 mobEntityMap 拆头文件无 ODR 问题。

---

## 五、低危（按主题分组；早间项与晚间新增项合并）

### 皮肤/渲染组（早间）

- **#10 `b62ef28` (t748) 背包 3D 预览蹲姿无颈限**：主模型头本地角 `clamp(±60)`（`Main.qml:4464`），`CharacterPreview3D.qml:221` 无钳制且注释声称同 Main.qml——蹲下仰视两处头姿不一致。修复：预览侧同款 clamp。**✅ 已修（commit 8886325）**：`Math.max(-60, Math.min(60, lookPitch + crouchBow))` 对齐 Main.qml「补偿鞠躬量之后再钳」，注释同步改写。
- **#11 `4f14040` 皮肤 revision 缓存逐版累积不清理**：旧 revision 文件与无后缀文件永久残留。修复：落盘成功后删旧路径。**✅ 已修（commit 8886325）**：playerSkinSource 落盘成功后删同 kind 的 `_r*.png` 旧版 + t731 无后缀旧名（另一 kind 前缀不匹配不受波及）；实测植入 legacy+_r5 陈旧文件一次运行后仅剩当前 `_r0`。
- **#12 `4f14040` h==w/2 直返路径原地换文件不刷新**：64×32/128×64 族直返 pack 原路径，原地替换皮肤文件后 URL 不变不重读。修复：拼 `?r=<revision>` 查询串。**✅ 已修（commit 8886325）**：直返 URL 拼 `?r=<revision>`；Qt 6.11 Quick3D 贴图加载实证（qml.exe 对照实验）：存在文件 ± 查询串均静默加载、缺失文件必告警 `QSSG.warning: Failed to load image`（告警路径已剥查询串）→ 查询串对文件寻址透明、对 URL 缓存键有效。
- **#13 `47e11d9` (t747) UV 镜像残留在 ArmorLayerBox/MobModel（信息性）**：`armorlayerbox.cpp:30,34` 与 `mobmodel.cpp:26,30` 同源 kFace 仍是旧方向；当前贴图对称无可见差异，pack 不对称 armor layer 会镜像。修复：同款三面翻转移植或注释登记。**✅ 已修（commit 8886325）**：ArmorLayerBox kFace 表整体移植 PlayerSkinBox 翻转版（单模式 box-UV，零风险）；MobModel 语义不同——kFace 表被「pack 关全脸铺贴图」与「pack 开 box-UV」两模式共享，翻转只落 writeMobUV 的 box-UV 分支（+X/+Y/-Z 取 1-s.u），全脸模式保持程序 mob 贴图既定朝向零回归（燃烬者 g_boxUvAlways 路径头面内容对称，无可见差异）。
- **#14 `0bf8ffc` (t764) 图标书叠层缺"宽度>64"门**：`enchantBookOverlayTexture` 只判文件存在即按 demo 分区采样（`resourcepackmanager.cpp:2683-2712`），64×32 排版包会图标采碎且与放置态两态割裂（放置态已收紧 `Main.qml:8328-8329`）。修复：叠层探测与放置态同门。**✅ 已修（commit 8886325）**：enchantBookOverlayTexture 命中 pack 图后加 `pack.width() > 64` 门（与 Main.qml bookPackHit 的 entityTextureWidth>64 同款），宽 ≤64 按 miss 回退 qrc 程序贴图布局 0。

### 世界 tick/方块组（早间）

- **#15 `38b0e14` (L6) "水漫入支撑格→轨坍落"半边未生效**：`checkRailOnEdit` 失撑守卫 `id == Air`（`world.cpp:2340`），冰融成水后轨悬浮到水蒸发。修复：失撑判定改"本格现内容非 isTopFlushSupport 即坍落"。**✅ 已修（commit 42d0136）**：守卫改 `!isRail(oldId)`（任意本格编辑都复检），失撑读**本格现内容**对 `isTopFlushSupport` 判定；探针 P25（冰→水 setWaterSilent / 木板→火 setBlock 均即时掉轨，邻格写水不动轨）。
- **#16 `635fd02` (t804) 火吞木门只替换半格**：门在可燃表内，另半扇孤立残留无掉落。修复：门移出 flammable 表或写 Fire 前做半格联动。**✅ 已修（commit 42d0136）**：取"整门联动"——tickFire 蔓延写 Fire 前快照目标格（setBlock 会重置 state），命中门则按 state bit3 解码另一半（y±1）同窗写 Fire；探针 P26（上半门与原火非 6 邻接、仅联动可达→同窗烧穿；石头对照不燃）。
- **#17 `0e59f80` (t776) 墙插红石火把 S 窄带穿入支撑格 0.075**：`partialblockgeometry.cpp:819-827`，非满立方支撑（半砖/玻璃）下可见穿模。修复：S 带改单侧带或墙插预检收紧满立方。**✅ 已修（commit 42d0136）**：取"几何侧钳制"——S 带四角在附着轴上 clamp 到 [0,1]（底缘 0.2→0.125 梯形，中线偏移 ≤0.05），全部顶点留在本格内。取舍：预检收紧不能修存量存档已放的火把，且会禁掉半砖/铁砧墙旁的合法放置，故弃。
- **#18 `2edeb78` (L11) 手持玻璃被 Mask 裁成"空心框"**：手持立方材质 `Mask+0.5`（`Main.qml:2812-2813/4645`），demo 包 glass.png 实测 **76.6%** 像素 alpha<128 全部 discard。修复：手持路径对 Glass/Ice 族用 Blend 低透明度。**✅ 已修（commit 42d0136）**：新增 `heldCubeIsGlass()`（第一/第三人称两处），Glass 选 `Blend + opacity 0.45`（对齐世界玻璃段）。**偏差**：Ice 保持原样——t495 起世界冰面就是 Opaque（贴图全不透明，MC 对齐），Blend 反而引入不一致。
- **#19 `25ebe06` (t766) 铁砧 solid=false 后栅栏连接谓词漏改**：`partialblockgeometry.cpp:227-228` 仍 `isFence || isSolid`，栅栏不再向铁砧伸横档；同类：画不能钉铁砧墙、雪傀儡立铁砧不铺雪。修复：连接谓词改 `isCollidable ∨ isFullCube`（R1 口径）。**✅ 已修（commit 42d0136）**：三处（栅栏 connects λ / 画 cellOk / 雪傀儡 trail 支撑）统一 `isCollidable ∨ isFullCube`（R1 单一权威 a890bfa）。已接受放宽：门/台阶/雪层也满足复合谓词会连接（项目级简化登记）；ShapeNone 仍永不连接。探针 P27（栅栏向铁砧伸档 = 栅栏-栅栏档数；火把不连）。

### 出生点/进度组（早间）

- **#20 `06f2566` (t756) 读档后出生点停 (80,80,80) 常量直到首次死亡**：读档路径不 snap（`Main.qml:591-607`）；`setWidth/setDepth/setHeight` 重建世界不 emit seedChanged（`world.cpp:425-427` 潜伏坑）。修复：读档后补 resnap；尺寸 setter 补 emit。
- **#21 `06f2566` 保底三被动生物仍固定旧 (80,80) 邻域**：`Main.qml:803-813` 固定坐标，与 findSpawnColumn 出生点可能相距整个环扫半径。修复：改以 spawnColumnX/Z 为中心偏移。
- **#22 `676b843` (t752) farmer 重挂后旧档回放被父前置静默吞**：极窄边缘（`playerprogress.cpp:395` vs `:90-94`）。修复：回放处链式补前置或注释接受。

### 矿车组（早间）

- **#23 `a3194bd` (t770) 段中重选向 + clamp 的一次性 ~0.5 格横向瞬移**：`minecartmanager.cpp:730-736/:435-436`，被骑时玩家视点同步跳；探针只验收终态。修复：clamp 限幅渐进或改 dir 同时钉 pos。**✅ 已修（commit f9abcc6）**：`stepCartAlongRail` 贴轨收敛改预算制（每 tick 垂直轴最多 `kCartCenterSnapPerTick`=0.1 格，行进轴到心落位仍精确）；取舍：另一修法「改 dir 同时钉 pos」只是同一瞬移换时点，弃。矩阵探针 P23（段中重选向每 tick 横向 ≤0.101 + 限窗收敛 + 仍达死端）+ P12c(b) 补有界收敛宽限窗。
- **#24 `791eb61` (t775) 注释声称 mob 窒息已收口实际未迁移**：`world.h:144-146` 注释与事实不符（EntityManager 仍独立实现 `entitymanager.h:980/:1518-1521`）。修复：改注释或实际迁移。**✅ 已修（commit f9abcc6）**：注释按事实登记（t160 玩家 + t775 骑乘已收口 World 单链；t254 mob 窒息**未迁移**、EntityManager 独立实现同判据），未为对齐注释做代码迁移。`world.h`/`world.cpp` 两处同步。
- **#25（备查）`af9ec8e` clampShift 坡上目标列高位轨恒钳格边界**：静速车坡上穿透不可完全分离（冲量路径不受影响）。可接受为已知行为。
- **#26（备查）`2edeb78` (L8) 宽>64 启发式挡不住 2× 放大的非 demo 排版包**：已知启发式限制，严格优于修复前。

### 铁砧/刷怪笼/羊组（晚间新增）

- **#27 `1e8b577` (t806) 余烬门熄灭钩子只挂玩家挖掘路径**：`breakNetherPortalsAround`（`world.cpp:2581`）仅 `playercontroller.cpp:1269` 一处调用，未进 setBlock 钩子族（对照：末地门有 `checkEndPortalIntegrity` World 层钩子）——爆炸/焚毁等静默清格路径拆掉门框后门面残留。影响受黑曜石抗爆性限制，故低。修复：钩子并入 setBlock 的 check 族。
- **#28 `c1688fc` (t794) 铁砧砸伤"每次下落只结算首个命中拍"**：`anvilDamaged` 一次性标志（`entitymanager.cpp:5272/5307`），先穿玩家后落猪身则猪免伤、台阶上下两猪只伤上面那只；与头注释"每实体每次下落只伤一次"及 MC 语义不符。修复：按目标记录已结算（落体 serial + mob 已伤标记）。
- **#29 `c1688fc` (t794) 砸伤 XZ 判定用 1×1 满格**：`halfW+halfW` 测试（`entitymanager.cpp:5283-5284`）覆盖整格，宽于铁砧 12/16 视觉足印（FallingBlock halfW 恒 0.5，视觉缩 XZ 0.75）。修复：铁砧落体用 0.375 半宽。
- **#30 `8d80d73` (t787) 鱿鱼笼只能刷出"搁浅鱿鱼"**：spawn 位扫描硬性要求空气+固体底（`entitymanager.cpp:1096-1111`），MobSquid 复用同一扫描 → 鱿鱼全刷陆上慢爬。修复：鱿鱼走水格判定单独找位。
- **#31 `8d80d73` (t787) 被动刷怪笼被敌对预算/邻域闸门压制**：入口全局敌对 cap 早退（`entitymanager.cpp:1047-1050`）对被动笼同样生效，夜里敌对满 30 只时猪/羊笼全停（MC spawner 不受 hostile cap 约束）。修复：被动笼跳过敌对闸门，仅留总 cap 与同型 local cap。
- **#32 `13ade6f` (t789) 剪过毛的羊被打死仍掉羊毛**：onMobDied 羊分支无 sheared 守卫（`Main.qml:2267` 附近，mobDied 信号不携带 sheared），MC 1.0 剪毛羊死亡不掉羊毛；注释"已剪则死时不再多掉"与实现相悖（t300 遗留，t789 改了该行未收口）。修复：mobDied 再携 sheared 快照。**✅ 已修（commit c9dd880）**：mobDied 加第 8 参 `sheared`（Entity.deathSheared 致死瞬间快照，同 deathBaby/deathBurned 模式——0.5s 死亡动画窗内剪毛竞态安全）；Main.qml onMobDied 同步 8 参，羊分支三分支：烧死→熟羊肉（肉非毛不受剪毛影响）/ 未剪→sheepWoolDropId 羊毛 / 已剪→无羊毛掉落。旧 7 参连接兼容（新式信号槽允许槽参数少于信号）。矩阵探针：剪毛羊致死 sheared=true + 未剪对照 false（t789 死亡探针同时改挑未剪样本——其③段剪过 3 只，#32 前载荷不可分辨）。
- **#33 `13ade6f` (t789) `sheepWoolDropId` 无越界钳制**：`Main.qml:1960-1961` 对 woolIdx 无 [0,15] 钳（C++ tint 侧有 clamp，防御不对称）；脏值 16+ 会掉进床段 id。修复：`Math.max(1, Math.min(15, woolIdx))`。**✅ 已修（commit c9dd880）**：`const idx = Math.max(0, Math.min(15, woolIdx))`——下界取 **0 非 1**（#7 后 idx=0 是合法白→27 分支，非无效值；上界 15 与 C++ sheepWoolTintAt clamp 对称）。
- **#34 `c02adf6` (t777) 羊腿 skin 覆盖层只加在图鉴侧**：ResourceBrowser 有 4 个固定 #d6b890 腿罩（`:883-910`），游戏内毛茸态单材质整模乘 tint（`Main.qml:6837-6859`）→ 有色羊（18.2%）腿也被染色，图鉴"所见即所得"注释（`:96-98`）不成立。修复：游戏内复刻腿罩或改注释声明近似。**✅ 已修（commit c9dd880，选「跟随摆动腿罩」）**：游戏内毛茸态 Model 内复刻 4 腿罩——髋枢 Node（(±0.18,-0.12,±0.26) = addLegs 羊参腿枢）+ `eulerRotation.x = mobArmorLegSwingDeg(walkPhase, ±1)`（t560 护甲腿量化相位契约，与几何腿同幅同相；对角配对 前左+后右 +sw / 前右+后左 −sw 同 addLegs）+ 罩 Model (0,-0.16,0)@(0.19,0.34,0.19)（腿盒 +0.01/+0.02 防 z-fight，图鉴同尺寸）；色 `sheepLegCoverTint` = 裸肤 #d6b890 × terrainLight、hurtFlash 红覆盖。取舍：静态腿罩会在腿摆 ±0.5rad 时穿模露出染色腿脚 → 选随腿同摆（代价 = 毛茸态羊 delegate +4 Node/Model，羊量级可忽略）；白羊腿也换裸肤色与图鉴/裸态观感统一（机制等价 MC 羊腿不随毛色染）。纯 QML 呈现，矩阵不可锁，需人工目视。
- **#35 `9e3a543` (t782) setMobType 白名单仍是手工 `!=` 长链**：`mobmodel.cpp:334` 单行 16 项连不等——未来 mobType 18+（MobAnvil=18 已在 enum）误传仍静默钳成猪、无告警。修复：查表 + qCWarning。
- **#36 `9e3a543` (t782) 刷怪笼迷你态 rodSpin=45 被 6° 量化成 48°**：`mobmodel.cpp:404-412` 量化步长未向调用方声明（`Main.qml:9029` 传 45）。修复：传 48 或注释声明量化契约。

### 口径备注（无需改代码）

- **#37 `8c98ed8` (t778) pack 关闭态鱿鱼无眼**：几何眼整删 + 程序贴图不画眼，纯设计取舍（注释已文档化）；对齐 MC 需在 build_entities_pack.py 画眼纹素。
- **#38 `dd2f651` (t785) 蛋产"野生狼"是主动敌对型**：本工程 aiWolf 语义"未驯服即敌对"（t480 §9 刻意设计），但提交声称"机制等价 MC 野生"——MC 野狼中立。若以 MC 为验收口径，蛋产狼即被追咬是可感知偏差。
- **#39 `8d80d73` (t787) 被动笼注释"不挤占敌对 cap"为真但被闸门压制**：见 #31，语义可辩。

---

## 六、昨日 Review 修复项核验（第一批 fix(review) 提交）

- **`4f4dceb`（高#1 末影眼 + 高#2 火把翻转）**：修复完整（赋值恢复 + 断言防线；翻转斜下扩集双向覆盖，`world.cpp:2998-3020`）。
- **`83f9e43`（中#6 墙火把斜角供电）**：修复完整无回归。
- **`4f14040`（中#5 + 中#7）**：#5 完整；#7 主场景闭环，边缘残留见 #11/#12。
- **`4fb6075`（中#8 slim 皮肤）**：**无效，见高危 #1**。
- **`e0e355e`/`736e8b8`/`38b0e14`/`2edeb78`（低危批）**：除 #15/#18/#26 所列偏差外核实有效。
- **`a890bfa`（R1 谓词统一）**：谓词无误判；防不住"新静默路径不调用"缺口（见 #4）。
- **`128641c`（t805 船岸停恢复）**：逐字恢复无 over-revert。

## 七、审查过、未发现新问题的提交（65 个代码提交中的 45 个）

- **第一批**：`c35adf9`(t792)、`687e8d9`(t765)、`25ebe06`(t766 主体)、`0bf8ffc`(t764 主体)、`cac273d`(t750)、`5cfe06d`(t753)、`8d15f2c`(t754)、`a5e4f26`(t755)、`4f4dceb`、`83f9e43`、`fcbade7`(t772)、`c4d6e03`(t767)、`ae4067d`、`8ef2149`(t773)、`4c9720c`(t771)、`1627f09`(t803)、`07582f8`(t774)、`6d11999`(t801)、`ffe3f3e`、`d754011`、`e0e355e`、`736e8b8`。
- **第二批**：`fdbbb38`(t802)、`89435a6`(t791)、`0657179`(t795)、`6a0c3ba`(t796)、`1033646`(t797)、`188ec03`(t798)、`602db76`(t784)、`0fd157b`(t790)、`1727847`(t807)、`ccb82a1`(t793)、`dd2f651`(t785 主体)、`65433c3`(t786)、`8c98ed8`(t778)、`4d68510`(t779)、`9b70536`(t780)、`a7cf3bd`(t781)。

有发现的提交：第一批 19 个；第二批 `c1688fc`(t794)、`8d80d73`(t787)、`13ade6f`(t789)、`c02adf6`(t777)、`9e3a543`(t782)、`5a8105b`(t788)、`1e8b577`(t806)、`db450cf`(t783，正向修复)。

---

*主审：ZCode（GLM-5.3）· 第一批 7 路并行子系统审查 + 主审复核；第二批 3 路并行审查 + 主审自审 12 提交 · 皮肤/玻璃像素为 PIL 实测，QML 布局逐坐标推算，染色链死链经全工程 grep 溯源确认。*
