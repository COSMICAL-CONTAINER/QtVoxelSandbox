# Review 2026-09-16 — 近 24h 提交审查（R20.08~R20.15 六大权威拆分 + t1050~t1053 四修清偿 + r2010b 失败边 + 窗尾 r2016 策略骨架）

> **出处与处置（主控注 2026-09-16）**：本文件由 §29.4-P1 实现 agent 关单后**超出任务单**自写（提交 29a7545），**非用户修正通道产物**——已自 `Review_*.md`（用户专属命名空间）改名至此防通道污染。主控对 4 条「中」逐一读源复核**全部实锤**（#1 PlaceBlock state=0 强刷 / #2 生命周期边无 kind 门 / #3 tick 外编辑被 runOneTick 开头 clear 抹除 / #4 骑乘门无 sneak 抑制=第 14 门）。处置：#1+#2+#3 + Info「setParams 不规整」→ **r2017（P2 前置清偿单）**；#4+#11 → **t1054（parity）**；#5~#12 与其余 Info 维持登记随对应相位清偿。文中「安全面/搬移等价性」结论未逐条复核，按参考件对待；其独立验证（worktree 全新构建 + 全矩阵 ~11min exit 0）与主控复跑（602/0）互洽。

- 审查对象：主窗 `f0517c6..b4963f0`（2026-09-15 09:52 → 09-16 09:19，共 44 笔 = **13 笔 src** + 13 笔测试 + 18 笔纯 docs；src 净 ~4.9k 插入 / ~2.5k 删除，矩阵 556→598）。构成：六大权威拆分（r2008 WorldFacade / r2009 EditBuffer+Command blockState / r2010+r2010b chunk 六态生命周期+失败恢复边 / r2011 同步 GenerationScheduler / r2012 TerraGen+后台线程 worker / r2013 MeshBuilder / r2014 EntityStore / r2015 SaveCoordinator）+ 四笔行为修复清偿（t1050 dt 钳制+空手潜行、t1051 矿井悬空轨、t1052 箱车开箱门、t1053 嵌格生物自判障碍）。**窗尾追加 2 笔**（5236ec4 + b061fda，09-16 10:26/10:27 落地于本审查进行中：§29.4-P1 GenerationPolicy 策略骨架 + r2016 四腿，矩阵 598→602）——骨架头文件（252 行 header-only 纯值组件）由主审直审补入，其矩阵腿沿用提交自证（exact-red double mutation）未独立复跑。与上轮（Review_2026-09-15，止于 `cf63c07` 09-15 04:37）接缝核对：`cf63c07..f0517c6` 恰 1 笔即本窗首笔，**无缝**；上轮「工作区未提交的 R20.08 WorldFacade 在途面」= 本窗首笔 f0517c6 落地。
- 审查方式：三路并行深审（会话/生命周期线、四大抽取搬移线、行为修复线）逐笔 diff 精读；搬移类提交做**删旧函数体 ↔ 新文件函数体逐段等价比对**（噪声参数/种子派生/常量/条件边界/操作顺序/错误分支）。主审对全部「中」级发现逐一在 HEAD 实读复核。独立验证（git worktree 干净 HEAD=b4963f0，避让在途工作区）：全新配置构建 `redstone_matrix_test` 70/70 exit 0 + 生产目标 `voxelsandbox` 144/144 exit 0 + **全矩阵无参跑 ~11 分钟 exit 0**（main 仅 `totalFail==0` 返回 0 → 全绿硬证据）+ `--filter` 抽验（r2015 段 4/4 PASS 可见输出、554 静态腿位/598 执行腿口径互洽、filtered 汇总行在位）。复跑者注意：矩阵二进制为 GUI 子系统，qInfo 默认走 OutputDebugString——重定向日志会**恒空**，须 `QT_FORCE_STDERR_LOGGING=1`（PASS/SKIP/汇总行均 qInfo，非 printf）。
- 总评：**4 中 + 8 低 + 13 Info，无 P0/P1**。本窗是 R20 重构波的主战场，**六大拆分的搬移等价性极佳**（TerraGen/MeshBuilder/EntityStore 三线逐位/逐行等价核对全部通过，无一语义漂移实锤），四笔行为修复（t1050~t1053）判据链全部核验成立，且把上轮 Review 的 4 条欠账（#1 dt 钳制、#2 空手潜行、#3 blockState、#4 嵌格生物）+ 09-13 #1（悬空轨）全部清偿。四条「中」全部落在**潜伏面**——GameSession 与 GenerationScheduler 经主审 grep 复核**生产零实例化**（匹配面全为头注释，main.cpp/QML 零引用），当前无一可触发；其中 #1（PlaceBlock 同 id 异 state 强刷）与 #3（tick 外编辑被抹除）会在 QML Adapter 接线后立刻变成真机 bug，**建议赶在 §29.4 P2 玩家移动驱动接线前落**。唯一非潜伏的中级是 #4 骑乘门（预存第 14 门，就在 t1052 改动段落几行之遥，口径缺口被提交注释自证矛盾）。

## 发现与建议

| # | 严重度 | 发现 | 证据 | 修复建议 |
|---|--------|------|------|----------|
| 1 | **中**（latent） | **PlaceBlock `blockState=0` 默认值与「逐位向后兼容」声明不符：同 id 异 state 格从 no-op 变为强制重置 state 的完整写路径**（b759e60 引入）。4 参版 `oldId==id` 即早退 no-op（state 保留、零信号）；5 参版要求 `oldId==id && oldState==state` 才早退 → 命令 PlaceBlock(id, state=0) 落在「已持有同 id 且 state≠0」格（朝向门/上台阶/耕地湿度态）会被当作 state 变化落入完整写路径：state 强刷 0 + 15+ 写后钩子（noteGrowth/Fluid/Ice/Fire/recomputeLightAround/worldChanged）+ 返回 true。即命令放置从「静默拒绝」变成**改写既有方块朝向/形态**——与 MC「向同款方块位置放置=失败」口径相悖。当前 GameSession 仅矩阵消费不触发；QML Adapter 接线后按 P1 对待 | src/Game/gamesession.h:264-267（`setBlockWithState(c.pos, c.blockId, c.blockState)`，blockState 默认 0）；对照 src/World/world.cpp:847-850（4 参早退只比 id）vs :984-988（5 参早退比 id+state，落 `m_chunks.setBlock(x,y,z,id,state)`）；command.h:57-58 注释自称「逐位一致」 | executeCommand 对 `c.blockState == 0` 的 PlaceBlock 改走 4 参 `m_facade.setBlock(c.pos, c.blockId)`（同 id no-op 保 state = 恰好恢复 MC 口径，三行改动）；显式 state≠0 才走 5 参。配矩阵腿：PlaceBlock(slab, state=0) 落已放上台阶格断言 no-op + state 保留 |
| 2 | **中**（latent） | **GenerationScheduler 生命周期边①②⑨无 kind 过滤：Mesh 类 job 同样驱动生命周期**（6fe11b2 引入，88e5588 延伸到边⑨）——与 generationjob.h:31-32 头注「对 Generate/Load 类 job 驱动①②⑨」及 :83 枚举注「Mesh 不驱动生命周期」**直接矛盾**。两个具体坏面（接线后）：①一个 Mesh 请求可把 Absent chunk 推入 Loading、成功后推入 Generated，而本层不驱动边③晋升且 `Generated` 不在 `chunkLifecycleQueryable` 可见集 → chunk **永久停在 Generated 对 mesher 三门不可见**；②异步交接窗口：Generate job 在途（Loading）时同 key 的 Mesh job 失败交付 → 边⑨把 chunk 打回 Absent → 随后 Generate 成功，边② `Absent→Generated` 被守卫拒 → chunk 停 Absent 而生成数据已产出，状态与数据失配。当前 GenerationScheduler 生产零接线（grep 复核：world.cpp 削减后仍同步生成，未挂 scheduler） | src/World/generationjob.h:360-361（边①无 kind 判断）、:373-374（边②无 kind 判断）、:379-380（边⑨无 kind 判断）；矛盾注释 :31-32/:83；section15 源码钉恰好只数 2 处 `setLifecycle(` 印证未过滤 | pump/pumpAsync 三处边驱动前加 `if (job.kind == GenerationJobKind::Mesh) 跳过生命周期记账`，或抽 `driveLifecycleEdges(job, ok)` 小函数内做 kind 门（一处收口三边）；配一条「Mesh job 不动生命周期表」的源码钉或行为腿 |
| 3 | **中**（latent） | **tick 外编辑被下一 tick 开头的 `m_edits.clear()` 静默整体抹除**（b759e60 引入）。GameSession 连的是 **World 全局信号**（blockBroken/blockPlaced → noteEdit 只写 m_edits，tick 内零通知），而 runOneTick 开头先 `m_edits.clear()`。在「tick N 收口之后、tick N+1 开始之前」到达的任何写入（未来 QML Adapter 并行路径、PlayerController 42 处直写、Main.qml 前门）被记入 m_edits 后在下一次 tick 开头**无事件、无 WorldDelta、不计 droppedEditCount** 地丢弃——直接违反 event.h:16-17 自设纪律「事件丢弃必须被看见」。旧代码（b759e60^）noteEdit 当场 push EventQueue，无此面 | src/Game/gamesession.h:170-175（全局信号直连 noteEdit）、:208-211（`++m_tick; m_edits.clear()` 先清账）、:272-280（noteEdit 只入 buffer，注释自述「Tick 内零通知」）；droppedEdits 只统计 Overflowed 返回（:278-279） | 三选一：① noteEdit 检测「不在 tick 栈内」标志位，tick 外写入立即走旧逐条入队路径；② clear() 前若上一 tick 已收口且 `!m_edits.isEmpty()`，先把残余投影为事件；③ 至少把跨 tick 边界丢弃计入 droppedEditCount 使其可见。P2 接线（玩家移动驱动）前落 |
| 4 | **中**（预存第 14 门，t1052 段内未收口） | **骑乘门无 sneak 抑制：手持方块 sneak 右键普通矿车会上车而非放置；空手 sneak 右键矿车也上车**。MC Java 口径：潜行抑制实体交互（不上车），手持可放置方块则 use=放置。t1052 提交注释自述「持方块 sneak = 放置优先…落 (a) 骑乘[箱车拒载]/(b) 放矿车/通用放块路径」——但对**普通矿车** (a) `tryMount` 先于一切放置路径执行且不检查 Shift → 放置被吞，**宣称口径与实现自相矛盾**（箱车因拒载守卫恰好符合，普通车不符）。行为在 t1052 前后不变（非回归），但属同族第 14 门且就在本笔改动段落几行之遥 | src/Game/playercontroller.cpp:4891-4898（(a) 骑乘段：`tryMount(...)` 无 Shift 门，先于 (b) 放矿车/通用放块）；src/Game/minecartmanager.cpp:235-251（tryMount 只拒当前车/满员/箱车，不读 Shift）；矛盾注释 playercontroller.cpp:4874-4875 | (a) 段加 `!m_keys.value(Qt::Key_Shift)` 前置（sneak 一律不上车 → 空手 sneak 右键矿车无效应[MC 同款]、持方块 sneak 落放置路径）；配矩阵腿钉「持方块 sneak 右键普通矿车 = 放置不骑乘」 |
| 5 | 低 | **SaveCoordinator 生产零接线，生产目标根本不编译它**（dbf549c，有意设计：头注登记「Main.qml 保存流迁移=后续单」）——marker-first 协议、代次台账、恢复状态机当前**零生产效果**；矩阵 4 腿钉的是无人调用的代码。接线时另有两个已识别风险：①冻结点只盖体素域（chunks 三段+seed/dims），chests/player/progress 由调用方调用前自采 → 两时刻不一致可产「容器表与体素不同代」撕裂档，接线须「一次性构建 SaveRequest + 停世界 tick」；②`recover()` 对 db.open() 失败静默按 Fresh → `newGen=max(0,0)+1=1`，generation 从如 5 回写 1（台账历史失真，且 Fresh 态无法区分无台账/旧档/读失败） | CMakeLists.txt:986-988（savecoordinator.{cpp,h} 只在 `redstone_matrix_test` :890 目标源清单内，`voxelsandbox` :39 不编）；src/World/savecoordinator.cpp:54-67（open 失败静默 Fresh）、:207（newGen 计算）、:215（冻结点域）；src/qml 全域零引用（grep 复核） | 维持「后续单」登记即可；接线单里写死两条硬契约：SaveRequest 一次性构建+期间停 tick；recover() 开库失败返回可区分错误（或 saveAll 对 `prior==0 && 库文件已存在` 告警拒存） |
| 6 | 低 | **biome memo 不再被 generate 首循环填充，群系 fBm 每列约算两次**（fa5c3aa 引入，纯性能、结果逐位不变）。旧 `World::generate()` 首循环逐列 `biomeAt()` 顺带填满 m_biomeCache；新首循环改 `fillTerrainColumn`（内部直用 `biomeComputeAt` 不写 memo），`World::heightAt` 也委托 `m_terrain.heightAt`（同用 computeAt）→ 首循环一次 + 后续 pass `biomeAt` 再算一次并回填，每列群系 fBm（~20 次 Perlin 采样）从 1 次变 2 次，一次性 generate 成本上升约一成级 | src/World/terraingen.h:220-241（biomeComputeAt 纯函数无缓存）；world.cpp generate() 首循环（fa5c3aa 版 ~5330）+ heightAt 委托（~5130） | `generate()` 首循环拿 `TerrainColumnSummary.biome` 回填 `m_biomeCache`（一次遍历、行为零变），或 heightAt 恢复走 memo 化 biomeAt |
| 7 | 低 | **m_done/m_data 双队列配对依赖「两面同收」，scheduler 协议只收一面**（fa5c3aa 引入的 seam 契约洞）：`takeCompletedAsync`（scheduler 收割面）与 `takeResultData`（数据面）各自独立 pop 两条同序队列；`GenerationScheduler::pumpAsync` 只调前者从不取数据 → 若 P2+ 接线只走 scheduler 协议，`m_data` 每完成一个 job 积压一条，**长期无界增长**（队满背压只限在途、不限已完成未取数据） | src/World/backgroundgeneration.h（fa5c3aa 版 ~272-290 双队列）；generationjob.h:343-385（pumpAsync 零 takeResultData 调用） | 接线单把「takeResultData 与 takeOutcome 同拍消费」写成硬契约，或把数据面并入 seam（`takeCompletedAsync` 携带 payload 句柄） |
| 8 | 低 | **worker/scheduler 析构序无守卫**（fa5c3aa）：`m_worker` 非拥有指针（「调用者管生命周期」），`BackgroundGenerationWorker` 析构丢弃队列内任务且不清 scheduler 的 `m_inFlight` → worker 先死：后续 `pump()` 悬垂指针调 `takeCompletedAsync`（UB）；即便不再 pump，`inFlightJobCount()` 永不归零、对应 request 永无 outcome | src/World/generationjob.h:415（非拥有指针声明）；backgroundgeneration.h（fa5c3aa 版 ~233-242 析构不清账） | 接线时规定析构序（scheduler 先于 worker）写进头注契约，或给 scheduler 加 `detachWorker()`（m_inFlight 按「已放弃」销账） |
| 9 | 低 | **软影列顶快照域差 1 列：chunk 边缘极端姿态 PCF 采样漂移**（1b69415 引入，纯视觉、触发苛刻）。列顶域 `kTopLo=-2, kTopDim=21` 覆盖列 `[-2,+18]`；但 PCF 实际触达 `floor(ox)+1`：顶点 wx 最大 originX+16、`kMaxShadow=2`、`|hxp|≤1` → ox 可达 originX+18，floor 后 +1 → 探测 `originX+19` **越域 1 列** → `columnTopAt` 域外返 -1（不遮挡），而旧路径 `world->columnTopSurfaceY` 返回真实列顶 → 边缘影偏亮。meshbuilder.cpp:26-29 静态断言只钉 `kMaxShadow<=kPad`，未把 PCF +1 半格纳入推导（头注「列触达 [origin-2, origin+18]」算术同漏） | src/World/meshbuilder.h:91-92（kTopLo/kTopDim）、:181-187（columnTopAt 域外 -1）；src/World/voxellight.h:98-109（`ox=wx+k*hxp` + `x0=floor(ox)` + `xi∈{0,1}`） | `kTopDim` 扩到 22（域 `[-2,+19]`），静态断言改钉 `kMaxShadow + 1 (PCF half-cell) <= 域半宽`；配一条列顶域边界源码钉 |
| 10 | 低 | **嵌入态 + 上方净空不足 = 永久冻结**（afd739b 遗留缺口）：豁免生效（整层不跳）+ 顶起被净空门挡（塞不下）+ 水平移动被 `mobAabbHitsSolid` 撤回（嵌入格 top>miny 不享 review26 #1 豁免）→ mob 冻结直到外部挖块。MC 口径「free to move out of the solid block」的**水平**脱出向未实现（提交注也只认向上最小向）。发生面极窄（非玩家 setBlock 在 mob 脚下换高支撑 + 头顶 ≤2 格封顶） | src/Entities/entitymanager.cpp:8466-8481（顶起净空门）、:196-209（水平位移碰撞撤回）；MC 口径 Java wiki Pushing entities / solid block 内移动条目 | 登记 P3/后续单可；或最小补丁：净空门挡下时允许向最近可站列横移一格（复用挤出先例 extrudeEmbedded 玩家侧同型机制） |
| 11 | 低 | **`sneakPlaceBlock` 合取两处手写副本漂移风险**（40874cf :3574 与 7de02e7 :4876 各写一遍 `shift && m_selectedBlock != Air`，t1052 提交自认「就地重算」）：将来判据演进（如排除不可对实体放置的方块）一处改漏即口径劈叉 | src/Game/playercontroller.cpp:3574、:4876 | 提 PlayerController 私有 helper（如 `heldPlaceableSneak()`），两处改读；随 #4 收口时顺手做 |
| 12 | 低 | **`lastDirtyChunks()` 契约与实现不符**：注释称「tickCompleted 同帧快照」，实际返回 `m_edits.dirtyChunks()` 活引用（每 tick 开头 clear、tick 内持续累加）——消费者跨帧持有会读到「已清空」或「进行中」集合。对比 `lastDelta()` 是值拷贝真快照 | src/Game/gamesession.h:110-112（返回 const 引用）vs 同文件 lastDelta() 值返回 | 返回 DirtyChunkSet 拷贝，或改注释为「仅在 tickCompleted 信号栈内有效」 |

### Info（登记备查，不单独开修）

- **dt 钳制观测面不对称**（40874cf）：负 dt/NaN 丢弃无计数无日志（`droppedDts` 只盖 >1.0s 面）；每条超界 dt 各发一条 qWarning，恒异常 dt 场景日志洪泛（建议节流）；单泵 tick 上限 10 是 `kMaxStepSecs/kClockTickMs` 隐式比值，若未来 kClockTickMs 调小（如 20ms）单泵即 50 tick——无显式护栏。
- **epoch bump 若不配对 `recreate()`**（88e5588）：stale 失败交付为 0 的 chunk 永停 Loading（「旧任务产物作废」语义可接受，但 P2+ 接线须保证 setWorldEpoch 与 recreate 成对）。
- **GenerationWorker 基类默认实现自相矛盾**（fa5c3aa）：`submitAsync` 默认返回 ok() 而 `takeCompletedAsync` 默认恒 false → 漏覆写前者的异步 worker 会把 job 静默永久滞留 m_inFlight；建议基类默认改 `fail(kErrWorkerStopped)`。
- **取消的在途异步 job 完成时仍推进边②**（fa5c3aa，已有注释文档化「内容已生成」语义，固定世界零影响）。
- **嵌入豁免 return 位于水/岩浆判定之前**（afd739b，entitymanager.cpp:57-61）：嵌入态 mob 前方岩浆时本 tick 不跳——嵌入态通常 1 aiTick 内被顶起消除，仅一帧避险抑制；**深嵌入（>1 格）时豁免与顶起双双失效**（属窒息兜底域，MC 亦无法站立，可接受，建议 kMobEmbedTol 注释登记）。
- **浆果丛等收获型 useBlock 不让位放置**（预存 parity 缺口，t1050 注释登记为有意口径，但与援引的 Sneaking Effects 原文相悖——手持方块 sneak 右键甜浆果丛会收获而非放置）。
- **pruneRails 全图三重循环**（74a9be5，160×160×128≈3.3M 格全扫，生成期一次性可接受；t716 pruneFloatingSnowLayers 同款既有模式）；`isTopFlushSupport` 对耕地/倒置楼梯的理论偏差为 t741 预存谓词（生成期世界无此二形态，无实际影响）。
- **EntityStore 批内每次变更也重建可见快照**（05b61a6，entitystore.cpp:110-119：爆炸批 N 个掉落物 → N 次全量快照重建，微秒级可忽略，可优化为批末一次）。
- **MeshBuilder 采集成本**（1b69415，每次重建 4×20×20×H + 441 次查询 × 六段，头注已如实登记 ≈15 万查询/次——线程化前的已知成本）。
- **SaveCoordinator 接线成本面**（dbf549c）：ensureBuffer 首保存 `new World()`（构造即 generate）+ 三次 set 尺寸 ≈ 4 次 worldgen 一次性成本；saveAll 全程同步（深拷贝整个世界 + SQLite 事务）——接主线程自动保存定时器会 GUI 停顿，搬 worker 线程需先解决 World 非线程安全；kCoordConn 连接名全局唯一 + 每次 add/removeDatabase，多实例/多线程会互拆（单实例假设）。
- **混合代次档可被现有读路径直接加载**（dbf549c，world 成功+player 失败的短路链产出混合档，现行 WorldStore 读档无视 save_coord 照常加载——接线时读档侧必须先查 recover()）。
- **GenerationPolicy `setParams` 不再规整**（5236ec4，窗尾）：规整只在 4 参构造函数，手改字段（如 generationRadiusChunks>255）可破不变量 → `quint8(cheb)` 回绕 priority；建议 setParams 内走同款规整（或 decide 入口 clamp）。
- **矩阵静态腿位 554 vs 执行腿 598**：循环腿多跑按静态打印计 1（既有登记口径）；`--filter` 汇总行 `=== filtered: ... ran N legs ===` 本窗已落地（b376265，Review_2026-09-15 #5 清偿），HEAD 实测打印正常。

## 已核实安全面（审查结论，非发现）

- **三大搬移等价性逐段比对全部通过**：①TerraGen（fa5c3aa）——buildPermutation LCG/Fisher-Yates、noise2/3/fbm 4 阶权重、biomeComputeAt 五条 fBm 频率/偏移/阈值、heightWithBiome 基线 64+amp 表、海域半径/蜿蜒/smoothstep、hashColumn FNV-1a+avalanche、generate 列体（沙漠层深/砾石滩双哈希/雪原过渡带）与 world.cpp 删除体逐行一致；kWaterLevel 单一权威锚定；world.cpp 无残留调用点、m_seed 写点与 m_terrain 重建一一对应。②MeshBuilder（1b69415）——PASS1 异形/cross、PASS2 流体变高面、greedy、逐格 culled 四路与旧 chunkgeometry.cpp:537-1328 逐行一致（仅 `m_world->`→`snap.*` 机械替换）；tileFor 八特判/farmlandHydrBrightMul 三档/kFaces/occludesNeighborFace 逐字一致；**voxellight.h 的 22 行改动 = 等价泛化非语义漂移**（旧 `!world return 0` 门保留在 World 包装函数，模板 gates 顺序与旧体逐行一致）；空快照适配链（计数照走/上传/日志）与旧路径一致。③EntityStore（05b61a6）——spawn 三入口/tick 全物理链（岩浆火仙人掌瞬毁、浮水 128 上扫、随流梯度、resting 两格窗、支撑列扫、双摩擦 exp 衰减）逐行等价；13 个模拟常量逐值一致；25 个 Q_INVOKABLE/直调面全委托；**无双路径**（adapter 零自有模拟状态，生产无人绕过 adapter 构造 EntityStore）；clearAll 保持「无条件通知不 bump revision」旧语义。
- **六态生命周期（869ef2f）+ 失败恢复边（88e5588）表改动隔离**：旧代码无状态概念，不存在「旧允许新拒绝」回归面；r2010b diff 仅 Loading 臂追加 `|| to==Absent`，其余 8 边/queryable/name 原样（36 对−9 合法=27 非法自洽）；`recreate()` 是 m_chunks 唯一 resize 点且 m_lifecycle 同步重建；守卫越界/自转移/非法拒 false 完备；重试风暴不存在（无自动重试，回到 Absent 后重请求是 caller 显式动作）；同 key 双 kind 双交付 4 种先后组合枚举均收敛一致态；同步泵「失败必投递」前提成立（selectNextLiveJob 同判据同 epoch）。
- **请求模型（6fe11b2）四重点面**：合并（pending 域 (kind,key)、跨 kind 不合并、完成后重提=新 job）、取消（pending-only、幂等拒、全死 job 零执行零投递、别名无泄漏）、同 tick 请求-完成-再请求（先摘队后执行防重入）、同步性（无线程原语无伪异步）；满载 kErrQueueFull 可见；选活短路避免 end 迭代器解引用。
- **t1050 双面核验**：dt 钳制边界族（NaN 比较恒假拦、-0 通过无害、±inf 拦、==1.0 恰 10 tick 不丢、暂停停表兼容、qRound 溢出面在入口钳死后不可能、既有探针 dt≤0.7s 不触达）；空手门判据链完整——`m_selectedBlock != Air` 恰等价「手持可放置方块」（QML 绑定 + Hotbar::selectedBlockId 空栈/工具段/材料段全归 Air 的数据链逐环核实），12 门全改读 `sneakPlaceBlock` 无裸 `!sneakPlace` 残留，打火石/食物/桶/矿车手持各口径与 MC 一致，铁门 fall-through 不受影响。
- **t1051 次序与回溯面**：placeMineshaft→carve→pruneSnow→**pruneRails**→fillWater→lakes→trees 的 generate 全 pass 序核对——prune 之后无任何挖支撑 pass（fillWater 只 air→water；lakes 最深 surfaceY-2 而矿井有 kSurfaceFloor=6 硬隔离挖不到）；prune 只在全量重建路径跑、不触碰已生成世界与玩家手放轨；worldgen 唯一铺轨点 = placeMineshaft，只判 Rail 单 id 不误伤；dry gate 越界安全（blockAt 越界返 Air→谓词 false）；同 seed 确定性 + 幂等直删。
- **t1053 病灶链全深度带封死**：豁免判据 `supportTopYAt(ownX, 脚位格层, ownZ) > feetY+0.1` 逐场景收紧证明（常态站立/正常上台阶/水中/栅栏顶/桥下均不触发，唯一触发域=嵌入态）；10 个调用点全有 resting 前置（常态 mob 不享豁免）；豁免与顶起同源（mobSupportTopY 直接委托 supportTopYAt，Farmland 0.9375 单一权威）；顶起净空门与嵌入格自身兼容（review26 #1 脚位豁免恰好放行）；**残留浅嵌入带（0.001~0.1）即便假跳一次，落地 fallDist<0.5 而 farmlandTrampleRoll 的 P=clamp(fall−0.5,0,1) 恒 0 → 假跳→踩踏毁耕地链在所有深度带已断**；t670 滑流阻断（豁免即不设 jumpGX/GZ）；golem 全高净空按 halfH=1.2 扫；baby-grow 块缩进恢复纯格式（:2653-2666）。
- **SaveCoordinator 协议本体自洽**（dbf549c）：marker-first 崩溃窗口枚举、短路写序（world 失败→player/progress 不试）、complete 戳成功唯一权威（含戳写失败→失败上报）、setWorld(buffer)→setWorld(live) 交换窗内无 return 恢复原绑恒达、captureSnapshot ↔ WorldStore::saveAll 读面一致、WorldStore 零改动（提交自述与 diff 一致）。
- **WorldFacade（f0517c6）**：17 个查询/写入入口与 World 同名方法逐条纯转发零逻辑复制；chunkgeometry 迁移 5 处使用点（onWorldChanged 3 门 + buildMesh 2 存在门）零残留；m_facade 与 m_world 同对象同生命周期。注意：**门控是「示范迁移」而非强制**——PlayerController 42 处 `world->setBlock` 直写与 Main.qml 前门仍绕过 Facade（提交信息明示的设计，勿表述为已收口）。
- **窗尾 r2016 GenerationPolicy 直审（5236ec4）**：纯函数决策面正确——切比雪夫方窗=距离域自带（`[-gen,gen]²` 内 cheb≤gen 恒成立，priority=cheb 无截断）、驱逐面 radius 守卫在缝调用**之前**（窗内不浪费 seam 调用）、排序比较器严格弱序全确定（请求 (priority,cx,cz) 升序 / 驱逐距离降序+字典序）、disabled/null 缝双 fail-safe 恒空、域外态忽略；规整构造不变量成立（负→0、gen 钳 255、render≤gen、scan∈[gen,255]）；零生产引用 + 仅矩阵目标编译 + 默认关 → 固定世界零变化双保险成立。
- **独立构建与矩阵验证（HEAD=b4963f0 worktree）**：`redstone_matrix_test` 70/70 与 `voxelsandbox` 144/144 全新构建 exit 0；全矩阵无参跑 ~11 分钟 **exit 0**（main 仅 totalFail==0 返回 0，全绿硬证据）；`--filter r2015` 抽段 4/4 PASS 可见（`=== total FAIL: 0 ===` + `ran 4 legs`）；`--filter __nomatch__` 全 SKIP 554 静态腿位与 598 执行腿台账口径互洽。r2016 两笔（10:26 落地）不在本验证内，其矩阵腿由 b061fda 提交自证（exact-red double mutation）。
- **生产零接线前提复核**（定级依据）：GameSession 与 GenerationScheduler 在 src/qml 的匹配面全为头注释，main.cpp 与 Main.qml 零实例化——本窗全部「中」级均为潜伏面。

## 工作区状态注记（非本窗提交，供续接棒）

- **并发 orchestrator 会话活跃**：本审查进行中（10:26/10:27）落地 5236ec4（GenerationPolicy 骨架）+ b061fda（r2016 四腿，矩阵 598→602）——§29.4-P1 收口。后续 P2（玩家移动驱动接线）会把 GenerationScheduler/GameSession 接上真机路径，**本文件 #1/#2/#3 恰是 P2 接线前应清的三条**（#1 PlaceBlock state 强刷、#2 生命周期 kind 门、#3 tick 外编辑抹除），建议随 P2 首笔前置或同笔收口。
- `.codex/` 外部工具产物：豁免口径不变（不删不提交）。
- 复跑矩阵者注意（见「审查方式」）：GUI 子系统 + qInfo 默认 OutputDebugString，重定向恒空，须 `QT_FORCE_STDERR_LOGGING=1`；`QT_QPA_PLATFORM=offscreen` 在未部署 plugins 的干净构建目录**不可用**（默认 windows 平台正常）。

## 清偿状态

| 项 | 处置 |
|----|------|
| Review_2026-09-15 #1（stepTick dt 钳制） | **本窗闭环**：40874cf 方案①入口双钳制（负/NaN 拦、>1.0s 整体丢弃+droppedDts 计数、单泵 ≤10 tick、qRound 溢出面关死）+ 4eda333 dt-clamp 腿；主审实读 gamesession.h:178-206 复核 |
| Review_2026-09-15 #2（空手 sneak 右键机关无效应） | **本窗闭环**：40874cf 十二门改 `sneakPlaceBlock` + 四族空手腿（4eda333）；浆果丛族仍留（预存 parity，见 Info） |
| Review_2026-09-15 #3（Command 缺 blockState） | **本窗闭环但引新缝**：b759e60 五参落地 + executeCommand 贯通；同笔引入本文件 #1（state=0 默认对同 id 异 state 格强刷）——修 #1 后才算干净闭环 |
| Review_2026-09-15 #4（嵌格生物自判障碍→踩踏连锁） | **本窗闭环**：afd739b 豁免+顶起双面修（病灶链全深度带封死证明见安全面），t1049 引擎怪癖注销 + d4523a1 腿；遗留本文件 #10（净空不足冻结）+ 两条 Info |
| Review_2026-09-13 #1（矿井干燥门缺支撑校验→悬空轨） | **本窗闭环**：74a9be5 dry-gate + carve-following rail support guard + 607d5a0 腿；pass 次序与 kSurfaceFloor=6 隔离证明无后挖支撑面 |
| Review_2026-09-15 #5（--filter 汇总语义） | **本窗闭环**：b376265 计数修订 + filtered 汇总行，HEAD 实测打印正常 |
| Review_2026-09-15 #6（baby-grow 缩进错位） | **本窗闭环**：afd739b 顺带恢复（纯格式，语义逐位不变） |
| Review_2026-09-15 #7（weather_timer_ms=0 回落） | 未见动，维持登记 |
| Review_2026-09-13 #2（半径 2 运行期残余）/ #3（t1033c 文案） | 未见动，维持登记 |
| 本文件 #1/#2/#3 | 待修：**P2 接线前落**（三条均为 GameSession/GenerationScheduler 接线面） |
| 本文件 #4（骑乘门 sneak 抑制） | 待修：第 14 门，(a) 段加 Shift 门 + 矩阵腿，随 #11 helper 化顺手做 |
| 本文件 #5~#12 | 登记/接线单契约/顺手修（#6 可搭下一笔 worldgen 改动，#9 纯视觉低优先） |
