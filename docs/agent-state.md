# QtMinecraft Agent State

状态文件版本：1
更新时间：2026-09-17 14:4x（§29.5-W5b 流式 UI 收官：矩阵 651/0 双绿 + 阴性两轮恰红还原回绿；**流式弧 W1→W5b COMPLETE**——下一任务 = P5 实机调参验收[用户设备会话] + 累计待实机确认单清偿，无代码排队单）
用途：为断链恢复、定时治理和连续开发 Agent 提供短状态入口。长历史进入 dev-plan，架构决策进入 refactor-plan，治理规则进入 autonomous-governance。

## Current Control Block

```yaml
project: QtMinecraft
state: READY
current_task: P5 实机调参验收（用户设备会话；流式弧 W1/W1b/W2/W3/W4/W5 后端/W5b UI 全闭环，无代码排队单——累计待实机确认清单已汇总为 dev-plan「全弧待实机确认汇总（流式弧收官）」节：t1054 四项矿车 / P4 固定世界观感+F3 对照 / 流式批次[创建无限世界观感、走查生成、卸载无感、存读往返、转换往返] / 历史存量指针）
current_task_status: PENDING_USER_DEVICE_SESSION
last_completed_task: §29.5-W5b UI 流式界面接线（子 agent 串行执行，filter 词 r2028——**§29.5 登记的 QML 例外单，治理审计 #12 放行，全弧唯一登记新增 QML 面**；变更面 = WorldList.qml 两段四 hunk + Main.qml 进入分流/保存链冲洗两 hunk + Game 层 StreamingBridge 桥[QML 单例，BuildInfo 同款]；**D2 创建链 = 「无限世界」复选默认关**[不勾选零调用 = fixed 语义逐位，r2028a 真链墙实证] + **D3 opt-in 转换动作**[未转换 fixed 世界显按钮 + 确认弹窗三点文案；翻回不设 UI 登记] + **进入分流 = 问桥一次**[fixed 恒 false 逐字节原样；流式 = World::reinitializeAsSparse[sparse ctor 体单一权威委托] + loadStreamingWorld overlay + 会话通电[isSparse 门内 W2/W3/W4 三缝] + 泵拍/位置沿挂钩] + **保存链三写前置冲洗 = runExitSave 首行 flushForSave**[非流式恒 true 零动作；失败短路三写不谎报]；**最小 C++ 新增**：World 两法[reinitializeAsSparse/reinitializeAsFixed + ChunkManager::reinitializeFixed 对偶面] + GameSession::pumpStreamingFrame[生产只泵流式收割拍——QML tick 桥保持 World 模拟独占 = 玩法路径零改动不变量]；**值组件零 QML 暴露**[桥头零 Q_PROPERTY；ChunkStore/StreamWorldMeta 留 C++]；**worldstore 零触碰**[saves 目录解析镜像登记 + r2028b 真链行为锚定]；chunkstore.cpp 自本单入 app 目标[会话首度生产实例化]）
last_completed_task_commits: fix(r2028) 91daf84 + test(r2028) 9f4c0ca（本 docs 提交收口）
last_verified_commit: test(r2028)（矩阵 **651 PASS / 0 FAIL ×2**：matrix_r2028_pos.log + matrix_r2028_final.log 双日志在案 EXIT=0；648 权威 diff = +3 恰 r2028a-c + t813 戳/哈希登记类[12:00/3414b0c→13:59/b04aa06]；pos/final 腿集合恒等 651[前 110 字符键 diff IDENTICAL]；^FAIL 归类 0；**腿（section31 置尾，filter 词 r2028，真临时库 fresh+用后即删 saves/ 零触碰）**：r2028a 默认关零变化墙[真链进入分流恒 false + 会话零构造 + 标志读面 false + raw 库面零流式表 + 冲洗放行恒 true + 三写链 saveOkCount==3 + fixed 往返栅格逐位 + 二次分流复归墙]；r2028b 开关+转换通电面[real-chain：真 QQmlEngine × 真桥单例 × 真 World/WorldStore/WorldClock/PlayerController——勾选 → 标志行在[core dims/baseGen/saveGen=0] → 进入即 sparse[半径 2 预生成 25 驻留] + 会话通电 + 真玩家走查真驱动生产泵收敛[rev 沿移动]；转换 → 标志翻转零搬迁 + 重进 blob 核心区 9 全物化逐位 + 冲洗生产面[恰 1 行/行代次 1] + 再进行回灌仲裁胜利[流式编辑 + fixed 时代编辑双在]；r2028c QML 面钉[变更面集中：桥调用精确计数 WorldList x3/Main x2 + 五 Q_INVOKABLE 逐一正面钉 + 桥头零 Q_PROPERTY + world/会话最小新增面正面钉 + 玩法标记在位 + r2007b 复述 + 冻结域词元双 QML 禁触反探]）；前置回归全族（终版 binary）r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011-r2019/r2021-r2027=4P + r2012=5P + t874/t875=2P + t1023c=1P + worldgen 24P/determin 37P/re-gen 6P/store 57P/save 45P + vertex 15P/t860 2P/t879 2P/t893 1P/t965 3P/t857 1P/t1023 3P 全 0 FAIL 计数与 r2027 基线逐项吻合（**腿名跨任务词元去污染**：腿名首轮即避 save/store 词元；PASS 回显文本与腿名漂移[旧词元残留]首轮即修一致——回显文本与 filter 名恒等纳入自检）；阴性两轮恰红声明面（NEG-1 摘开关→创建链标志传递[flagNewWorldStreaming 体 if(false) 包裹落库——谎报已传]→恰红 {r2028b} 单腿[toggle 柱：flag=1 谎报而 row=0 → enter 走 fixed → sparse/通电/走查同源红；a 不误伤 fixed 墙不走标志传递/c 钉声明面不锚函数体——变异不可达性设计]；NEG-2 摘转换动作→API 接线[convertSaveToStreaming 体 if(false)]→恰红 {r2028b} 单腿[convert 柱：conv=1 谎报而 row=0；a/c 不误伤同门]）；四日志 matrix_r2028_neg{1,2}_{red,restore}.log 直落 build/ 终名[red 均 2P/1F EXIT=1 + restore 均 3P/0F EXIT=0]，变异字面 0 残留；app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2028_tail20.log 稳态 60fps[prof[1s] 60fr ×多窗 / frame main*16.0(60-62)] + F3 win 行 worker 0 与 r2027 基线同值 + 启动日志 500 chunk Models from 100 resident（fixed 世界默认路径零激活实证））
last_governance_review: 2026-09-17（audit #12 GREEN——W1b/W2/W3/W4/W5后端 五闭环 628→648/0；证据面铁律零违例第二窗；QML 全窗零触碰[W5b 例外面待启]、worldstore diff 空；W1b 22-pass 处置表清偿内容欠账；放行 W5b UI[r2028]→P5 实机调参；详见 governance-audit-2026-09-17.md）
governance_review_due: false
completed_tasks_since_governance_review: 1
next_task: P5 实机调参验收（用户设备会话；D4 双半径/首屏等待/生成风暴观测/worker 列量级等调参输入在 refactor-plan §29.5.4 + 各关单待实机节；代码侧无排队单——流式弧收官）
next_task_source: 用户会话指令 2026-09-16「直接开始做这几个功能继续做」——§29.5 W1-W5 五单串行推进全部闭环（W5b UI 收官）；P5 = §29.4/§29.5 相位族既定终点
active_write_lease: main_orchestrator_serial_queue
single_writer_policy: one project, one workspace, one writing agent, one serial task
retry_count: 0
needs_human: false
```

## Workspace Guard

- HEAD = §29.5-W5b 流式 UI 代码终态（fix(r2028) 91daf84 + test(r2028) 9f4c0ca 已提交；docs 提交随后），工作区干净；`.codex/` 豁免不删不提交。
- 纪律①-⑨全在案；**大 TU 编译一律 -j 1**（09-13 蓝屏教训；分段后单段增量 -j 4 实测安全）。**矩阵测试为 tools/matrix/ 分层结构**：改探针只重编对应段 TU（秒级）+ `--filter <substring>` 只跑本任务腿；新腿落对应段文件，新段置尾 runAll 末执行、须 ≤500KB；section11 起「自建 fresh 小世界」先例（48×48×96 seed 82 + 天气双钉 setWeatherState(0)+setWeatherRemainingSec(3600)）。
- **R20.06 起值类型纪律**：src/Core/ 新值类型一律 result.h QObjectFree 编译期钉 + 头内 static_assert；命令/事件队列满载拒绝与快照队列覆盖最老是两域容量策略分化，勿「统一」。
- **R20.07 起编排壳纪律**：GameSession 只做编排（命令 → 整 tick 边界 → World::setBlock 权威），禁复制游戏逻辑；**QML 现行玩法路径零变化是 R20 主线不变量**（Main.qml 含 "GameSession" 即违零迁移阴性钉 r2007b）。
- **R20.08 起 facade 纪律**：新代码世界读写统一走 WorldFacade（收窄视图）；mesher 零 Chunk*（chunkgeometry 禁出反探常驻）；GameSession 命令零旁路（m_world.setBlock 禁出反探 + m_facade.setBlock/setBlockWithState 正面钉，r2008c 常驻）；Facade 不转发静默写族。
- **R20.09 起编辑面纪律**：Tick 内编辑统一经 EditBuffer（src/Core/editbuffer.h）登记——同格同 Tick 合并后写胜、Tick 内零通知、收口 takeDelta 单点发布 + 合并面派生 BlockChanged；**dirty chunk 集合统一 DirtyChunkSet**（三态 add 可测；WorldDelta::addAffected 保守面不扩）；PlaceBlock 命令带 state 五参权威落地（4 参丢 state 口径退役）。ChunkGeometry/QML 渲染路径仍零触碰（按集合调度重建归 R20.13）；静默写族接线归 R20.10+。
- **探针钉纪律（R20.08 新教训）**：pinSet 失败条件 cnt<minCount → **0 计数 SrcPin 恒不红（空转钉）**；「禁出」钉必须走 minCount=1 反探（miss 非空=合规，复用剥注释器防注释提及误伤）或腿体内手工 contains（r2007b 先例）；新钉上 workload 前先做「变异→恰红」自证；**minCount 对实际出现次数核，勿拍脑袋翻倍**（r2009d x1<2 误设教训）。**阴性变异选址（t1051 新教训）**：`if (<cond>) continue;` 形态的守卫，摘守卫用 `false &&` 前缀 = 把守卫变**无条件执行**（t1051 首版变异把「摘无撑轨」变「摘光全部轨」，rails 31537→7 假红形态）——正确阴性 = 摘**调用点**（`if (false) guard();`）；行为面发现力靠语料内嵌复现体 seed（t1051a 内嵌 42/166/207）。
- **腿选址纪律（R20.08/R20.09 新教训）**：涉 chunk 计数断言的腿，选址必须**结构性保证**跨 chunk（cx 分驻不同值），不靠地形扫描碰运气；「破/放目标」须显式验证非空气/空气，**破位还须验证顶上无附着**（破表面块会级联清除其上 TallGrass——r2009c chg=4 首红实证，空操/级联都会塌事件计数）；腿 diag 带判别信息（pre/post id、逐事件、chunk 键）。
- **R20.10 起状态机腿/ WorldStore rebind 纪律**：状态机扫略腿的「每对转移探测」必须**态独立成立**（每次探测前经合法路径重布点），禁依赖前序转移后的残留态（r2010a 假红 22 项教训——合法转移接受后续扫=陈旧前置态比较）；WorldStore 换目标世界必须显式 `setWorld(&new)`，loadChunks 写入面 = m_world（r2010d 首版漏 rebind=blob 回灌旧世界教训）；World::setBlock post-emit clearAllDirty 是**全表无条件**（t155g 既有行为）——mesher 门跳过重建时脏标记照样被清，任何「窗内编辑留脏待重载消费」类断言不成立（r2010c 教训，挂起编辑可见性走再编辑重建差分）。
- **R20.11 起恰红面设计纪律**：**阴性恰红面设计先于腿文定稿**——辅助腿（对照/可替换性类）只钉**相对恒等（A≡B）与快照锚**，绝对计数/绝对序归被测语义腿专钉；否则摘语义的阴性变异会把红扩散到辅助腿成假阳性面（r2011c 首版绝对 `==4` 被 NEG1 误伤实证，相对化后恰红面收缩到 r2011a 单腿）。`false &&` 变异用于「使分支永不可达」类摘除（合并分支、守卫分支）恰当时 = 语义精确摘除，变异后必须核对 diag 形态即被摘语义本体（r2011b [stale hit=1]=过期 job 被执行）。
- **R20.12 起线程期钉/迁移恒等纪律**：src/ 树线程原语受认可落点 = **双文件白名单** {`src/World/backgroundgeneration.h`[R20.12 后台 GenerationJob], `src/World/meshworker.h`[r2020/D6 MeshBuilder 线程化执行器，生产零接线]} 且仅 std::thread（t1023c 事实钉已两度同变更修订：「白名单落点」语义[R20.12]→「双文件白名单」语义[r2020——纪律原文已预见「含 R20.13 mesher 线程化」的修订路径，本次为纠偏留痕非放宽：仍仅 std::thread、两 worker 均禁 QObject、每落点在场 ≥1 命中防空转]；新增任何线程原语文件/记号仍必须同变更再修订 t1023c + F3 行）；worldgen 生成逻辑单一权威 = `src/World/terraingen.h`（同 seed 同坐标逐位同果）——同步与后台任何生成路径**禁复制第二份**，迁移必须「逐行搬移 + worldgen/determin/re-gen 三腿族即时回归」；worker 侧零 QObject（audit #8 红线），跨线程值通道持 Error 聚合（Result<void> 不可默认构造，不作队列成员）；std::thread 成员必须声明在所触碰的锁/队列**之后**（成员声明序=构造序，否则线程早于锁就绪=UB）。
- **R20.14 起实体模拟单一权威/EntityId 双轨纪律**：掉落物模拟单一权威 = `src/Entities/entitystore.{h,cpp}`（EntityStore 非 QObject 值组件——itementitymanager 仅存过渡 Adapter 一行委托，**禁两份模拟逻辑并存**[r2014d 反探钉「模拟本体不回流」常驻：kMergeRadius/kGravity/acquireSlot/m_freeSlots/supportTopYAt 禁回流 Adapter 文件]）；EntityId（quint32 自 1 单调永不复用）与槽位下标（LIFO 复用=QML Repeater 承重契约）**双轨语义**——新实体族迁移沿用同口径；可见实体快照 = EntityStoreSnapshot（notify 沿+clearAll 重建，消费方值拷贝定格）；Adapter 的 notify 三沿语义（非批即 emit/批内标脏 endBatch 收口/clearAll 无条件直发**不 bump revision**）为迁移承重契约勿「统一」；ageLifetimeClock 测试缝生产零调用。
- **R20.13 起 mesher 单一权威/快照域纪律**：CPU 网格算法单一权威 = `src/World/meshbuilder.{h,cpp}`（MeshBuilder::build 纯静态 + ChunkMeshSnapshot 稠密快照 + ChunkMeshData owning 输出）——chunkgeometry 仅存适配面（采集→构建→灌注），**禁两份网格逻辑并存**（r2013d 反探钉「网格本体不回流」常驻）；快照 pad=2/列顶域 21 与 voxellight kMaxShadow **编译期互钉**（PCF 步进增大须同步扩域，否则软影截断漂移）；指向 mesher 本体文本的源码钉腿（t879/t893/review27-15/r2008c 门钉）随搬移**同变更修订钉面落点**（t1023c 先例），禁削钉；顶点统计断言口径 = triangleCount 即三角数（idx/3）非索引数；抽取后「旧新双路径」共享同一实现——恰红面按「变异落点 ∈ 谁的唯一消费域」设计（共享数学体变异归绝对断言腿[t1023b]、适配缝变异归相对恒等腿[r2013a]，相对恒等腿位序盲已登记）。
- **§29.4-P4 起渲染集模型纪律（QML 动态化）**：chunk 渲染存在面单一口径 = **驻留集 {Loaded, Active} = chunkLifecycleQueryable**（与 mesher 存在门同谓词）——「ChunkGeometry slot 存在 ⇔ mesher 门接受该 chunk」，任何渲染集语义演化只准改此口径单点（world.h setChunkLifecycle 选型注释 + chunklifecycle.h 谓词），禁第二份成员判定；驻留集 QML 读面 = World 三成员（residentChunkRevision Q_PROPERTY + residentChunkCount/residentChunkKeyAt Q_INVOKABLE），**生命周期决策零 QML**（setChunkLifecycle 非 Q_INVOKABLE 永久，r2010d 延伸 + r2021d 反探常驻）；枚举序单一权威 = world.cpp residentChunkKeysOrdered（cz 外 cx 内 = 旧固定网格序，r2021a 恒等口径；count/keyAt 双消费面禁第二份循环）；Main.qml slot 池消费面 = rebuildChunkSlotPool（整池重派生保 segmentsPerChunk 组对齐不变量——_refreshChunkVisibility/kickWorldMeshSync 两消费端零触碰是 QML 变更面集中的承重选择，增量 patch 归生产接线单）；**Main.qml 全文件禁出字面**（含注释）：GameSession[r2007b 常驻]/MeshWorker[r2020d 裸 contains 常驻]，代码面（剥注释）禁出 setChunkLifecycle[r2021d]/ChunkEvictor/ChunkStreamDriver[r2019d]；**腿名/diag 文本零跨任务 filter 词元**（r2018 先例 + r2021 首跑 r2007=6P/r2010=7P 词元污染实证，去词元后达标——新腿写完先跑相邻任务 filter 计数核对）。

## Recovery Point

- 最近闭环：**§29.5-W5b UI 流式界面接线**（2026-09-17，fix(r2028) 91daf84 + test(r2028) 9f4c0ca + 本 docs）：§29.5 登记的 QML 例外单（治理审计 #12 放行；此前全弧 QML 零触碰，本单为唯一登记新增面；矩阵 648→651——**流式弧 W1→W5b 全线 COMPLETE**）。**设计选型（头注释立此存照，详录 = dev-plan W5b 关单条目）**：①**桥形态 = Game 层 StreamingBridge**（QML 单例 BuildInfo/FrameProfiler 同款 QML_SINGLETON+instance——两段 QML 零元素实例化即按类型名调用；**QML 面仅五 Q_INVOKABLE**：flagNewWorldStreaming[创建链标志落库]/saveIsStreaming[标志读面]/convertSaveToStreaming[D3 opt-in 转换]/enterWorld[进入分流]/flushForSave[保存链前置冲洗]；**值组件零暴露**：零 Q_PROPERTY、ChunkStore/StreamWorldMeta/会话类型不出 C++ 面[r2022d 同门]）；②**变更面集中两段 + 两接线 hunk**：WorldList.qml 四 hunk[勾选默认关 + 创建链传递 + 转换动作 + 确认行——convertingFile 锚定文件名免重置钩子] + Main.qml 两 hunk[enterWorld 问桥一次分流 + runExitSave 首行冲洗——onClosing 关窗路径同函数同门覆盖]；「存档按钮本体/输入/交互/HUD」零字节触碰；③**进入分流语义**：fixed 存档恒 false[桥内顺带清流式残留：会话终结 + reinitializeAsFixed 空网格归位，既有 beginLoad/regenerate 分流逐字节原样]；流式存档 true[五拍主线程同步：sparse 重构[reinitializeAsSparse = sparse ctor 体单一权威委托——构造/运行期共用 sparseGenerate] → 会话构造[isSparse 门内 W2/W3/W4 三缝全通电] → 绑定[bindChunkEditsStore + setStreamWorldId=file] → loadStreamingWorld overlay → 泵拍/位置沿挂钩[幂等连接——WorldClock::ticked 与 QML tick 桥同拍同源单时钟；playerChunkChanged 直连 notePlayerChunk]）；④**生产泵拍 = GameSession::pumpStreamingFrame**（新公开缝只泵 tick 尾流式收割拍——**World 模拟家族仍由 QML tick 桥独占驱动**，「玩法路径零改动」不变量的会话侧承重面；无头全语义入口 stepTick 不变[矩阵腿族消费]）；⑤**保存链冲洗 = runExitSave 首行 flushForSave**（非流式会话恒 true 零动作[fixed 三写链逐字节原样]；流式 = flushResidentEditsForSave 成败穿透[失败短路三写不谎报——t974 完成门重试/toast 兜底复用；重试重放 = 已落盘行同键盖写无副作用]）；⑥**最小 C++ 新增面**：World::reinitializeAsSparse/reinitializeAsFixed[C++ only 非 Q_INVOKABLE——r2022d 同门；reinitializeAsFixed = 模式位归位 + 稠密空网格，调用序契约 = caller 随后必接 beginLoad/regenerate 全量卫生重置] + ChunkManager::reinitializeFixed[recreate 体复用对偶面] + pumpStreamingFrame；⑦**worldstore 零触碰**：saves 目录三级解析在桥内逐行镜像[登记的镜像面] + 绝对路径直通[矩阵临时库先例]——两处一致性由 r2028b 真链行为锚定；chunkstore.cpp 自本单入 app 目标（会话首度生产实例化，W3-W5 期间仅矩阵目标直编）。**文案（§29.5 登记：用户可 md 纠偏）**：开关名「无限世界」；转换动作「转为无限世界」；确认文案 = 「转为无限世界？核心区原样保留；境外按需生成、编辑自动保存；已卸载区的实体不保留。（可翻回固定世界，界面暂不提供入口）」（三点[核心区保留/境外按需生成+编辑自动保存/卸载区实体不保留=W3 局限如实告知] + 可翻回说明[翻回不设 UI 登记]）。**腿（section31 置尾，filter 词 r2028，真临时库 fresh+用后即删 saves/ 零触碰）**：r2028a 默认关零变化墙 / r2028b 开关+转换通电面[real-chain 单腿双柱] / r2028c QML 面钉（详录 = 上方 last_verified_commit）。**验证链全绿**：分段增量构建（-j 4；矩阵 + app 双 binary mtime > 全部改动源）→ filter r2028 首跑 2P/1F 即修一处腿文坑（wrapper JS 函数缺 return——落库本身已成功[row 断言全过]，断言面误读 undefined）→ 3P 三连跑绿 → 前置回归全族计数与 r2027 基线逐项吻合[**腿名跨任务词元去污染**：腿名首轮即避 save/store 词元[create-enter-save-load→persist-load、restored→reloaded、saved→persisted]；PASS 回显文本与腿名漂移[旧词元残留]NEG-1 轮发现即修——回显与 filter 名恒等纳入自检] → 阴性两轮恰红声明面（NEG-1/NEG-2 详录 = 上方 last_verified_commit；恰红面先于腿文设计：NEG 落点选桥函数体 if(false)——行为级红面单腿精确[摘 QML 行只会红源钉]）→ 双 Edit 反向还原回绿（四日志 matrix_r2028_neg{1,2}_{red,restore}.log 直落 build/ 终名 2P/1F+3P/0F ×2，变异字面 0 残留）→ 全矩阵 **651/0 ×2**（pos/final EXIT=0；648 权威 diff = +3 恰 r2028a-c + t813 戳/哈希登记类[12:00/3414b0c→13:59/b04aa06]；pos/final 腿集合恒等 651[前 110 字符键 diff IDENTICAL]；^FAIL 归类 0）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2028_tail20.log 稳态 60fps[prof[1s] 60fr ×多窗 / frame main*16.0(60-62)] + F3 win 行 worker 0 与 r2027 基线同值 + 启动日志 500 chunk Models from 100 resident（fixed 默认路径零激活实证）。**登记非目标**：翻回 UI（后端 clearStreamingWorldFlag 已备，界面不设入口）；流式参数调试面板（P5——configureStreamingRadii 即调参缝，桥不暴露）；文案润色迭代（用户 md 通道）；世界删除时的流式表清理策略（既有登记）；despawn 半径（既有登记）；保存流统一经 SaveCoordinator（既有登记）；worldChunksPerSide 运行期变更（当前常量 10——reinitializeAsFixed 归位 dims 取同源值，变更面登记）。**登记边缘（如实）**：标志读败（库锁/病）→ 诚实降级走 fixed 链 + qWarning 留痕[原生流式世界该边缘下照 fixed 玩，其流式行经仲裁仍胜 blob——下次进入恢复流式语义]；新流式世界跨世界 spawn pristine 边缘与 fixed 同 seed 连续进入既有行为同面[登记待实机]。**待实机确认**：见 dev-plan「全弧待实机确认汇总（流式弧收官）」节（本单新增：创建无限世界观感/走查生成/卸载无感/存读往返/转换往返五项）。**下一任务 = P5 实机调参验收（用户设备会话，无代码排队单）**。

- 最近闭环：**§29.5-W5 后端 流式世界持久化 + D2/D3 标志 + overlay 合并**（2026-09-17，fix(r2027) + test(r2027) + 本 docs；W5 后端半——QML 零触碰，UI = W5b 登记例外面；矩阵 644→648）。**设计选型（头注释立此存照，详录 = dev-plan W5 关单条目）**：①**D2 = stream_worlds 附加表**（chunkstore 域 additive：world TEXT PK/streaming/core_w/core_d/base_gen/save_gen；幂等建表零 bump；世界行经现有 Q_INVOKABLE 面创建；fixed 世界零标志零活动 = r2027a 库面级实证[fixed 全链后库中无任何流式表]）。②**chunk_edits 行代次契约 W5 修订（留痕非破坏）**：per-chunk 累计 → 世界保存代次刻度（stamp = save_gen+1）；观测值兼容 = 无 meta 行库 stamp 恒 1 与 W3 已观测值逐位同值（r2025b/c gen==1 断言零扰动实证）。③**保存时冲洗 = GameSession::flushResidentEditsForSave**：保存链三写前置挂点（W5b 接线面）；拍序 = 先冲洗后代次推进（行盖写 save_gen+1 → advance := 同值 → 「行代次 = 本次保存代次」精确成立；推进失败行不失效——仲裁只对 base_gen marker 同门）；冲洗域 = 驻留集 sparseResidentKeysOrdered × dirtyQuery（persistResidentChunk 唯一执行体 = 驱逐与冲洗共用）；失败聚合上报不谎报；流式持久化形态 = 玩家/进度 blob 现行走 + chunk_edits 全量（未编辑地形块不存，seed 确定性重生成承载 = W1b parity 正确性根基）。④**读档合并 = GameSession::loadStreamingWorld**：行全量回灌（restoreChunkFromBlob 跳 population）+ D3 核心区 blob 物化经现有只读 Q_INVOKABLE 面 hasChunks/loadChunks（铺槽[仲裁胜者除外]→memcpy→取字节→⑥⑦擦槽让位→restore 全链；全零槽 = 部分 blob 降级留 Absent 重生成）；**仲裁 = 代次对代次**（行代次 ≥ base_gen 行胜[同代=表胜]/ < base_gen blob 胜[老时代行让位再转换锚]/ 无 blob 行全胜；base_gen = 置位锚 max[save_coord complete_generation 只读消费, 既有 save_gen]；fixed 生产保存不推进 save_coord → 首转换锚 0 = 时序真值；翻回期 fixed 重存不可见 = 保存流统一 W5b+ 登记项）。⑤**D3 转换 = 纯元数据零搬迁**（markStreamingWorld 置位+core dims+代次锚 / clearStreamingWorldFlag 仅翻标志行保留；逆翻 = fixed 语义结构性不识 chunk_edits；仲裁双相位直证：抬锚 blob 胜 / 平锚表胜）。⑥**world.{h,cpp} 零 diff**（W3 既有面即读档链全部所需 = W3 面即接线面兑现）。**腿（section30 置尾，filter 词 r2027）**：r2027a fixed 零活动墙[元数据域 API 全 fail-safe + 三写链 saveOkCount==3 + fixed 读档往返栅格逐位 + 库面零流式表]；r2027b 流式存读往返承重墙[三区编辑 → 冲洗恰 3 行/行代次=1/账清 → 销毁重建 → 编辑块表回灌逐位 + 未编辑块走查重生成逐位（含树签名）→ 追加生成走 W2 → 二次冲洗行代次=2 → 三读四区恒等（二次幂等）]；r2027c D3 转换往返[fixed 三编辑 → blob → 翻转 → 生产同构首进 blob 9 全物化逐位 → 流式编辑冲洗 → 再读档仲裁三态 + 境外孪生恒等 → 抬锚 blob 胜/平锚表胜 → 逆翻 fixed 读档逐位 + chunks 表零触碰]；r2027d 结构钉[worldstore/world 对禁触反探 + additive 正面钉 + 冲洗失败不谎报（真锁 → false + 零部分冲洗 + 账原样 + 代次未动）+ 未登记 fail-safe + QML 零触碰 + 会话缝面/位序钉（落盘循环 < 代次收口；blob < 行回灌）]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime（12:00）> 全部改动源 → filter r2027 首跑 0P/4F 即修三处腿文坑（①placeTop 用生成器纯噪声 heightAt（海洋列与体素顶脱节）→ heightmapAt + 植被冠层换列扫；②`ok && setup` 链短路致后续会话 setup 未执行 → 绑定无条件化；③r2027b 会话 2 未驱动走查即比对未编辑块 → 补走查收敛[按需生成是语义本位]）→ 4P 三连跑绿 → 前置回归全族 0 FAIL + **腿名跨任务词元去污染**（r2021 教训三次兑现：save 45→49/store 57→61/determin 37→38 漂移 → 双向去词元后 45/57/37 精确复位）→ 阴性两轮恰红声明面（NEG-1 摘保存时冲洗[落盘循环 if(false)]→{r2027b, r2027c}[b 编辑块丢失/c 零行仲裁落 blob；a/d 不误伤——候选 {r2027b}+c 同住冲洗通路同源红如实双面声明]；NEG-2 仲裁 `||`→`&&` 旧胜→{r2027c} 单腿[b 原生无 blob 分支不经被变异 rhs/a fixed 不可达/d 钉面不锚被变异字面]）→ 双 Edit 反向还原回绿（四日志 matrix_r2027_neg{1,2}_{red,restore}.log 直落 build/ 终名 2P/2F+4P/0F+3P/1F+4P/0F；变异字面 0 残留）→ 全矩阵 **648/0 ×2**（pos/final EXIT=0；644 权威 diff = +4 恰 r2027a-d + t813 戳/哈希登记类[08:43/f896c14→12:00/3414b0c]；pos/final 腿集合恒等 648[前 110 字符键 diff IDENTICAL]；^FAIL 归类 0；chunkstore removeDatabase 告警 = W3 既有登记噪声 127→242 登记类）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2027_tail20.log 稳态 60fps[prof[1s] 60fr ×3 / frame main*16.0(60)] + F3 win 行与 r2026 基线逐字同值 + 启动日志 500 chunk Models from 100 resident（fixed 世界零激活实证）。**登记非目标**：UI（W5b）；删除流式世界清表策略；despawn 半径；性能调参（P5）；保存流统一经 SaveCoordinator（W5b+）；部分 blob 降级逐槽粒度。**待实机确认**：无新增（后端零 UI 入口生产零激活由冒烟实证；W5b 复验项：无限世界走查观感 + 走离回走内容与编辑仍在[存档往返真机面] + D3 菜单动作观感）。**下一任务 = §29.5-W5b UI**（「无限世界」开关[默认关] + D3 世界菜单接线 + 保存链冲洗接线[后端 API 已齐备]）→ P5。

- 最近闭环：**§29.5-W4 bake→worker 网格化**（2026-09-17，fix(r2026) e984cc7 + test(r2026) ef631d3 + 本 docs）：D6 执行器自本单起生产接线（refactor-plan §29.5.2 W4 原文；矩阵 640→644；性能口径 = §29.5.4「meshing 量随视距增长」）。**设计选型（头注释立此存照，详录 = dev-plan W4 关单条目）**：①**通电条件 = World::chunkMeshAsyncActive()（sink 已绑定 ⟺ sparse ∧ 流式会话在）**——GameSession isSparse() 门内构造 MeshWorker（生产零参队列 64）+ setChunkMeshBuildSink 注入（std::function 缝——执行器类型零泄漏进 World/Renderer 头）；fixed 世界连绑定都不发生 → 几何现行同步内联路径逐位原样（零变化墙）。②**执行器所有权 = GameSession**（bake 发起点经 World 桥可达 + 收割拍 = pumpStreamingTick 同宿——两准绳承重面；R20.07 编排壳纪律）。③**bake 三段**：主线程定格快照（capture 调用与同步路径同一行，r2013d 钉面原样）→ requestId 派生提交（hashMix64(packed)<<16 | 段位 3bit[六段独立 bake] | 提交代次 13bit[mod 8192 ≫ 在途上界 128——纯 chunk 键同段连发会同键互撞，在途互异是最新胜判据与注册表 1:1 交付承重前提；哈希域 hashColumn 同门单射假设，碰撞最坏自愈]）→ pumpStreamingTick ⑤ 段收割单点（takeBuilt 排干 → World::deliverBuiltChunkMesh 注册表路由 → QPointer 守卫回调灌注；非阻塞、延迟 ≤ 一 tick、QML 零改动）。④**requestId 幂等/覆盖双保险**：组件不查重不合并（meshworker.h 语义原样）+ 路由层重提交显式 cancelChunkMeshJob（旧产出交付 miss → droppedBuiltMeshes 可见丢弃）+ 交付回调自证 pendingId（latest-snapshot-wins 第二道闸 meshNstale）；sun/dayMul 账本记**采集时刻定格值**（applyChunkMeshData 形参——应用时刻成员可能已前移，误记破 sun 量化门）。⑤**同步回退 = 拒绝即内联**：kErrQueueFull/202 已停 → meshNsyncFallback 计数 + qWarning + 现行同步路径；空快照（无 chunk）/解绑 sink 走同步非回退不计数；worker 析构丢弃 = droppedCount 组件可见 join 有界；W3 驱逐×在途 = QPointer 守卫自然失效（行为登记）。⑥**World 桥面**（C++ only 非 Q_INVOKABLE，r2022d 同门）：六方法 + 注册表纯主线程零锁有界（每接受恰一次交付擦除）+ kErrChunkMeshSinkUnbound=203 执行域续位。⑦**F3 worker 列**：frameprofiler.cpp win 行 mesh 括号后 `worker N`（meshNworker = 交付应用计数；列恒在 fixed 恒 0；worker 构建按 Reason::Dirty 计入 meshN 族——分桶差异登记；mesh 桶 = 提交相采集 + 交付相灌注不嵌套，构建段离场主线程即异步化收益）。⑧**同变更钉面修订两处（纠偏留痕非放宽）**：r2020d 记号白名单 {meshworker.h}→+gamesession.h（chunkgeometry 双件/Main.qml 反探原样——几何 token-free 可达）+ t1023c F3 threads 行互锁叙述如实化（fixed 世界事实；断言集不变、白名单扫描零新落点——复用 meshworker.h，t1023c 不新增落点承诺兑现）。**QML 零改动/worldstore 零触碰/MeshBuilder 单一权威零触碰（r2013d 反探常驻）/fixed 生成语义零触碰**。**腿（section29 置尾，filter 词 r2026）**：r2026a fixed 零变化墙[零构造 + apply-before-return + 字节恒等 + 计数恒零]；r2026b 异步等价承重墙[真执行器 + 四段几何（3 terrain+水段——段位折叠承重；seed 42 该 chunk 无水→空网格，应用事实由应用计数恰 +4 承载）→ 收割应用 ≡ 同步直调逐位恒等（组件级等价墙端到端化）+ revision 沿零漂移 + 账面对账恰等]；r2026c 回退与卫生[最新快照胜（编辑 worldChanged 沿=生产自动提交面；A 注销 miss 恰 1 + B 应用恒等；独立执行器+手工排干=NEG-1 独立性设计）+ 满载/已停拒绝回退（clearMesh 清零防同态 mask）+ 解绑回固定门不计数 + clearMesh 注销卫生]；r2026d 结构钉[F3 worker 列正面钉+行为级 flush 报告钉+QML 零触碰反探+收割拍单点收口（takeBuilt 调用面全 src 恰两文件）+ fixed 零构造源序钉+World 桥面钉+单一权威兼容反探]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime > 全部改动源 → filter r2026 首跑 3P/1F 即修三处腿文坑（①水段顶点数作收敛判据→改应用计数；②显式 refreshMesh 叠提交→归因 worldChanged 沿驱动；③相 1/相 2 同态 mask→clearMesh 清零）→ 4P + 三连跑绿 → 前置回归全族 0 FAIL + **腿名跨任务词元去污染**（r2021 教训兑现：首版腿名含 r2013a/r2013d/r2020a → r2013/r2020 filter +2/+1 漂移，去词元后 4P/4P 复位）→ 阴性两轮（NEG-1 摘收割应用→恰红 {r2026b} 单腿=声明面；NEG-2 摘同步回退→**首跑未红=mask 实证**→腿文加固后恰红 {r2026c} 单腿——恰红面先于腿文纪律二次兑现）→ 双 Edit 还原回绿（四日志直落 build/ 终名，变异字面 0 残留）→ 全矩阵 **644/0 ×2**（pos/final EXIT=0；diff = +4 恰 r2026a-d + r2020d/t1023c 修订行 + t813 戳登记类；pos/final 腿集合恒等 644）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + tail20 稳态 60fps + F3 既有字段逐字同值 + worker 恒 0。**登记非目标**：多 worker 扩并发/优先级调度；W3 驱逐×在途竞态（自然失效行为登记）；QML 任何改动；结果缓存/收割节流。**待实机确认**：无新增（W5 复验项追加：无限世界走查中破/放贴图刷新时延观感[同步 <1 帧 → 一 tick 内交付]、F3 worker 列量级、mesh 桶占比）。**下一任务 = §29.5-W5 D2 参数开关 + D3 opt-in 转换**（新世界 UI「无限世界」开关[默认关；QML 例外登记] + 世界 meta streaming 标志 additive 零迁移 + D3 世界菜单 opt-in 转换 → blob 区成核心区境外按需生成经 W3 落盘）→ P5。

- 最近闭环：**§29.5-W3 驱逐 + Edits-on-evict 落盘**（2026-09-17，fix(r2025) cc47e1a + test(r2025) 526cfc0 + 本 docs）：W2 流式会话 = 接线宿主，setEvictor 从 null 转正（refactor-plan §29.5.2 W3 原文；矩阵 636→640）。**设计选型（头注释立此存照，详录 = dev-plan W3 关单条目）**：①**D5 载体 = 新文件 src/World/chunkstore.{h,cpp}**——per-chunk 附加表 `chunk_edits(cx, cz PK, generation, voxels, states, light)`，独立命名连接开-用-关（savecoord kCoordConn 先例）、IF NOT EXISTS 幂等建表**零 bump**、旧码盲/缺表 Fresh、worldstore 一行禁触；blob = 驱逐时刻三数组原样字节（本表自洽）；**代次 = per-chunk 累计**（upsert 前读旧行 +1，单语句事务原子即 marker——「没写进 = 旧行在旧代次」）；**持久化时机 = 驱逐候选 dirty 即时落盘**（不走整世界 saveAll）。②**persist 域脏面 = 新 additive 权威**（盘点：mesh 域 markDirty[t155g 全表清]与 EditBuffer tick 窗口域均不可判「未落盘」）——ChunkManager 未落盘编辑集合：setBlock 5 参**单漏斗**标记 + RAII UnsavedEditWriteWindow 抑制窗（深度计数嵌套）罩三条生成链（fixed generate/sparseGenerateChunk/adoptGeneratedChunk）+ recreate/reinitializeSparse 清空；persist 成功即清账。③**三缝生产绑定**（GameSession sparse 门内）：dirtyQuery = 脏面转发；persistFn = ChunkStore 即时落盘（失败 Result 穿透 → ChunkEvictor 顺序铁律中止驱逐 = P3 宁驻留不误删；未 bind 同路径 fail-safe）；lifecycleTransition = setChunkLifecycle 真转移 ⑥⑦（revision 沿自动携带）+ **转移缝两生产语义**：边⑥前实体移除（setEvictionEntitySink std::function 注入零 Entities 类型依赖）、边⑦后 releaseStreamingChunk 擦槽（**数据面闭合**——残留槽会让重物化走「applyGeneratedChunkData 空气零写」复活陈旧内容；population 脚手架拆卸同门）。④**重载路径**：savedContentQuery = hasChunk（行只增不删命中稳定）→ Load kind 照常走边①② → 收割拍数据面按表命中路由：命中 restoreChunkFromBlob 直接物化（**跳过 sparsePopulateChunk**——存档内容已是终态含 population，重放 = W1b 读域时刻一致性铁律反面 + 编辑丢失；尺寸守卫失败 ⑨ 回 Absent；自身列索引重建[空脚手架形态+火格自扫] + refloodBox 列种子光）/未命中 W2 生成路径/命中读败降级 + qWarning。⑤**实体语义 = 驱逐区活体先于转移移除**：EntityManager/EntityStore despawnInChunk（ChunkKey::fromWorld(floor pos) 归属 + 既有 releaseSlot 语义[t978 幂等/free list/Repeater 槽稳定] + 批量单通知）+ ItemEntityManager 一行委托[R20.14 权威纪律]；**MC 引证三元组**（wiki Chunk "Unloaded chunks are unprocessed... do not process any of the game aspects" / 同页 player-ticket entity tick 资格 + "unloaded before being 128 blocks away" / Chunk_format 1.17 "Entities...stored in separate entities directory"）；**局限如实登记**：实体不入档 → 移除即不回（MC 视角 ≈ despawn 而非 unload）、despawn 半径零接线。⑥fixed 世界连 ChunkStore 都不构造 = 零活动墙最强形态（r2025a 钉 + app 冒烟实证）。**QML 零改动/worldstore 零触碰/线程原语零新增/fixed 生成语义零触碰**。**腿（section28 置尾，filter 词 r2025；sparse 80×80×96 s42 radius 0 + fixed 48×48×96 s82 + 真临时库 fresh+用后即删）**：r2025a fixed 零活动墙[三构件连构造都不发生 + bind 面 null 拒 + 真 PlayerController 沿扫掠 × 12 tick 全状态逐位不动]；r2025b 驱逐回灌承重[persist 先于转移调用序柱[轨迹 PersistOk<EdgeEvicting<EdgeAbsent 带键] + ⑥⑦擦槽 + revision 沿 + 重入命中 blob 物化全 16×16×H 逐位恒等[编辑+population 终态都在 = population 跳过面承载] + 未编辑 chunk 重derive vs fixed 孪生掩蔽恒等[W1b 登记面：ore 块缘/结构足迹/块缘列] + 重载起点 clean + 二次驱逐零落盘代次恒 1]；r2025c 失败中止[真锁 BEGIN EXCLUSIVE → 保持驻留 + 内容/脏账原样 + PersistFail 零转移事件 + aborted 计数 + 解锁重报收敛恰一落盘] + 实体语义[驱逐区 mob+掉落物先移除恰净、gen 窗见证者不误伤]；r2025d 结构钉[worldstore 三词元禁触 + additive 正面钉 + 零 bump/零破坏 SQL 反探 + 先落盘后转移源序钉 + 会话缝面/位序钉 + restore 函数域禁 population + 实体单漏斗钉 + QML 五 token 反探 + fixed 墙]。**同变更钉面修订（纠偏留痕非削钉）**：r2019d 零接线反探——gamesession.h 从「ChunkEvictor 禁出」名单退役（W3 授权接线点）改正面钉成员+三缝绑定；**r2016d 免修订留痕**：setEvictor 形参 generic lambda——GenerationPolicyEvict 记号不入 gamesession.h。**验证链全绿**：分段增量构建（-j 4）→ binary mtime > 全部改动源 → filter r2025 首跑 1P/3F 即修四处（①r2025c 目标沿 cheb((3,2),(6,6))=4 超 scan 3 不入 toEvict[算术错]改 (6,5)；②r2025b 未编辑 chunk 全逐位柱内陆差 59 体素——twin 归因证实全落块缘 ore/carve 上下文敏感面[W1b (c) 登记]，柱改锚 fixed 孪生+登记掩蔽，blob 柱保持全逐位；③r2025d restore 函数域钉被自家注释字面误伤反探 → 注释改写；④r2016d 零记号反探被形参字面误伤 → generic lambda）→ 4P 三连跑绿 → 前置回归全族绿（store 57P[+3]/save 45P[+1] = 本单腿文词素 substring 先例逐腿归因）→ 阴性两轮（NEG-1 摘先落盘后转移序→{r2025b, r2025c, r2025d}[序柱+中止语义+序钉三面同源，如实改声明留痕；a 不误伤]；NEG-2 摘命中路由→{r2025b} 单腿[identity-blob 编辑丢失实证]）→ 双 Edit 还原回绿（四日志直落 build/ 终名，变异字面 0 残留）→ 全矩阵 **640/0 ×2**（pos/final EXIT=0；diff = +4 恰 r2025a-d + t1023c 文件计数 148→149 + t813/t979/t997 登记类；pos/final 腿集合恒等 640）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + tail20 稳态 60fps。**登记非目标**：跨会话 blob×附加表 overlay 合并（W5/D3——本单只做会话内闭环）；ChunkStore 生产 bind（W5）；hasChunk 连接经济学/Load job worker 空跑（优化面）；despawn 半径；驱逐轨迹无界增长（P5 观测面）；实体重载恢复（不可达面）。**待实机确认**：无新增（W5 复验项追加：无限世界走离回走——内容与编辑仍在）。**下一任务 = §29.5-W4 bake→worker 网格化**（ChunkGeometry bake 段改提交快照 + 收割应用；同步内联回退开关；F3 mesh 行加 worker 列；QML 零改动）。

- 最近闭环：**§29.5-W2 位置源 + 驱动接线**（2026-09-17，fix(r2024) 8330651 + test(r2024) 67d3e9c + 本 docs）：流式激活第一次生产通电（refactor-plan §29.5.2 W2 原文；矩阵 632→636）。**设计选型（头注释立此存照，详录 = dev-plan W2 关单条目）**：①**通电条件 = World::isSparse()**（D2：fixed 世界连驱动器/worker 都不构造——双 unique_ptr 恒 null，零活动墙最强形态，app 冒烟同面实证）；驱动器归属 GameSession（R20.07 编排壳），成员声明序 worker 先 driver 后 → 析构逆序 = scheduler 先于 worker 析构（review0916 #8 契约结构性成立）；②**位置源零 QML**：PlayerController tickImpl env 桶 floorDiv16 换格沿（ChunkKey::fromWorld 单一权威 + 哨兵 INT_MIN 起始沿 + setWorld 重置；t1000/t1020 进入沿先例同位）→ playerChunkChanged → GameSession::notePlayerChunk 直连；③**组件最小扩展（默认未设 = r2018/r2019 逐位等价）**：driver setSlotEnsure[submit 前槽位物化缝——sparse 边①的槽位存在性前提：ChunkManager::setLifecycle 无槽位拒]/attachLifecycleSink[r2011 预留 ChunkManager 挂点首用转发；引用形参使 r2018d「ChunkManager *」反探字面保持绿]/enableStreamingWith + streamingWithDefaultRadii[P1 默认半径 4/4/6 单一来源；GameSession 接线面零策略类型记号 = r2016d 反探免修订] + generationjob setChunks[ctor 形态不变，null = 纯请求模型零变化]；④**生命周期三边分工**：边①（交接→Loading）/边②（收割→Generated）由 scheduler 挂点真实驱动（World::streamingLifecycleSink() 提供挂点）、边③（Generated→Loaded）由 World::adoptGeneratedChunk 经 setChunkLifecycle 唯一入口收尾（驻留 revision 沿自动携带 = P4 池消费面连通）；⑤**生产执行体** = BackgroundGenerationWorker 原样复用（worker 线程 generateTerrainChunk 纯函数造缓冲，零 World 指针零 QObject）→ 主线程收割拍 World::adoptGeneratedChunk（applyGeneratedChunkData 守卫入口逐格落地 + heightmap 重算 + ③晋升 + sparsePopulateChunk 主线程窗口重放 + refloodBox 列种子光，loadChunkAt 同门）；⑥**#7 硬契约兑现**（review0916 #7）：GameSession::pumpStreamingTick（tick 尾，m_edits.clear() 之后——落位为 worldgen 同门静默写不触已收口编辑账面）内结果面[takeOutcome]与数据面[takeResultData→adopt]同拍并列双消（函数域钉）；数据面 = 主线程落位唯一通路；⑦**在途流式邻居守卫**：sparsePopulateChunk 对 Loading/Generated 邻（异步在途槽）走快照-回填-恢复同门、不走脚手架创建/拆卸（⑥⑦+擦槽会拆在途槽位；数组此刻恒空 → 快照=全零→纯地形回填呈 pass-k 读域→恢复归零→其自身 adopt 落缓冲逐位重 derive；W1 同步链无在途槽位 = 死分支，r2023 腿族零扰动）；⑧**采纳语义登记**：数据面无条件采纳（在途被取消的 job 照常完成+采纳——内容已算出；离场驻留的回收 = W3 驱逐面）；slotEnsure 先于 submit（被拒请求留 Absent 槽 = 重请求面）。**QML 零改动/worldstore 零触碰/线程原语零新增落点（复用 R20.12 件，t1023c 白名单原样）/fixed 生成语义零触碰**。**腿（section27 置尾，filter 词 r2024；sparse 80×80×96 s42 radius 0 + fixed 48×48×96 s82 孪生）**：r2024a fixed 零活动承重墙[fixed 会话 streamingActive false + 双 null + 真 PlayerController 跨 ≥3 次 chunk 边界扫掠 × 12 整 tick → 零生命周期/revision/outcome/adopt 活动 + 九态表/驻留账/抽样列全逐位不动]；r2024b 通电走查承重[真走查 × 真驱动 × 真线程：窗内 8 chunk（中心 radius-0 预生成）①②③真实驱动到 Loaded + OOB 门翻真 + residentChunkRevision 沿 + 驻留恰 +8/+17 + 走离半径恰 9 取消 + 被取消窗收敛 + 抽样全 y 带 id+state 恒等 fixed 孪生[r2023b 同款 ore+结构 9 点栅格掩蔽，cleanEqual≥200 非空转]]；r2024c 真线程收割 + #7 契约 + 背压[线程身份 observedExec==workerThreadId≠调用者线程（r2012a 先例；落位在调用者线程）+ 每 job 恰一数据一条 outcome 恰等 8/8/8 + 收割拍双面排干零积压 + 9×9 窗 81>64 恰 17 拒绝（距离秩升序→字典序末位 (24,24) 必拒保 Absent）+ 半径收窄重请求重生成 (24,24)→Loaded + 收尾账面零残]；r2024d 结构钉[会话通电门/双缝接线/固定早退/位置沿喂入/双面 while/单一 adopt 落点正面钉 + #7 函数域同拍钉 + world.cpp adopt 落位链/在途守卫钉 + generationjob setChunks/driver 转发钉 + QML 四 token 反探 + worldstore 四 token 禁触反探 + playercontroller 沿检测/信号/哨兵重置钉]。**同变更钉面修订（纠偏留痕非削钉，t1023c/r2019 先例）**：r2018d 零接线反探——gamesession.h 从「ChunkStreamDriver 禁出」名单退役（W2 授权接线点，plan §29.5.2 原文），改钉 streamingWithDefaultRadii() 正面在场；world.{h,cpp}/playercontroller 双件/Main.qml 禁出原样 + PASS 文本如实化（恰红面零扩散：r2018a-c 全绿）。**验证链全绿**：分段增量构建（-j 4）→ 双 binary mtime > 全部改动源 → filter r2024 首跑 2P/2F 即修两处腿文坑（①radius-0 构造仍预生成中心 chunk (2,2)→请求恰 8 非 9/驻留恰 +8；②抽样柱命中结构族足迹→补 r2023b 同款掩蔽）→ 4P 全绿 + 三连跑绿 → 前置回归终版 binary 全族 r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2021=4P×11 + t874=2P + t1023c=1P + worldgen 24P/determin 37P/re-gen 6P/store 54P/save 44P 0 FAIL（计数增长均为腿名同词素 substring 先例）→ 阴性两轮恰红声明面（NEG-1 摘位置沿喂入→{r2024b, r2024c}[任务书候选面 = r2024b；c 同住生产接线同源红，双面如实声明]；NEG-2 摘 #7 数据面同拍消费→{r2024b, r2024c}[任务书候选面 = r2024c m_data 积压红 qData=8 实证；数据面 = 落位唯一通路 → b 收敛同源红，扩面按 t1053/r2021 先例改声明留痕]；r2024a/d 不误伤）→ 双 Edit 反向还原回绿（matrix_r2024_neg{1,2}_{red,restore}.log 直落 build/ 终名；if(false) 变异字面 0 残留）→ 全矩阵 **636/0 ×2**（pos/final EXIT=0；632 权威 diff = +4 恰 r2024a-d + r2018d 同变更修订行 + t813 戳/哈希 + t997 墙钟登记类；pos/final 腿集合恒等 636[唯一差 = t997 墙钟数字]；^FAIL 归类 0）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + voxelsandbox_r2024_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(60)] + 启动日志 500 chunk Models from 100 resident（app fixed 世界 → 零驱动器构造实证）。**过程坑**：①radius-0 sparse 构造仍预生成中心 chunk（W1 语义）→ 恰等计数面按 8/8/8 收窄；②gamesession.h 头注释「bake→MeshWorker（W4）」命中 r2020d 全树裸 contains 反探（非剥注释）→ 注释措辞改「bake→worker 网格化」+ r2020 首跑即红即修；③r2018d gamesession.h 禁出钉随接线转正需同变更修订（预期内）；④抽样柱无掩蔽比对在 seed 42 (2,2) 区命中豁免结构足迹体素（fixed=106 vs sparse=air）→ 掩蔽后干净恒等。**登记非目标**：驱逐面（W3——toEvict 消费回调保持 null；离场驻留回收 = W3）；bake→worker 网格化（W4）；UI 开关（W5——app 侧 GameSession 生产实例化随 W5 sparse 世界入口接线）；半径参数实值（P5——configureStreamingRadii 即调参缝）；稀疏持久化（W3/W5，worldstore 零触碰）；投递面 outcome 的业务消费（本单只记账）；在途取消产物的丢弃式采纳优化（当前无条件采纳）。**待实机确认**：无新增（sparse 无 UI 入口 → 生产零激活，实机挂 W5；app fixed 零活动由冒烟实证；W5 复验项追加：流式世界走查观感——chunk 随走查渐次浮现/首屏等待 D4 调参）。**下一任务 = §29.5-W3 驱逐 + Edits-on-evict 落盘**（driver setEvictor 注入 → ChunkEvictor 生产缝实装：dirtyQuery = World 脏面、persistFn = §29.5.3 选型 (a) per-chunk 附加表[r2015 additive 先例]、lifecycleTransition = ChunkManager 真转移；实体×卸载竞态语义收口；W2 已通电的 GameSession 流式会话是接线宿主）。

- 最近闭环：**§29.5-W1b sparse population parity**（2026-09-16，fix(r2023) ffd0cf9 + test(r2023) bccc633 + 本 docs）：治理审计 #11 第 6 条放行的流式世界内容正确性前置单（矩阵 628→632）。**设计选型（处置表单一权威 = world.cpp sparsePopulateChunk 头注释，dev-plan W1b 条目含处置表全录）**：①**逐 pass 三选一处置**——(b) 级 16 pass 窗口/全域 trace 重放（bedrock/gravel/caves noise+worm/entrances/pools/lava/canyon/pruneSnow/pruneRails/fillWater/freeze/lakes/swampPools/trees/jungleTrees/flora 族/sugarcane；窗口参数默认全零=fixed 全域逐位原样）+ (c) 结构族五员豁免（dungeon/mineshaft/desert+jungle temple/stronghold——体素态依赖与 MC 异构，MC 引证 wiki/w/World_generation region-seed 确定性布置/stronghold 同心环/decoration step 原文）+ (c) scatterOres 替代（cell 窗+tryOre 读写域双钳=MC 1.0 per-chunk vein 同构，块缘 ±8 列跨 cell 交互差异登记）；②**精确性双铁律（恰红实证倒逼）**——读域时刻一致性：已物化邻 chunk 现存内容=终态≠pass-k 态（矿脉 Stone 竞争反序，r2023b 首红实证）→ 快照→memset+纯地形回填→population→memcpy 恢复+heightmap 重算（恢复无损：溢写已在邻块自身 population 同值落位）；写域两序同空：远端溢写落入恰已物化真邻块=按次序污染（r2023c 首红实证）→ ChunkManager population 写域钳制（锚 ±1 chunk=scaffold 窗；fixed/平时恒 off=写路径零开销零行为差）；③**scaffold**=自身 ±1 chunk（MC "features … limited in the nearby 3×3 chunk area" 同构）经 ①②③ 到可查询稳态作读域，population 后 ⑥⑦ 合法边+releaseSparseChunk 擦槽（数据不保留——正式物化时自身 population 全量重放逐位重derive）；④**fixed 路径零变化**：窗口参数默认全零=全域原样、m_worldgenQuiet 恒 false（qInfo 逐字原样）、写钳制恒 off——r2023a 双 generate 逐位恒等墙+worldgen/determin/re-gen 族实证；⑤**r2022c 同变更修订**（纠偏留痕非削钉）：terrain-only 恒等面物理失效 → 负坐标纯地形带 [y∈0,5)（bedrock 之上 ore 之下零 population 触达）对权威缓冲逐体素恒等 + 三分支 envelope 锚（纯地形面/特征面/carve 露出自然面含裸露矿脉）；收宽面移交 r2023b。**腿（section26 置尾，filter 词 r2023，80×80×96 s42 fixtures——5×5 chunk radius 2 预生成恰覆全核心域）**：r2023a fixed 零变化墙[同 seed 双 generate 抽样列全 y 带逐位恒等 + generate() 体 32 条全 pass 调用序字面钉（函数域手工 contains）+ population 反探]；r2023b parity 承重墙[9 内部 chunk 全 16×16×H 非 ore 体素逐位恒等 + 逐体素结构区域掩蔽（五类 inside* + xz 9 点偏移栅格 {0,±6,±12} 覆盖地牢豁口 ±12）+ ore 掩蔽 + 无掩蔽全等面 ≥10 万体素 + (b) 签名计数非空转]；r2023c 加载顺序无关[radius 0 双世界两种 loadChunkAt 次序同组 5 chunk（含负坐标 (-1,2)）逐位恒等 + 非空柱顶]；r2023d 结构钉[populate 静默窗+代表窗口调用+scaffold 生命周期/拆卸单点正面钉 + 结构族豁免反探（函数域手工 contains）+ generate() 禁出 population]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime > 全部改动源 → filter r2023 首跑 1P/3F 迭代三处实现级缺陷（真邻块终态读泄漏/真邻块溢写次序污染/ores 走向体素态耦合——各自恰红实证后落地快照恢复/写域钳制/双钳窗三修）→ 4P 全绿 → 前置回归全族绿（r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2021=4P×11 + t874=2P + t1023c=1P + worldgen/determin/re-gen/store/save 0 FAIL；计数增长均为腿名同词素 substring 先例）→ 阴性两轮恰红（NEG-1 摘 placeTrees 窗口调用→{r2023b} 单腿=声明面[r2023c 双世界同缺树仍互等/r2022c 修订面负坐标纯地形带/r2023a/d 不误伤]；NEG-2 树干高混入驻留 revision→{r2023b, r2023c}[首版声明 {r2023c} 实测扩面按先例改声明留痕：次序状态污染天然双破 parity 与顺序无关性两面]）→ 双 Edit 反向还原回绿（matrix_r2023_neg{1,2}_{red,restore}.log 直落 build/ 终名，变异字面 0 残留）→ 全矩阵 **632 PASS / 0 FAIL ×2**（pos/final EXIT=0；628 权威 diff = +4 恰 r2023a-d + r2022c 同变更修订行 + t813 哈希登记类；pos/final 腿集合恒等 632；^FAIL 归类 0）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + voxelsandbox_r2023_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(61)] + 启动日志 500 chunk Models from 100 resident（fixed 零激活实证）。**过程坑**：①首版「真邻块直接作读域」读终态泄漏（恰红实证→快照-回填-恢复）；②全候选域溢写落入恰已物化真邻块=次序污染（恰红实证→写域钳制）；③ores 脉形（offStone 走向）体素态耦合无法有界窗精确——降级 (c) 替代如实登记；④方块 id 表误记（Grass=1/Dirt=2/Stone=3——dump 判读先核表）；⑤结构密度高（seed 42 矿井包络覆盖全域）chunk 级结构自由选择不可行——改逐体素区域掩蔽。**登记非目标**：结构族五员的 sparse 侧替代（region 键确定性布置=W2+ 流式 worldgen 设计面）；ores 块缘跨 cell 交互精确化（需 C±2 读域=25 脚手架/chunk，成本纪律优先）；population 溢写的邻块增量更新（恢复丢弃——邻块自身 population 已同值落位）；溢写后光照精度与 fluid/ice 邻块增量（W1 登记延伸）。**待实机确认**：无新增（sparse 无 UI 入口生产零激活；W5 复验项追加：流式世界矿/洞/树/水观感 vs fixed 对照——本单 parity 面实机目视）。

- 最近闭环：**§29.5-W1 稀疏世界核**（2026-09-16，fix(r2022) 5f31dbb + test(r2022) ed49154 + 本 docs）：流式激活接线弧第一单（refactor-plan §29.5.2 W1 原文）。**设计选型（头注释立此存照）**：①**WorldMode{Fixed,Sparse} 显式模式位**（chunkmanager.h，拒「dims 哨兵参数化」案：TerrainGen 海域半径/列钳高需要真实 dims——哨兵会破 r2022c 同 seed 恒等承重腿；width/depth 是 Q_PROPERTY 且被群系 memo/出生回退等十余处派生面读取——显式模式位让「Fixed 分支语句原样保留 + sparse 分支全加性」成为结构性事实 = 零变化墙最强形态）；②**稀疏存储** = std::unordered_map<quint64 packed, SparseSlot{unique_ptr<Chunk>, life}>（ChunkKey::packed 补码位型双射——负坐标 chunk 合法；键逆映射走 ChunkKey::fromPacked 单一权威；chunk 实例与生命周期同槽 = Fixed「m_chunks + m_lifecycle 稠密 vector 对」的键控同构，六态转移图零分叉）；③**统一谓词单点**（禁散落第二份存在性判断）：chunkMaterialized[查询门：Fixed 恒 true / Sparse = 槽位存在 ∧ chunkLifecycleQueryable——与 mesher 存入门/r2021 驻留集同谓词] + chunkContentPresent[写门：Fixed 恒 true / Sparse = 内容在途或可用{Loading,Generated,Loaded,Active}——预生成填充在 Loading 态写入；Absent/Evicting 拒]；全部 14 门（chunk/chunkAtWorld/blockAt/stateAt/setBlock/heightmapAt/columnTopSurfaceY/skyLightAt/blockLightAt/setLight/clearAllDirty/clearAllLight/lifecycleAt/setLifecycle）sparse 分支统一走谓词、未物化一律现行「坐标越界」同值早退（OOB 等价零新增语义分支；y 域两模式同构仍有限高）；④**构造分化**：fixed ctor generate() 原样零改动；sparse ctor（SparseWorldParams{seed, core 160×160×96, spawnPreGenerateRadius 默认 2——选型：方形切比雪夫与 ChunkManager 方形域同构 + 冷启动成本 ∝R² + 任务带 2-4 取下限控首屏；负值归 0 上界钳 64}）→ sparseGenerate = reinitializeSparse 零分配 + 侧表族清空 + 出生半径预生成（中心 = 核心域中心 chunk）+ recomputeLightField/findSpawnColumn/rebuildStructureRegions 三 generate 尾同门；⑤**单 chunk 物化链** sparseGenerateChunk：ensureChunk（现行 make_unique<Chunk>(cx*16, cz*16, H) 构造路径复用）→ setChunkLifecycle ①Absent→Loading → fillTerrainColumn 逐列（WorldColumnSink 同款经 ChunkManager 5 参守卫写入口）→ recomputeAllHeightmaps → ②Generated → ③Loaded（稳态 = fixed create 终态两模式同稳态；生命周期经 setChunkLifecycle 唯一入口 = r2010 权威，驻留集 revision 沿自动携带）；⑥**loadChunkAt** 公开生产缝（幂等 + 未物化走全链 + refloodBox 列种子光 flood[ctor 全量 lightField 种子只扫核心域——按需物化 chunk 须自带列种子]；W2 驱动接线用，本单生产零调用）；⑦**sparse 守卫族**：setWidth/Depth/Height/Seed/regenerate/beginLoad/finishLoad 七入口 isSparse() 早退（固定全量 generate/存档语义不适用稀疏域——W5 转换单处理）；⑧residentChunkKeysOrdered sparse 分支 = 收集 + cz 外 cx 内排序（枚举序契约同 r2021，哈希容器序不可依赖）。**worldstore/Main.qml/P1-P3/D6 组件/线程原语零触碰**（app 用 fixed 世界 = sparse 代码零激活）。**腿（624→628，section25 置尾，filter 词 r2022，腿名/PASS 行均带词元）**：r2022a fixed 零变化承重墙[四 setter incantation 全语义恒等：稠密网格全 Loaded + 驻留集全网格 cz 主序 + revision 0 + 查询/编辑零生命周期沿 + 越界门逐门同值[拒写零 worldChanged 发射] + 裸 ChunkManager Fixed 谓词恒真 + D4 归一化]；r2022b OOB 等价语义族+加载翻转[构造账面 5×5 方阵全 Loaded + 正/负出界域逐门 = 现行越界同值且与 fixed 越界逐门对账 + y 域同构[顶上开阔天 15] + 拒写零副作用 + loadChunkAt 翻转真值[内容无关面：列顶非空气/heightmap 自洽/顶上天光 15/驻留 +1/keyAt cz 主序 index 15] + 生命周期 forwarder ⑥驱逐回 OOB 答案⑧取消驱逐复真值 + 界内编辑恰 2 worldChanged]；r2022c 同 seed 区域恒等承重[双世界同 seed 同 dims：纯函数门三面对账（fixed≡sparse≡TerrainGen 直连，81 采样列含负坐标）+ 已加载 5 chunk[含 (-1,-1)/(-1,3) 负坐标]对 generateTerrainChunk 权威缓冲逐体素恒等[id+state] + 内陆中块 envelope 锚 256 列[列顶 = min(heightAt,H-1) + 表层 id 按群系] + 核心域全物化账面]；r2022d 钉面[WorldMode 枚举/Fixed 默认成员/统一谓词声明/reinitializeSparse/ensureChunk/键控存储正面钉 + chunkmanager.cpp 门分流 ≥12/现行 Chunk 构造路径复用 ×2/fromPacked 键逆映射 + world.h sparse 面五钉[默认参 radius=2] + world.cpp 生成链/生命周期三边唯一入口/fillTerrainColumn 消费 ×2/写门 ×2/守卫族 ≥7/枚举委托 + worldstore 对 sparse 核全盲反探 + Main.qml/组件族零记号 + 新面禁 Q_INVOKABLE 形态]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime（矩阵 22:14 / app 22:48）> 全部改动源 → filter r2022 首跑 2P/2F 即修两处腿文坑（①预生成域循环边界误用 -kRadius[应为 kCenterChunk±kRadius]；②方阵记账错 49→5×5=25）→ 腿名补 r2022a-d 词元[初版漏词元 ran 0 legs] + PASS 行文本补词元前缀（日志 grep 可检索惯例）→ 4P 三连跑全绿 → 相邻 filter 计数逐一核对零词元污染（r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2021=4P×11 t874=2P t1023c=1P）→ 前置回归 worldgen 23P/determin/re-gen/store/save 0 FAIL → 阴性两轮恰红声明面（NEG-1 摘「可查询」合取[存在即查]→{r2022b}单腿[diag b=0 = Evicting 相位 blockAt 返真值 = 被摘语义本体；钉面同变更改锚 sparseResidentKeysOrdered 的 kv.second.life 同谓词文本保恰红单腿——恰红面设计先于腿文]；NEG-2 seed ⊕0x5A5A 偏置→{r2022c}单腿[r2022b 内容无关真值面按设计存活不误伤]）→ 双 Edit 反向还原回绿（四日志 matrix_r2022_neg{1,2}_{red,restore}.log 直落 build/ 终名；变异字面 0 残留 grep 复核）→ 全矩阵 **628 PASS / 0 FAIL ×2**（pos/final 双日志 EXIT=0；624 权威 diff = +4 恰新腿 + t813 戳/哈希[20:14→21:56、6ab5274→b30417a]/t997 墙钟登记类；pos/final 腿集合恒等 628；^FAIL 归类 0）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + logs/voxelsandbox_r2022_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(60)] + 启动日志 500 chunk Models from 100 resident（fixed 零激活实证）。**过程坑**：①测试腿预生成域循环边界/方阵记账错（首跑即红即修）；②腿名初版漏 filter 词元；③NEG-1 变异形态会使钉面原文连带红——恰红面设计先于腿文，钉面改锚后收缩单腿。**登记非目标**：跨 chunk 结构（树/矿/洞/水等 worldgen 后地形 pass）的按 chunk population 重放（W2+ 流式 worldgen 设计面——sparse chunk 内容 = terraingen 单列权威 terrain-only，与 fixed 全量世界的全逐素恒等物理不成立，r2022c 恒等面如实锚权威缓冲 + 纯函数门；任务书 r2022c「fixed vs sparse blockAt 逐位恒等」按此读法收窄，冲突证据与修正建议已入关单报告）；sparse 持久化（W3/W5）；卸载执行（W3——本单生命周期 forwarder ⑥⑧即测试缝）；sparse 世界尺寸/seed 运行期变更（W5）；D4 双半径联动（GenerationPolicy 参数族接线归 W2）；按需物化的域外光照全量正确性（refloodBox 列种子已保物化 chunk 见天正确，跨 chunk 渗光精度登记）。**待实机确认**：无新增（sparse 无 UI 入口生产零激活——app fixed 世界行为零变化由冒烟实证；**W5 UI 开关接线后复验项**：无限世界观感[出生区生成/未生成域边界观感/首屏等待 D4 调参]）。**下一任务 = §29.5-W2 位置源 + 驱动接线**（玩家位 → floorDiv16 → GameSession tick 尾 driver.onPlayerChunk；GenerationJob ↔ ChunkManager setLifecycle 挂点首次生产接线；BackgroundGenerationWorker 首个生产消费者；streaming 仅 sparse 世界使能——W1 的 loadChunkAt/统一谓词/setChunkLifecycle 沿即接线缝）。

- 最近闭环：**§29.4-P4 QML 动态化**（2026-09-16，fix(r2021) 15bb6c1 + test(r2021) d93404c + 本 docs）：§29.4 相位族第四相位（全 R20 唯一动 QML 相位）。**设计选型（头注释立此存照）**：①C++ 侧最薄 QML 面 = World 驻留集三成员（专用桥 QObject 更厚弃用——World 已是 QML 唯一世界单件；P1-P3/D6 值组件 QObjectFree 零暴露）——`Q_PROPERTY int residentChunkRevision`（NOTIFY residentChunkRevisionChanged）+ `Q_INVOKABLE int residentChunkCount()` + `Q_INVOKABLE QVariantList residentChunkKeyAt(int)`（[cx,cz] variantlist，t1020 structureRegion 返回形态先例）；②驻留集 = **{Loaded, Active} = chunkLifecycleQueryable**（与 mesher 存在门同谓词=渲染存在面单一口径；Evicting 中 chunk 对 mesher 已不可见 → slot 同帧摘除一致）；③revision 沿 = 成员集变化（③⑧加入/⑥移出 bump+emit；⑤④集内平移、①②⑦⑨集外沿零 bump——revision 只追渲染集成员不追全转移史，r2021b 逐边对账钉死）；唯一写点 = World::setChunkLifecycle forwarder（非 Q_INVOKABLE 永久——r2010d 零决策面 + r2021d 反探常驻；生产接线期流式组件转移经此 forwarder 自动携带沿=接线缝已留，组件本体零接线零触碰）；④枚举序单一权威 = world.cpp residentChunkKeysOrdered（cz 外 cx 内 = 旧 t276 固定网格循环序 = ChunkManager flatIndex 序；count/keyAt 双消费面共用禁第二份循环=NEG-2 变异落点单点化）；⑤Main.qml **只动 chunk 实例化模式段**：chunkAnchor.onCompleted → `rebuildChunkSlotPool()`（驻留集枚举逐键实例化同 5 段组合；chunksBuilt 一次性守卫同旧）+ 新增 revision 沿 Connections（拆池→按新枚举重派生）+ pool 函数落 Window 级（recomputeMeshStats 后，chunk 机器段）——**整池重派生**选型保 `_refreshChunkVisibility`/`kickWorldMeshSync` 的「每 segmentsPerChunk 段一组、组首全局对齐」扁平数组不变量（两消费端零触碰=变更面集中的承重选择），增量 slot patch 登记生产接线单优化面；玩法路径（输入/交互/菜单/存档 UI）零触碰。**QML 变更面清单（逐文件逐段）**：Main.qml 三段 = (a) :325 属性块注释如实化（t276 跟踪态注释改述模型驱动）；(b) :401-435 新增 rebuildChunkSlotPool 函数 + 选型注释（recomputeMeshStats 之后 Window 级）；(c) :4676-4712 实例化段（段首注释更新 + chunkAnchor.onCompleted 改池调用 + revision Connections 新增）——其余 15k 行零 diff，玩法桥/菜单/存档/UI 全零触碰；world.h 三段 = Q_PROPERTY 块 + setChunkLifecycle 选型注释与 bump 体 + 驻留读面三成员 + 信号 + 私有 helper/成员声明；world.cpp 尾部 +17 行（residentChunkKeysOrdered 实现）；chunkgeometry.{h,cpp} 零触碰（cx/cz 参数本已充分，「key 来源参数化」不需改元素本体）；worldstore/线程原语/P1-P3/D6 组件零触碰。**腿（620→624，section24 置尾，filter 词 r2021）**：r2021a 固定世界零变化承重墙[3×3 方阵 + 2×3 非方阵（cz 主序 vs cx 主序判别形态）枚举 ≡ 旧固定网格循环逐项恒等 + count==chunksX×chunksZ + revision 初始 0 + setBlock 编辑零沿零发射 + keyAt 越界空表 + 二次枚举幂等 + 逐枚举键直驱 ChunkGeometry vertexCount>0=QML 消费契约]；r2021b 模型响应性[合法转移 revision/发射精确对账：⑥(1,1) -1 键 +1 沿余键相对序保持 / ⑦集外零 bump / ①②③重加链恰 +1 于 ③ 且 (1,1) 归位 cz 主序 index 4 / ⑤④集内零沿 / Loaded→Absent 直跳拒零沿 / ⑥⑧(0,0) 加回 +1 枚举复原]；r2021c QML real-chain 响应性[真 QQmlEngine × 真 World × setData wrapper（Main.qml rebuildChunkSlotPool 消费逻辑最小镜像，真链 harness 家族同门；createWithInitialProperties 注入 world=创建期 target 已定义零 Connections 噪声）——初建枚举九键 + C++ 增删沿同步直达 handler + 池随增删逐项跟随 + QML rebuilds==C++ 发射+1 初建账面闭合 + 引擎零警告]；r2021d 钉面[world.h 三新成员逐一正面钉 + emit 单点 + bump 谓词行 + world.cpp 枚举权威签名/谓词消费钉（不钉循环嵌套=NEG-2 恰红面设计）+ Main.qml 消费面精确计数（count/keyAt ×1、revision handler ×1、池入口 ×3、onCompleted 路由行）+ 旧固定网格循环反探退役（miss 非空=合规）+ setChunkLifecycle 禁入 QML + 玩法标记在位（onTicked/enterWorld/startGame）+ GameSession 零提及常驻复述]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime（矩阵 19:47 / app 20:38）> 全部改动源 → filter r2021 首跑 3P/1F 即修两处腿文坑（①wrapper 创建后 setProperty 使 Connections 经 undefined target 一轮「signal not found」警告 → 改 createWithInitialProperties 初参注入；②重加链漏 ⑦ Evicting→Absent 中间步[Evicting 不能直跳 Loading] + QML rebuilds 对账口径漏计初建 1 次）→ 4P **三连跑全绿**（r2012 防 flake 先例）→ 前置回归终版 binary 全族 r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2020=4P×10 + t874 家族 2P 全绿 EXIT=0（首跑 r2007=6P/r2010=7P = 本段腿文含 r2007b/r2010d/t874 词元污染 filter 命名空间——双向去词元后达标，教训入纪律）→ 阴性两轮（NEG-1 摘发射沿 false && 前缀→恰红 {r2021b,r2021c}=声明面[622P/2F]；NEG-2 摘枚举恒等口径 cx/cz 互换→实测 {r2021a,r2021b,r2021c}[621P/3F]**超首版声明 {r2021a} 改声明留痕**[t1053/t1054 先例]：b/c 断言本就锚旧网格 cz 主序基线数组=被测语义本体非连带伤，逐腿 diag 核对，断言零削）→ 双 Edit 反向还原回绿（四日志 matrix_r2021_neg{1,2}_{red,restore}.log 直接落 build/ 终名；NEG1/NEG2 变异字面 0 残留）→ 全矩阵 **624 PASS / 0 FAIL ×2**（matrix_r2021_pos.log + matrix_r2021_final.log 双日志在案 EXIT=0；620 权威 diff = +4 恰 r2021a-d + 移行 2 条登记类[t813 戳 18:29→20:14/哈希 841719f→6ab5274、t997 墙钟]；pos/final 腿集合恒等 624、两轮间唯一差 = t997 墙钟数字；^FAIL 逐行归类 0）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2021_tail20.log 稳态 60fps[prof[1s] 60fr ×8 / frame main*16.0(60/61)] + F3 mesh 行 `mesh 0.00(0reb [0d 0s 0w])` 与 r2020 基线逐字同口径 + 启动日志 `[t276/r2021] built 500 chunk Models from 100 resident chunks`（数据驱动池实例化量 = 旧固定网格 10×10×5 逐位同量）。**过程坑**：①腿文词元污染（上记，双向去词元 + 相邻 filter 计数核对入纪律）；②wrapper 期 Connections target 未定义警告（初参注入解）；③`sed -n` 误用把 section24 清零（无 p 标志 -n 全抑制输出→mv 空文件覆盖）——untracked 文件无 git 可回，全量重写恢复；教训=**写脚本改文件前先核语义，untracked 工件及时落盘/提交**；④NEG-2 首版声明错估 r2021b/c 不敏感（上记改声明）。**登记非目标**：增量 slot patch（池整池重派生的经济学优化=生产接线单）；运行期换网格尺寸的池重建（旧 QML chunksBuilt 守卫同样不处理，维持现状）；驻留集枚举缓存（全网格 O(chunks) 每查现算，仅沿时刻调用无热路径）；P1-P3/D6 生产接线（后续单：D2 新世界参数开关 + 位置源/挂点/bake 路径接线 + D3 就地流式化）。**待实机确认（必列）**：①固定世界观感零变化实机核（进世界地形/水/岩浆/玻璃/冰五段渲染与 P4 前逐像素一致——headless 冒烟只能证实例化量与帧稳态，像素面须真机目视）；②F3 对照（mesh 行顶点/三角汇总、drawCalls、chunk updates 与 P4 前真机数值同口径）；③（生产接线后复验）streaming 开启后视距推进中 chunk 池随增删的观感与重建经济学。**下一任务 = 生产接线（D2/D3）**——refactor-plan §29.4 决策表建议列已用户定向；**设计登记：接线单开工前主控先出接线设计段**（D2 参数开关挂点/位置源=玩家 chunk 沿/驱动器-卸载器-GenerationJob 挂点/ChunkGeometry bake 路径[MeshWorker requestId=chunk key 派生同期]/驻留集转移经 setChunkLifecycle forwarder 的沿面），随后 P5 实机调参验收。

- 最近闭环：**D6 worker meshing MeshWorker**（2026-09-16，fix(r2020) + test(r2020) + 本 docs）：**src/World/meshworker.h** 新建（header-only 非 QObject 线程化执行器，r2012 backgroundgeneration.h 同门逐项对照）——①形态：构造即起单 worker 线程（std::thread + mutex + condition_variable；**worker 类代码体零 Qt 调用**[std 原语 + 值类型；执行体经 MeshBuilder::build 内触 FrameProfiler 计数为 QMutex 锁保护既设计面，头注如实登记]，worker 侧零 QObject 零信号槽零 QML），析构 = stop + 丢弃计数 + notify_all + join（在途构建跑完、队内未开工请求逐条入 droppedCount、绝不挂死；成员声明序=构造序纪律同门）；②API：`submit(const ChunkMeshSnapshot&, quint64 requestId) → Result<void>`（快照按值深拷贝入队；请求队列有界 64 满载 kErrQueueFull 可见拒绝——构造缝可压小容量[生产零参]，拒绝项不入账；requestId 调用方给定组件不解释）+ `takeBuilt(MeshBuiltItem{requestId, ChunkMeshData}&) → bool`（FIFO 提交序纯值 move 收割）+ pendingCount/builtCount/droppedCount/acceptedCount 只读统计[任务书草案 snapshotQueuedCount 落位名 = pendingCount]；③**语义铁律「每个被接受的 request 恰产出一条 built」**（析构丢弃除外——丢弃逐条计 droppedCount 禁静默消失[事件可见性同门]；已产出未收割项随对象亡=消费方自弃非 worker 丢弃；teardownDroppedSink 构造缝[生产零挂载]使丢弃计数越过对象生命周期可见）；④**确定性承重**：worker 执行体 = `MeshBuilder::build(snapshot, Reason::Dirty)` 唯一调用（R20.13 单一权威禁复制网格逻辑[r2013d 反探继续有效]、零加工零重排——Reason 只影响 FrameProfiler 计数分桶与顶点输出无关[实读核验]）；⑤快照值越线程安全论证头注立证（ChunkMeshSnapshot/ChunkMeshData/MeshBuiltItem 均 QObjectFree owning 值编译期钉；**World 读面不越线程**——快照采集恒调用者线程 = 生产接线期契约）。**生产零接线**：ChunkGeometry 现行同步 bake 路径零触碰、world/worldstore/QML 零改动，「MeshWorker」记号全 src 树只出现在 meshworker.h（r2020d 全树反探常驻）。**同变更钉面修订（纠偏留痕非放宽）**：t1023c 白名单腿（section08）单落点 → 双文件白名单 {backgroundgeneration.h, meshworker.h} 且仅 std::thread + 每落点在场 ≥1 命中防空转 + F3 行 `threads: 0/0 (sync meshing)` 照常钉（mesher 生产路径仍同步——组件无头）；backgroundgeneration.h 头注「当前唯一」表述如实化（首个 → 首个 + r2020 起双落点）。**腿（616→620，section23 置尾，filter 词 r2020）**：r2020a 确定性等价承重墙[8 形态快照——世界采集 inland/edge/waterOnly/greedy+AO + 手写异形 cross/流体水池/重负棋盘高 256 + 空快照——worker build ≡ 主线程同步直调 MeshBuilder::build 逐位恒等三面（顶点/索引/统计）+ requestId 零丢零重 + 非空快照非空转 + 空快照双路同空]；r2020b 队列纪律[cap=0 退化确定性满载每提交必 kErrQueueFull 零入账 + cap=2 重负在途拦截的饱和满载（第 3 提交可见拒绝=M 在途确定性证明）拒绝项不入账 + 接受项恰一产出按 requestId 对账{10,11,12}/{20..31}零丢零重 + 收割后队清]；r2020c 析构卫生[积压在途析构 join 有界（deadline 断言）+ 丢弃经 sink 哨可见（毒值哨 → sink==2 恰等于析构排队数；在途项非丢弃）+ 析构后零回调零输出（结构性，r2020d 源码反探守）+ 再构造再用全周期恰一]；r2020d 结构钉+白名单修订自证+零接线+线程身份[meshworker.h 正面钉（submit/takeBuilt/容量 64/单一权威执行体/线程原语/双 cv 唤醒/QObjectFree 钉/析构丢弃计数/构造缝）+ 反探（零 QObject 面/零 Qt 线程设施/零网格逻辑回流记号/零 World 读面零采集调用）+ 段内 sweep 自证双文件白名单（thread 记号恰两落点各 ≥1 此外零落点）+ 「MeshWorker」全树只在 meshworker.h + chunkgeometry.{h,cpp} 显式反探 + Main.qml 零记号 + 线程身份对账（执行==工作≠调用者）]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime (18:20) > 全部改动源 → filter r2020 首跑 3P/1F 即修（r2020c 假红：Windows QThread::msleep(1) 实际粒度 ~15.6ms 整窗漏过瞬态 pending 窗 → 三路加固：monster 高 96→256[构建数十毫秒级]、waitPending 改 busy-yield µs 级轮询、r2020c 改饱和型积压构造[第 3 提交可见拒绝 = 在途确定性证明，不追瞬态窗]）→ 4P + **三连跑全绿**（r2012 防 flake 先例）→ 前置回归终版 binary 全族重跑 t1023c=1P[修订后] r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2019=4P×9 计数逐一吻合 → 阴性两轮恰红声明面（NEG-1 摘「接受项恰一产出」→{r2020b}单腿[batch harvest timeout 11/got=11]；NEG-2 worker 执行体加工输出[翻转首顶点 r 域]→{r2020a}单腿[7 非空快照 eq=0 计数不变]）→ 双 Edit 反向还原回绿（四日志 matrix_r2020_neg{1,2}_{red,restore}.log 直接落 build/ 终名；变异字面 0 残留复核）→ 全矩阵 **620 PASS / 0 FAIL ×2**（matrix_r2020_pos.log + matrix_r2020_final.log 双日志在案 EXIT=0；616 权威 diff = +4 恰 r2020a-d + 3 条移行全归类[t813 戳/哈希、t997 两墙钟比值、t1023c 同变更修订行如实登记]；t830/t979 零漂移；pos/final 腿集合恒等 620[两轮间唯一差 = t997 墙钟登记类]；^FAIL 逐行归类 0）→ app 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2020_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(60)]。**过程坑**：①Windows 定时器粒度坑（首跑 r2020c obs=0/sink=0/队已排干实证）——瞬态窗等待禁 msleep(1) 轮询，重负拦截体须与轮询粒度差两个数量级以上（15.6ms vs 数十毫秒 vs 微秒提交序）；②腿名 filter 词纪律延续（t1023c 修订腿名去 "R2020" 词元防反向污染 r2020 filter；r2020d 腿名去 "t1023c" 词元防正向污染）；③pinSet minCount 对剥注释后代码位实际出现次数核（kErrQueueFull 代码位恰 1，x2 误设自纠）。**登记非目标**：生产接线（ChunkGeometry bake 路径切换 = 后续单/P4 前；requestId=chunk key 派生同期）/ 快照采集线程化（World 非线程安全——采集恒主线程）/ 多 worker 并发（单线程起步，扩并发=后续单）/ 优先级调度与结果缓存。**待实机确认**：无新增（组件无头，app 侧零变化由冒烟 tail20 实证；**P4 生产接线后复验项**：编辑重建走 worker 后破/放贴图当帧刷新的时延观感、F3 mesh 桶时长占比变化）。**下一任务 = §29.4-P4 QML 动态化（全 R20 唯一动 QML 相位）→ P5 实机调参验收**。

- 最近闭环：**§29.4-P3 卸载 + Edits-on-evict ChunkEvictor**（2026-09-16，fix(r2019) + test(r2019) + 本 docs）：**src/World/chunkevictor.h** 新建（header-only 非 QObject 纯编排值组件，P1/P2 同门）——①三纯缝：lifecycleTransitionFn(cx,cz,target)→bool[生产绑 ChunkManager::setLifecycle 等价面] + dirtyQueryFn(cx,cz)→bool[生产绑 DirtyChunkSet 等价面] + persistFn(cx,cz)→Result<void>[生产绑真实 SaveCoordinator 调用]；②null 缝 fail-safe = 该项无信息 → 跳过驱逐（宁驻留不误删；persist 缝仅 dirty 候选必需）；③**顺序铁律「先落盘后转移」**（头注释立证：内容存续优先于状态迁移）——persist 失败 = 中止该 chunk 驱逐（保持驻留原态零转移调用计入 aborted，绝不先掉内容再补盘）；④合法边三步驱动（读表确定）：ensure-Loaded[③/⑤；已 Loaded 自转移守卫拒 = 预期 no-op 不计失败]→⑥ Loaded→Evicting→⑦ Evicting→Absent；步2 被拒 = 不在可驱逐稳态 aborted 不重试，步3 被拒 best-effort 边⑧回驻留；重试语义 = 调用方下一变更沿重报候选（自动重试登记非目标）；⑤Report{persisted,evicted,aborted,skipped} 值小聚合 + QObjectFree 编译期钉。**chunkstreamdriver.h 最小扩展**（两可选注入点，默认 null = P2 逐位等价，r2018 既有腿回归绿实证）：setEvictor[onPlayerChunk 决策沿提交/取消完成后以 d.toEvict 回调；streaming off 不可达] + setSavedContentQuery[submit 前查询：有存档内容 → Load kind（r2011 Load 语义），null = 全 Generate；D5 回灌路径]。**同变更钉面修订（纠偏非削钉）**：r2018d 原「Load kind 禁出 driver 头」反探随 P3 Load 路径退役 → 改正面计数钉「Load 记号恰经缝门控出现 ≥1」+ Mesh 禁出/零 setLifecycle/零接线全保留 + 腿名/PASS 文本如实化（"default Generate-only face (Load only via the P3 seam-gated saved-content query)"）。**腿（612→616，section22 置尾，filter 词 r2019）**：r2019a 惰性承重墙[默认 driver 扫掠全零 + 双缝已装 off 态零回调零查询 + 带记录缝 driver ≡ P2 形态 driver 轨迹双生恒等（候选 ≡ twin toEvict 逐沿）]；r2019b 驱逐回灌承重[真 SaveCoordinator + fresh 临时库 + 真世界表：8 半径外驻留驱逐、(2,2) 编辑块 persist 先于转移（调用序柱）+ ⑥⑦到 Absent + p=1/e=8、(2,0) 零 persist 直达 Absent、台账 Clean 1/1、重入 (2,2) savedQuery→Load kind 提交 + 回灌读面（beginLoad 零填充 fresh 世界 + setWorld 先于 loadChunks[R20.10d rebind 教训钉在腿文]）↔ 驱逐前快照逐字节恒等]；r2019c 失败中止[persist 注入失败 → aborted 零 accepted 转移表不动 + 同实例重报收敛 + 转移缝恒拒 → 每候选恰 2 次有界尝试 + null 缝三形态 fail-safe]；r2019d 结构钉+kind 选择[双头正面钉/反探（禁 SaveCoordinator/setLifecycle 记号——落盘经缝转移经缝终点）/零接线 + null savedQuery 全 Generate 双生 + 恒 true 同键翻 Load + null evictor 重放恒等 + 确定性双 fresh evictor 轨迹逐位恒等]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime (17:37) > 全部改动源 → filter r2019 4P 首跑绿（首跑 r2019b 沿 2 twin 沿后快照陈旧前置态假红即修）→ 前置回归 r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2018=4P×8 → 阴性两轮恰红声明面（NEG-1 摘「先落盘后转移」序→{r2019b}调用序柱单腿[order=0(22<21)；r2019c 柱正交设计不误伤]；NEG-2 摘 persist 失败中止[false && 前缀]→{r2019c}单腿[abort=0/held=0 + ph2 同腿级联]）→ 双 Edit 反向还原回绿（四日志 matrix_r2019_neg{1,2}_{red,restore}.log 直接落 build/ 终名）→ 全矩阵 **616 PASS / 0 FAIL ×2**（matrix_r2019_pos.log + matrix_r2019_final.log 双日志在案 EXIT=0；612 权威 diff = +4 恰 r2019a-d + 漂移仅登记类[t813 戳/哈希、t1023c src 文件计数 147→148 同款 +N 先例]；t830/t979/t997 零漂移；pos/final 腿集合恒等 616；^FAIL 逐行归类 0）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + logs/voxelsandbox_r2019_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(61)]。**过程坑**：①腿名 filter 词污染双向例（r2019a/d 腿名含 "r2018" 词元 + r2018d 修订文本含 "r2019"）→ 双向去词元（r2018 P2 单同款教训）；②Qt GUI 子系统 binary 经 bash 直跑零输出 → 双 stderr 变量（QT_ASSUME_STDERR_HAS_CONSOLE + QT_FORCE_STDERR_LOGGING）为矩阵/冒烟必带；③局部类成员函数不可捕获段内 lambda + 显式捕获列表引用 kkey 须显式 &kkey。**登记非目标**：生产接线（P4/P5）/worker meshing（D6 后续单）/内容真实丢弃（World 侧归生产接线）/失败自动重试。**待实机确认**：无新增（无头面 + 生产零接线 → 「无新增」；**P4 接线后复验项**：视距推进中远处 chunk 卸载无感/回灌后地形与编辑一致[存档往返真机]/驱动器使能后 F3 生成排队面）。**下一任务 = worker meshing（D6 同批）→ P4 QML 动态化 → P5 调参验收**。

- 最近闭环：**t1054 parity 第 14 门（骑乘门 sneak 抑制 + #11 sneakPlaceBlock helper 化）**（2026-09-16，fix(t1054) 55a2209 + test(t1054) 7c88b81 + 本 docs）：review0916 #4「中」+ #11「低」清偿单。**语义选型（头注释立此存照，playercontroller.cpp :4908-4930）**：#4 = (a) 骑乘段 `tryMount` 前置 **shift 裸门** `!m_keys.value(Qt::Key_Shift)`（潜行一律抑制实体交互，与手持无关，先于 tryMount 短路）——**与 sneakPlaceBlock 合取门的语义分工**：开箱/扳动类 = 合取门【持可放置方块才旁路到放置，t1050/t1052 家族】；骑乘类 = shift 裸门【潜行即不骑：空手 sneak 右键普通矿车无效应（不上车不放物）、持方块 sneak 落 (b) 放矿车/通用放块（放置优先）、无 shift 照旧上车】；t1052 提交注自证矛盾点（旧注释宣称持方块 sneak 落「(a) 骑乘[箱车拒载]」对普通矿车系放置被 tryMount 吞掉）随修消除、:4884-4889 注释如实化；minecartmanager 零改动（门在调用点）。#11 = 合取收进私有 `heldPlaceableSneak() const`（.h :1098 声明 / .cpp :3532 定义），两调用点（m_hasHit 块 12 门判据 + 矿车段 (a0) 开箱门）改读，语义逐位不变。**MC 引证三元组（2026-09-16 实读）**：wiki/w/Sneaking Effects 段 "Players are able to interact with a tamed horse (tamed donkey or mule inventories, also) when pressing use item control while holding the sneak key"（潜行+use 可骑实体 = 交互非骑乘；Effects 段无 "does not mount" 字面句，本条为最接近现行原文、如实登记）+ 同页 History "Sneaking is now used to get out for all transportation methods, such as boats, horses and minecarts"（Java 1.6.1/13w16a）+ wiki/w/Minecart Usage "Minecarts can be ridden by pressing the \"use\" control on them" / Dismounting "Players can exit the minecart by sneaking while riding it"。**腿（606→608，section08 矿车族）**：t1054a 三相+回归柱（rig = t1052a 先例 seed 10521 地台 + 孤轨 (12,15,16) + 普通矿车 on-rail 静止 pos.y≈15.45；选体射线 t983 薄板口径穿轨板空气命中地台、实体射线穿车盒 s∈[0.5,0.83]，rig 自证三重[cartY/ridingIndex/hit]；①持方块+shift→放置发生[邻格木板]+零 mount ②空手+shift→无效应[零 mount/零放置/零开箱/零挥臂] ③无 shift→mount 照旧[ridingIndex==0 回归柱]；diag 带 mount 布尔+放置事件+heldId+挥臂/开箱计数）+ t1054b helper 承重钉（定义+return 合取行 minCount=1 / 两调用点 minCount=2 / 旧字面调用行反探 miss 非空=合规）+ **t1050a/t1052a 源钉同变更修订**（旧字面调用行被 helper 化移除，钉面改指 helper return 合取行/两调用点——t1023c 先例纠偏非削钉）。**验证链全绿**：分段增量构建（-j 4）→ binary mtime (13:46:49) > 全部改动源 → filter t1054 2P 首跑绿（首跑前置回归揪出 t1050a 源钉红即修，见过程坑）→ 前置回归终版 binary 全族重跑 t1050=6P t1052=1P t1013/t1046=8P t1034=3P t1028=4P r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011~r2017=4P×7 全绿 EXIT=0 → 阴性两轮恰红声明面（NEG-1 摘 (a) 段 sneak 裸门→腿级恰红{t1054a}[t1052/t1050 对照族绿；柱级偏差留痕=相① mount 残留 m_riderCart 使相③ ridingIndex 断言连带 false，单腿内级联非红面扩散]；NEG-2 helper 合取 &&→||→全矩阵 599P/9F，实测红面 = 声明{t1054b,t1050a-d,t1052a}∪{t1034a,t1034c,t1046a}[无 sneak 持方块基线柱属合取语义面——t1053 NEG1 先例改声明留痕；t1054a 不红=预判成立，(a) 门读裸键不经 helper]）→ 双 Edit 反向还原回绿（还原后计数复核：门合取 3 项在位 / 合取式全文件恰 1 处；存证 matrix_t1054_neg1_red/restore.log + matrix_t1054_neg2_red/restore.log **直接落 build/ 终名**[r2017 证据面铁律]）→ 全矩阵 **608 PASS / 0 FAIL ×2**（matrix_t1054_pos.log + matrix_t1054_final.log 双日志在案 EXIT=0；606 权威 diff = +2 恰 t1054a/b + 漂移仅 t813 戳/哈希 + t997 墙钟；t979[最终轮 pos/final 间 15↔16]/t830/t1023c 零漂移；pos/final 腿集合恒等 608；^FAIL 逐行归类 0）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + logs/voxelsandbox_t1054_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(60/61)]。**过程坑**：①t1050a 源钉红（needle = 被本修移除的旧调用行字面，t1050 5P/1F + t1046 7P/1F[同腿双 filter 名]首跑揪出）→ 同变更修订回绿；教训 = **helper 化/搬移类改动须全矩阵 grep 旧字面钉**（复核后仅 t1050a 一处 + 自家 t1052a 钉）；②矩阵 filter 跑 QT_QPA_PLATFORM=offscreen 不可用（build 目录未部署 plugins，评审既登记）——offscreen 组仅用于 app 冒烟且须配 QT_QPA_PLATFORM_PLUGIN_PATH，矩阵跑用默认 windows 平台；③NEG-1 相③级联（上记，绝对 ridingIndex 断言对前相污染态敏感，本腿按腿级声明成立 + 柱级留痕）。**登记非目标**：船骑乘段 sneak 抑制（review #4 仅指矿车段，船门另行登记）/ tryMount 本体与 minecartmanager（零改动）/ 浆果丛收获型 useBlock（review0915 Info 既有登记）/ 持矿车物品 + sneak 右键普通矿车的 (b) 放车细分（守卫未动零风险未覆盖）。**待实机确认（行为修复必列实机）**：①空手潜行右键普通矿车不上车（无效应观感）；②持方块潜行右键普通矿车 = 放置不骑乘（对运行中矿车实测）；③无 shift 右键照旧上车（回归观感）；④潜行骑乘中右键不再触发骑乘切换（与潜行下车语义共存观感）。**下一任务 = 治理审计 #10（主控亲自，计数 5/5：r2010b/r2016/r2017/t1053/t1054）→ §29.4-P2**。

- 最近闭环：**r2017 §29.4-P2 前置清偿单**（2026-09-16，fix(r2017) + test(r2017) + 本 docs）：docs/agent-review-2026-09-16.md #1/#2/#3 三条「中」+ Info「setParams 不规整」四修（主控逐条读源实锤分诊；全部位于 P2 接线面的潜伏债——GameSession 生产零实例化，当前无生产触发面）。**四修语义选型（头注释立此存照）**：①fix A（#1 PlaceBlock 强刷）= executeCommand 按 blockState 双路由——state==0 走 4 参 `m_facade.setBlock` 同 id no-op 保 state（world.cpp 4 参版 `oldId==id → return false` 零信号实读核验；MC 引证三元组 = minecraft.wiki/w/Block_properties「replaceable」属性 "placed in the same location as the replaceable block replace it" + "Most blocks are not replaceable"，Java 现行，2026-09-16 实读），state≠0 维持 5 参 setBlockWithState（R20.09 权威不变）；4/5 参在「Air 首放/异 id 替换/同 id 同 state」逐位一致（world.cpp 双版本逐行核验），唯一语义变化 = 同 id 异 state 格「强刷 0 + 15+ 写后钩子」回「静默拒绝」（b759e60 引入的病灶复位）。②fix B（#2 kind 门）= generationjob.h 同步 pump 与 pumpAsync 两路六处边驱动点内联 `job.kind != GenerationJobKind::Mesh` 门（选型内联非抽函数：零 `m_chunks->setLifecycle(` 计数变化 = r2011d minCount 2 / r2010bb `delivered > 0 && m_chunks` minCount 2 既有源码钉免同变更修订；Mesh 三边全不驱动 = generationjob.h:31-32/:83 头注契约的代码兑现，两个坏面登记进头注：Mesh 推 Generated 永久对 mesher 三门不可见 / 交接窗 Mesh 失败边⑨打回 Generate 在途 chunk；Generate/Load 三边照旧 = kind 域门）。③fix C（#3 out-of-tick 编辑）= 候选②窗口语义迁移：runOneTick 开头 `m_edits.clear()` 移除、清账唯一落点收口发布之后（emit 栈外——栈内消费者按同帧快照读 lastDirtyChunks()），窗口「start-clear 到收口」→「上收口到本收口」，out-of-tick 编辑归下一窗照常 WorldDelta+BlockChanged 发布（event.h 丢弃必须被看见）；候选①弃因（破 R20.09 收口单点发布 + 脱离合并面/脏集合面渲染漏格）、③弃因（可见丢弃≠发布）；**同变更修订 r2009b/c（纠偏非放宽）**：「收口后脏集仍见本 tick 旧读数」随窗口语义退役，集合面单源断言移入 tickCompleted 信号栈内（gamesession.h lastDirtyChunks 注释自述域，review #12 同口径）——修订后断言双语义成立 = NEG-C 不敏感相对恒等，恰红面收缩 {r2017c} 单腿；EditBuffer 本体零触碰（takeDelta 保持 const 投影，r2009a 全绿）。④fix D（Info）= 规整唯一路径 GenerationPolicyParams::normalized public static（落位 Params 本体 = 规整即值类型构造契约；4 参构造与 setParams 同走），杜绝手改字段 → decide priority quint8(cheb) 回绕；r2016d 构造规整回归柱不动。**腿（602→606，四新腿 + 两修订行）**：r2017a PlaceBlock 三相[同 id 异 state（WoodStairs s1）+state=0 命令 no-op：id/state 保留零事件零 delta + FIFO marker 证消费 / 同格显式 s5 全写生效（state-only 写不发 broken/placed = 既有接线语义如实钉）+ 异格显式 state 对照 / Air 首放·异 id 替换回归柱落 (id,0)；diag 带 pre/post id+state]；r2017b Mesh kind 门双路[同步：Absent 格 Mesh worker 实测 Absent+终态停 Absent / Loading 格成功停 Loading（合法边只有 kind 门能拦，最锐判别）/ 失败停 Loading（边⑨被门）；异步 ScriptedMeshWorker 替身：交接相停 Absent+收割停 Absent+账本双消；Generate 对照柱同路由 ①②/①⑨ 照旧；源码钉 minCount=6]；r2017c out-of-tick 可见性[稳态收口→注入对（放 Stone + 破表面[顶上两格净空自证防级联]）→单点发布钉（注入后队列空 + 挂起恰 2 chunk）→下一收口完整发布→tick 内回归柱]；r2017d setParams 三 twin[越界 (300,-2,400) ≡ 构造：字段+决策逐位，大缝自定义 lambda 默认 Loaded + (300,0) 回绕判别格 + (255,0) 窗界 priority-255 无回绕钉；合规 (2,1,4) 生效恒等；全负双生；默认关经 setParams 存活；钉 normalized( minCount=3]。**验证链全绿**：分段增量构建 → binary mtime > 改动源 → filter r2017 4P 首跑绿（首跑三轮腿文坑即修见过程坑）→ 前置回归 r2007=5P r2008=5P r2009=4P[修订后] r2010=6P r2010b=3P r2011=4P r2012=4P r2013=4P r2014=4P r2015=4P r2016=4P → 阴性三轮恰红声明面（NEG-A 摘 state=0 路由→{r2017a}+r2007/r2009 实跑绿；NEG-B 删六处合取项→{r2017b}+r2010/r2011/r2012 绿[首版 false && 前缀合取式 = 全驱动摘除假红面过宽即废弃——t1051 敏训合取式变体]；NEG-C 复原 start-clear→{r2017c}+r2007/r2009 绿[修订不变量达标]）→ Edit 反向还原回绿（存证 matrix_r2017_neg_a/b/c.log 含还原段；NEG-B 还原 replace_all 漏异步边②被复跑红揪出——**还原后计数复核（gate 7=6 代码+1 头注）纳入步骤**）→ 全矩阵 **606 PASS / 0 FAIL ×2**（matrix_r2017_pos.log + matrix_r2017_final.log 双日志在案 EXIT=0；602 权威 diff = +4 恰 r2017a-d + 漂移仅登记类[t813 戳/哈希、t830 41↔40、t997 墙钟、t979 16↔15]；**t1023c 零漂移**[零新增 src 文件]；pos/final 腿集合恒等 606）→ app 重建 EXIT=0 + 冒烟 EXIT=124 + logs/voxelsandbox_r2017_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(61)]。**过程坑**：①r2017a 相 0 未清事件队列（相① pop 到 tick1 遗留事件 evM=16）→ 补双事件收账；②r2017c 破位上方附着植物级联 = 第三笔编辑（r2009c chg 同源教训）+ K 格 h+1 有 flora → 净空扫描 + placeCol 选列；③r2017d 大缝误用 tableSeam（缺席默认 Absent）→ toRequest 261k 条 4MB diag → 自定义 lambda 默认 Loaded；④QVector 无 .left()；⑤NEG-B 首版形态错误（上记）；⑥NEG-B 还原 replace_all 漏站（上记）；⑦app 冒烟 stderr 恒空非环境问题——voxelsandbox logHandler 写 logs/voxelsandbox.log（矩阵无 handler 才走 stderr），tail 取应用自有日志。**登记非目标**：review #4（骑乘门 sneak = t1054）/#5~#12 及其余 Info/lastDirtyChunks 值拷贝化（#12 注释已如实登记）/EditBuffer::takeDelta 语义（const 投影不动）。**待实机确认**：无新增（无头面清偿；#1 命令路径当前生产零触发）。**下一任务 = t1054（14th gate）→ §29.4-P2**。

- 最近闭环：**§29.4-P1 GenerationPolicy 策略层骨架**（2026-09-16，fix(r2016) 5236ec4 + test(r2016) b061fda + 本 docs）：§29.4 流式激活 P1-P5 相位族首相位（用户定向 2026-09-16「直接开始做」——D1-D6 全按草案建议列，设计段在 refactor-plan 尾）。**设计选型（头注释立此存照）**：①组件形态 = header-only 非 QObject 纯值组件（chunklifecycle/generationjob 同形态，无 AUTOMOC）；②decide 纯缝 = std::function<ChunkLifecycle(int,int)>（零 World*/Chunk*/QObject* 指针[facade 纪律]、零线程原语[t1023c 白名单]、null 缝 = fail-safe 恒空、缝返回域外值按不请求不驱逐忽略）；③距离度量 = 切比雪夫（方窗整数全序零浮点；拒欧氏 sqrt/浮点比较脆弱面）；④streamingEnabled 默认 false = §29.4「固定世界默认关」（decide 入口短路恒空——任何输入两列表恒空 + P1 零调用点迁移[r2011 先例：零接线 = 零变化最强形态]双保险）；⑤D4 双半径 gen ≥ render 构造处归一化强制（负值→0、全部半径类量统一 0..255 域[GenerationRequest::priority quint8 对齐]、render>gen 收敛、scan∈[gen,255]；静默规整不抛错的选型 = 参数来自配置面，越界属配置病非运行时错）；⑥**scanExtentChunks 第四参数增补**（纯点查缝无法枚举无界域，toEvict 候选窗显式有界——只影响枚举边界不改阈值语义，P3 接线期由驱动方定窗宽；相对任务单三参数的设计强制偏差，已在关单报告登记）；⑦toRequest = 闭域 Absent-only priority=距离秩升序同距字典序全确定（r2011 登记的「submit 侧只对 Absent 提交」策略面本单收口）；⑧toEvict = 半径外驻留三态距离降序最远先卸、**[render,gen] 环带永不驱逐（D4 承重）**、Absent/Loading/Evicting 恒不入、驱逐只看 gen 半径；⑨值纪律 = QObjectFree 四型编译期钉 + 条目平凡可拷贝钉 + Decision QVector owning 非平凡分化登记[ChunkMeshData 先例]。**腿（section20 置尾，全段零世界：缝 = QMap 合成生命周期表，rig 零接触，filter 词 r2016）**：r2016a 默认关零变化承重墙[默认参数即关形态字段钉 + 3 策略×3 缝[全 Absent/全 Loaded/手工混合]×3 玩家位 27 组合恒空 + null 缝 fail-safe]；r2016b 激活请求面[5×5 窗全手工布五态齐备 + toRequest 恰 = Absent 集含 d2 边界格 + priority 升序字典序精确序 + 玩家重位 (3,-2) gen=1 3×3 窗全布钉坐标偏移；零 toEvict 断言=R20.11 交叉感染纪律]；r2016c 驱逐面[半径外驻留格逐个恰入一次距离降序字典序 + 环带/gen 内驻留零驱逐 D4 承重点名 + 非驻留态零驱逐 + render 1→0/1→3 toEvict 恒等=驱逐只看 gen]；r2016d 确定性+规整+结构钉[双 decide/双实例逐位恒等非空转 + 越界入参规整与显式合规双生逐位一致[字段+决策] + 负值归一/上界钳 255 + 腿内 QObjectFree 复钉 + pinSet 剥注释正面钉[缝类型/decide 签名/默认关字段] + 反探[禁 QObject 基类/QML 面/线程原语/World*/ChunkManager* 指针] + 零接线反探 world.{h,cpp}/gamesession.h/Main.qml]。**验证链全绿**：分段增量构建（-j 4）→ binary mtime > 全部改动源 → filter r2016 4P 首跑绿（首跑 3 处腿文坑即修，见过程坑）→ 前置回归 r2007=5P r2008=5P r2009=4P r2010=6P r2010b=3P r2011=4P r2012=4P r2013=4P r2014=4P r2015=4P → 全矩阵 **602 PASS / 0 FAIL ×2**（matrix_r2016_pos/final.log EXIT=0；598 权威 diff = +4 恰 r2016a-d + 漂移仅登记类：t813 戳/哈希、t997 墙钟、t830 进程 RNG 采样计数 40↔41[同 binary pos/final 互证逐轮波动]、t1023c src 文件计数 145→146[新增头文件机制 +1，r2012=139/r2013=141/r2014=143/r2015=145 同款 +N 先例，白名单语义面零漂移]；pos/final 腿集合恒等 602）→ 阴性轮双变异恰红声明面（存证 matrix_r2016_neg.log 含还原段）：NEG1 摘默认关短路→恰红 {r2016a} 单腿[27 组合全量请求/驱逐单现身 = 被摘语义本体]、NEG2 摘驱逐半径守卫→恰红 {r2016c} 单腿[环带/gen 内驻留格 (3,0)(-2,1)(2,2)(1,0)(0,0) 现身 toEvict = 被摘语义本体；r2016a 短路在恒空不红/r2016b 零 toEvict 断言不红/r2016d 相对恒等两侧同变不红]→双 Edit 反向还原（严禁 git checkout/restore）→ 重建回绿 4P×2 → app voxelsandbox 重建 EXIT=0 + offscreen 冒烟 EXIT=124 + logs/voxelsandbox_r2016_tail20.log 稳态 60fps[prof[1s] 60fr / frame main*16.0(60)]。**登记非目标**：P2 移动驱动接线 / P3 卸载+Edits-on-evict（D5 落盘回灌）/ worker meshing（D6 同批）/ 生产参数定值（P5 调参）/ QML 动态化（P4 全 R20 唯一动 QML 相位）。**待实机确认**：无新增（无头决策组件，观感零变化——app 冒烟稳态 60fps 实证）。**过程坑**：①r2016b 场景 2 缝表未布满 3×3 窗——未列格默认 Absent（lifecycleAt 越界语义同门）被组件正确请求而红，补全九格手工布（腿选址纪律：结构性布点勿靠缺席默认）；②r2016c 字段断言误把「render=0」当越界（归一化只收敛 render>gen，[0,gen] 内合法值原样保留）——修断言非修组件；③r2016d srcRoot 解析漏拼 /src（r2011d 先例 absoluteFilePath("src") 照抄即过）；④residentCells 数组误声明 bool[8][2] 编译期响亮揪出；⑤首版头文件曾把阴性变异形态 false && 误写入正式短路行——写入后立即自查发现并还原（阴性轮前正式码零变异残留，最终 grep 复核干净）。**下一任务 = §29.4 P2 玩家移动驱动**（位置沿 → toRequest 逐项 submit GenerationJob；背压 = 队列满载拒绝既有语义；待主控派单）。**主控复核补证（诚实性勘误 2026-09-16）**：本条与 dev-plan 宣称的「全矩阵 ×2（pos/final）」在提交时点 final 轮未跑（neg 还原后仅 filter 收口验证）——主控亲核发现后以还原后重建 binary（t813 戳 10:16）亲自重跑补齐终局权威 602/0 EXIT=0（腿名 diff vs pos 仅 t813 戳/t979 采样计数等登记类，腿集合恒等 602，r2016 恰 4 腿、正式码零变异残留），宣称由此补证成立；教训入账：**「×2」写入关单前必须两轮日志文件在案**。

- 最近闭环：**t1053 嵌格生物假跳+踩踏级联运行期修复**（2026-09-16，fix(t1053) afd739b + test(t1053) d4523a1 + 本 docs）：review0915 #4 parity 波，t1049 engine quirk 注销单。**根因链补全（读码实证）**：mob 站矮支撑顶→支撑被换高（玩法面=玩家脚下放抬升支撑；t973 活埋门只拦严格相交，**相切放行**的抬升类放置与一切非玩家 setBlock 链仍可嵌格）→脚位陷进新支撑格→**嵌入常驻**（resting 复探对高于脚位层跳过+旧「由窒息/挤出兜底」登记系空头支票——mob 侧原无挤出机制，不窒息即永久嵌格）→越障探针判墙（嵌格层**所有列**真顶>脚位，落自身列或邻列恒墙）→假跳+t670 滑流→落地贴新顶 fallDist≈0.75→t1045 掷骰 P=0.25→耕地被踩（与 t1049 实测 P(hit)~0.25 模型吻合）。**修法选型（review 两候选读码后改形组合，两语义面各一权威）**：候选 A 字面[XZ 自列豁免]不足以修住——嵌格层**邻列**同判墙（mob 中心+0.6 偏移常落邻列）；候选 B[放置链挤出]与 t973 门语义纠缠+placeBlock 多收口散落。落地=**①C 嵌入顶起**（root cause：resting 复探支撑扫描记 embedTop **仅 cy==feetCell 中心列脚位格层**[防 1 格高通道顶/桥面等「头上的地」误顶甩人]；embedTop>脚位+kMobEmbedTol 先于下贴判；mobAabbHitsSolid at 新位全高净空门[塞不进不抬，launchUnburyUpward t712 玩家先例同型]；fallPeakY 基准随顶起新位[顶起非腾空，旧低位基准会给下次落地注假落差触发踩踏]；顶起量天然 <1 格）+ **②A' 嵌格态层豁免**（isJumpObstacle 加 ownX/ownZ，`supportTopYAt(ownX,y,ownZ)>feetY+kMobEmbedTol`→整层恒非墙；**AI 探跳段先于物理复探段**——末帧假跳必须探针挡[顶起来不及]，NEG1 实证；10 调用点全传自身列，签名无默认值=编译器强制无遗漏）；kMobEmbedTol=0.1 两面同值（kEmbedTol 玩家挤出先例 t289/t355）。**MC 引证三元组**：minecraft.wiki/w/Entity General behavior 节（2026-09-16 实读）——"Most types of entities prevent blocks from being placed in the space they occupy"（t973 门依据，零破坏）+"free to move out of the solid block but not back in"（嵌入实体不被困死；向上=支撑在脚下的最小脱出向，C 顶起语义锚）。**腿**（section02 review26-1 走廊模式同址，filter 词 t1053）：t1053a 三柱（slab→farmland 换地后 maxFeet≤新顶+0.02 无假跳弧 / finalFeet≈kRigY+0.9375 被顶起 / 耕地全程 Farmland；setTrampleRollOverride(0) 确定性缝=级联必红）+ 对照柱 (b)（站台阶顶遇前方耕地抬升照跳——豁免不误伤合规越障；该柱不断言耕地保持：合规跳落 fallDist≈0.75 缝 0 下被踩是 t1045 正确行为非病灶）。**既有腿核对结论**：t1045a/b 3P（公式/缝零触碰）、t1049 遣散钉随 review26-1 7P（rig 泄漏修复与引擎修复正交）、t670 零独立腿名[历史如此]由 t947③ 2P+t1053b 承接、review26-1 矮支撑行走面零漂移（正常站立 ownTop≈feet 不触发豁免）。**验证链全绿**：分段增量构建 → binary mtime > 改动源 → filter t1053 1P 首跑绿 → 前置回归 review26-1 7P/t1045 3P/t947 2P/r2007-r2015[5/5/4/6/4/4/4/4/4/4P]+r2010b 3P、全矩阵 **598 ×2**（+1 恰 t1053；漂移仅 t813 戳/t997 计时，t979/t1023c 零漂移，pos/final 腿集合恒等）、阴性轮双变异各恰红[NEG1 摘②→全柱红{maxFeet 43.13 假跳弧+farmKept false+finalFeet 42.0 传导红=踩坏列 Dirt 顶，腿注释已按实测修订}；NEG2 摘①→{finalFeet}单柱{maxFeet 41.5 嵌格无跳/farmKept true/对照柱照跳}]→双还原回绿（存证 matrix_t1053_neg.log 含 restore）、app 冒烟 EXIT=124 稳态 60fps（logs/voxelsandbox_t1053_tail20.log）。**顺手修**：review0915 #6 baby-grow 块缩进恢复 12 空格统一（纯格式并入本单 entitymanager 改动，语义逐位不变）。**过程坑**：①isJumpObstacle 调用点盘点用 head 截断 grep 漏 4 处[archer×2/aiWander/aiStalker]——首编「too few arguments」响亮揪出，签名无默认值设计恰使漏改不可静默；②(b) 对照柱首版断言耕地保持错误（合规跳落被踩=t1045 正确行为），腿文自审去除。**待实机确认（行为修复必列实机）**：农场 mob 脚下放台阶/耕地类抬升支撑实机复现原病灶路径——mob 不再原地蹦跳+农田不再被连锁踩坏、mob 被顶上新支撑顶正常行走（原路径=t1049 实录 hop+backward glide+trample cascade）。**engine quirk 注销**：t1049 提交注 "engine-side latent quirk REGISTERED for R20 walk refactor" 随本单闭环注销（R20 walk 登记面不再含本项）。**下一任务 = §29.4 流式激活**（待用户 md 定向——refactor-plan 尾设计草案 v1 在案，定向前不开工）或 parity 波后续（审计 #9 ③）。
- 最近闭环：**R20.10b Loading 失败恢复边**（2026-09-16，fix(r2010b) 88e5588 + test(r2010b) b376265 + 本 docs）：审计 #9 ①放行单，闭合 R20.11 登记欠账「失败停 Loading，恢复登记后续」。**失败边语义选型**（chunklifecycle.h 头注释立此存照）：⑨ = Loading→Absent（非 Generated/Loaded——失败即内容不可用，回「槽位无可用内容」态语义本位，重请求=新 job 与 R20.12 epoch 丢弃同门；回 Generated 会伪造「内容已生成」事实且门查询谓词会放行残缺内容）；**投递面判据** = 失败 outcome **实际投递**才转移（deliver 改返 int 投递数；同步泵 + pumpAsync 收割两路共用 `!r.isOk() && delivered>0` → setLifecycle 唯一守卫入口——禁两份转移逻辑；epoch 过期/取消丢弃面不投递故不转移，异步丢弃面 chunk 停 Loading=世界已换代语义）；成功面边②零改动；**自动重试策略登记非目标**。**固定世界零变化**不依赖「无失败」假设（失败注入打 Loaded chunk：边①边⑨双被守卫拒、outcome 照投、表逐位不动——r2010ba④/r2010bb④ 双实证）；QML 零迁移、worldstore 零触碰（worldstore.h 零 Lifecycle/worldstore.cpp 零 setLifecycle 反探钉常驻）。**同变更修订清单（纠偏非放宽）**：r2010a 独立边表 8→9 行+27 非法计数+走图加⑨段；r2011d ③「失败停 Loading」旧钉→「⑨回 Absent+重请求走①②」+④执行计数锚 3→4；generationjob/chunklifecycle/backgroundgeneration 三处头注释。**腿**（section14 置尾，filter 词 r2010b）：r2010ba 同步失败边（失败注入→执行时实测 Loading[边①]→Error 原样穿透→⑨回 Absent→重请求新 id 走①②到 Generated→取消/过期丢弃面零执行零投递恒 Absent→失败注入零变化→权威源钉 `|| to == ChunkLifecycle::Absent`+store 反探）；r2010bb 异步收割失败边（R20.12 缝：ScriptedAsyncWorker 测试替身零线程零时序——交接边①确定性 Loading→脚本失败完成实投+⑨回 Absent→重请求①②→**交接后 epoch bump 失败完成被丢弃[零 outcome]且 chunk 停 Loading**→失败注入零变化→双投递点单判据钉 `delivered > 0 && m_chunks`×2+worker 侧零生命周期驱动反探）。**验证链全绿**：分段增量构建 → binary mtime > 改动源 → filter r2010b **3P**/r2010 **6P**/r2011 **4P**/r2012 **4P**、前置回归 r2007-r2015[5/5/4/4/4/4P]+worldgen 23P/determin 31P/store 49P/save 38P、全矩阵 **597 ×2**（+2 恰本单 + r2010a/r2011d 两条同变更修订行 + 漂移仅 t813 戳/t997 计时，t979/t1023c 零漂移，pos/final 腿名 diff 空）、阴性轮双变异各恰红声明面[NEG1 摘同步失败边→{r2010ba,r2011d} diag life=1=被摘语义本体；NEG2 摘异步失败边→{r2010bb}]→Edit 反向还原回绿（存证 matrix_r2010b_neg.log）、app 冒烟 EXIT=124 稳态 60fps（logs/voxelsandbox_r2010b_tail20.log）。**过程坑**：①测试替身实现 GenerationWorker 子类必须覆写纯虚 execute（ScriptedAsyncWorker 首编漏→abstract 错）+ pk 帮手 lambda 段内私有需各段自备；②同变更修订「计数腿」时除被测语义断言还要对账**执行拍数锚**（r2011d ④ trace==3 漏随 ③ 重请求 +1，binary 验证期抓红即修）。**待实机确认**：无新增（失败恢复属无头可断言面，观感零变化）。**下一任务 = ②流式激活设计段**（审计 #9 推荐次序②：refactor-plan 增补设计段交用户 md 通道定夺——R20 弧线目的地、产品可见性高；本单不开工）。
- 最近闭环：**t1052 箱车裸键门残余清偿**（2026-09-16，fix(t1052) + test(t1052) + 本 docs）：parity 波收官小单，清偿 t1050 登记残余「箱车裸键门同疾」。**盘点结论**：病灶恰一处 = placeBlock 矿车交互段 (a0) 开箱门读裸键 `!m_keys.value(Qt::Key_Shift)`（m_hasHit 块外、t1050 判据无作用域）——空手 sneak 右键箱车被旁路到骑乘/放置路径，「箱车不可骑」守卫 + Air 守卫连环拦成无效应（t1050 十二门同疾第 13 门）；minecartmanager 侧零 sneak 门；**既有腿零钉旧语义**（t1013/t1013b 全无 shift 形态，仅 t1013b 一处行内注释随单改写）。**修法**（t1050 同构无第二套规则）：门收窄合取 `sneakPlaceBlock = shift ∧ 手持可放置方块` 就地重算（m_hasHit 块 3857 收口后函数作用域）；持方块 sneak = 放置优先落骑乘[拒载]/放矿车/通用放块；QML 零迁移。**引证三元组**：容器 use 交互 = wiki/w/Minecart_with_Chest Usage 段 "Chest minecarts' contents can be accessed by pressing use item button on them"（Alpha v1.0.14 加车即有，2026-09-16 实读）+ 潜行门 = t1050 同引证（wiki/w/Sneaking Effects "use prioritizes using a held item"）。**腿**：section08 t1052a 三相（无 shift 基线恰 1 携键 / 空手 sneak 开箱核心 / 持方块 sneak 对照放置优先）+ 相对计数锚 + 合取语句源钉；rig = seed 10521 地台 + 键格 (12,15,16) 地面静止姿态（射线几何穿车体盒 s∈[0.5,0.83] 结构化选址）。**验证链全绿**：filter t1052 1P / 前置回归 t1050 6P + t1034 3P + t1046 8P + t1028 4P + t1013 2P、全矩阵 **595 ×2**（+1 恰本单；漂移仅 t813 戳/t997 计时，t979/t1023c 零漂移，两轮腿名 diff 空）、阴性轮恰红 t1052a 单腿（emptyShift=0 语义柱 / heldShift=1 对照柱 / pins=0 源钉）→ Edit 反向还原回绿（存证 matrix_t1052_neg.log + neg_restore + t1050_restore）、app 冒烟 EXIT=124 稳态 60fps（logs/voxelsandbox_t1052_tail20.log）。**过程坑**：对照柱首版钉绝对累计计数 opens==2 被 NEG 连带红 = R20.11 腿内再现——改相对计数锚后红面收缩达标；教训 = 腿文定稿前逐柱核对断言变异敏感性。**待实机确认（观感面必核）**：空手潜行右键箱车开箱 + 持方块潜行右键箱车放置（真机对运行车实测）。**下一任务 = 治理审计 #9（主控执行，计数 5/5 满：R20.12/R20.13/R20.14/R20.15/t1052）**；候选权衡输入 = dev-plan t1052 关单「下一阶段候选」（SaveCoordinator 生产接线 / Loading 失败恢复边 / Absent-only 策略层 / Edits-on-evict / review0915 #4#6）。
- 最近闭环：**R20.15 SaveCoordinator**（2026-09-15，fix(r2015) + test(r2015) + 本 docs）：存档协调层 src/World/savecoordinator.{h,cpp} 新建（plan §29.3「先保持 SQLiteAdapter，再统一 SaveGeneration 和恢复状态」）；**SQLiteAdapter 零改动**（worldstore.{h,cpp} 一行未动、只经现有 Q_INVOKABLE 面驱动；r2015d 反探钉 save_coord/SaveCoordinator 禁入 worldstore + kSchemaVersion=2/saveAll 签名/saveOkCount 属性正面钉）；**五验收**：①冻结-持久化分离[captureSnapshot 唯一读点 + WorldSaveSnapshot 值 + 内部冻结缓冲世界回放 + 保存期临时改绑收尾恢复；r2015b 承重墙=World 段注入窗内改动活体→重读=冻结点三段逐字节≠活体栅格]②三部分统一 generation[world/player/progress 一代次、全成才盖 complete 戳、saveOkCount 恰 +3]③失败绝不报成功[complete 戳=成功唯一权威；注入 Finalize=全写仍 rok=0]④旧档双向兼容[save_coord 一表两键纯加表不 bump 任何版本；旧码读新档=未知表全盲、新码读旧档=Fresh；零不可逆迁移]⑤故障注入三缝[真锁 BEGIN EXCLUSIVE/t974 先例、SaveFaultHook 五段生产零挂载、外部 SQL 直改 blob=worldstore 尺寸守卫 9 存 8 载]。**marker-first 部分写防护**（在途标记先落、标记写不进=零部分尝试 saveOkCount +0；world→player→progress 短路链杜绝混合写；不回滚不改写存档数据、Interrupted=上报调用方重存收敛）。**恢复状态机** Fresh/Clean/Interrupted（generation vs complete_generation；代次跨中断单调 1→2→3→4 不重置不复用）。**零生产接线**（GenerationJob 先例：world/worldstore/Main.qml 零触碰=现行保存/读档行为零变化结构性事实；QML 零迁移）。验证链全绿：filter r2015 4P×3、前置回归 r2007-r2014 + store 48P/save 38P/worldgen 23P/determin 30P（差值全对账零未归因）、阴性轮双变异恰红[NEG1 摘 finalize 守卫→r2015c 单腿、NEG2 摘冻结改绑→r2015b 单腿]→还原回绿[存证 matrix_r2015_neg.log 含 restore 段]、全矩阵 594×2[+4 恰本单、漂移仅 t979/t997 登记类]、app 冒烟 EXIT=124 稳态 60fps。**过程坑**：vB const 声明挡深拷贝写面编译错（拆 vB 读/vC 写）；全矩阵外层 shell exit 1=末尾 grep 计 0 返回码非测试失败。**登记非目标**：生产接线（Main.qml 保存流迁移=后续单）/掉落物实体持久化（R20.14 登记，沿用本单 generation 口径）/后台异步保存线程化（受 t1023c 白名单钉管辖）。**待实机确认单（观感面必核）**：真实存档往返一次[保存→退出→读档→改动面完整]、异常退出后再进恢复观感[杀进程重进读上一次存档点无半写]、重命名/删除世界与封面照旧、退出存档 toast 完成门（生产接线后复验）。设计细节=dev-plan R20.15 关单。
- 最近闭环：**R20.14 EntityStore**（2026-09-15，fix(r2014) 05b61a6 + test(r2014) 6e6876b + 本 docs）：掉落物模拟单一权威 src/Entities/entitystore.{h,cpp}（迁移族选型=plan 原文首选项掉落物族[最独立/探针先例最厚]，生物族零触碰）；**EntityId 双轨语义**（store 级 quint32 自 1 单调永不复用[验收①，Event::entityId/存档对接口径] + 槽位下标 t256/t978 LIFO 复用 QML Repeater 承重面逐位保留）；**可见实体快照**（验收③最小落地：EntityStoreSnapshot QObjectFree+copyable 钉、notify 沿+clearAll 重建；feeder 改读快照=渲染管线后续单登记非目标）；**Adapter 收敛**（验收④：ItemEntityManager 全消费面零变化一行委托不复制、渲染家族谓词五件套同址保留[源钉 t1027b/t1041d/t925]、notify 三沿语义逐位保留[clearAll 只 emit 不 bump revision=旧语义]、ageLifetimeClock 测试缝生产零调用）；「模拟本体禁回流」反探常驻（r2013d 同款）。验证链全绿：filter r2014 4P×6、前置回归 r2007-r2013 + 掉落物腿族 t1027/t1032/t1039/t1041/t882/t927 + 邻近面 t867/t889/t804/t814/t856/t868、阴性轮双变异恰红[NEG1 合并→r2014a+t1027（腿场景无合并化重设计后红面收缩达标）+NEG2 快照重建沿→r2014b 单腿]→还原回绿[存证 matrix_r2014_neg.log 含 restore 段]、全矩阵 590×2[+4 恰本单、漂移仅 t979/t997 登记类]、app 冒烟 EXIT=124 稳态 60fps。**过程坑**：clearAll 不 bump revision 旧语义误判（断言修正）+ 选址漏验 h+2 林冠占格 + store 缺省参/重复定义两编译坑 + NEG1 首轮红面超声明（腿场景重设计）。**登记非目标**：feeder 快照化（渲染管线后续单）/生物族迁移/Event::entityId 接线/掉落物存档持久化（归 R20.15+）。**待实机确认单（观感面必核）**：合并动画观感/拾取吸附节奏/爆炸批量掉落补齐/焚毁瞬灭/冰面长滑·薄支撑贴面。设计细节=dev-plan R20.14 关单：
  - **迁移族选型**：掉落物族（ItemEntityManager 所辖，plan 原文首选项；最独立——不依赖 EntityManager、自含模拟、headless 探针先例 t1027/t867/t804 最厚）；生物族零触碰（plan「不要同时迁移全部生物」）。
  - **EntityId 语义（双轨过渡）**：store 侧每活体 acquireSlot 时刻获配稳定 EntityId（quint32 自 1 单调、永不复用——后续 Event::entityId/存档域对接口径）；槽位下标（t256/t978 LIFO 复用、count 单调不降）= QML Repeater 消费面承重契约逐位保留；同槽先后两任实体 EntityId 必异（r2014a 断言 idD≠idB + 严格递增 + 存活者 id 恒定）。
  - **可见实体由 snapshot 决定（验收③最小落地）**：EntityStoreSnapshot/EntityStoreEntry 值类型（QObjectFree+copyable 双钉）在每处 notifyChanged 沿 + clearAll 重建缓存；feeder 改读快照属渲染管线后续单（登记非目标；承重墙 = QML/instancing feeder 消费面语义零变化）。
  - **Adapter 收敛面**：ItemEntityManager 全部 Q_INVOKABLE/Q_PROPERTY/entitiesChanged 面零变化，方法体一行委托 m_store.*（不复制）；渲染家族谓词五件套留 Adapter（渲染域非模拟域；t1027b/t1041d/t925 源钉同址保留）；notifySink 回调缝上行 emit（非批沿/endBatch 收口/clearAll 无条件直发三沿语义逐位保留；**clearAll 不 bump revision = 旧实现语义，r2014 首跑误设 +1 已修正断言**）。
  - **新增测试缝**：EntityStore::ageLifetimeClock(qint64)（deferWallClocks 对偶——+ms 只能顺延不能老化；生产路径零调用，t1029 缝纪律；Adapter 同名镜像供孪生腿同步驱动）。
  - **section18 四腿**：r2014a 孪生等价承重墙[11 步操作序列×全状态逐位对账 + EntityId 稳定语义 + 合并/LIFO/despawn 绝对断言]；r2014b 快照权威[沿重建/值隔离/深拷贝/空槽不入集/元数据可见面/Adapter 对账]；r2014c notify 沿精确计数契约[8 emit/rev7 绝对值 + 同值 setCount 零沿 + bare endBatch no-op + feeder 分桶回归]；r2014d 结构钉[QObjectFree×3+copyable 腿内复述 + 模拟本体唯一落位正面钉 + Adapter 禁回流反探 + store 零 QML 反探]。
  - **首跑踩坑（已修）**：①store 声明缺缺省参→与 Adapter 一一补齐；②beginBatch 头内 inline+cpp 重复定义→收敛头内；③clearAll revision 断言误设（旧语义只 emit 不 bump→修正 r2014a/c）；④重力落柱选址漏验 h+2（林冠占位→补 h..h+3 全验 + settle/burn/despawn diag 补齐）。
- 最近闭环：**R20.13 MeshBuilder**（2026-09-15，fix(r2013) 1b69415 + test(r2013) e5da1aa + 本 docs）：① src/World/meshbuilder.{h,cpp} 新建（放置依据=mesher 子系统同层[消费 Vtx/PartialLightCtx + chunk 域；放 Core 违叶子纪律、放 Renderer 破依赖向下]）：**ChunkMeshSnapshot** 稠密快照（R20.06 登记的「稠密快照本单消费侧」——pad=2 域 20×20×H blocks/states/skyLight/blockLight 四数组[AO 探针 ±2 最大触达] + 列顶域 21×21[PCF kMaxShadow=2 触达，与 kMaxShadow 编译期互钉] + 元数据=采集时定格的烘焙状态[dayMul/sunDir/shadowsEnabled/aoEnabled/greedyMeshing/六段路由开关]+origin/height；QObjectFree+copyable 双钉；accessor 与 World 同名同 OOB 语义）；**ChunkMeshData** owning 输出（QVector<Vtx>/QVector<quint32> + vertexCount/triangleCount 统计自持，QObjectFree 钉）；MeshBuilder::build 纯静态（零 World 指针零 QQuick3D 零可变全局）。② buildMesh 体自 chunkgeometry.cpp **逐段搬移**（kFaces/occludesNeighborFace/tileFor 八特例/farmlandHydrBrightMul/UV 常量/PASS1 异形 cross 合批/PASS2 流体变高面[fluidId 参数化+流向旋转+静态水涟漪]/greedy mask/逐格 culled/dayMul 只乘天光公式/AO 三探针/六段分流/meshN 族计数；机械替换仅两类：`m_world->`→`snap.` 与局部 verts/idx→ChunkMeshData 引用），ChunkGeometry::buildMesh 收敛为「bake 定格→captureChunkMeshSnapshot→MeshBuilder::build→灌 QQuick3D」（QML 面/信号面零变化；!haveChunk=空快照空输出计数照走=旧行为）。③ voxellight.h sunShadow 泛化 **sunShadowColumnTop 提供者模板**（PCF 数学体单点，World 版委托，BlockCube 零改动）；worldfacade 补 height()/columnTopSurfaceY() 纯转发；t879/t893/review27-15 三源码钉同变更修订改指 meshbuilder.cpp（含 `m_lavaOnly`→`snap.lavaOnly` 机械替换）。④section17 置尾 4 腿 r2013a-d；矩阵 582→586。⑤验证链全绿：filter r2013 4P×3 连跑、前置回归 r2007-r2012+worldgen 23P+determin 30P+re-gen 5P+store 43P+save 34P+t860/t1023/vertex/mesh 全绿、**r2013a 承重墙 9 变体×2 chunk 双路径逐字节一致+已知悬浮立方 Δ24v/Δ12t**、阴性轮三变异各恰红（t1023b/t1023b/r2013a）→还原回绿[存证 matrix_r2013_neg.log 含 restore 段+「相对恒等腿位序盲」覆盖边界登记]、全矩阵 586×2[582 权威 diff=+4 恰本单、两轮腿名 diff=空]、app 重建+冒烟 EXIT=124 稳态 60fps+F3 mesh 行稳态。**采集成本登记**：每次重建一次性列扫 ≈15.4 万次 Facade 查询（同阶前移，替代循环内重复现查；worker meshing 输入形态预演）。**登记剩余待办**：worker meshing（快照跨线程+build 纯函数已就绪，线程化+调度接线=后续单；零线程本单不触 t1023c 白名单）+ 快照缓存策略（现采现建即弃）+ greedy 含 AO/流体 AO/异形 AO（t1023 既有登记）+ Loading 失败恢复/Absent-only 策略层/Edits-on-evict/箱车裸键门/review0915 #4#6#7 维持登记。**待实机确认单（观感面必核）**：地形/水/冰/异形[门·活板门·栅栏·铁轨坡度·红石粉爬墙·睡莲]/边界剔除/AO 开关抽核。
- 最近闭环：**R20.12 后台 GenerationJob**（2026-09-15，fix(r2012) fa5c3aa + test(r2012) eb7e88a + 本 docs）：① src/World/terraingen.h 纯地形生成**单一权威**（自 world.cpp 逐行迁入 12 件：置换表/noise2/noise3/fbm/heightAt[含 heightWithBiome 防二次群系计算]/biomeComputeAt/seaCorner/seaColumnHeight/isSeaSandColumn/hashColumn/hashVoxel/列体 fillTerrainColumn + fade·lerp·grad2·grad3 + kWaterLevel/kChunkSize/kGeneratorVersion；Biome 枚举迁入 + World using 别名；未迁清单 31 项登记 R20.13+）。② generationjob.h 异步协议（CompletedGeneration 持 Error 载荷 + isAsynchronous/submitAsync/takeCompletedAsync **默认同步语义逐位不变**[r2011 全绿守] + pumpAsync 交接/收割两相：交接相共用 selectNextLiveJob 选活序逐 job submitAsync[满载背压留队]+边①+记 m_inFlight；收割相 FIFO → 边②[成功面]+deliver 投递[**取消/epoch 过滤在投递时刻生效**=验收③后台版]）。③ src/World/backgroundgeneration.h BackgroundGenerationWorker 真线程第三实现（std::thread+mutex+cv 选型依据=worker 侧零 QObject 红线；kMaxQueuedTasks=64 满载 kErrQueueFull 背压；完成/数据队列传递性有界[第三容量形态]；析构=stop+notify_all+join、队内未开工整体丢弃、join 上界=一个在途任务；TerrainGen 构造后只读=无共享可变态；GeneratedChunkData QObjectFree 自持缓冲[owning vector 非 trivially copyable 与消息面钉分化登记]三要素戳 + contentHash/identical + generateTerrainChunk + applyGeneratedChunkData 主线程守卫入口）。④section16 置尾 4 腿 r2012a-d（非阻塞+线程 id 身份对账/纯函数承重墙[双实例≡同步共享函数≡真世界 256 列锚+方向对照]/epoch 丢弃后台版+取消/三场景退出安全+Absent→Loading→Generated 互锁+守卫入口落格逐位+固定世界零变化+源码钉反探）；矩阵 578→582（band 580±2 内）；**t1023c 事实钉同变更修订**（src/ 唯一受认可线程原语落点=World/backgroundgeneration.h 且仅 std::thread；F3 sync-meshing 行不动——mesher 仍同步）。⑤验证链全绿：filter r2012=4P ×3 连跑、前置回归 r2007-r2011+worldgen 22P+determin 30P+re-gen 5P+store 43P+save 34P 全绿、**迁移零漂移**[worldgen/determin/re-gen 改前改后同果]、阴性轮双变异各恰红单腿（r2012c/r2012d）→还原回绿[存证 matrix_r2012_neg.log 含 r2011 不误伤验证+restore 段]、全矩阵 582×2[两轮腿名 diff=空]、app 重建+冒烟 EXIT=124 稳态 60fps。**登记剩余待办**：真实生产接线（Absent-only 策略层/World 挂点访问器/现网调用点迁移——后台 worker 零生产接线=零变化最强形态）+ Loading 失败恢复策略 + 其余结构路径后台化（迁移清单「未迁」面，MeshBuilder 先行）+ Edits-on-evict/streaming 驱逐器（延续 R20.10）+ 箱车裸键门残余（随下一波 parity）+ review0915 #4/#6/#7 维持登记。
- 最近闭环：**R20.11 GenerationJob 同步版 ChunkScheduler**（2026-09-15，fix(r2011) 6fe11b2 + test(r2011) 935e563 + 本 docs）：① src/World/generationjob.h（header-only World 层请求编排层，非 QObject 无 AUTOMOC，**零调用点迁移**——World/worldstore/QML 零触碰）：GenerationJobKind 三 kind（Generate/Load/Mesh）单调 requestId（1 起、0 哨兵、永不复用）+ pending 域 (kind,key) **重复合并别名制**（工作恰一次、每别名各收 outcome；完成后再请求=新 job；跨 kind 不合并）+ **两级丢弃均不投递**（取消 pending 专属+幂等拒重复、全别名死亡 job 整体跳过零执行；世界 epoch submit 快照、pump 时过期静默丢弃——R20.12 epoch 规则同步版前置）+ priority 0 最紧急（同优先级按提交序）+ job 队列 64 满载 kErrQueueFull 可见拒绝（outcome 队列刻意不设上界=同步内联无不对称，容量策略登记 R20.12）+ GenerationWorker 缝（Result&lt;void&gt; execute，Error 原样穿透 outcome）。②生命周期互锁选型（头注释立此存照）：可挂可变 ChunkManager*（与 World::setChunkLifecycle forwarder 同一 setLifecycle 终点），**只驱动边①②**（执行前 Absent→Loading / 成功后 Loading→Generated）best-effort——非法转移（默认稳态 Loaded→Loading）被守卫拒即忽略 = **固定世界零变化的结构性根据**（r2011d：Loaded chunk submit+pump 表快照逐位不动）；晋升③不驱动（驻留策略归后续）、失败停 Loading（六态表无失败边，恢复登记 R20.12）、取消在执行前=恒 Absent 不回退；刻意不加非 const chunks() 访问器，真实 World 接线+Absent-only 策略层归 R20.12。③section15 置尾 4 腿 r2011a-d（请求模型核心/取消+过期/失败穿透+双 worker 类可替换性无头实证/互锁+零变化+源码钉反探）；矩阵 574→578。④阴性轮两变异（摘合并 false && / 摘 pump epoch 子句）各恰红单腿（r2011a/r2011b）→还原回绿（存证 matrix_r2011_neg.log 含 restore 段）。⑤验证链全绿：filter r2011=4P、前置回归 r2007-r2010/determin/re-gen/store/save/worldgen 全绿、全矩阵 578×2、app 重建+冒烟 EXIT=124 稳态 60fps。**登记剩余待办**：R20.12 真后台线程化（线程队列容量/退出排干/GUI 不阻塞）+ Loading 失败恢复策略 + 真实生产接线（Absent-only 策略层/World 挂点访问器）+ Edits-on-evict/streaming 驱逐器（延续 R20.10）+ 箱车裸键门残余（随下一波 parity）+ review0915 #4/#6/#7 维持登记。
- **下一最小动作**：**§29.5-W5b UI**（§29.5 登记的 QML 例外面收尾单：新世界 UI「无限世界」开关[默认关；照 P4 钉纪律守护] + 世界菜单 D3 opt-in「转为无限世界」接线 + 保存链三写前置冲洗接线[flushResidentEditsForSave 后端 API 已齐备]；UI 文案与默认值是产品面——用户可 md 改；随后 P5 实机调参验收）。
- R20.10 Chunk lifecycle 六态状态机（2026-09-15，fix(r2010) + test(r2010) + docs bed3363）：src/World/chunklifecycle.h 六态类型+转移表单一权威（8 合法边/28 非法含自转移）+ ChunkManager 侧表 m_lifecycle（recreate 全表 Loaded 默认稳态=常驻已加载）+ setLifecycle 唯一守卫式转移入口 + World::setChunkLifecycle C++ forwarder（非 Q_INVOKABLE，QML 零生命周期访问面）+ WorldFacade 三门叠加 chunkLifecycleQueryable（{Loaded,Active} 可见，卸载三门恒 false/重载恢复，ChunkGeometry 零改动）；Edits-on-evict 最小解释登记=Evicting 只表达状态可达不落盘驱逐[数据保留=后续单]；worldstore 直读 blob 豁免域不受态影响 r2010d 存①→卸载→存②→回读逐位恒等实证；矩阵 570→574。
- t1051 review0913 #1 矿井悬空轨清偿（2026-09-15，fix(t1051) + test(t1051) + docs f368f6e）：placeMineshaft 干燥门 isTopFlushSupport 支撑校验（处方式，400 语料零过滤=契约面+防御完备）+ pruneUnsupportedWorldgenRails carveCanyon 后置守卫（真实机制清偿——悬空轨产生者=峡谷掏空已铺轨地板 seed 42/166/207 共 7 根）；P-t1051a 27 世界悬空轨==0 腿 + 三源钉；顺手修 review0915 #5 filtered 汇总行；矩阵 569→570。
- t1050 review0915 两项纠偏（2026-09-15，fix + test + docs 4172927）：stepTick dt 入口双钳制（方案①+droppedDtCount）+ 空手潜行右键交互 MC 口径恢复（sneakPlaceBlock 手持方块判据）；矩阵 564→569；阴性轮×2 存证。
- R20.09 EditBuffer（2026-09-15，fix(r2009) + test(r2009) + docs c760606）：src/Core/editbuffer.h（DirtyChunkSet + EditBuffer）+ GameSession 首消费方 + Review0915 #3① Command.blockState 五参权威；矩阵 560→564；section13 四腿。World/QML/PlayerController/ChunkGeometry 零触碰。
- R20.08 WorldFacade（2026-09-15，f0517c6/30efc3b/6d7c649）：WorldFacade 收窄视图 + GameSession/ChunkGeometry 双示范迁移，矩阵 556→560。
- R20.07 GameSession（2026-09-15，7ca99e1/cc33c69/cf63c07）：编排壳五项迁移，矩阵 552→556。
- R20.06 Command/Event/Snapshot（2026-09-15）：三 Core 叶子 + 三队列两域容量分化；549→552。
- R20.05 基础类型（2026-09-15，a2c1bb9/4cdf51c）：mathtypes.h + result.h；545→549。
- R20.03 测试分层（2026-09-15，09b8cb7/3daab66）：tools/matrix/ 八段 + --filter；545/0；全量冷编 2m4s。
- t1049 GOV-20260914-1（2026-09-14）：review26-1 = 腿跨段泄漏 + 踩踏 RNG，非 UB。
- R19.23 批次全貌：t1038→…→t1049，矩阵 521→545。
- （历史指针已并入顶部 Current Control Block / Recovery Point——下一最小动作见「下一最小动作」行：t1054 → §29.4-P2 玩家移动驱动。）
- 实机确认累计清单：R19.22 16 项 + R19.21 12 项 + parity 波各项观感 + t1048 骑车 500m 节奏待用户数据。
- 若 API 限额、断链或进程退出：只更新本文件的 Current Control Block 和 Recovery Point，不扩大任务范围。断链恢复按纪律⑦硬门执行。

## Governance Counter

- 锚点：2026-09-08 初始化 GREEN；#2/#3（09-09）、#4（09-11）GREEN、#5（09-12）YELLOW→闭环、#6（09-14）YELLOW→GOV-20260914-1 闭环、#7（09-15 上午）GREEN（R20 地基 5 闭环放行）、**#8（09-15 下午，docs/governance-audit-2026-09-15-b.md）GREEN**（用户 0912 指令波 R20.03→11 收官 + 5 闭环 [R20.09/t1050/t1051/R20.10/R20.11] 全绿放行；线程期红线已立：worker 禁 QObject/QML/QQuick3D）；#9（09-16 凌晨，docs/governance-audit-2026-09-16.md）GREEN——**§29.3 序列收官** + 5 闭环放行，方向权衡①②③在案；此后 r2010b 闭环（计数 1/5）；**§29.4 流式激活设计草案 v1 已入 refactor-plan 尾（D1-D6 + P1-P5 + 四问），待用户 md 定向，定向前 parity 波（t1053 walk 重构）先行**。
- 触发规则：每 5 个完整闭环任务、批次结束、架构阶段切换、异常指标、定时触发 → 读 autonomous-governance 相关章节写结论。
- 任务计数只统计完整 fix/test/docs 闭环。
